# 阶段验收门禁核对与测试报告实施记录

> 前端插件化 · 05 阶段验收 · 01 阶段验收门禁核对与测试报告 · 实施记录

[文档首页](../../../../../../文档首页.md) › [01 阶段验收门禁核对与测试报告](../05_阶段验收_01_阶段验收门禁核对与测试报告.md) › 实施　|　[详细设计](../设计/01_详细设计_05_阶段验收_01_阶段验收门禁核对与测试报告.md) · [测试记录](../测试/01_测试_05_阶段验收_01_阶段验收门禁核对与测试报告.md)

## 1. 实施信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 阶段验收门禁核对与测试报告](../05_阶段验收_01_阶段验收门禁核对与测试报告.md) |
| 对应需求 | [05-9](../../../../需求/05_需求_阶段验收.md#r05-9) |
| 详细设计 | [01_详细设计_05_阶段验收_01_阶段验收门禁核对与测试报告](../设计/01_详细设计_05_阶段验收_01_阶段验收门禁核对与测试报告.md) |
| 实施日期 | 2026-09-24 |
| 实施人 | minjian |
| 实施环境 | 开发机（Node v24.20.0 / Vitest 5.0.1 / vue-tsc；Python + uv）；mjbk（GitLab CE、Kiwi TCMS 容器）——mjbk 侧**只做证据查询与用例登记**（GitLab API / Kiwi），不重跑后端重负载实测（决策项 6） |
| Kiwi 用例 | **2189**（平台骨架 / P2 / CONFIRMED / 自动化；登记输入 `test/scripts/kiwi/cases/2026-09-24_阶段五05-01_阶段验收门禁核对与测试报告.json`，回读 `exports/2026-09-24_阶段五05-01_阶段验收门禁核对与测试报告_登记结果.json`） |
| 提交 | 见 §6（设计 / 实施与测试记录 / 回写按仓库独立提交） |
| 结论 | 完成（M5 门禁 **7 行全部达标**（1 项带限定）；[阶段测试报告](../../../../01_测试报告_前端插件化.md)归档；三类文档状态一致；计划 §7 遗留台账 13 项逐条归口） |

## 2. 实施概览 <a id="overview"></a>

```mermaid
flowchart LR
    A["12 项待拍板逐项确认"] --> B["详细设计定稿"]
    B --> C["证据采集：前端门禁现场重跑 + GitLab API + Kiwi"]
    C --> D["门禁 7 行逐条判定与证据索引"]
    D --> E["阶段测试报告撰写归档"]
    E --> F["需求侧收口：新增 05-9 与总览同步、门禁表迁出"]
    F --> G["状态回写：计划头部/两表/甘特/§5/§7、任务与域总览"]
    G --> H["Kiwi 2189 登记 → 收口断言用例"]
    H --> I["第二 AI 独立复核 + 异常项处置"]
    I --> J["全量验证全绿"]
```

结果小结：阶段五 M5 门禁 7 行（八类扩展点 / 运行时加载 / `shared` 单实例 / 隔离 / 清单与发布治理 / 兜底与归因 / 首个样例）逐条给出结论与证据索引，**全部达标**（1 项带限定并注明归口）；阶段测试报告（六节 + 附录 A）归档；计划头部计数与工时、§1、§2 / §3、§4 甘特、§5 结论与里程碑对照、§7 台账全部回写；需求侧门禁表迁出并顺延新增 `05-9`；`check-status --stage 05_前端插件化` 硬 0 / 软 0。

## 3. 实施过程 <a id="process"></a>

1. **决策确认（12 项，先于动笔）**：验收落点形态、工时与窗口、门禁表落点、甘特形态、Kiwi 用例条数与落点、报告数据范围、计划 §7、头部规范化、README、总体规划回写、历史缺陷处置、独立复核——逐项用 question 确认（推荐项在前、附差异说明），全部取推荐项（对齐记录见[设计 §10](../设计/01_详细设计_05_阶段验收_01_阶段验收门禁核对与测试报告.md#alignment)）。
2. **详细设计定稿**：落[本任务设计](../设计/01_详细设计_05_阶段验收_01_阶段验收门禁核对与测试报告.md)（目标与边界 / 门禁核对手法与证据来源 / 报告结构 / 计划回写口径 / 测试设计与验收映射 / 登记落点 / 开放项）。
3. **证据采集（前端现场重跑）**：`npm run check`（core 96 文件 1180 条 / vue 2 / 5 / ui-ep 61 / 901）、宿主 `vue-tsc` + `lint` + `test:cov`（17 文件 101 条，语句 89.71% / 分支 76.39%）+ `build` + `budget`（首屏 177.3 KB）、`modules/{demo,sample}` 的 lint / typecheck / test（11 / 21 条）/ build / budget（11.6 / 13.2 KB）/ `guard:isolation`（产物面零违规）、平台侧 `check-module-isolation`（源码面 20 文件）/ `check-shared-whitelist` / `check-module-manifest` / `check-shared-deps`（实例数 = 1）。
4. **证据采集（mjbk 查询）**：GitLab API（项目 2）取阶段五推送窗口 main 流水线 **#302 ~ #311（10 条：success 5 / failed 5）** 与失败岗位（`desktop-check` / `backend-test` / `module-build`）；`defect-auto` 开放缺陷单（27 条，含阶段窗口 iid 27 / 28；**收口后经确认批量核销，复核时 `opened=0` / `closed=47`**）；Kiwi TCMS 平台回读各任务策展编号（976 ~ 983）。
5. **门禁 7 行逐条判定**：按设计 §4 的核对手法与证据来源逐行核对（结果见 §5.1），结论内联回写[计划 §5](../../../../计划/01_计划_前端插件化.md#accept)，并新增「里程碑对照」表（M5 ↔ 2026-09-24 达成）。
6. **阶段测试报告撰写归档**：`bms文档/项目/05_前端插件化/01_测试报告_前端插件化.md`（六节 + 附录 A：命令与原始输出、流水线窗口、缺陷查询结果）。
7. **需求侧收口**：新增 `需求/05_需求_阶段验收.md`（需求 `05-9`）；总览 §1 / §2 / §3 / §4 与计数同步（8 条 → 9 条）；**§5 M5 门禁表迁出**（需求不承载验收与进度，《需求文档规范》「进度口径」节），改为指向计划 §5 与报告的说明。
8. **状态回写**：计划头部（已完成 9 项，剩余 0 项）、§1 排期对象 / 执行序 / 工时（`已完成 152h；剩余 0h；阶段合计 **152h**`，check-status S1 可校验形态）、§2 表题补完成区间并追加 `05_01` 行、§3 保留空表并更新注文、§4 补里程碑甘特 + 一句注、§5 结论内联与里程碑对照、§7 新增遗留台账（13 项）+ 台账说明；任务文档状态 / 完成日期与收口结论注；域总览子任务表状态。
9. **收口断言用例**：先登记 Kiwi（回读 **2189**），再写 `backend/libs/bms_core/tests/crosscut/test_m5_closure.py`（4 个断言面），与 `test_m2_closure.py` / `test_m3_closure.py` 同口径（文档断言 + 不写死易漂移计数）。
10. **独立复核与全量验证**：第二 AI 独立复核（见 §4）→ 全门禁复跑全绿（[§5.2](#verify)）。

## 4. 问题与处置 <a id="issues"></a>

**实施期问题**：

| # | 问题 / 现象 | 原因 | 处置与落点 |
| --- | --- | --- | --- |
| 1 | 需求总览 §5 长期承载 M5 门禁表，与「需求不承载验收与进度」冲突 | 基线建立时（2026-09-21）沿用旧口径 | 本任务整表迁至计划 §5（内联结论 + 里程碑对照），需求总览删除该节（决策项 3）；收口断言用例反向校验「需求总览不应再承载门禁表」 |
| 2 | 计划头部无「已完成 N 项，剩余 M 项」、工时行非校验形态 → `check-status` 的 S1 计数与工时子校验长期**静默跳过** | 阶段五基线沿用早期写法 | 规范化为标准形态（决策项 8），S1 校验生效；明细合计已核对为 152h |
| 3 | 阶段五推送窗口 10 条流水线中 5 条 failed，若直接写「全绿」会失真 | 失败均为实施期中间推送（含模块工程接入与 CI 集成缺口） | 报告 §1 与附录 A 按「窗口 + 状态分布 + 失败岗位 + 后续转绿」如实记录，并逐条落 §2 过程问题表 |
| 4 | `module-build` 在第二个模块接入时失败（#309 / #310） | 逐模块 `guard:isolation` 对全量模块扫描（集成缺口，实际由 `04_01` 修复） | 本任务不重复处置（已在 `04_01` 修复为 `--module .`，#311 转绿）；报告 §2 登记为过程问题 |
| 5 | 历史 27 条开放自动缺陷单（含阶段五窗口 2 条） | `defect-capture` 只建单 / 追评，关闭须人工（《测试规范》§9） | 经决策项 11：**本任务不擅自处置**，登记计划 §7 第 5 项与报告 §4 第 5 项；**收口后经用户逐项确认按阶段二同口径批量核销（评论 + 关闭，附定级与转绿证据），第 5 项闭环** |
| 6 | 门禁第 5 行（模块清单与发布治理可用）若不加限定，会掩盖「模块工程覆盖率门禁未启用」 | 模块侧门禁与包侧口径不同 | 结论后加「（限定：… → 计划 §7 第 4 项）」（决策项 3） |

**第二 AI 独立复核（独立视角只读复核，逐条挑战证据链）**：

| # | 复核发现 | 性质 | 处置与落点 |
| --- | --- | --- | --- |
| a | 「阶段五流水线全绿」类表述与 10 条窗口内 5 条 failed 的事实冲突 | 证据表述风险 | 报告 §1 / 附录 A 改为窗口 + 状态分布 + 失败岗位逐条列示，并说明「均为实施期迭代、同任务内转绿」，避免夸大 |
| b | 「模块清单与发布治理可用」若不注明模块工程覆盖率门禁未启用，会掩盖缺口 | 结论完备性 | 门禁第 5 行加限定与归口；计划 §7 第 4 项单列；报告 §3 覆盖率节写明模块工程口径 |
| c | Kiwi「本阶段新增 9 个编号」需与平台 / 台账对账，不能按「上号 +1」推算 | 事实性核对 | 以各任务测试记录 §1 与平台回读为准（976 ~ 983 + 2189） |
| d | 报告头部计数与工时须与需求总览 / 计划 §1 **三处逐字一致** | 口径一致性 | 头部 meta「9 条需求 / 152h」与计划 §1、需求总览 §2 交叉核对 |
| e | 收口断言用例若写死 9 / 152h，后续回写易误红 | 测试可维护性 | 用例改为结构自洽断言（头部计数 == 已完成表行数、剩余 0、工时行可校验），不写死计数 |
| f | 计划 §7 台账若只写「见测试记录」则无归口结论 | 闭环完备性 | 台账 13 项逐条给「影响 + 去向」，并加台账说明（收口新增 / 限定项） |

**审核通过项**：门禁 7 行与《总体项目规划》阶段五行逐条对位无缺漏；任务 / 域总览 / 计划三处状态与工时一致（H5 / H6 / H7 / S1 全绿）；报告头部 meta 与需求总览 / 计划计数逐字一致；阶段窗口失败流水线归属可从 GitLab API 复核；历史自动单未擅自处置。

## 5. 验证结果 <a id="verify"></a>

### 5.1 门禁 7 行核对（核对手法与实测结果）

| # | 验收点 | 核对命令 / 手法 | 实测结果 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | 八类扩展点注册表可用 | 八域注册表契约断言 + 拒重档位 + 未注册不可用护栏用例 | `core` 96 文件 / 1180 条全绿；八域注册 / 解析 / 拒重 / 未命中齐备（Kiwi 976） | 达标 |
| 2 | 宿主运行时加载模块 | 清单驱动加载 + 挂载卸载 + 远端解析用例 + 端到端实跑 | 宿主 17 文件 / 101 条全绿；`modules.json` 双模块 `mode: remote` 版本化；02_01 实跑 `/demo` 404 计数 0（Kiwi 977 / 978） | 达标 |
| 3 | `shared` 依赖单实例 | 构建期产物级与运行期双层实例数断言 + 白名单 / 体积护栏 | `check-shared-deps`：实例数 = 1（宿主 1 / 模块 0）；`check-shared-whitelist` 白名单 6 项通过（Kiwi 979） | 达标 |
| 4 | 模块不污染宿主 | 隔离扫描源码面 + 产物面 + 宿主五类污染 fixture 护栏 | 源码面 20 文件零违规；demo 产物面 CSS 2 / 自有 JS 3、sample CSS 3 / 自有 JS 4 零违规（Kiwi 980） | 达标 |
| 5 | 模块清单与发布治理可用 | 清单可见性 / 版本发现 / 契约版本 / 发布回滚 / 停用启用 + 契约用例工厂与齐备护栏 | `check-module-manifest` 通过（清单可解析、条目版本化、版本发现一致、sourcemap 就位、契约用例齐备）；回滚端到端演练 0.1.0 → 0.2.0 → 0.1.0（Kiwi 981）。**（限定：模块工程覆盖率门禁未启用 → 计划 §7 第 4 项）** | 达标（限定） |
| 6 | 失败可兜底、问题可按模块归因 | 超时 / 重试 / 错误上报字段 / 耗时与体积归因 / Web Vitals / sourcemap 用例 | 宿主用例全绿；超时 10s + 单模块重试、错误带模块名与版本、三段耗时与体积归因、sourcemap 随发布归档（Kiwi 982） | 达标 |
| 7 | 首个模块样例独立发布 | 独立构建产物 + 八类注册与菜单 + 运行时加载 + 清单发布回滚 + 接入指南 | sample 5 文件 / 21 条全绿；容器入口 13.2 KB（预算 16 KB）；产物面隔离零违规；接入指南落知识档案《微前端》（Kiwi 983）。**（限定：生产 / mjbk 部署形态与灰度进阶 → 计划 §7 第 1 / 2 项）** | 达标（限定） |

### 5.2 本地门禁与自检

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 前端核心三包 | `npm run check`（bms 根） | core 96 / 1180、vue 2 / 5、ui-ep 61 / 901 全通过 |
| 宿主应用 | `npx vue-tsc -b && npm run lint && npm run test:cov && npm run build && npm run budget` | 17 文件 / 101 条；语句 89.71% / 分支 76.39%；首屏 177.3 KB（预算 260 KB） |
| 模块工程 | `npm run lint && typecheck && test && build && budget && guard:isolation`（demo / sample） | 11 / 21 条全通过；容器入口 11.6 / 13.2 KB；产物面隔离零违规 |
| 平台脚本护栏 | `check-module-isolation` / `check-shared-whitelist` / `check-module-manifest` / `budget:shared` | 全部通过（隔离零违规；白名单 6 项；清单与版本发现一致；依赖实例数 = 1） |
| 后端全量 | `cd backend && uv run pytest -q` | 1345 passed / 37 skipped（阶段二快照 1341 + 本任务 4 条） |
| 收口断言用例 | `uv run pytest libs/bms_core/tests/crosscut/test_m5_closure.py -q` | 4 passed（Kiwi 2189） |
| 静态检查 | `uv run ruff check .` / `ruff format --check .` / `pyright` | 见[测试记录 §3](../测试/01_测试_05_阶段验收_01_阶段验收门禁核对与测试报告.md#run) |
| 文档状态一致性 | `check-status.py --stage 05_前端插件化` | 硬规则 0 / 软提示 0 |
| 基座自检 | `check-base.py` / `check-links.py` / `check-backend-base.py` | 三者全通过（清单与磁盘一致、无断链、继承链与迁移链对齐） |
| 本地预检 | `check-preflight.py --fast` | 全部通过 |

## 6. 偏差与遗留 <a id="deviations"></a>

- **设计偏差（1 处）**：设计 §3 交付物清单未列仓库入口 `bms/README.md` 的阶段状态行同步；实施中发现其阶段五行仍写「阶段未开工 / 8 项 / 132h」、阶段二行仍写「待启动」（均明显滞后于实际），一并按事实更正（阶段入口索引一致性，未改动阶段二文档本身），落本记录。
- **口径说明（非偏差）**：后端重负载与生产部署形态不现场重跑，按决策项 6 复用已归档证据并在报告与门禁结论中注明限定。
- **新增遗留（1 项）**：计划 §7 第 5 项（历史 27 条开放自动缺陷单 + 「修复即流转关闭」流程缺口）——**已于收口后经用户确认批量核销闭环**（评论 + 关闭，附定级与转绿证据），治理脚本与 `defect-capture` 现场 dump 缺口转阶段二计划 §7 第 36 / 37 项。
- **提交清单**（各仓库独立提交，待用户指令）：bms 仓——需求（新增 05_需求_阶段验收 + 总览同步）、任务（05 域父 / 子任务 + 设计 / 实施 / 测试）、计划回写、阶段测试报告、README 同步、收口断言用例（`test(05_01)`）；测试资产仓——Kiwi 登记输入与回读结果。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
