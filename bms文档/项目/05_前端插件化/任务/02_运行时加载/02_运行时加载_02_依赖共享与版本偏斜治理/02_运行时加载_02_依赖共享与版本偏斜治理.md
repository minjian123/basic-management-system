# 02 依赖共享与版本偏斜治理

> 前端插件化 · 02 运行时加载 · 子任务 02（需求 05-4）

[文档首页](../../../../../文档首页.md) › [02 运行时加载](../02_运行时加载.md) › 02 依赖共享与版本偏斜治理　|　[← 父任务](../02_运行时加载.md)

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02 |
| 父任务 | [02 运行时加载](../02_运行时加载.md) |
| 对应需求 | [05-4](../../../需求/02_需求_运行时加载.md#r05-4) |
| 工时（重估） | 16h |
| 依赖 | `02_01`（MF 接入与模块独立构建） |
| 负责人 | minjian |
| 状态 | 已完成 |
| 完成日期 | 2026-09-21 |

## 2. 任务内容 <a id="content"></a>

1. **`shared` 单例声明**：框架级依赖（框架本体 / 路由 / 状态 / UI 组件库 / 核心包）在宿主与模块构建配置中声明为 `shared` 且 `singleton`，并声明 `requiredVersion`。
2. **版本来源与偏斜检查**：共享依赖版本取自依赖清单（单一来源）；模块声明版本与宿主不一致时按既定口径处置（告警 / 拒绝加载）并留痕。
3. **构建期验证**：生产构建验证宿主与模块产物**无重复实例**。
4. **CI 断言**：脚本 / 用例断言依赖实例数为 1，纳入门禁，失败即阻断。
5. **升级契约**：框架大版本升级视为跨团队契约，升级流程与兼容窗口成文。

## 3. 完成标准 <a id="accept"></a>

共享依赖声明（`singleton` + `requiredVersion`）成文且生效；生产构建无重复实例、CI 依赖实例数断言可执行并纳入门禁；版本不一致场景有明确处置与用例；升级契约（大版本协同升级流程与兼容窗口）成文。

## 4. 参考文档 <a id="ref"></a>

- [需求 05-4](../../../需求/02_需求_运行时加载.md#r05-4)
- 《[架构设计 · 前端架构](../../../../../设计/架构设计/08_架构设计_前端架构.md)》§9 治理表「依赖共享与版本偏斜」
- 《[架构设计 · 测试与质量](../../../../../设计/架构设计/32_架构设计_测试与质量.md)》依赖与构建节
- 《[架构设计 · 前端模块契约](../../../../../设计/架构设计/35_架构设计_前端模块契约.md)》
- 域 02 [`02_01` Module Federation 接入与模块独立构建](../02_运行时加载_01_ModuleFederation接入与模块独立构建/02_运行时加载_01_ModuleFederation接入与模块独立构建.md)（**前置契约已交付**：MF 工具链（`@module-federation/vite` 1.22.1 + `@module-federation/runtime` 2.9.0，均精确锁定）、宿主**纯 host** 接入（`name` + `shared`，无静态 `remotes`）与运行期远端登记 / 加载（登记须显式声明入口类型 `module`，`force` 幂等）、`shared` 声明的两侧落点与**框架三件单例**基线（`vue` / `vue-router` / `pinia`）。**实测结论（本任务须在其上决策）**：① 宿主共享 UI 组件库（`element-plus`）会被插件生成「整库 share provider 块」并由宿主入口**静态引入**（首屏 +334.5 KB gzip，实测 `eager: false` 无效）；② 平台基座包（`@bms/core` / `@bms/ui-ep`）因宿主以 `resolve.alias` 消费、MF 探测不到而**未进入共享面**（现按双侧各自打包处理）；③ 模块侧产物含各共享依赖的本地回退块，可作「重复实例」计量与裁剪的切入点）

> 交付物：详细设计、实施记录、测试记录（随任务开工建立，落本目录 `设计/`、`实施/`、`测试/`）。

> **完成情况（2026-09-21）**：共享声明收归**单一来源**（共享面白名单 + 版本要求 + 受控非共享项）；宿主为提供方、模块为消费方（`import:false` 不打包本地副本 + 版本不满足即拒绝）；**构建期**产物级断言（两侧声明一致 / 无副本 / 依赖实例数 = 1）与白名单 / 体积护栏 + **运行期**实例数断言（真实 MF 运行时，共享域内版本条目数恒为 1）双层纳入门禁（CI job `shared-deps-check`）；升级契约成文（规范节 + 资料《微前端 · 依赖升级契约》）。**偏差**：需求原文列「UI 组件库 / 核心包」共享，经受控实测（首屏 660.7 KB；`eager:false` + `treeShaking runtime-infer` 实测启动仍异步拉取约 433 KB，属指标失真）与机制判定（基座包经 alias 消费、MF 探测不到）后**锁定为受控非共享项**，需求措辞已按结论回写；模块构建拆为「远端产物 / 独立预览产物」两目标。**遗留**：模块工程类型声明生成报错（`#TYPE-001`，既有）、基座包发布版本化后的共享复评、版本协商属演进路线——详见实施记录 §6。Kiwi 用例 **979**。
