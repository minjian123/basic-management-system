# 03 测试 · 可观测与失败兜底

> 前端插件化 · 03 隔离与治理 · 子任务 03（需求 05-7）· 测试记录

[文档首页](../../../../../../文档首页.md) › [03 隔离与治理](../03_隔离与治理_03_可观测与失败兜底.md) › 测试记录　|　[← 实施记录](../实施/01_实施_03_隔离与治理_03_可观测与失败兜底.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [03 可观测与失败兜底](../03_隔离与治理_03_可观测与失败兜底.md) |
| 对应需求 | [05-7](../../../../需求/03_需求_隔离与治理.md#r05-7) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_03_隔离与治理_03_可观测与失败兜底.md) |
| 实施记录 | [01 实施](../实施/01_实施_03_隔离与治理_03_可观测与失败兜底.md) |
| 测试日期 | 2026-09-22 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1 / jsdom）；Web Vitals 以 `web-vitals` mock 触发回调；超时以注入 `timeoutMs` 断言（不真等 10s） |
| Kiwi 用例 | **982**（分类「平台骨架」/ P2 / CONFIRMED / 标签「自动化」；一任务一条） |
| 结论 | 通过（超时与降级重试 / 错误按模块归因 / 耗时与体积 / Web Vitals 按模块 / sourcemap 关与体积留痕全绿；门禁与基座校验全通过） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例组 | 类型 | 自动化文件 / 入口 | 覆盖点 |
| --- | --- | --- | --- |
| **加载超时与阶段计时**（新增） | 单元 | `core/tests/module-loader.spec.ts` | 整个 `load()` 超时拒绝（原因明示）；`resolve` / `setup` 各自计时与成败标注；`setup` 抛错记录并原样上抛；超时常量口径 |
| **遥测器与上报实现**（新增） | 单元 | `core/tests/module-telemetry.spec.ts` | 记录与按模块聚合（加载 / 错误 / Vitals；平台归 `platform`）；环形上限丢最旧；可替换上报实现；上报注册表同键拒重 / 按名与缺省解析 / 未登记返回 `undefined` |
| **错误上报带模块名与版本**（新增） | 单元 | `apps/desktop/tests/module-observability.spec.ts` | 活动模块作用域补模块名与版本；`BaseObject.reportError` 经 `configureBase` sink 归因；平台兜底；显式 `reportModuleError` 按模块与阶段 |
| **Web Vitals 按模块归属**（新增） | 单元 | 同上（mock `web-vitals`） | 回调按活动模块归属；非模块路由归 `platform` |
| **模块路由作用域与渲染耗时**（新增） | 单元 | 同上 | 路由就绪更新活动模块并记 `render` 耗时；平台路由清除活动模块 |
| **单模块重试**（新增） | 单元 | `apps/desktop/tests/module-hosting.spec.ts` | 失败后仅重挂该模块（路由恢复、错误态清空），不整页刷新 |
| **sourcemap 关与体积留痕**（新增） | 单元 | `apps/desktop/tests/module-release.spec.ts` | 缺 sourcemap 拒绝发布；发布记录含 `size` 与 `sourcemap`；清单护栏拦截缺 map |
| 清单解析 / 发布 / 回滚 / 停用回归 | 单元 | 同上 | `03_02` 既有断言全量回归（fixture 增 sourcemap 占位） |
| 宿主装载与渲染消费回归 | 单元 | `module-hosting` / `module-manifest` / `module-registration` / `module-federation` | 清单驱动装载、装配与卸载、远端解析器、停用与契约版本拒绝等（含新增重试） |
| 全量门禁（回归） | 门禁 | 根 `pnpm run check`、宿主与模块工程门禁 | 类型 / Lint / 单测 / 覆盖率 / 构建 / 预算 / 隔离扫描全通过 |
| 平台脚本（回归） | 命令实跑 | `check-module-isolation.mjs` / `check-shared-whitelist.mjs` / `check-module-manifest.mjs` | 源码面与产物面隔离零违规、白名单合规、清单 / sourcemap / 契约用例齐备、按模块体积汇总 |
| 文档与基座（回归） | 门禁 | `check-base.py` / `check-links.py` / `check-status.py --strict` | 清单与磁盘一致、断链与失效锚点为零、阶段状态一致 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
# 核心三包（typecheck / lint / test）
cd bms && pnpm run check

# 宿主（PC）
cd frontend/apps/desktop && pnpm exec vue-tsc -b && pnpm run lint && pnpm run test:cov && pnpm run build && pnpm run budget

# 模块工程（远端产物 + sourcemap + 元数据 + 隔离护栏）
cd frontend/modules/demo && pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run build && pnpm run budget && pnpm run guard:isolation

# 平台脚本（本地与 CI 同口径）
cd bms && node frontend/scripts/check-module-isolation.mjs
node frontend/scripts/check-shared-whitelist.mjs
node frontend/scripts/check-module-manifest.mjs

# 真实发布（演示模块；sourcemap 关 + 体积字段）
node frontend/scripts/release-module.mjs publish --module demo --force --by minjian

# 文档与基座校验
python3 scripts/tools/base-check/check-base.py
python3 scripts/tools/base-check/check-links.py
python3 scripts/tools/check-docs/check-status.py --stage 05_前端插件化 --strict
```

```text
> core    96 files / 1179 tests 通过；vue 2 / 5；ui-ep 61 / 901（根 pnpm run check 全绿）
> host    17 files / 101 tests 通过；vue-tsc / lint 通过
> cov     host 语句 89.87% / 分支 77.03% / 函数 84.37% / 行 90.22%（阈值 70%）
> budget  宿主首屏 177.1 KB（预算 260 KB）；单文件 80.1 KB（预算 150 KB）
> module  3 files / 11 tests 通过；lint / typecheck 通过
> module budget  容器入口 11.6 KB / 6 块（预算 12 KB / 8 块 / 上限 8）；页面异步块命中 DemoHome / DemoToolbox
> msg     dist/remoteEntry.js.map 存在（sourcemap 关通过）；module.meta.json = { name: demo, version: 0.1.0, contractVersion: 1 }
> isolation 源码面 5 个文件零违规；产物面 CSS 2 个 / 模块自有 JS 块 3 个零违规
> shared  白名单 vue / vue-router / pinia / element-plus / @bms/core / @bms/ui-ep；依赖与实装版本合规；体积在阈值内
> manifest 通过：清单可解析、远端条目版本化、版本发现一致、sourcemap 就位、契约用例齐备
> manifest 体积 demo：入口 gzip 11.6 KB（最大块 6.2 KB，6 块，异步块 22）
> publish republish：demo@0.1.0 → http://localhost:5002/demo/0.1.0/remoteEntry.js（入口 gzip 11.6 KB / 6 块）
> base    清单一致与跨层引用合规；无措辞残留
> links   断链 0 / 失效锚点 0
> status  阶段五 91 项检查：硬规则不合规 0，软提示 0
> kiwi    982 新建 1
```

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | `guard-jsdoc` 拦截多行签名与内联泛型 | 单行签名 + 抽出命名聚合接口（实施记录 §4 问题 1） |
| 2 | `guard-manifest` 要求能力文件 `identifier` 与登记表一致 | `BaseModuleTelemetry` 改用 `identifier` + `get key()`（实施记录 §4 问题 2） |
| 3 | 观测用例 `sink.record is not a function` | 测试改从 `observability` 索引导入单例版 `reportModuleError`（实施记录 §4 问题 4） |
| 4 | 观测面板响应式写法触发 lint | `ref(moduleSnapshot())` 快照 + `refresh()`（实施记录 §4 问题 5） |
| 5 | 开 sourcemap 后构建哈希变化、入口体积微升 | 认定为正常；重发归档并留痕（实施记录 §4 问题 6） |

## 5. 覆盖率 <a id="coverage"></a>

| 侧 | 语句 | 分支 | 函数 | 行 | 口径 |
| --- | --- | --- | --- | --- | --- |
| 宿主（`apps/desktop` `test:cov`） | 89.87% | 77.03% | 84.37% | 90.22% | 高于《测试规范》前端 70% 阈值（`observability/` 全部覆盖：scope / webVitals / reporter / index） |
| 核心（`packages/core`） | — | — | — | — | 随根 `pnpm run check` 全量用例；新增 `module/telemetry` 与加载器超时 / 阶段计时 / 观测基类由专项用例覆盖 |
| 平台脚本（`module-metrics` / `release-module` / `check-module-manifest`） | — | — | — | — | 以 **fixture + 临时工作区** 成对验证（护栏脚本口径，与既有 `check-shared-*` 一致，不采覆盖率） |
| 模块工程（`modules/demo`） | — | — | — | — | 模块侧门禁为 `typecheck` / `lint` / `test` + 构建产物断言（含 sourcemap）；覆盖率门禁随 `04_01` 统一口径 |

## 6. 偏差与遗留 <a id="deviations"></a>

- **未覆盖项**：真实后端上报通道（本期内存记录 + 可替换插件）、生产 / mjbk 部署形态（sourcemap 与产物托管 / 私有化映射 / CDN）、按模块 SLO 与告警阈值、移动端观测面板、跨模块 Vitals 切片。
- **后续任务接收项**：① `04_01` 样例模块按 sourcemap / 体积 / 上报口径接入，契约用例经同一工厂，接入指南含可观测与 sourcemap 检查项；② 可观测子系统阶段登记真实 `BaseModuleReporter` 实现（不改宿主调用面）；③ 生产部署阶段落地 map 与产物的制品库 / CDN 托管。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》与《[测试规范](../../../../../../规范/测试规范.md)》编写
