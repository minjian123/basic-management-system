# 02-8 批量迁移与租户库初始化接口占位

> 项目骨架 · 02 后端基座 · 02 四层基类 · 嵌套子任务 08

[文档首页](../../../../../../文档首页.md) › [02 四层基类](../02_后端基座_02_四层基类.md) › 02-8 批量迁移与租户库初始化接口占位　|　[← 父任务](../02_后端基座_02_四层基类.md)

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02-2-8 |
| 父任务 | [02 四层基类](../02_后端基座_02_四层基类.md) |
| 对应需求 | [02-9](../../../../需求/02_需求_后端基座.md#r02-9) |
| 工时（重估） | 1h（占位口径重估：原 5h） |
| 依赖 | 03（数据访问接口占位）、05（平台库基础表） |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

> **本阶段口径（接口占位）**：只落接口 / 抽象声明与占位实现（返回固定或批量数据，便于上层先跑通链路），不连真库、不跑迁移、不建表；下方为实现要点，随对应后续阶段填实现。
1. Alembic 结构（`backend/alembic/`）：env.py 按数据源读 URL、区分平台库/租户库迁移上下文；versions/ 三套方言脚本（不写方言 SQL、达梦注意 schema 前缀）；SQLite 开发库自动建表
2. 批量迁移脚本 `ops/migrate_tenants.py`：参数 `--target all|tenant_code|platform`、`--db mysql|postgres|dameng`、`--dry-run`；遍历 sys_tenant 逐库迁移；失败不中断后续库、输出成功/失败/跳过汇总、失败库重跑幂等
3. 新租户初始化 `ops/init_tenant.py`：建库 → 三套迁移 → 幂等种子；种子机制（backend/alembic/seeds/ 或 ops/seeds/，幂等可重复，与结构迁移分离）；本阶段种子仅空租户基线
4. 迁移验证矩阵：三库各跑「迁移 → 建表检查 → 回滚/再迁移」；CI 三库流程与 05_01（三库集成与流水线收口）衔接

## 3. 完成标准 <a id="accept"></a>

接口 / 抽象声明就位、依赖注入可解析、应用可启动不报错、占位实现返回固定或批量数据可支撑上层链路；`pytest`、`ruff`、`pyright` 全绿。

> 实现期验收要点（随后续阶段）：迁移脚本三库各执行通过（sys_tenant + sys_module 表就位）；migrate_tenants.py --dry-run 输出正确库清单、实际执行后 demo 租户库 schema 与平台库一致；init_tenant.py 新租户（t1）建库→迁移→种子一次跑通且重跑幂等无报错。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 数据架构》「迁移策略」节
- 《数据库开发规范》「迁移规范（Alembic）」节
- 《架构设计 · 多租户路由》「租户开通与停用」节
- 《项目规划说明》「后端核心」节

> 本文档依《文档生成规范》编写
