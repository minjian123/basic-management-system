# 根基类与集合体系测试记录

> 项目骨架 · 02 后端基座 · 01 根基类与集合体系 · 测试记录

[文档首页](../../../../../../文档首页.md) › [01 根基类与集合体系](../02_后端基座_01_根基类与集合体系.md) › 01 测试　|　[← 父任务](../02_后端基座_01_根基类与集合体系.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 根基类与集合体系](../02_后端基座_01_根基类与集合体系.md) |
| 对应需求 | [02-1](../../../../需求/02_需求_后端基座.md#r02-1)、[02-2](../../../../需求/02_需求_后端基座.md#r02-2)、[02-3](../../../../需求/02_需求_后端基座.md#r02-3)、[02-4](../../../../需求/02_需求_后端基座.md#r02-4) |
| 详细设计 | [01_详细设计_01_根基类与集合体系](../设计/01_详细设计_01_根基类与集合体系.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_根基类与集合体系.md) |
| 测试日期 | 2026-09-13 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv） |
| Kiwi 用例 | 14 / 15 / 16 / 17（继承断言并入既有用例，不新增编号） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

本任务为登记核对，重点验证「登记与代码的继承关系一致」；继承断言并入既有 Kiwi 用例，其余为回归。

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 15 | 有序集合继承链 | 单元 | `tests/core/test_collections.py::test_inheritance_chain` | `BaseSorted → BaseObject`；`SortedList` / `SortedDict` / `SortedSet` → `BaseSorted` |
| 17 | 跨副本封装继承链 | 单元 | `tests/core/test_redis_collections.py::test_inheritance_chain` | `RedisSortedSet` / `RedisSortedDict` / `RedisSnapshot` → `BaseObject` |
| 16 | 进程内并发集合继承链（既有） | 单元 | `tests/core/test_concurrent.py` | `ConcurrentSorted* → BaseConcurrentSorted → BaseSorted → BaseObject`（核对） |
| 14 | 模块基类继承 `BaseObject`（既有） | 单元 | `tests/core/test_base_object.py` | `BaseRepository` / `BaseService` / `BaseSchema` → `BaseObject`（核对） |
| — | 既有功能回归 | 单元 / 接口 | `tests/*` | 有序 / 并发 / Redis / 仓储 / 服务 / Schema / API 全部回归 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
72 passed, 2 skipped in 0.26s

$ uv run pytest -q -rs
SKIPPED [1] tests/integration/test_redis_collections_integration.py:34: 未配置 BMS_TEST_REDIS_URL，跳过真实 Redis 集成用例
SKIPPED [1] tests/integration/test_redis_collections_integration.py:51: 未配置 BMS_TEST_REDIS_URL，跳过真实 Redis 集成用例

$ uv run ruff check .
All checks passed!

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 2 条继承断言用例通过；全量 72 passed，2 skipped（均为真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 无 | — | 断言一次通过 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 841 语句，覆盖率 100%**（任务级快照；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 真实 Redis 集成用例（2 条）需 `BMS_TEST_REDIS_URL`，本机未配置即跳过，随 04_02（三库集成与流水线收口）重验证层启用；已在计划「后续阶段待办」表登记。
- 无未覆盖项。
- **后续变更回写（2026-09-13）**：`BaseObject` 序列化口径在 02-2-3 升级为「声明字段优先」（Pydantic `model_dump()` / dataclass `dataclasses.fields` / 其余 `__dict__`），分型序列化用例并入 Kiwi 14（`test_public_fields_declared_first`）；原继承断言不变，见 [02-2-3 测试记录](../../02_后端基座_02_模块基类/02_后端基座_02_模块基类_03_请求响应基类/测试/03_测试_01_请求响应基类.md)。

> 本文档依《文档生成规范》编写
