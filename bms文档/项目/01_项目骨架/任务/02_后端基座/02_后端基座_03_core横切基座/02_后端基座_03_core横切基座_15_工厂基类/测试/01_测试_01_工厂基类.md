# 工厂基类测试记录

> 项目骨架 · 02 后端基座 · 03 core 横切基座 · 15 工厂基类 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-3-15 工厂基类](../02_后端基座_03_core横切基座_15_工厂基类.md) › 测试记录　|　[← 父任务](../02_后端基座_03_core横切基座_15_工厂基类.md)

## 1. 测试信息 <a id="info"></a>

| 项 | 值 |
| --- | --- |
| 需求 | [02-54](../../../../../需求/02_需求_后端基座.md#r02-54) |
| Kiwi 用例 | **778**（02-3-15 工厂基类：契约 / 四域工厂基类 / 既有工厂迁移 / 关键工厂可替换） |
| 自动化文件 | `tests/core/test_factory.py`（新增）；各域迁移回归复用既有用例 |
| 执行日期 | 2026-09-20 |

## 2. 覆盖范围 <a id="scope"></a>

| 用例 | 断言 |
| --- | --- |
| `test_base_factory_contract` | `BaseFactory` 抽象不可实例化；`validate` 默认空实现；子类校验 / 创建生效 |
| `test_domain_factory_bases` | 四域工厂基类均继承 `BaseFactory` 且键就位 |
| `test_plugin_factory_is_zero_arg_callable` | `BasePluginFactory` 零参可调用（兼容 `PluginImpl` 口径） |
| `test_factory_registry_register_and_resolve` | 工厂专用注册表登记 / 解析按次新建 / 重名拒 / 未登记拒 |
| `test_engine_and_session_factory_migration` | `EngineFactory` / `SessionFactory` 挂 `BaseDbFactory`，经工厂链产出可用（SQLite） |
| `test_id_generator_factory` | `IdGeneratorFactory` 挂 `BaseIdFactory`，`create` / 校验 / `reconfigure` |

迁移回归（复用既有用例）：应用启动 `ApplicationFactory().create(None)` + lifespan；`get_db` 依赖；WorkerId 接线与 ORM 主键生成（Kiwi 30）；插件装配 provider 一致；`ops/check_plugins.py` 离线校验。

## 3. 执行结果 <a id="result"></a>

```
uv run pytest --cov=app --cov-branch --cov-report=term --cov-fail-under=70 -q
567 passed, 8 skipped, 1 failed in 3.66s
Total coverage: 99.35%（门禁 ≥70% 达标）
uv run ruff check .   # All checks passed
uv run pyright        # 0 errors
```

## 4. 问题与偏差 <a id="issues"></a>

- 1 条失败为 **既有失效**：`tests/crosscut/test_m3_closure.py::test_gate_table_has_conclusions_and_evidence`——断言阶段三需求总览含「M3 验收门禁」节，实际文档已重排为「需求域文档地图」；**与本次改动无关**（基线复现），登记遗留待阶段三文档侧修正。
- 护栏调整：`tests/crosscut/test_plugin_registration.py` 的 `_ports()` 增加「跳过 `app.core.factory`」——工厂基类不属能力域端口，属设计预期。

> 本文档依《文档生成规范》编写
