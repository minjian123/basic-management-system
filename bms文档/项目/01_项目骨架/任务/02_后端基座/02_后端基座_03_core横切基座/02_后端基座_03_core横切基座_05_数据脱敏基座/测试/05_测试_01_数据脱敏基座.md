# 数据脱敏基座测试记录

> 项目骨架 · 02 后端基座 · 03 core 横切基座 · 05 数据脱敏基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-3-5 数据脱敏基座](../02_后端基座_03_core横切基座_05_数据脱敏基座.md) › 01 测试　|　[← 父任务](../02_后端基座_03_core横切基座_05_数据脱敏基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [05 数据脱敏基座](../02_后端基座_03_core横切基座_05_数据脱敏基座.md) |
| 对应需求 | [02-29](../../../../../需求/02_需求_后端基座.md#r02-29)（含提前交付的 [02-24](../../../../../需求/02_需求_后端基座.md#r02-24) 权限校验契约） |
| 详细设计 | [05_详细设计_01_数据脱敏基座](../设计/05_详细设计_01_数据脱敏基座.md) |
| 实施记录 | [05 实施记录](../实施/05_实施_01_数据脱敏基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 39（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 39 | 契约继承与能力域标识 | 单元 | `tests/masking/test_masking.py::test_inheritance_and_key` | `NullMasker → BaseMasker → BaseCapability → BaseObject`；`placeholder` 标记；`key == "masking"`；`MASK_STRATEGIES` 六项 |
| 39 | 字段注册 | 单元 | `…::test_register_rules` | 默认 / 指定策略；重复注册以最后一次为准；`masked_fields` 与 `rules()` 稳定序 |
| 39 | 占位原样返回 | 单元 | `…::test_null_masker_passthrough` | `mask` / `reveal` 原样返回；未注册字段原样返回 |
| 39 | 权限联动（基座内判定） | 单元 | `…::test_check_plain_uses_injected_checker` | 占位检查器 → `check_plain()` 为 True；拒绝实现 → False（调用方不传权限） |
| 39 | 依赖提供者 + 序列化掩码 | 单元 | `…::test_get_masker_injects_context_and_masks` | 经 `Depends(get_masker)` 注入后响应被掩码（`phone` → `***`）；请求结束 `current_masker` 复位 |
| 39 | 无掩码器直通 | 单元 | `…::test_serialization_passthrough_without_masker` | 无掩码器 / 复位后序列化与既有逐字段一致；`BaseSchema.masked_fields` 默认空集 |
| 39 | 权限契约继承与恒定允许 | 单元 | `tests/permission/test_permission.py::test_inheritance_and_key`、`::test_null_checker_allows` | 继承链与 `key == "permission"`；`check` 恒定 True；`require` 放行 |
| 39 | 强制校验失败 | 单元 | `…::test_require_raises_permission_error` | 拒绝实现下 `require` 抛 `PermissionError`（30001 / 403） |
| 39 | 依赖工厂 | 集成（ASGI） | `…::test_require_permission_dependency_allows`、`::test_require_permission_dependency_denies` | 占位放行 200；拒绝实现下统一响应 403 / code 30001 |
| 13 | 既有序列化回归 | 单元 | `tests/schemas/test_base_schema.py`（未改动） | `to_dict() == model_dump()`、分页默认值精确字典、ID 字符串化（含嵌套）全部通过 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
194 passed, 2 skipped in 0.53s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
124 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 39 用例（9 条）通过；既有 Kiwi 13 序列化用例未改动、全数通过；全量 194 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 首轮 2 条用例失败：`Token was created in a different Context` | `get_masker` 为同步生成器依赖，FastAPI 经线程池执行 enter / exit，上下文变量跨 Context | 改异步生成器依赖后通过；约束写入详细设计 §7 与 docstring（详见[实施记录](../实施/05_实施_01_数据脱敏基座.md)「问题与处置」） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 2009 语句，覆盖率 99%**。

- 本任务新增模块（`app/masking/`、`app/permission/`、`app/core/context.py` 新增段、`app/schemas/base.py` 掩码段）**100% 覆盖**。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实掩码规则与 `reveal` 解密、嵌套模型掩码 → **四 认证与安全**；`data:plain` 真实权限查询 → **五 RBAC**；导出侧脱敏 → 导入导出阶段；日志脱敏 → **03_02 日志体系**；AI 交互落库前掩码 → **十七 AI 能力**——均登记在计划「后续阶段待办」表，本任务仅覆盖序列化接入点与直通口径。
- 真实 Redis 集成用例（2 条）需环境变量配置，本机未配置即跳过；已登记计划「后续阶段待办」表（随 04_02 重验证层启用），非本任务遗留。
- 结论：本任务无未闭环遗留（遗留项均已在计划「后续阶段待办」表与 02-3-9 任务文档登记去向）。

> 本文档依《文档生成规范》编写
