# 分布式锁基座测试记录

> 项目骨架 · 02 后端基座 · 03 core 横切基座 · 06 分布式锁基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-3-6 分布式锁基座](../02_后端基座_03_core横切基座_06_分布式锁基座.md) › 01 测试　|　[← 父任务](../02_后端基座_03_core横切基座_06_分布式锁基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [06 分布式锁基座](../02_后端基座_03_core横切基座_06_分布式锁基座.md) |
| 对应需求 | [02-30](../../../../../需求/02_需求_后端基座.md#r02-30) |
| 详细设计 | [06_详细设计_01_分布式锁基座](../设计/06_详细设计_01_分布式锁基座.md) |
| 实施记录 | [06 实施记录](../实施/06_实施_01_分布式锁基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 40（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 40 | 契约继承与能力域标识 | 单元 | `tests/lock/test_lock.py::test_inheritance_and_key` | `NullDistributedLock → BaseDistributedLock → BaseCapability → BaseObject`；`placeholder` 标记；`key == "distributed_lock"` |
| 40 | 锁 key 拼接与常量 | 单元 | `…::test_build_lock_key` | 租户形态 `bms:t1:lock:engine:create`、全局形态 `bms:global:lock:ddl:sys_user`；`DEFAULT_LOCK_TTL=30`、`DEFAULT_WAIT=0.0` |
| 40 | 占位恒定成功 | 单元 | `…::test_null_lock_always_succeeds` | `acquire` 返回非空令牌；`release` / `extend` 返回 `True`；未持锁 key 亦成功（不连 Redis） |
| 40 | `hold` 上下文与参数透传 | 单元 | `…::test_hold_runs_critical_section_and_releases` | 上下文内取得令牌；`ttl` / `wait` 透传给 `acquire`；退出后调用 `release(key, token)`（记录型测试锁断言调用序列） |
| 40 | `hold` 失败语义 | 单元 | `…::test_hold_raises_conflict_when_not_acquired` | `acquire` 返回 `None` → 抛 `ConcurrentConflictError`（code 10004 / http_status 409）；临界区未执行；未调用 `release` |
| 40 | 依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullDistributedLock`；路由经 `Depends(get_distributed_lock)` 取到同一实例（类型名 + 同一性）；`api.deps` 可导出提供者 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
200 passed, 2 skipped in 0.53s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
127 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 40 用例（6 条）通过；既有 194 条用例无回归；全量 200 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 首轮 ruff `F401` | 测试文件未使用 `FastAPI` 导入 | 移除导入（详见[实施记录](../实施/06_实施_01_分布式锁基座.md)「问题与处置」） |
| 首轮 pyright 2 类报错 | `@asynccontextmanager` 返回注解弃用；测试访问契约类未声明属性 | `hold` 改标 `AsyncGenerator[str]`；用例改断言类型名与实例同一性 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 2055 语句，覆盖率 99%**。

- 本任务新增模块（`app/lock/`）**100% 覆盖**。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实 Redis 实现（SETNX 竞争、令牌误放防护、TTL 到期、`wait` 等待）→ **六 通用能力**（多副本实测随十一 分布式与监控阶段）；消费方临界区用例随各消费方阶段（幂等 / beat / 引擎创建 / DDL 串行）。
- 真实 Redis 集成用例建议纳入重验证层 Redis job（需 `BMS_TEST_REDIS_URL`），与既有 2 条 Redis 跳过用例同口径。
- 结论：本任务无未闭环遗留（遗留项均已在计划「后续阶段待办」表登记去向）。

> 本文档依《文档生成规范》编写
