# 03-1 异常体系与统一响应

> 项目骨架 · 02 后端基座 · 03 core横切基座 · 嵌套子任务 01

[文档首页](../../../../../../文档首页.md) › [03 core横切基座](../02_后端基座_03_core横切基座.md) › 03-1 异常体系与统一响应　|　[← 父任务](../02_后端基座_03_core横切基座.md)

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02-3-1 |
| 父任务 | [03 core横切基座](../02_后端基座_03_core横切基座.md) |
| 对应需求 | [02-13](../../../../需求/02_需求_后端基座.md#r02-13) |
| 工时（重估） | 6h |
| 依赖 | 工程骨架（01_02 backend 工程就位、01_03 分层目录与 `app.state` 约定） |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. `app/core/exceptions.py`：统一异常基类 `BizError`（携带错误码与参数）+ 常用子类（参数异常 / 认证失效 / 权限不足 / 资源不存在 / 冲突等）；阶段一的 `NotFoundError` 改为继承 `BizError`。
2. 全局异常处理器：`BizError` → 统一响应 `{code, message, data}`（HTTP 状态码与业务错误码分离）；未捕获异常 → 500 且不暴露堆栈（开发环境可含 `trace_id`）。
3. 统一响应模型 `ApiResponse`（Pydantic），成功 `code=0`；错误码平台段 `1xxxx`~`9xxxx` 建立骨架段位并登记《架构设计 · 接口与集成》「错误码」节。
4. 业务异常在 services 层抛出、api 层不 catch；`main.py` 注销阶段一临时 404 处理器；api 层回归「取数 → 组装响应」。
5. 测试：先登记 Kiwi 用例，再写自动化（各异常分支 + 统一响应结构断言）；替换阶段一 demo 的过渡断言，回归行为不变（成功路径响应与 404 状态码一致）。

## 3. 完成标准 <a id="accept"></a>

`BizError` 及各子类可用；业务异常经全局处理器转统一响应结构；未捕获异常返回 500 且不泄露堆栈；错误码段位登记；阶段一临时处理器移除；行为回归通过；`uv run pytest` / `ruff check .` / `pyright` 全绿。

## 4. 参考文档 <a id="ref"></a>

- 《[后端开发规范](../../../../../../规范/后端开发规范.md)》「异常与错误码」节
- 《[API接口规范](../../../../../../规范/API接口规范.md)》「统一响应与错误码」节
- 《[架构设计 · 接口与集成](../../../../../../设计/架构设计/06_架构设计_接口与集成.md)》「错误码」节

> 本文档依《文档生成规范》编写
