# 异常体系与统一响应测试记录

> 项目骨架 · 02 后端基座 · 03 core 横切基座 · 01 异常体系与统一响应 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-3-1 异常体系与统一响应](../02_后端基座_03_core横切基座_01_异常体系与统一响应.md) › 01 测试　|　[← 父任务](../02_后端基座_03_core横切基座_01_异常体系与统一响应.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 异常体系与统一响应](../02_后端基座_03_core横切基座_01_异常体系与统一响应.md) |
| 对应需求 | [02-9](../../../../../需求/02_需求_后端基座.md#r02-9) |
| 详细设计 | [01_详细设计_01_异常体系与统一响应](../设计/01_详细设计_01_异常体系与统一响应.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_异常体系与统一响应.md) |
| 测试日期 | 2026-09-13 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv） |
| Kiwi 用例 | 20 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 20 | 异常体系 | 单元 | `tests/core/test_exceptions.py` | `BizError` 属性；`BizError` 继承 `BaseObject` 且 `to_dict` 可用；`__str__` 回落 i18n key；7 子类码位（引用 `ErrorCode`）与 `http_status` |
| 20 | 错误码基座 | 单元 | `…::test_error_segments` | `ErrorSegment` 段位与平台码位一致 |
| 20 | 统一响应模型 | 单元 | `…::test_api_response_ok` | 默认 `{0, ok, null}`；`ok(data)` |
| 20 | 业务异常处理器 | 接口 | `tests/api/test_errors.py::test_not_found_returns_unified_response` | demo 详情不存在 → 404 + `code=10002`、`data=null` |
| 20 | 参数校验处理器 | 接口 | `…::test_validation_error_returns_param_error` | `name` 空 → `code=10001` 统一结构 |
| 20 | 请求 id | 接口 | `…::test_request_id_header_echoed` | `X-Request-Id` 回写响应头 |
| 20 | 未捕获异常 | 接口 | `…::test_uncaught_exception_returns_500_without_stack` | 500 + `code=10000`，响应不含堆栈细节 |
| 1–10 | demo CRUD / 根路由回归 | 接口 | `tests/api/*` | 统一响应结构下 CRUD 与根路由全绿 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
103 passed, 2 skipped in 0.52s

$ uv run ruff check .
All checks passed!

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增异常体系与处理器用例通过；全量 103 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 未捕获异常用例类型检查告警 | `fastapi.testclient.TestClient` 类型信息不全 | 改用 `httpx.ASGITransport`（`raise_app_exceptions=False`） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 1077 语句，覆盖率 100%**（任务级快照；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 真实 Redis 集成用例（2 条）需 `BMS_TEST_REDIS_URL`，本机未配置即跳过，随 04_02（三库集成与流水线收口）重验证层启用。
- 认证 / 权限错误码实现与测试随对应阶段（已登记：阶段一计划「后续阶段待办」表）；trace_id 链路追踪随日志体系（已登记：02-3-4 任务内容）。

> 本文档依《文档生成规范》编写
