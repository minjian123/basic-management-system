# 健康检查项注册表基座测试记录

> 项目骨架 · 02 后端基座 · 03 core 横切基座 · 12 健康检查项注册表基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-3-12 健康检查项注册表基座](../02_后端基座_03_core横切基座_12_健康检查项注册表基座.md) › 01 测试　|　[← 父任务](../02_后端基座_03_core横切基座_12_健康检查项注册表基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [12 健康检查项注册表基座](../02_后端基座_03_core横切基座_12_健康检查项注册表基座.md) |
| 对应需求 | [02-39](../../../../../需求/02_需求_后端基座.md#r02-39) |
| 详细设计 | [12_详细设计_01_健康检查项注册表基座](../设计/12_详细设计_01_健康检查项注册表基座.md) |
| 实施记录 | [12 实施记录](../实施/12_实施_01_健康检查项注册表基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 45（平台登记：分类「平台骨架」、P2、CONFIRMED、标签「自动化」；待回填） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 45 | 注册表契约继承与能力域标识 | 单元 | `tests/health/test_health_registry.py::test_inheritance_and_key` | `NullHealthCheckRegistry → BaseHealthCheckRegistry → BaseCapability → BaseObject`；`placeholder` / `describe()`；`key == "health_check_registry"` |
| 45 | 检查项契约可引用 | 单元 | `…::test_base_health_check_contract` | 实现 `name` + 异步 `check()` 返回 `HealthCheckResult`；`BaseHealthCheck → BaseObject` |
| 45 | 依赖清单单向复用 | 单元 | `…::test_dependencies_reuse` | `health.DEPENDENCIES is fallback.DEPENDENCIES`（同一对象、七项） |
| 45 | 结果 / 报告数据契约 | 单元 | `…::test_result_and_report_contract` | `detail` / `items` 默认值；frozen 不可赋值（`FrozenInstanceError`） |
| 45 | 聚合模板（全通过） | 单元 | `…::test_aggregate_all_healthy` | 顺序＝注册顺序、`healthy is True` |
| 45 | 聚合模板（含失败项） | 单元 | `…::test_aggregate_with_failure` | 一项未通过 → 总体 `False`、其余项仍在、`detail` 保留 |
| 45 | 聚合模板（单项抛异常） | 单元 | `…::test_aggregate_handles_exception` | 该项 `healthy=False` / `detail="检查执行异常"`（不泄露异常）、不中断其他项 |
| 45 | 占位固定通过（不探依赖） | 单元 | `…::test_null_registry_fixed_pass` | 注册被忽略、`checks() == ()`、`aggregate()` → `healthy=True` / `items=()` |
| 45 | 注册表依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullHealthCheckRegistry`；路由经 `Depends(get_health_check_registry)` 取到同一实例 |
| 45 | `/readyz` 占位就绪 | 集成（ASGI） | `tests/api/test_health.py::test_readyz_placeholder_is_ready` | GET `/readyz` → 200、`{"status":"ok","checks":[]}` |
| 45 | `/readyz` 未就绪映射 | 集成（ASGI） | `…::test_readyz_unhealthy_returns_503` | 替换注册表（含失败项）→ 503、`status="degraded"`、`checks` 明细 |
| 2 | `/healthz` 回归 | 集成（ASGI） | `…::test_healthz_returns_ok` | GET `/healthz` → 200、`{"status":"ok"}`（占位路由并存不互扰） |

回归范围：全量用例（含接口冒烟与 `/healthz` 存活探针）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
263 passed, 2 skipped in 0.93s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
159 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 45 用例 11 条全部通过；既有用例无回归（`/readyz` 加入后接口冒烟仍全绿）；全量 263 passed，2 skipped（真实 Redis 集成用例，环境未配置，随 04_02 重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| pytest 收集 `import file mismatch` | 能力域用例与 api 用例同名 `test_health.py` | 改名 `tests/health/test_health_registry.py`（详见[实施记录](../实施/12_实施_01_健康检查项注册表基座.md)「问题与处置」） |
| ruff `B010` | frozen 契约用例 `setattr` 常量字段名 | 改用变量字段名后 `setattr` |
| Kiwi 45 平台登记尚未完成 | 本地无 mjbk Kiwi TCMS 管理凭据 | 代码已以 `@pytest.mark.kiwi_id(45)` 关联；待用户在平台登记后回填本行 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 2529 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**均 100% 覆盖**：`app/health/base.py`（45 语句）、`app/api/health.py`（15 语句）。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实 DB / Redis / MinIO 探针（连通性、超时、探针缓存与频率限流、多副本去重、失败告警联动）→ **03_03 健康检查 / 可观测性 / 运维阶段**（计划「后续阶段待办」表「健康检查项真实实现」行）。
- Kiwi 平台登记：用例 45 待登记（见第 4 节）。
- **处理偏差与遗留（2026-09-14 闭环）**：遗留项去向已在计划 §3.1「健康检查项真实实现」行与需求 02-39 `> 注：` 块登记；消费方标注见[实施记录](../实施/12_实施_01_健康检查项注册表基座.md)「处理偏差与遗留」；唯一未闭环项为 Kiwi 45 平台登记。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成（除 Kiwi 45 平台登记待办）。

> 本文档依《文档生成规范》编写
