# 可观测性基座测试记录

> 项目骨架 · 02 后端基座 · 03 core 横切基座 · 11 可观测性基座（指标与链路） · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-3-11 可观测性基座](../02_后端基座_03_core横切基座_11_可观测性基座（指标与链路）.md) › 01 测试　|　[← 父任务](../02_后端基座_03_core横切基座_11_可观测性基座（指标与链路）.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [11 可观测性基座（指标与链路）](../02_后端基座_03_core横切基座_11_可观测性基座（指标与链路）.md) |
| 对应需求 | [02-26](../../../../../需求/02_需求_后端基座.md#r02-26) |
| 详细设计 | [11_详细设计_01_可观测性基座](../设计/11_详细设计_01_可观测性基座.md) |
| 实施记录 | [11 实施记录](../实施/11_实施_01_可观测性基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 44（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 44 | 指标契约继承与能力域标识 | 单元 | `tests/metrics/test_metrics.py::test_inheritance_and_key` | `NullMetrics → BaseMetrics → BaseCapability → BaseObject`；`placeholder` / `describe()`；`key == "metrics"` |
| 44 | 指标类型与候选名清单 | 单元 | `…::test_metric_kinds_and_names` | `METRIC_KINDS` 三项；`METRIC_NAMES` 六类无重复且全带 `bms_` 前缀 |
| 44 | 依赖清单单向复用 | 单元 | `…::test_dependencies_reuse` | `metrics.DEPENDENCIES is fallback.DEPENDENCIES`（同一对象、七项） |
| 44 | 占位恒定空操作 | 单元 | `…::test_null_metrics_is_noop` | 三入口（含 `labels` / `value`）均返回 `None`、不抛错 |
| 44 | 指标依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullMetrics`；路由经 `Depends(get_metrics)` 取到同一实例 |
| 44 | 链路契约继承与能力域标识 | 单元 | `tests/tracing/test_tracing.py::test_inheritance_and_key` | `NullTracer → BaseTracer → BaseCapability → BaseObject`；`key == "tracer"` |
| 44 | 请求头常量与 id 口径 | 单元 | `…::test_constants_and_id_generators` | `X-Trace-Id`；trace 32 位 / span 16 位 hex 且不重复 |
| 44 | 父子链与上下文贯穿 | 单元 | `…::test_span_chain_and_context` | 起点 `parent_span_id is None`；嵌套继承 `trace_id` 且记 `parent_span_id`；进入可见、退出复位 |
| 44 | 复用入站链路 id | 单元 | `…::test_span_reuses_inbound_trace_id` | 上下文已有 trace id 时 span 沿用（不被新建覆盖） |
| 44 | 异常路径复位 | 单元 | `…::test_span_resets_on_exception` | span 内抛错仍结束并复位（上下文不泄漏） |
| 44 | 链路依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | 接口内 span 的 trace id 与响应头 `X-Trace-Id` 一致（端到端贯穿） |
| 44 | 中间件生成与贯穿 | 集成（ASGI） | `tests/api/test_middleware.py::test_generates_trace_id_and_propagates_context` | 无入站头时响应头回写 32 位 hex，接口内上下文可见同一值 |
| 44 | 中间件回显入站头 | 集成（ASGI） | `…::test_echoes_inbound_trace_id` | 带入站 `X-Trace-Id` 时原样沿用并回写 |
| 44 | 中间件复位语义 | 集成（ASGI） | `…::test_resets_and_restores_previous_context` | 各请求链路 id 独立；请求结束恢复到中间件之前的值 |
| 44 | 非 HTTP 作用域直通 | 单元 | `…::test_non_http_scope_passthrough` | `lifespan` 作用域下游照常执行、不写链路上下文 |
| 44 | 链路上下文变量 | 单元 | `tests/core/test_context.py::test_trace_context` | `current_trace_id` / `current_span_id` 设置 / 读取 / 复位 |

回归范围：Kiwi 36（租户上下文，随本任务扩充链路断言）+ 全量用例（含接口冒烟，验证中间件不影响既有响应）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
252 passed, 2 skipped in 0.63s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
156 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 44 用例 16 条全部通过；既有用例无回归（中间件加入后接口冒烟仍全绿）；全量 252 passed，2 skipped（真实 Redis 集成用例，环境未配置，随 04_02 重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 首轮中间件覆盖率 90%（2 行未覆盖） | 非 HTTP 作用域直通分支无用例 | 补 `test_non_http_scope_passthrough`；覆盖率恢复 100%（详见[实施记录](../实施/11_实施_01_可观测性基座.md)「问题与处置」） |
| 首轮 pyright 1 项 `reportUnknownArgumentType` | 测试内 `send` 形参声明为 `object` | 形参改用 `starlette.types.Message`，移除 `pyright: ignore` |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 2471 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**均 100% 覆盖**：`app/metrics/base.py`、`app/tracing/base.py`、`app/api/middleware.py`、`app/core/context.py`。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实指标（`/metrics` 快照、分位、标签维度）、真实链路（采样、otel-collector 上报、出站透传）、日志 `trace_id` 关联（03_02）→ **分布式与监控 / 日志体系阶段**（计划「后续阶段待办」表「Prometheus 指标接入」「链路追踪与 OpenTelemetry 接入」两行）。
- 说明：占位链路生成的是本地 `uuid4` hex ID，真实 OTel 接入后 ID 来源不同，切换后链路不可跨阶段比对（已知、可接受，已在设计第 13 节登记）。
- **处理偏差与遗留（2026-09-14 闭环）**：真实指标 / 链路 / 日志关联用例去向已在计划 §3.1「Prometheus 指标接入」「链路追踪与 OpenTelemetry 接入」两行与需求 02-26 `> 注：` 块登记；消费方标注见[实施记录](../实施/11_实施_01_可观测性基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
