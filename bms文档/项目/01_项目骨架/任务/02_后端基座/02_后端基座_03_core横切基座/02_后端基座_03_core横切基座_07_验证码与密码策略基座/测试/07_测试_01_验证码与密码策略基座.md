# 验证码与密码策略基座测试记录

> 项目骨架 · 02 后端基座 · 03 core 横切基座 · 07 验证码与密码策略基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-3-7 验证码与密码策略基座](../02_后端基座_03_core横切基座_07_验证码与密码策略基座.md) › 01 测试　|　[← 父任务](../02_后端基座_03_core横切基座_07_验证码与密码策略基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [07 验证码与密码策略基座](../02_后端基座_03_core横切基座_07_验证码与密码策略基座.md) |
| 对应需求 | [02-31](../../../../../需求/02_需求_后端基座.md#r02-31) |
| 详细设计 | [07_详细设计_01_验证码与密码策略基座](../设计/07_详细设计_01_验证码与密码策略基座.md) |
| 实施记录 | [07 实施记录](../实施/07_实施_01_验证码与密码策略基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 41（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 41 | 验证码契约继承与能力域标识 | 单元 | `tests/captcha/test_captcha.py::test_inheritance_and_key` | `NullCaptcha → BaseCaptcha → BaseCapability → BaseObject`；`placeholder` 标记；`key == "captcha"` |
| 41 | 验证码 key / TTL / 场景常量 | 单元 | `…::test_key_ttl_and_scenes` | `build_captcha_key("u1") == "bms:global:captcha:u1"`；`CAPTCHA_TTL == 300`；`CAPTCHA_SCENES` 三项 |
| 41 | 挑战值对象 | 单元 | `…::test_challenge_value_object` | `captcha_id` 非空、`image == b""`、`expires_in == CAPTCHA_TTL`、`scene` 透传、frozen 不可变（赋值抛 `FrozenInstanceError`） |
| 41 | 占位恒定通过 | 单元 | `…::test_null_captcha_always_passes` | 任意编号与验证码（含空串）均返回 `True`（不连 Redis） |
| 41 | 密码策略契约继承与能力域标识 | 单元 | `tests/password/test_password.py::test_inheritance_and_key` | `NullPasswordPolicy → BasePasswordPolicy → BaseCapability → BaseObject`；`key == "password_policy"` |
| 41 | 违规原因码清单 | 单元 | `…::test_violation_codes` | `PASSWORD_VIOLATIONS` 七项、无重复、均非空 |
| 41 | 占位恒定允许 | 单元 | `…::test_null_policy_always_allows` | `validate` 返回空元组（含空密码、带用户名）；`expired`（一年前改密、注入 `now`）`False`；`reused`（有 / 无历史）`False` |
| 41 | 依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves`（两文件各一条） | `create_app()` 装配 `NullCaptcha` / `NullPasswordPolicy`；路由经 `Depends(get_captcha)` / `Depends(get_password_policy)` 取到实例；`api.deps` 可导出两提供者 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
209 passed, 2 skipped in 0.56s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
133 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 41 用例（9 条）通过；既有 200 条用例无回归；全量 209 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 无 | 首轮四门禁一次全绿（含 `frozen` 值对象的 pyright 忽略注释写法一次到位） | 无需处置 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 2120 语句，覆盖率 99%**。

- 本任务新增模块（`app/captcha/`、`app/password/`）**100% 覆盖**。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实实现（Pillow 出图、Redis 存取与单次失效、sys_config 策略规则、`pwd_history` 比对、90 天有效期）→ **四 认证与安全**；验证码强制策略与账号锁定 → 登录侧 / 任务调度；登录失败限流与计数窗口 → 02-3-10（任务文档已标注联动口径）——均登记在计划「后续阶段待办」表（「验证码与密码策略真实实现」行及相关行）。
- 真实 Pillow / Redis 集成用例已登记计划「后续阶段待办」表「Redis 集成用例」行（验证码出处 02-3-7），随重验证层 Redis job 执行（需 `BMS_TEST_REDIS_URL`），与既有 2 条 Redis 跳过用例同口径。
- 结论：本任务无未闭环遗留（遗留项均已在计划「后续阶段待办」表登记去向）。

> 本文档依《文档生成规范》编写
