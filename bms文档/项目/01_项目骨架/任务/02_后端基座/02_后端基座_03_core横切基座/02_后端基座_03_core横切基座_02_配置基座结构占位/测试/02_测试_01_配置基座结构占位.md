# 配置基座结构占位测试记录

> 项目骨架 · 02 后端基座 · 03 core 横切基座 · 02 配置基座结构占位 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-3-2 配置基座结构占位](../02_后端基座_03_core横切基座_02_配置基座结构占位.md) › 01 测试　|　[← 父任务](../02_后端基座_03_core横切基座_02_配置基座结构占位.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02 配置基座结构占位](../02_后端基座_03_core横切基座_02_配置基座结构占位.md) |
| 对应需求 | [02-10](../../../../../需求/02_需求_后端基座.md#r02-10) |
| 详细设计 | [02_详细设计_01_配置基座结构占位](../设计/02_详细设计_01_配置基座结构占位.md) |
| 实施记录 | [02 实施记录](../实施/02_实施_01_配置基座结构占位.md) |
| 测试日期 | 2026-09-13 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv） |
| Kiwi 用例 | 21 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 21 | 默认值与分区 | 单元 | `tests/core/test_config.py::test_defaults_and_sections` | 八分区齐备；`app` / `server` / `log` / `database` / `redis` / `minio` / `security` / `cors` 默认值 |
| 21 | 嵌套默认不共享 | 单元 | `…::test_nested_defaults_not_shared` | `allow_origins` / `pool` / `replicas` 不共享可变默认 |
| 21 | 读取接口单例 | 单元 | `…::test_get_settings_singleton` | `get_settings()` 单例；`cache_clear()` 后重载 |
| 21 | 取值校验 | 单元 | `…::test_env_validation` | `env` 合法三值通过；非法值 `ValidationError` |
| 21 | 序列化 | 单元 | `…::test_serialization` | `Settings().to_dict()` 含八分区；`to_json` 稳定序 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
112 passed, 2 skipped in 0.34s

$ uv run ruff check .
All checks passed!

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增配置基座用例通过；全量 112 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 非法 `env` 用例类型检查告警 | `Literal` 约束下直接传非法字面量 | 改用 `model_validate` 触发运行时校验 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 1163 语句，覆盖率 100%**（任务级快照；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 真实读取（文件 / 环境变量 / 分层 / 启动校验）的测试归 03-1；本任务只覆盖占位结构与默认值。
- 真实 Redis 集成用例（2 条）需 `BMS_TEST_REDIS_URL`，本机未配置即跳过，随 04_02 重验证层启用。

> 本文档依《文档生成规范》编写
