# 降级与熔断基座测试记录

> 项目骨架 · 02 后端基座 · 03 core 横切基座 · 08 降级与熔断基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-3-8 降级与熔断基座](../02_后端基座_03_core横切基座_08_降级与熔断基座.md) › 01 测试　|　[← 父任务](../02_后端基座_03_core横切基座_08_降级与熔断基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [08 降级与熔断基座](../02_后端基座_03_core横切基座_08_降级与熔断基座.md) |
| 对应需求 | [02-32](../../../../../需求/02_需求_后端基座.md#r02-32) |
| 详细设计 | [08_详细设计_01_降级与熔断基座](../设计/08_详细设计_01_降级与熔断基座.md) |
| 实施记录 | [08 实施记录](../实施/08_实施_01_降级与熔断基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 42（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 42 | 降级契约继承与能力域标识 | 单元 | `tests/fallback/test_fallback.py::test_inheritance_and_key` | `NullFallbackPolicy → BaseFallbackPolicy → BaseCapability → BaseObject`；`placeholder` 标记；`key == "fallback"` |
| 42 | 依赖清单与动作枚举 | 单元 | `…::test_dependencies_and_actions` | `DEPENDENCIES` 七项且无重复；`FallbackAction` 四成员取值 `raise` / `default` / `skip` / `degrade` |
| 42 | 占位不降级 | 单元 | `…::test_null_policy_never_degrades` | 清单内各依赖、未知依赖、带 `exc` 场景均返回 `FallbackAction.RAISE` |
| 42 | 降级依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullFallbackPolicy`；路由经 `Depends(get_fallback_policy)` 取到实例并返回 `raise` 动作 |
| 42 | 熔断契约继承与能力域标识 | 单元 | `tests/circuit/test_circuit.py::test_inheritance_and_key` | `NullCircuitBreaker → BaseCircuitBreaker → BaseCapability → BaseObject`；`key == "circuit_breaker"` |
| 42 | 三态枚举与清单单向复用 | 单元 | `…::test_state_enum_and_shared_dependencies` | `CircuitState` 三态取值；`circuit.base.DEPENDENCIES is fallback.base.DEPENDENCIES`（同一对象） |
| 42 | 占位恒定闭合 | 单元 | `…::test_null_breaker_always_closed` | 清单内各依赖 `allow` 恒 `True`、`state` 恒 `closed`；`record_failure` / `record_success` 后状态不变且仍放行 |
| 42 | 熔断依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullCircuitBreaker`；路由经 `Depends(get_circuit_breaker)` 取到实例并返回 `closed` 状态 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
217 passed, 2 skipped in 0.57s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
139 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 42 用例（8 条）通过；既有 209 条用例无回归；全量 217 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 熔断用例初版含未定义依赖参数 | 起草时误留占位参数与未使用导入 | 重写用例（移除多余参数、改模块级别名做清单同一性断言），首轮 `ruff` / `pytest` 即通过（详见[实施记录](../实施/08_实施_01_降级与熔断基座.md)「问题与处置」） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 2186 语句，覆盖率 99%**。

- 本任务新增模块（`app/fallback/`、`app/circuit/`）**100% 覆盖**。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实降级矩阵与真实熔断（阈值触发、半开恢复、跨实例一致）→ **性能与安全 · 分布式与监控**（计划「降级与熔断真实实现」行）；告警联动 → 02-3-11 可观测性基座（任务文档已标注协同契约）；限流降级 → 02-3-10（任务文档已标注降级动作取用口径）。
- 结论：本任务遗留已全部登记去向（收尾「处理偏差与遗留」步骤复核）。

> 本文档依《文档生成规范》编写
