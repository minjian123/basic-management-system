# 日志基座接入点测试记录

> 项目骨架 · 02 后端基座 · 03 core 横切基座 · 04 日志基座接入点 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-3-4 日志基座接入点](../02_后端基座_03_core横切基座_04_日志基座接入点.md) › 01 测试　|　[← 父任务](../02_后端基座_03_core横切基座_04_日志基座接入点.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [04 日志基座接入点](../02_后端基座_03_core横切基座_04_日志基座接入点.md) |
| 对应需求 | [02-12](../../../../../需求/02_需求_后端基座.md#r02-12) |
| 详细设计 | [04_详细设计_01_日志基座接入点](../设计/04_详细设计_01_日志基座接入点.md) |
| 实施记录 | [04 实施记录](../实施/04_实施_01_日志基座接入点.md) |
| 测试日期 | 2026-09-13 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv） |
| Kiwi 用例 | 23 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 23 | 门面继承 / 工厂 | 单元 | `tests/core/test_logging.py::test_inheritance_and_factory` | `StdoutLogger → BaseLogger → BaseObject`；`get_logger()` 返回 `BaseLogger` |
| 23 | 初始化幂等 / 级别 | 单元 | `…::test_configure_logging_idempotent_and_level` | 重复 `configure_logging` handler 数不变；级别取 `settings.log.level` |
| 23 | 各方法 stdout 输出 | 单元 | `…::test_methods_output_to_stdout` | `debug` / `info` / `warning` / `error` / `exception` 均输出 stdout、不落文件 |
| 23 | 上下文绑定 | 单元 | `…::test_bind_context` | `bind` 返回新门面，绑定字段只影响新实例 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
121 passed, 2 skipped in 0.45s

$ uv run ruff check .
All checks passed!

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增日志基座用例通过；全量 121 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 无 | — | 用例一次通过 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 1243 语句，覆盖率 100%**（任务级快照；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- structlog JSON / 上下文 / 脱敏 / 请求中间件的测试归 03-2；`trace_id` 链路追踪随日志体系 / OpenTelemetry。
- 真实 Redis 集成用例（2 条）需 `BMS_TEST_REDIS_URL`，本机未配置即跳过，随 04_02 重验证层启用。

> 本文档依《文档生成规范》编写
