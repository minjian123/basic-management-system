# 权限校验基座测试记录

> 项目骨架 · 02 后端基座 · 03 core 横切基座 · 09 权限校验基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-3-9 权限校验基座](../02_后端基座_03_core横切基座_09_权限校验基座.md) › 01 测试　|　[← 父任务](../02_后端基座_03_core横切基座_09_权限校验基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [09 权限校验基座](../02_后端基座_03_core横切基座_09_权限校验基座.md) |
| 对应需求 | [02-24](../../../../../需求/02_需求_后端基座.md#r02-24) |
| 详细设计 | [09_详细设计_01_权限校验基座](../设计/09_详细设计_01_权限校验基座.md) |
| 实施记录 | [09 实施记录](../实施/09_实施_01_权限校验基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | **39**（复用，履历 [02-3-5](../../02_后端基座_03_core横切基座_05_数据脱敏基座/02_后端基座_03_core横切基座_05_数据脱敏基座.md)；平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过（本任务不新增用例，无新增 / 修改测试代码） |

## 2. 测试范围与用例 <a id="scope"></a>

本任务为核销式交付，契约代码与用例同源于 02-3-5（同一交付批次），**复用既有 Kiwi 39 的权限部分 5 条**，不重复登记：

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 39 | 权限契约继承与能力域标识 | 单元 | `tests/permission/test_permission.py::test_inheritance_and_key` | `NullPermissionChecker → BasePermissionChecker → BaseCapability → BaseObject`；`BaseNullObject` 标记；`key == "permission"` |
| 39 | 占位恒定允许 | 单元 | `…::test_null_checker_allows` | `placeholder is True`；`describe()` 含「占位实现」；`check("data:plain") is True`；`require("data:plain")` 不抛错 |
| 39 | 强制校验失败语义 | 单元 | `…::test_require_raises_permission_error` | 拒绝实现下 `require` 抛 `PermissionError`，`code == 30001`、`http_status == 403` |
| 39 | 依赖工厂放行（集成） | 集成（ASGI） | `…::test_require_permission_dependency_allows` | `require_permission("data:plain")` 放行 200；`Depends(get_permission_checker)` 取到实例（`key == "permission"`） |
| 39 | 依赖工厂拒绝（集成） | 集成（ASGI） | `…::test_require_permission_dependency_denies` | 拒绝实现下 403；统一响应体 `code == 30001`、`data is None` |

回归范围：全量用例（含 Kiwi 13 序列化用例——02-3-5 掩码接入点的既有行为不变）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
217 passed, 2 skipped in 0.80s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
139 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：Kiwi 39 权限部分 5 条全部通过；全量 217 passed，2 skipped（真实 Redis 集成用例，环境未配置，随 04_02 重验证层启用）；本任务零代码 / 零用例改动，无回归。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 无新增问题 | 本任务零代码改动、用例复用 | 02-3-5 实施期的「同步生成器依赖破坏上下文变量」问题已在当时定位并改为异步生成器依赖（见[02-3-5 实施记录](../../02_后端基座_03_core横切基座_05_数据脱敏基座/实施/05_实施_01_数据脱敏基座.md)），本次复跑无复现 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 2186 语句，覆盖率 99%**。

- 权限能力域 `app/permission/`（`__init__.py` / `base.py`）**100% 覆盖**（自有用例覆盖契约、提供者与依赖工厂两侧分支）。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实 RBAC 判定（角色并集、主体链中间实体）、权限分层推导（菜单 / 表单 / 业务）、字段权限 `visible` / `editable`、权限码集合缓存失效 → **五 RBAC**（计划「后续阶段待办」表「权限分层真实实现」「`data:plain` 真实权限查询与 RBAC 主体链」两行）；接口侧逐接口接入的用例随各模块实现阶段补充。
- 结论：本任务遗留已全部登记去向（收尾「处理偏差与遗留」步骤复核）。

> 本文档依《文档生成规范》编写
