# 对象存储扩展基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 18 对象存储扩展基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-18 对象存储扩展基座](../02_后端基座_04_跨阶段基座_18_对象存储扩展基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_18_对象存储扩展基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02-4-18 对象存储扩展基座](../02_后端基座_04_跨阶段基座_18_对象存储扩展基座.md) |
| 对应需求 | [02-45](../../../../../需求/02_需求_后端基座.md#r02-45) |
| 详细设计 | [18_详细设计_01_对象存储扩展基座](../设计/18_详细设计_01_对象存储扩展基座.md) |
| 实施记录 | [18 实施记录](../实施/18_实施_01_对象存储扩展基座.md) |
| 测试日期 | 2026-09-20 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 845（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 845 | 契约继承与能力域标识 | 单元 | `tests/storage/test_multipart.py::test_contract_inheritance_and_identity` | `BaseMultipartUpload → BasePluggable → BaseCapability`；`NullMultipartUpload → BaseNullObject`；`key` / `plugin_key` / `placeholder` / `describe()` |
| 845 | 常量清单 | 单元 | `…::test_constants` | `DEFAULT_PART_SIZE == 5 * 1024 * 1024`、`NULL_UPLOAD_ID == "null-upload-id"` |
| 845 | 数据契约与取值校验 | 单元 | `…::test_data_contracts_defaults_and_validation` | 四契约字段与默认值（`mime` / `part_size` / `content_type` / `etag` / `stored_at`）；`size=0` / `part_size=0` 拒 |
| 845 | 占位初始化会话 | 单元 | `…::test_null_initiate_session` | 会话标识递增（`null-upload-id-1/2/3`）；`key` 回显；分片粒度缺省 5MB 与入参覆盖；`total_parts` 取整 |
| 845 | 占位分片 / 合并 / 取消 | 单元 | `…::test_null_part_complete_abort` | 分片回显（`part_no` / `etag` / `size`）；`complete` 回显会话 key / size；`complete` 与 `abort` 后会话失效 |
| 845 | 占位异常处置 | 单元 | `…::test_null_session_errors` | 未知会话（`upload_part` / `complete` / `abort`）与越界分片（0 / `total_parts + 1`）均抛 `NotFoundError` |
| 845 | 占位秒传恒定未命中 | 单元 | `…::test_null_check_always_miss` | `check` 恒返回 `None` |
| 845 | 依赖解析与插件清单 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app` + lifespan 装配 `NullMultipartUpload`；`resolve_plugin` 同一实例；`/api/v1/plugins` 该能力 provider `null`、实现仅 `null` |
| 845 | 占位路由与异常分支 | 集成（ASGI） | `…::test_placeholder_routes` | 初始化会话 / 表单分片上传 / 越界分片 404 / 合并回显 / 重复合并 404 / 取消 `data: null` / 秒传未命中 `data: null` / 非法 `sha256` 与 `size=0` 与非法请求体 10001 |
| 845 | 幂等接线 | 集成（ASGI） | `…::test_placeholder_route_idempotency` | 无幂等键不触达幂等基座；带键经 `begin` / `save`（键为 `bms:global:idem:…`）；重复请求经 `load` 复用首次会话 |
| 845 | 秒传命中响应映射 | 集成（ASGI） | `…::test_dedup_route_maps_hit` | 命中时路由把既有对象引用映射为命中响应（含 `stored_at` 序列化） |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest --cov=app -q
653 passed, 8 skipped in 6.85s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
330 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ python3 scripts/tools/base-check/check-base.py .
[base-integrity] 通过：无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名。

$ python3 scripts/tools/base-check/check-backend-base.py .
[check-backend-base] 通过：继承链 / 错误码段位 / 迁移链全部对齐。
```

结果汇总：新增 Kiwi 845 用例 11 条全部通过；前置契约适配（`test_router_base.py` 补 `file`、`test_plugin_registration.py` 补 `multipart_upload`）通过。8 skipped 为真实 Redis 集成用例（环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 2 项前置契约断言失败 | 新增 `file` 路由与 `multipart_upload` 能力域，登记表键 / 端口清单增长 | 按预期适配（[实施记录](../实施/18_实施_01_对象存储扩展基座.md)「问题与处置」），不改断言语义 |
| 秒传命中分支在占位实现下不可达 | `NullMultipartUpload.check` 恒未命中（占位口径） | 以依赖覆盖注入「命中替身」用例覆盖路由映射分支（`…::test_dedup_route_maps_hit`） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 5870 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**100% 覆盖**：`app/storage/base.py`、`app/storage/null.py`、`app/schemas/storage.py`、`app/api/file.py`。
- `app/storage/minio.py` 未覆盖 6 行为**既有**代码的真实 SDK 导入与异常续抛分支（本任务未改动，随文件管理阶段真实实现用例补）。
- 整体未覆盖 22 行均为既有代码占位分支（本任务未改动）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实分片托管与秒传判定 / 会话落库与状态机 / 孤儿回收 / 类型与大小校验 / 配额联动（文件管理阶段）、幂等真实去重（通用能力阶段）、登录态鉴权与文件权限码（认证 / RBAC / 文件管理阶段）→ 见计划 §3.1 与需求 02-45 `> 注：` 块。
- Kiwi 平台登记：已完成（2026-09-20 于 mjbk Kiwi TCMS 登记用例 845）。
- 占位期已知项：内存会话表无 TTL / 无上限、`check` 恒未命中——占位语义，非缺陷。
- **处理偏差与遗留**：真实实现 / 幂等 / 鉴权去向已在计划 §3.1 与需求 02-45 `> 注：` 块登记；消费方见[实施记录](../实施/18_实施_01_对象存储扩展基座.md)「偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
