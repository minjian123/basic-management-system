# 文件存储基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 01 文件存储基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-1 文件存储基座](../02_后端基座_04_跨阶段基座_01_文件存储基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_01_文件存储基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 文件存储基座](../02_后端基座_04_跨阶段基座_01_文件存储基座.md) |
| 对应需求 | [02-17](../../../../../需求/02_需求_后端基座.md#r02-17) |
| 详细设计 | [01_详细设计_01_文件存储基座](../设计/01_详细设计_01_文件存储基座.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_文件存储基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 47（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 47 | 存储契约继承与能力域标识 | 单元 | `tests/storage/test_storage.py::test_inheritance_and_key` | `NullObjectStorage → BaseObjectStorage → BaseCapability → BaseObject`；`placeholder` / `describe()`；`key == "object_storage"` |
| 47 | 常量清单 | 单元 | `…::test_constants` | `STORAGE_BUCKET == "bms-files"`；`DEFAULT_PRESIGN_TTL == 3600` |
| 47 | 数据契约默认值与不可变 | 单元 | `…::test_data_contracts_defaults_and_frozen` | `StoredObject`（`content_type` / `etag` 默认 None）/ `PresignedUrl`（`method` 默认 GET）；frozen（`FrozenInstanceError`） |
| 47 | 占位 put 回显 | 单元 | `…::test_null_put_echoes` | `StoredObject`（key / size=len(data) / content_type / `etag == NULL_ETAG`） |
| 47 | 占位 get / exists / delete | 单元 | `…::test_null_get_exists_delete` | `get` 返回 `b""`；`exists` 恒 True；`delete` 返回 None |
| 47 | 占位 presign 固定返回 | 单元 | `…::test_null_presign_fixed` | `PresignedUrl`（url 为 `NULL_PRESIGNED_URL`；expires_in / method 回显） |
| 47 | 存储依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullObjectStorage`；路由经 `Depends(get_object_storage)` 取到同一实例 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
278 passed, 2 skipped in 0.96s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
165 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 47 用例 7 条全部通过；既有用例无回归（装配对象存储后接口冒烟仍全绿）；全量 278 passed，2 skipped（真实 Redis 集成用例，环境未配置，随 04_02 重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| `ruff` `E501`（`deps.py` docstring 超长） | 补「对象存储」后超 120 列 | 改写为两行 docstring（详见[实施记录](../实施/01_实施_01_文件存储基座.md)「问题与处置」） |
| `NotFoundError` 抽象方法仅文档引用 | import 会触发 `F401` | 不在模块内 import，真实实现抛出（docstring 注明语义） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 2637 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**均 100% 覆盖**：`app/storage/base.py`（52 语句）、`app/storage/__init__.py`。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实对象存储（MinIO / 本地文件系统、预签名 URL）、分片上传与断点续传、类型 / 大小 / SHA256 / 配额校验、孤儿分片回收 → **文件管理阶段 / 多租户路由 / 任务调度**（计划「后续阶段待办」表「文件存储真实实现」行）。
- Kiwi 平台登记：已完成（2026-09-14 于 mjbk Kiwi TCMS 登记用例 47）。
- 结论：本任务遗留已全部登记去向（收尾「处理偏差与遗留」步骤复核）。

> 本文档依《文档生成规范》编写
