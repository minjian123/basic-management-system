# 租户自助与品牌基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 24 租户自助与品牌基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-24 租户自助与品牌基座](../02_后端基座_04_跨阶段基座_24_租户自助与品牌基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_24_租户自助与品牌基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02-4-24 租户自助与品牌基座](../02_后端基座_04_跨阶段基座_24_租户自助与品牌基座.md) |
| 对应需求 | [02-51](../../../../../需求/02_需求_后端基座.md#r02-51) |
| 详细设计 | [24_详细设计_01_租户自助与品牌基座](../设计/24_详细设计_01_租户自助与品牌基座.md) |
| 实施记录 | [24 实施记录](../实施/24_实施_01_租户自助与品牌基座.md) |
| 测试日期 | 2026-09-20 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 891（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」；登记前实测平台最新号 890） |
| 结论 | 通过（678 passed, 8 skipped, 0 failed） |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 891 | 契约继承与能力域标识 | 单元 | `tests/tenant/test_tenant_self_service.py::test_contract_inheritance_and_identity` | `BaseTenantSelfService → BasePluggable → BaseCapability`；`NullTenantSelfService → BaseNullObject`；`placeholder` / `key` / `plugin_key` |
| 891 | 常量与数据源键助手 | 单元 | `…::test_constants_and_db_key` | 主题模式 / 租户状态 / 切换生效方式取值；默认品牌主色；`build_tenant_db_key` 派生 |
| 891 | 数据契约字段与缺省 | 单元 | `…::test_data_contract_defaults` | 摘要五字段；概览缺省（空列表 / 无当前租户 / 非多租户）；切换结果缺省（生效 / `token` / 不重发 / 无令牌）；品牌缺省（默认主色 / `system` / 允许强调色 / 不禁暗色） |
| 891 | 占位「我的租户」 | 单元（异步） | `…::test_null_my_tenants_fixed_single_tenant` | 固定单租户、当前租户固定演示租户（忽略入参） |
| 891 | 占位切换 | 单元（异步） | `…::test_null_switch_echoes_without_side_effect` | 恒定回显（编码原样 / 数据源键派生）、零副作用、未知编码不抛错且结果一致 |
| 891 | 占位品牌 | 单元（异步） | `…::test_null_brand_platform_default` | 固定平台默认品牌；带 / 不带编码结果一致 |
| 891 | 依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | 应用装配占位实现；`resolve_plugin` 取到同一实例；装配 provider 为 `null` |
| 891 | 当前租户编码助手 | 单元 | `…::test_current_code_of_helper` | 有上下文取编码；解析链豁免路径（无上下文）为空 |
| 891 | 登录口径 | 单元（路由结构） | `…::test_route_auth_scope` | 路由依赖计数：我的租户 / 切换挂登录依赖、品牌免登录 |
| 891 | 占位路由：我的租户 | 集成（ASGI） | `…::test_placeholder_route_my_tenants` | 概览回显（单租户 / 当前租户 / 非多租户） |
| 891 | 占位路由：切换 | 集成（ASGI） | `…::test_placeholder_route_switch` | 切换结果回显字段；空编码 → 业务码 10001；无幂等键亦可用 |
| 891 | 切换幂等复用 | 集成（ASGI，替身注入） | `…::test_switch_idempotency_reuses_first_result` | 同键二次提交复用首次结果；幂等键为 `bms:demo:idem:k-1`（作用域绑当前租户位） |
| 891 | 占位路由：品牌（免登录） | 集成（ASGI） | `…::test_placeholder_route_brand_without_auth` | 品牌回显；带 / 不带编码结果一致 |

回归范围：全量用例（含接口冒烟与前置契约断言）。测试替身口径：幂等基座替身仅按鸭子类型提供三方法、**不继承能力端口**（避免以占位名污染进程级注册表）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest --cov=app -q
678 passed, 8 skipped in 7.71s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
339 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ python3 scripts/tools/base-check/check-base.py
[base-integrity] 通过：无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名。

$ python3 scripts/tools/base-check/check-backend-base.py
[check-backend-base] 通过：继承链 / 错误码段位 / 迁移链全部对齐。
```

结果汇总：新增 Kiwi 891 用例 13 条全部通过；前置契约适配（`test_router_base.py` / `test_plugin_registration.py` 补 `tenant` / `tenant_self_service`）通过；02-4-23 记录的既有失败 `tests/crosscut/test_m3_closure.py` **未复现**。8 skipped 为需真实 Redis 的集成用例（环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 用例文件同名冲突（`import file mismatch`） | 测试目录无 `__init__.py`，basename 需全库唯一，与既有 `tests/db/test_tenant.py` 撞名 | 重命名为 `test_tenant_self_service.py`；Kiwi 891 用例文本同步更新（见[实施记录](../实施/24_实施_01_租户自助与品牌基座.md)「问题与处置」） |
| 空编码断言走的是契约校验而非手工分支 | 请求契约 `str_strip_whitespace` + 最小长度 1 在契约层拦截，经全局校验处理器收口 10001 | 断言保持不变（业务码 10001），手工分支已移除并记入实施记录 |
| 2 项前置契约断言失败 | 新增能力域致登记表键 / 端口清单增长 | 按预期适配（[实施记录](../实施/24_实施_01_租户自助与品牌基座.md)「问题与处置」） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 6182 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**100% 覆盖**：`app/tenant/base.py`、`app/tenant/null.py`、`app/tenant/__init__.py`、`app/schemas/tenant.py`、`app/api/tenant.py`。
- 整体未覆盖 22 行均为既有代码占位分支（本任务未改动，随对应回补阶段）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实取数 / 角色名（RBAC 阶段）、令牌与会话换发（认证阶段）、品牌来源与平台超管租户管理（租户管理阶段）→ 见计划 §3.1 与需求 02-51 `> 注：` 块。
- Kiwi 平台登记：已完成（2026-09-20 于 mjbk Kiwi TCMS 登记用例 891，并为文件重命名同步更新用例文本）。
- 切换幂等复用：占位幂等存储恒定首次、不缓存，复用语义经**替身注入**验证；真实复用（Redis 去重 + 首次结果存取）随性能与安全阶段生效，无需改调用面。
- **处理偏差与遗留（2026-09-20 闭环）**：真实实现 / 令牌换发 / 品牌来源去向已在计划 §3.1 与需求 02-51 `> 注：` 块登记；消费方 `08_03_02` 见[实施记录](../实施/24_实施_01_租户自助与品牌基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
