# 出站集成基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 06 出站集成基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-6 出站集成基座](../02_后端基座_04_跨阶段基座_06_出站集成基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_06_出站集成基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [06 出站集成基座](../02_后端基座_04_跨阶段基座_06_出站集成基座.md) |
| 对应需求 | [02-22](../../../../../需求/02_需求_后端基座.md#r02-22) |
| 详细设计 | [06_详细设计_01_出站集成基座](../设计/06_详细设计_01_出站集成基座.md) |
| 实施记录 | [06 实施记录](../实施/06_实施_01_出站集成基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 52（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 52 | 两契约继承与能力域标识 | 单元 | `tests/outbound/test_outbound.py::test_inheritance_and_keys` | 两契约继承链；`placeholder` / `describe()`；`key` 为 `http_client` / `webhook_sender` |
| 52 | 常量清单 | 单元 | `…::test_constants` | `DEFAULT_TIMEOUT == 10`；`DEFAULT_MAX_RETRIES == 3` |
| 52 | 数据契约默认值与不可变 | 单元 | `…::test_data_contracts_defaults_and_frozen` | `WebhookResult`（`status_code` None / `attempts` 1）；`HttpResponse` 字段；frozen（`FrozenInstanceError`） |
| 52 | 占位 HTTP 固定成功 | 单元 | `…::test_null_http_client_fixed` | `HttpResponse`（`status_code == 200`、空 body） |
| 52 | 占位 Webhook 固定成功 | 单元 | `…::test_null_webhook_sender_fixed` | `WebhookResult`（`delivered is True`、`status_code == 200`、`attempts == 1`） |
| 52 | 两提供者依赖解析 | 集成（ASGI） | `…::test_dependency_providers_resolve` | `create_app()` 装配两占位单例；路由经两提供者取到同一实例 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
307 passed, 2 skipped in 1.01s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
180 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 52 用例 6 条全部通过；既有用例无回归（装配出站基座后接口冒烟仍全绿）；全量 307 passed，2 skipped（真实 Redis 集成用例，环境未配置，随 04_02 重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

无阻塞问题；`ruff` / `format` / `pyright` 首轮全绿。

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 2892 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**均 100% 覆盖**：`app/outbound/http.py`（25 语句）、`app/outbound/webhook.py`（23 语句）、`app/outbound/__init__.py`。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实 httpx 封装（超时 / 重试 / 熔断）、Webhook 签名与退避重试 / 失败记录 / 异步推送 → **系统集成阶段**（计划「后续阶段待办」表「出站集成真实实现」行）。
- Kiwi 平台登记：已完成（2026-09-14 于 mjbk Kiwi TCMS 登记用例 52）。
- **处理偏差与遗留（2026-09-14 闭环）**：真实 httpx / 超时重试熔断 / Webhook 签名与退避用例去向已在计划 §3.1「出站集成真实实现」行与需求 02-22 `> 注：` 块登记；消费方标注见[实施记录](../实施/06_实施_01_出站集成基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
