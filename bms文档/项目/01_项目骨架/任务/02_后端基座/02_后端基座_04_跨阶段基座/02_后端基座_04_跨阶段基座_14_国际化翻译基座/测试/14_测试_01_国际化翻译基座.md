# 国际化翻译基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 14 国际化翻译基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-14 国际化翻译基座](../02_后端基座_04_跨阶段基座_14_国际化翻译基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_14_国际化翻译基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [14 国际化翻译基座](../02_后端基座_04_跨阶段基座_14_国际化翻译基座.md) |
| 对应需求 | [02-37](../../../../../需求/02_需求_后端基座.md#r02-37) |
| 详细设计 | [14_详细设计_01_国际化翻译基座](../设计/14_详细设计_01_国际化翻译基座.md) |
| 实施记录 | [14 实施记录](../实施/14_实施_01_国际化翻译基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 60（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 60 | 契约继承与能力域标识 | 单元 | `tests/i18n/test_i18n.py::test_inheritance_and_key` | `NullTranslator → BaseTranslator → BaseCapability → BaseObject`；`placeholder` / `describe()`；`key == "translator"` |
| 60 | 常量清单 | 单元 | `…::test_constants` | `DEFAULT_LOCALE == "zh-CN"`；`SUPPORTED_LOCALES` 两项无重复 |
| 60 | 占位固定返回 | 单元 | `…::test_null_translator_fixed` | `translate` 原样返回 key（含 locale / params）；`resolve_locale` 返默认；`load_messages` 空映射 |
| 60 | 翻译依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullTranslator`；路由经 `Depends(get_translator)` 取到同一实例 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
353 passed, 2 skipped in 1.18s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
206 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 60 用例 4 条全部通过；既有用例无回归（装配翻译器后接口冒烟仍全绿）；全量 353 passed，2 skipped（真实 Redis 集成用例，环境未配置，随 04_02 重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| `ruff format` 1 文件待格式化 | 新增用例多行调用换行 | `ruff format` 规整（详见[实施记录](../实施/14_实施_01_国际化翻译基座.md)「问题与处置」） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 3311 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**均 100% 覆盖**：`app/i18n/base.py`（27 语句）、`app/i18n/__init__.py`。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实语言包 / Redis 缓存 / Babel 提取 / i18n 附表 → **国际化阶段 / 前端组件体系**（计划「后续阶段待办」表「国际化翻译真实实现」行）。
- Kiwi 平台登记：已完成（2026-09-14 于 mjbk Kiwi TCMS 登记用例 60）。
- **处理偏差与遗留（2026-09-14 闭环）**：真实语言包 / 缓存 / Babel / i18n 附表用例去向已在计划 §3.1「国际化翻译真实实现」行与需求 02-37 `> 注：` 块登记；消费方见[实施记录](../实施/14_实施_01_国际化翻译基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
