# AI 对话基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 22 AI 对话基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-22 AI 对话基座](../02_后端基座_04_跨阶段基座_22_AI对话基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_22_AI对话基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02-4-22 AI 对话基座](../02_后端基座_04_跨阶段基座_22_AI对话基座.md) |
| 对应需求 | [02-49](../../../../../需求/02_需求_后端基座.md#r02-49) |
| 详细设计 | [22_详细设计_01_AI对话基座](../设计/22_详细设计_01_AI对话基座.md) |
| 实施记录 | [22 实施记录](../实施/22_实施_01_AI对话基座.md) |
| 测试日期 | 2026-09-20 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 820（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」；平台自增主键） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 820 | 契约继承与能力域标识 | 单元 | `tests/chat/test_chat.py::test_contract_inheritance_and_identity` | 三契约 → `BasePluggable` / `BaseCapability`；三空实现 → `BaseNullObject`；`key` / `plugin_key` |
| 820 | 常量与数据契约 | 单元 | `…::test_constants_and_data_contracts` | 事件类型 / 消息状态 / 四模式 / 开关键；`ChatStreamEvent` / `ChatSession` / `ChatSessionMessage` / `ChatActionResult` 字段集与默认值 |
| 820 | 空实现固定返回（流） | 单元 | `…::test_null_stream_fixed_returns` | 句柄 `stream_id` 固定；事件序列单条 `done`（占位文本）；`stop` False |
| 820 | 空实现固定返回（会话 / 动作） | 单元 | `…::test_null_session_store_and_action_gate` | 会话空 / None / False；确认撤销恒 `unknown` |
| 820 | 依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | 装配三空实现；提供者同一实例 |
| 820 | 占位路由 | 集成（ASGI） | `…::test_placeholder_routes` | stream 单条 done / stop false / 会话空 / 详情 null / 消息空 / 删除 false / 确认撤销 unknown |
| 820 | 表声明 | 单元 | `…::test_ai_chat_log_table_declaration` | `ai_chat_log` 表名与关键字段（`session_id` / `module` / `status` 等） |
| 820 | 内存实现 + 路由 | 集成（ASGI） | `…::test_routes_with_in_memory_implementations` | 会话列表 / 详情 / 消息 / 删除；`stop` 命中断流；确认前置与撤销状态 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest --cov=app -q
610 passed, 8 skipped in 5.28s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
312 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ python3 scripts/tools/base-check/check-base.py .
[base-integrity] 通过：无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名。

$ python3 scripts/tools/base-check/check-backend-base.py .
[check-backend-base] 通过：继承链 / 错误码段位 / 迁移链全部对齐。
```

结果汇总：新增 Kiwi 820 用例 8 条全部通过；前置契约适配（`test_router_base.py` 补 `chat`、`test_plugin_registration.py::_EXPECTED_PLUGIN_KEYS` 补三能力键）通过；全量无失败。8 skipped 为真实 Redis 集成用例（环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 路由 / 插件登记断言失败 | 新增 `chat` 路由与三能力域 | 按预期适配补登记项（[实施记录](../实施/22_实施_01_AI对话基座.md)「问题与处置」） |
| `check-backend-base.py` 报 `ChatStreamHandle` 未登记 | 直接继承 `BaseObject` 的句柄类 | 《后端基类清单》§9 / §10 补登记 |
| pyright strict 泛型未参数化 | `Field(default_factory=list)` | 改为 `list[<类型>]` 实参 |
| 占位分支（stop / 会话 / 动作）无成功路径覆盖 | 占位恒无命中 | 内存实现经 `dependency_overrides` 校正（停止语义 / 删除 / 确认前置） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 5262 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**100% 覆盖**：`app/chat/__init__.py`、`app/chat/base.py`、`app/chat/null.py`、`app/api/chat.py`、`app/schemas/chat.py`、`app/models/ai.py`。
- 整体未覆盖行为既有代码占位分支（本任务未改动，随对应回补阶段）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实流式传输 / 会话投影 / 确认撤销执行 / 开关读取 / `ai_chat_log` 建表分片 / prompt 注入防护与限流预算（AI 阶段）、权限码 `ai:chat`（RBAC 阶段）、登录态真实鉴权（认证阶段）→ 见计划 §3.1 与需求 02-49 `> 注：` 块。
- Kiwi 平台登记：已完成（2026-09-20 于 mjbk Kiwi TCMS 登记用例 820，与 `@pytest.mark.kiwi_id(820)` 一致）。
- **处理偏差与遗留（2026-09-20 闭环）**：真实实现 / 权限 / 鉴权去向已在计划 §3.1 与需求 02-49 `> 注：` 块登记；消费方见[实施记录](../实施/22_实施_01_AI对话基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
