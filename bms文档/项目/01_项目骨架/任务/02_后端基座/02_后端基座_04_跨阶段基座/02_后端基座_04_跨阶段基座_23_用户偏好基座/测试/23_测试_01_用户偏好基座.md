# 用户偏好基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 23 用户偏好基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-23 用户偏好基座](../02_后端基座_04_跨阶段基座_23_用户偏好基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_23_用户偏好基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02-4-23 用户偏好基座](../02_后端基座_04_跨阶段基座_23_用户偏好基座.md) |
| 对应需求 | [02-50](../../../../../需求/02_需求_后端基座.md#r02-50) |
| 详细设计 | [23_详细设计_01_用户偏好基座](../设计/23_详细设计_01_用户偏好基座.md) |
| 实施记录 | [23 实施记录](../实施/23_实施_01_用户偏好基座.md) |
| 测试日期 | 2026-09-20 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 780（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过（唯一失败为既有 `test_m3_closure.py`，非本次引入） |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 780 | 契约继承与标识 | 单元 | `tests/preference/test_preference.py::test_contract_inheritance_and_identity` | `BasePreferenceStore → BasePluggable → BaseCapability`；`NullPreferenceStore → BaseNullObject`；`placeholder` / `key` |
| 780 | 常量清单 | 单元 | `…::test_constants` | 分隔符 `.`、长度 128、值 65536、键数 200 |
| 780 | 键规范校验 | 单元 | `…::test_key_validation_and_domain` | 合法 / 非法键判定；`domain_of` 取首段 |
| 780 | 占位回退默认值 | 单元 | `…::test_null_store_falls_back_to_default` | `get` 回退 `None` / `default`；`set` 空操作；`reset` 恒 False |
| 780 | 依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullPreferenceStore`；`resolve_plugin("preference", None)` 取到同一实例 |
| 780 | 占位路由 | 集成（ASGI） | `…::test_placeholder_route` | PUT 回显 / GET 回退 `null` / DELETE `data:null` / 非法键 10001 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest --cov=app -q
581 passed, 8 skipped, 1 failed in 3.94s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
292 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ python3 scripts/tools/base-check/check-base.py .
[base-integrity] 通过：无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名。

$ python3 scripts/tools/base-check/check-backend-base.py .
[check-backend-base] 通过：继承链 / 错误码段位 / 迁移链全部对齐。
```

结果汇总：新增 Kiwi 780 用例 6 条全部通过；前置契约适配（`test_router_base.py` / `test_plugin_registration.py` 补 `preference`）通过。**唯一失败为既有 `tests/crosscut/test_m3_closure.py`**（阶段三「后端插件化」需求总览缺「M3 验收门禁」节，改动前即失败，与本任务无关）。8 skipped 为真实 Redis 集成用例（环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 全量 1 项失败：`test_m3_closure.py` | 既有：阶段三文档缺「M3 验收门禁」节 | 超出本任务范围，登记为既有遗留（见[实施记录](../实施/23_实施_01_用户偏好基座.md)第 6 节） |
| 2 项前置契约断言失败 | 新增 `preference` 能力域致登记表键 / 端口清单增长 | 按预期适配（[实施记录](../实施/23_实施_01_用户偏好基座.md)「问题与处置」） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 4748 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**100% 覆盖**：`app/preference/base.py`、`app/preference/null.py`、`app/schemas/preference.py`、`app/api/preference.py`。
- 整体未覆盖 22 行均为既有代码占位分支（本任务未改动，随对应回补阶段）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实持久化 / 缓存 / key 白名单 / 全量 / 批量接口（通用能力阶段）、登录态真实鉴权（认证阶段）、模型真实建表（数据库设计）→ 见计划 §3.1 与需求 02-50 `> 注：` 块。
- Kiwi 平台登记：已完成（2026-09-20 于 mjbk Kiwi TCMS 登记用例 780）。
- 既有失败项：`test_m3_closure.py`（阶段三文档口径），本任务不改。
- **处理偏差与遗留（2026-09-20 闭环）**：真实实现 / 鉴权 / 建表去向已在计划 §3.1 与需求 02-50 `> 注：` 块登记；消费方见[实施记录](../实施/23_实施_01_用户偏好基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
