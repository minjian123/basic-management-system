# 组织主数据查询基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 16 组织主数据查询基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-16 组织主数据查询基座](../02_后端基座_04_跨阶段基座_16_组织主数据查询基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_16_组织主数据查询基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [16 组织主数据查询基座](../02_后端基座_04_跨阶段基座_16_组织主数据查询基座.md) |
| 对应需求 | [02-43](../../../../../需求/02_需求_后端基座.md#r02-43) |
| 详细设计 | [16_详细设计_01_组织主数据查询基座](../设计/16_详细设计_01_组织主数据查询基座.md) |
| 实施记录 | [16 实施记录](../实施/16_实施_01_组织主数据查询基座.md) |
| 测试日期 | 2026-09-20 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 843（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 843 | 契约继承与能力域标识 | 单元 | `tests/org/test_org.py::test_contract_inheritance_and_identity` | 两契约 → `BasePluggable` / `BaseCapability`；两空实现 → `BaseNullObject`；`key` / `plugin_key`（`org_data_source` / `org_name_resolver`）/ `placeholder` / `describe()` |
| 843 | 常量与数据契约 | 单元 | `…::test_constants_and_data_contracts` | 三常量取值；四数据契约字段集与默认值；`OrgUser.masked_fields == {"phone","email"}`；`OrgDept` 嵌套树 |
| 843 | 组织错误码登记 | 单元 | `…::test_org_error_codes_registered` | `ErrorCode` `30101`~`30103` 取值正确 |
| 843 | 空实现固定 / 批量返回 | 单元 | `…::test_null_fixed_and_batch_returns` | users / posts 固定单条页（page / size 回显）；dept_tree 两节点树；resolve_names 按 ids 批量 |
| 843 | 依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配两空实现；`resolve_plugin(...)` 同一实例 |
| 843 | 占位路由 | 集成（ASGI） | `…::test_placeholder_routes` | 四路径固定返回；dept-tree 返回列表（不分页）；`id_in` 非法值 → 10001 |
| 843 | 内存实现 + 路由 | 集成（ASGI） | `…::test_routes_with_in_memory_implementations` | 关键字 / 部门 / 含子级 / 状态 / 分页参数透传；部门树、批量回显经内存实现 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
629 passed, 8 skipped in 5.91s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
322 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ python3 scripts/tools/base-check/check-base.py .
[base-integrity] 通过：无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名。

$ python3 scripts/tools/base-check/check-backend-base.py .
[check-backend-base] 通过：继承链 / 错误码段位 / 迁移链全部对齐。
```

结果汇总：新增 Kiwi 843 用例 7 条全部通过；既有用例无回归（装配组织能力域后接口冒烟仍全绿）；全量 629 passed，8 skipped（真实 Redis / MinIO 等集成用例，环境未配置，随重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 护栏用例 3 条失败 | 能力域插件键 / 路由键清单未含新能力域 | 清单补 `org_data_source` / `org_name_resolver` / `org`（详见[实施记录](../实施/16_实施_01_组织主数据查询基座.md)「问题与处置」） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 5543 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**均 100% 覆盖**：`app/org/base.py`（71 语句）、`app/org/null.py`（24 语句）、`app/org/__init__.py`、`app/api/org.py`（32 语句）。
- 未覆盖 22 行均为既有代码占位分支（`app/tasks/base.py`、`app/repositories/*` 等，本任务未改动，随对应阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实组织取数 / 数据范围过滤 / 脱敏规则 / 权限码 → **RBAC / 认证与安全 / 组织模块阶段**（计划 §3.1「组织主数据真实实现」行）。
- Kiwi 平台登记：已完成（2026-09-20 于 mjbk Kiwi TCMS 登记用例 843）。
- **处理偏差与遗留（2026-09-20 闭环）**：真实取数 / 数据范围 / 脱敏用例去向已在计划 §3.1「组织主数据真实实现」行与需求 02-43 `> 注：` 块登记；消费方见[实施记录](../实施/16_实施_01_组织主数据查询基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
