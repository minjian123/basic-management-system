# 首页工作台卡片提供者基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 15 首页工作台卡片提供者基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-15 首页工作台卡片提供者基座](../02_后端基座_04_跨阶段基座_15_首页工作台卡片提供者基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_15_首页工作台卡片提供者基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [15 首页工作台卡片提供者基座](../02_后端基座_04_跨阶段基座_15_首页工作台卡片提供者基座.md) |
| 对应需求 | [02-38](../../../../../需求/02_需求_后端基座.md#r02-38) |
| 详细设计 | [15_详细设计_01_首页工作台卡片提供者基座](../设计/15_详细设计_01_首页工作台卡片提供者基座.md) |
| 实施记录 | [15 实施记录](../实施/15_实施_01_首页工作台卡片提供者基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 61（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 61 | 契约继承与能力域标识 | 单元 | `tests/dashboard/test_dashboard.py::test_inheritance_and_keys` | `BaseDashboardCardProvider → BaseObject`、注册表继承链；`placeholder` / `describe()`；`key == "dashboard_card_registry"` |
| 61 | 常量清单 | 单元 | `…::test_constants` | `CARD_TYPES == ("builtin", "dataset")` |
| 61 | 注册表模板解析与未命中 | 单元 | `…::test_registry_template_resolution` | 注册后 `metadata` / `fetch` 委托回结果、`keys` 顺序＝注册顺序；未命中抛 `NotFoundError` |
| 61 | 占位空卡片集 | 单元 | `…::test_null_registry_empty_cards` | 注册空操作、`get` None、`keys` 空；`metadata` / `fetch` 固定空映射 |
| 61 | 工作台依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullDashboardCardRegistry`；路由经 `Depends(get_dashboard_card_registry)` 取到同一实例 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
358 passed, 2 skipped in 1.15s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
209 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 61 用例 5 条全部通过；既有用例无回归（装配工作台卡片注册表后接口冒烟仍全绿）；全量 358 passed，2 skipped（真实 Redis 集成用例，环境未配置，随 04_02 重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| `ruff format` 1 文件待格式化 | 新增用例多行调用换行 | `ruff format` 规整（详见[实施记录](../实施/15_实施_01_首页工作台卡片提供者基座.md)「问题与处置」） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 3362 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**均 100% 覆盖**：`app/dashboard/base.py`（48 语句）、`app/dashboard/__init__.py`。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实卡片注册 / 模板解析 / 布局缓存 / 内置卡转发 / 数据集卡 → **首页工作台阶段 / 报表 BI / 前端组件体系**（计划「后续阶段待办」表「首页工作台卡片真实实现」行）。
- Kiwi 平台登记：已完成（2026-09-14 于 mjbk Kiwi TCMS 登记用例 61）。
- 结论：本任务遗留已全部登记去向（收尾「处理偏差与遗留」步骤复核）。

> 本文档依《文档生成规范》编写
