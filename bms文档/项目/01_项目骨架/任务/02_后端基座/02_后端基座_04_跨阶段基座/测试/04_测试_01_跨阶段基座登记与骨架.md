# 跨阶段基座登记与骨架测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 测试记录

[文档首页](../../../../../../文档首页.md) › [02-4 跨阶段基座](../02_后端基座_04_跨阶段基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [04 跨阶段基座](../02_后端基座_04_跨阶段基座.md) |
| 对应需求 | [02-13](../../../../需求/02_需求_后端基座.md#r02-13) |
| 详细设计 | [04_详细设计_01_跨阶段基座登记与骨架](../设计/04_详细设计_01_跨阶段基座登记与骨架.md) |
| 实施记录 | [04 实施记录](../实施/04_实施_01_跨阶段基座登记与骨架.md) |
| 测试日期 | 2026-09-13 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv） |
| Kiwi 用例 | 24 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 24 | 缓存基座 | 单元 | `tests/crosscut/test_cross_phase_bases.py::test_cache_region` | key 规范（租户 / global）、TTL 默认、收写删、版本陈旧判定 |
| 24 | 数据范围 | 单元 | `…::test_data_scope_and_repository_hook` | `NullDataScope` 无过滤 / 恒允许；`_apply_data_scope` 挂载 |
| 24 | 分片路由 | 单元 | `…::test_sharding_router_and_repository_hook` | `NullShardingRouter` 返回逻辑表；注入路由返回物理表 |
| 24 | 事件基座 | 单元 | `…::test_event_base` | 信封字段 / 序列化；发布（含事务）与消费契约 |
| 24 | 任务基座 + 模型 | 单元 | `…::test_task_base_and_models` | `BaseTask` 契约；`sys_task` / `sys_task_log` 建表写入（SQLite） |
| 24 | 审计基座 | 单元 | `…::test_audit_base` | `FieldChange` 字段；`AuditCapturer` 契约与事件生成 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
127 passed, 2 skipped in 0.51s

$ uv run ruff check .
All checks passed!

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增六类骨架用例通过；全量 127 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| pyright 未知类型告警 | `EventEnvelope.payload` 默认工厂未标注泛型 | 实施记录 §4 已记；改 `dict[str, object]` |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 1359 语句，覆盖率 100%**（任务级快照；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 六类真实实现的集成测试随回补阶段；本阶段仅契约层单测。
- 真实 Redis 集成用例（2 条）需 `BMS_TEST_REDIS_URL`，本机未配置即跳过，随 04_02 重验证层启用。

> 本文档依《文档生成规范》编写
