# 全文检索查询契约基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 21 全文检索查询契约基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-21 全文检索查询契约基座](../02_后端基座_04_跨阶段基座_21_全文检索查询契约基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_21_全文检索查询契约基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02-4-21 全文检索查询契约基座](../02_后端基座_04_跨阶段基座_21_全文检索查询契约基座.md) |
| 对应需求 | [02-48](../../../../../需求/02_需求_后端基座.md#r02-48) |
| 详细设计 | [21_详细设计_01_全文检索查询契约基座](../设计/21_详细设计_01_全文检索查询契约基座.md) |
| 实施记录 | [21 实施记录](../实施/21_实施_01_全文检索查询契约基座.md) |
| 测试日期 | 2026-09-20 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 821（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」；平台自增主键） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 821 | 契约继承与能力域标识 | 单元 | `tests/globalsearch/test_global_search.py::test_contract_inheritance_and_identity` | 三契约 → `BasePluggable` / `BaseCapability`；三空实现 → `BaseNullObject`；`key` / `plugin_key` |
| 821 | 常量与数据契约 | 单元 | `…::test_constants_and_data_contracts` | 可检索域 / 降级原因 / 默认条数 / 页深上限；三类结果字段集与降级三字段默认值 |
| 821 | 错误码登记 | 单元 | `…::test_search_error_codes_registered` | `ErrorCode` `10101`~`10107` 取值 |
| 821 | 空实现固定返回 | 单元 | `…::test_null_fixed_returns_with_degrade` | 域空 / 三类空结果 + `degraded=True` + 原因与替代入口 |
| 821 | 依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | 装配三空实现；提供者同一实例 |
| 821 | 占位路由 | 集成（ASGI） | `…::test_placeholder_routes` | global / logs / files 空 + 降级；logs 缺时间范围 `code=10001` |
| 821 | 内存实现 + 路由 | 集成（ASGI） | `…::test_routes_with_in_memory_implementations` | 多域分组 / 域过滤 / 审计命中 / 文件命中 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest --cov=app -q
620 passed, 8 skipped in 5.80s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
317 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ python3 scripts/tools/base-check/check-base.py .
[base-integrity] 通过：无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名。

$ python3 scripts/tools/base-check/check-backend-base.py .
[check-backend-base] 通过：继承链 / 错误码段位 / 迁移链全部对齐。
```

结果汇总：新增 Kiwi 821 用例 7 条全部通过；前置契约适配（`test_router_base.py` 补 `search`、`test_plugin_registration.py::_EXPECTED_PLUGIN_KEYS` 补三能力键）通过；全量无失败。8 skipped 为真实 Redis 集成用例（环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 路由 / 插件登记断言失败 | 新增 `search` 路由与三能力域 | 按预期适配补登记项（[实施记录](../实施/21_实施_01_全文检索查询契约基座.md)「问题与处置」） |
| 缺时间范围未返回 422 | 全局处理器把请求校验失败转统一响应 | 断言改判 `code == 10001`（与既有接口口径一致） |
| 占位恒空导致成功路径无覆盖 | 占位三方法恒空 | 内存实现经 `dependency_overrides` 校正（分组 / 过滤 / 命中） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 5409 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**100% 覆盖**：`app/globalsearch/__init__.py`、`app/globalsearch/base.py`、`app/globalsearch/null.py`、`app/api/search.py`。
- 整体未覆盖行为既有代码占位分支（本任务未改动，随对应回补阶段）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实 ES 索引 / 查询 DSL / 多域聚合 / 权限并集过滤 / 跨月路由 / 降级兜底 / 限深限流（全文检索阶段）、文件解析与重解析（全文检索 + 文件管理阶段）、权限码 `log:query` / `file:query`（RBAC 阶段）、登录态真实鉴权（认证阶段）→ 见计划 §3.1 与需求 02-48 `> 注：` 块。
- Kiwi 平台登记：已完成（2026-09-20 于 mjbk Kiwi TCMS 登记用例 821，与 `@pytest.mark.kiwi_id(821)` 一致）。
- **处理偏差与遗留（2026-09-20 闭环）**：真实实现 / 权限 / 鉴权去向已在计划 §3.1 与需求 02-48 `> 注：` 块登记；消费方见[实施记录](../实施/21_实施_01_全文检索查询契约基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
