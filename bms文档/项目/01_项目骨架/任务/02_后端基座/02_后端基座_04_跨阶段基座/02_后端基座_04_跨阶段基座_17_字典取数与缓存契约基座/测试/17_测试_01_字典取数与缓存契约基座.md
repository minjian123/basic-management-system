# 字典取数与缓存契约基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 17 字典取数与缓存契约基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-17 字典取数与缓存契约基座](../02_后端基座_04_跨阶段基座_17_字典取数与缓存契约基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_17_字典取数与缓存契约基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [17 字典取数与缓存契约基座](../02_后端基座_04_跨阶段基座_17_字典取数与缓存契约基座.md) |
| 对应需求 | [02-44](../../../../../需求/02_需求_后端基座.md#r02-44) |
| 详细设计 | [17_详细设计_01_字典取数与缓存契约基座](../设计/17_详细设计_01_字典取数与缓存契约基座.md) |
| 实施记录 | [17 实施记录](../实施/17_实施_01_字典取数与缓存契约基座.md) |
| 测试日期 | 2026-09-20 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 844（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 844 | 契约继承与能力域标识 | 单元 | `tests/dict/test_dict.py::test_contract_inheritance_and_identity` | 三契约 → `BasePluggable` / `BaseCapability`；`DictCacheRegion` → `CacheRegion` 且 `domain=dict`；三空实现 → `BaseNullObject`；`key` / `plugin_key` / `placeholder` / `describe()` |
| 844 | 常量与数据契约 | 单元 | `…::test_constants_and_data_contracts` | 四常量取值；`DictItem` 字段集与默认；三参数对象字段与默认 locale；结果契约默认（`items=None` / `has_more=False`） |
| 844 | 字典错误码登记 | 单元 | `…::test_dict_error_codes_registered` | `ErrorCode` `40101`~`40103` 取值正确 |
| 844 | 空实现固定 / 批量 / 翻译 | 单元 | `…::test_null_by_type_fixed_version_and_filters`、`…::test_null_batch_and_translate` | 版本一致 `items=None`；按值 / 关键字 / 级联 / 上限过滤与 `has_more`；空字典 `()`；批量按类型与版本一致；翻译按 values 占位 |
| 844 | 缓存 key 与版本判定 | 单元 | `…::test_cache_region_keys_and_version` | `dict_key`（租户 / 全局）/ `version_key` 拼接；`is_version_current`；空缓存 `get` / `set` / `delete` / `get_global_version` |
| 844 | 依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | 应用装配三空实现；`resolve_plugin(...)` 与三提供者解析到同一实例 |
| 844 | 占位路由 | 集成（ASGI） | `…::test_placeholder_routes` | `GET /dicts/{type}` 固定返回 / 版本一致 `items=null` / 按值过滤；`POST /dicts/batch` 按类型与版本一致 |
| 844 | 内存实现 + 路由 | 集成（ASGI） | `…::test_routes_with_in_memory_implementations` | 参数透传（`version` / `keyword` / `parent_id` / `values` / `limit`）；版本比对；批量 locale |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
641 passed, 8 skipped in 6.33s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
327 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ python3 scripts/tools/base-check/check-base.py .
[base-integrity] 通过：无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名。

$ python3 scripts/tools/base-check/check-backend-base.py .
[check-backend-base] 通过：继承链 / 错误码段位 / 迁移链全部对齐。
```

结果汇总：新增 Kiwi 844 用例 9 条全部通过；既有用例无回归（装配字典能力域后接口冒烟仍全绿）；全量 641 passed，8 skipped（真实 Redis / MinIO 等集成用例，环境未配置，随重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 护栏用例 3 条失败 | 能力域插件键 / 路由键清单未含新能力域 | 清单补 `dict_source` / `dict_translator` / `dict_cache_region` / `dict`（详见[实施记录](../实施/17_实施_01_字典取数与缓存契约基座.md)「问题与处置」） |
| 新增模块覆盖率初测 96% | 翻译 / 缓存提供者与空缓存写删无路由触达 | 依赖解析用例显式调用三提供者 + 缓存用例补断言，回到 100%（同上） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 5704 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**均 100% 覆盖**：`app/dict/base.py`（89 语句）、`app/dict/null.py`（46 语句）、`app/dict/__init__.py`、`app/api/dict.py`（41 语句）。
- 未覆盖 22 行均为既有代码占位分支（`app/tasks/base.py`、`app/repositories/*` 等，本任务未改动，随对应阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实字典取数 / 缓存 / 写路径 / 后端翻译 / 高级查询 / 权限码 / 错误码抛错 → **通用能力·字典模块阶段 / 通用能力阶段 / RBAC 阶段**（计划 §3.1「字典真实取数与缓存」行）。
- Kiwi 平台登记：已完成（2026-09-20 于 mjbk Kiwi TCMS 登记用例 844）。
- **处理偏差与遗留（2026-09-20 闭环）**：真实取数 / 缓存 / 翻译 / 高级查询用例去向已在计划 §3.1「字典真实取数与缓存」行与需求 02-44 `> 注：` 块登记；消费方见[实施记录](../实施/17_实施_01_字典取数与缓存契约基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
