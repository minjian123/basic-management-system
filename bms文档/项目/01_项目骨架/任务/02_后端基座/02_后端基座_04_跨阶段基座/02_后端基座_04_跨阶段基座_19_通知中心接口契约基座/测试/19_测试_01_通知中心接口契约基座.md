# 通知中心接口契约基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 19 通知中心接口契约基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-19 通知中心接口契约基座](../02_后端基座_04_跨阶段基座_19_通知中心接口契约基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_19_通知中心接口契约基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02-4-19 通知中心接口契约基座](../02_后端基座_04_跨阶段基座_19_通知中心接口契约基座.md) |
| 对应需求 | [02-46](../../../../../需求/02_需求_后端基座.md#r02-46) |
| 详细设计 | [19_详细设计_01_通知中心接口契约基座](../设计/19_详细设计_01_通知中心接口契约基座.md) |
| 实施记录 | [19 实施记录](../实施/19_实施_01_通知中心接口契约基座.md) |
| 测试日期 | 2026-09-20 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 812（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」；预估号 782 已被自动导入用例占用，按平台自增主键取 812） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 812 | 契约继承与能力域标识 | 单元 | `tests/notification/test_notification.py::test_contract_inheritance_and_identity` | `BaseNotificationCenter → BasePluggable → BaseCapability`；`NullNotificationCenter → BaseNullObject`；`key` |
| 812 | 数据契约与事件负载 | 单元 | `…::test_data_contracts_and_event_payload` | `Notification` 默认值；`NotificationPayload` 五字段；`NOTIFICATION_NEW_EVENT` |
| 812 | 占位固定返回 | 单元 | `…::test_null_center_fixed_returns` | `list` 空分页（回显 page/size）/ `detail` None / `unread_count` 0 / `mark_read` 0 / `mark_all_read` 0 / `delete` False |
| 812 | 依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | 装配 `NullNotificationCenter`；提供者同一实例 |
| 812 | 占位路由 | 集成（ASGI） | `…::test_placeholder_routes` | 列表空分页 / 筛选 JSON / 非法 filters 10001 / 未读 0 / 详情 null / 已读 0 / 全部已读 0 / 删除 false |
| 812 | 成功路径与角标口径（内存实现） | 集成（ASGI） | `…::test_routes_with_in_memory_center` | 查看即已读后未读递减 / `mark_read` 返回操作后总数 / `mark_all_read` 归零 / `delete` 后详情 null |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest --cov=app -q
599 passed, 8 skipped in 4.79s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
305 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ python3 scripts/tools/base-check/check-base.py .
[base-integrity] 通过：无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名。

$ python3 scripts/tools/base-check/check-backend-base.py .
[check-backend-base] 通过：继承链 / 错误码段位 / 迁移链全部对齐。
```

结果汇总：新增 Kiwi 812 用例 6 条全部通过；前置契约适配（`test_router_base.py` 补 `notification`、`test_plugin_registration.py::_EXPECTED_PLUGIN_KEYS` 补 `notification_center`）通过；全量无失败。8 skipped 为真实 Redis 集成用例（环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| Kiwi 预估号 782 被占用 | 平台自动导入用例占位（pk 782 起） | 先登记取得 812 再回填（[实施记录](../实施/19_实施_01_通知中心接口契约基座.md)「问题与处置」、计划 §3 偏差列） |
| pyright strict `item` 类型未知 | `json.loads` 返回 `Any` | `cast("list[object]", items)` 后再校验 |
| 占位中心下路由成功分支未覆盖 | 占位恒空 / None | 内存测试中心经 `dependency_overrides` 校正（查看即已读 / 角标口径） |
| `_parse_filters` 错误分支未覆盖 | 正常路径不触发 | 补非法 JSON / 非数组断言（10001），覆盖至 100% |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 5037 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**100% 覆盖**：`app/notification/__init__.py`、`app/notification/base.py`、`app/notification/null.py`、`app/api/notification.py`。
- 整体未覆盖行为既有代码占位分支（本任务未改动，随对应回补阶段）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：`sys_notification` 取数落库 / `notification.new` 推送触发 / 渠道偏好过滤与按 locale 渲染 / 按用户加锁与幂等（通知公告阶段）、动作权限码（RBAC 阶段）、登录态真实鉴权（认证阶段）→ 见计划 §3.1 与需求 02-46 `> 注：` 块。
- Kiwi 平台登记：已完成（2026-09-20 于 mjbk Kiwi TCMS 登记用例 812）。
- **处理偏差与遗留（2026-09-20 闭环）**：真实实现 / 权限 / 鉴权去向已在计划 §3.1 与需求 02-46 `> 注：` 块登记；消费方见[实施记录](../实施/19_实施_01_通知中心接口契约基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
