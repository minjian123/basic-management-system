# 表单字段类型注册表基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 13 表单字段类型注册表基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-13 表单字段类型注册表基座](../02_后端基座_04_跨阶段基座_13_表单字段类型注册表基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_13_表单字段类型注册表基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [13 表单字段类型注册表基座](../02_后端基座_04_跨阶段基座_13_表单字段类型注册表基座.md) |
| 对应需求 | [02-36](../../../../../需求/02_需求_后端基座.md#r02-36) |
| 详细设计 | [13_详细设计_01_表单字段类型注册表基座](../设计/13_详细设计_01_表单字段类型注册表基座.md) |
| 实施记录 | [13 实施记录](../实施/13_实施_01_表单字段类型注册表基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 59（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 59 | 契约继承与能力域标识 | 单元 | `tests/fieldtype/test_fieldtype.py::test_inheritance_and_keys` | `BaseFieldType → BaseObject`、注册表继承链；`placeholder` / `describe()`；`key == "field_type_registry"` |
| 59 | 常量清单 | 单元 | `…::test_constants` | `COLUMN_TYPE_DIALECTS` 四项无重复；`NULL_COLUMN_TYPE == "varchar(255)"` |
| 59 | 注册表模板解析与未命中 | 单元 | `…::test_registry_template_resolution` | 注册后 `validate` / `column_type` 委托回结果、`keys` 顺序＝注册顺序；未命中抛 `NotFoundError` |
| 59 | 占位恒定通过 / 固定映射 | 单元 | `…::test_null_registry_fixed` | 注册空操作、`get` None、`keys` 空；`validate` 恒空违规、`column_type` 恒 `NULL_COLUMN_TYPE` |
| 59 | 字段类型依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullFieldTypeRegistry`；路由经 `Depends(get_field_type_registry)` 取到同一实例 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
349 passed, 2 skipped in 1.12s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
204 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 59 用例 5 条全部通过；既有用例无回归（装配字段类型注册表后接口冒烟仍全绿）；全量 349 passed，2 skipped（真实 Redis 集成用例，环境未配置，随 04_02 重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| `ruff` `E501`（fieldtype 签名 / deps docstring 超长） | 补「字段类型」后超 120 列 | `ruff format` 折行 + docstring 拆行（详见[实施记录](../实施/13_实施_01_表单字段类型注册表基座.md)「问题与处置」） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 3281 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**均 100% 覆盖**：`app/fieldtype/base.py`（52 语句）、`app/fieldtype/__init__.py`。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实字段类型校验 / 四库类型映射 / 动态 DDL → **表单定制阶段 / 数据访问与分片**（计划「后续阶段待办」表「字段类型注册表真实实现」行）。
- Kiwi 平台登记：已完成（2026-09-14 于 mjbk Kiwi TCMS 登记用例 59）。
- 结论：本任务遗留已全部登记去向（收尾「处理偏差与遗留」步骤复核）。

> 本文档依《文档生成规范》编写
