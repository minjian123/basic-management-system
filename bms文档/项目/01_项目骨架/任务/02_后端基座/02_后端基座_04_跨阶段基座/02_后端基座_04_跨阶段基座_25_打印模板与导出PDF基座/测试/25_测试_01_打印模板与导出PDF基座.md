# 打印模板与导出 PDF 基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 25 打印模板与导出 PDF 基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-25 打印模板与导出 PDF 基座](../02_后端基座_04_跨阶段基座_25_打印模板与导出PDF基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_25_打印模板与导出PDF基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02-4-25 打印模板与导出 PDF 基座](../02_后端基座_04_跨阶段基座_25_打印模板与导出PDF基座.md) |
| 对应需求 | [02-52](../../../../../需求/02_需求_后端基座.md#r02-52) |
| 详细设计 | [25_详细设计_01_打印模板与导出 PDF 基座](../设计/25_详细设计_01_打印模板与导出PDF基座.md) |
| 实施记录 | [25 实施记录](../实施/25_实施_01_打印模板与导出PDF基座.md) |
| 测试日期 | 2026-09-20 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 906（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」；登记前实测平台最新号 905——号段已被 CI 自动导入用例推进） |
| 提交 | 实施代码（`docs` / `feat` 与登记回写随本任务链条提交） |
| 结论 | 通过（698 passed, 8 skipped, 0 failed） |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 906 | 契约继承与能力域标识 | 单元 | `tests/print/test_print_exporter.py::test_contract_inheritance_and_identity` | 两契约 `→ BasePluggable → BaseCapability`；两占位 `→ BaseNullObject`；`placeholder` / `key` / `plugin_key` 一致 |
| 906 | 常量与产物对象 key 助手 | 单元 | `…::test_constants_and_object_key` | 五组取值集合取值；内容类型 / 缺省纸张 / 对象 key 域段 / 占位文案与占位取址 / 单据键缺省位；`build_print_object_key` 带租户与缺省（`global`）两种结果 |
| 906 | 数据契约字段与缺省 | 单元 | `…::test_data_contract_defaults` | 模板变量两字段；模板定义缺省（不限单据类型 / 空变量 / `active`）；单据数据缺省（单据键空 / 字段与明细为空）；渲染选项缺省（纸张 / 方向 / 色调 / 水印 / 打印人）；导出产物缺省（`size` 0 / PDF 内容类型 / 文件标识与取址空 / 有效期 0）；批量结果缺省（任务标识空 / 计数 0 / 明细为空） |
| 906 | 占位模板清单 | 单元（异步） | `…::test_null_template_list_is_empty` | 固定空清单；带 `biz_type` 与不带结果一致 |
| 906 | 占位模板详情 | 单元（异步） | `…::test_null_template_get_not_found` | 恒定未命中：码位 `50201` / HTTP 404 / 提示含模板键 |
| 906 | 占位单条导出 | 单元（异步） | `…::test_null_export_pdf_placeholder_artifact` | 对象 key 按助手派生、文件名 `{模板键}.pdf`、占位取址与占位文案、`size` 0、有效期取对象存储缺省；零副作用（连调两次一致）；传入渲染选项结果不变 |
| 906 | 占位单条导出（无单据键） | 单元（异步） | `…::test_null_export_pdf_without_biz_key` | 单据数据未带单据键时对象 key 末段回落缺省位 |
| 906 | 占位批量打印（两模式） | 单元（异步） | `…::test_null_batch_print_modes` | 空键 → 空汇总（计数 0 / 明细为空 / 任务标识空 / 占位文案）；逐份 → 明细条数与文件名逐键；合并 → 单条明细、以批内首个单据键为标识；两模式汇总计数一致 |
| 906 | 错误码与段位基 | 单元 | `…::test_error_codes_and_segment_base` | 三码位 `50201` ~ `50203` 与 HTTP 状态（404 / 404 / 200）；继承链 三子类 `→ PrintError → FileError → BizError`；段位归属文件段（万位 5） |
| 906 | 依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | 应用装配两占位实现；`resolve_plugin` 分别取到同一实例；装配 provider 两键均为 `null` |
| 906 | 登录口径 | 单元（路由结构） | `…::test_route_auth_scope` | 路由键 `print`；四路径集合；四路径各 1 个登录依赖 |
| 906 | 占位路由：单条导出 | 集成（ASGI） | `…::test_placeholder_route_export` | 产物字段回显（对象 key / 文件名 / 占位取址 / 内容类型 / 有效期 / 占位文案）；空模板键 → 业务码 10001；带渲染选项结果一致 |
| 906 | 占位路由：批量打印 | 集成（ASGI） | `…::test_placeholder_route_batch` | 汇总计数与逐份明细条数一致；空单据键集合 → 业务码 10001 |
| 906 | 导出幂等复用 | 集成（ASGI，替身注入） | `…::test_export_idempotency_reuses_first_result` | 同键二次提交复用首次结果；幂等键为 `bms:demo:idem:k-1`（作用域绑当前租户位） |
| 906 | 批量幂等（首次结果不可读） | 集成（ASGI，替身注入） | `…::test_batch_idempotency_without_cached_result_executes` | 非首次且首次结果不可读时不阻断，继续执行并登记键 `bms:demo:idem:k-2` |
| 906 | 批量幂等复用 | 集成（ASGI，替身注入） | `…::test_batch_idempotency_reuses_first_result` | 同键二次提交复用首次结果；幂等键为 `bms:demo:idem:k-3` |
| 906 | 占位路由：模板来源 | 集成（ASGI） | `…::test_placeholder_route_templates` | 清单为空（带 / 不带单据类型一致）；模板详情 404 且业务码 `50201` |
| 906 | 模板映射分支 | 集成（ASGI，替身注入） | `…::test_route_templates_mapping_with_stub_provider` | 替身来源返回模板时清单与详情字段一一对应（含变量清单） |

回归范围：全量用例（含接口冒烟与前置契约断言）。测试替身口径：幂等与模板来源替身仅按鸭子类型提供方法、**不继承能力端口**（避免以占位名污染进程级注册表）；「首次结果不可读」场景由独立替身（`begin` 恒非首次、`load` 恒空）驱动。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest --cov=app -q
698 passed, 8 skipped in 8.58s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
345 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ python3 scripts/tools/base-check/check-base.py
[base-integrity] 通过：无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名。

$ python3 scripts/tools/base-check/check-backend-base.py
[check-backend-base] 通过：继承链 / 错误码段位 / 迁移链全部对齐。

$ python3 scripts/tools/base-check/check-links.py
[check-links] 通过：全库相对链接无断链、无失效锚点。
```

结果汇总：新增 Kiwi 906 用例 18 条全部通过；前置契约适配（`test_router_base.py` / `test_plugin_registration.py` 补 `print` / `print_exporter` / `print_template`）通过；02-4-23 记录的既有失败 `tests/crosscut/test_m3_closure.py` **未复现**。8 skipped 为需真实 Redis 的集成用例（环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 用例中的 `ErrorCode.FILE` 断言失败 | 段位基 `FileError` 不占缺省码位（未新增 `50001`），`ErrorCode` 无该成员 | 改用段位枚举 `ErrorSegment.FILE` 校验万位归属（见[实施记录](../实施/25_实施_01_打印模板与导出PDF基座.md)「问题与处置」） |
| 覆盖率 2 处缺口（`app/api/print.py` 行 90 / 222） | 渲染选项映射非空分支与批量幂等复用分支未被触达 | 补两条用例（带渲染选项的导出请求、批量幂等复用），补后新增模块 100% |
| 2 项前置契约断言失败 | 新增能力域致登记表键 / 端口清单增长 | 按预期适配（键元组补 `print`、端口清单补两键），不改变断言语义 |
| 模板详情用例（404）与模板映射用例（非空清单）无法由同一占位实现覆盖 | 占位模板来源恒定空清单 / 恒定未命中，映射分支不可达 | 以替身注入（覆盖 `get_print_template_provider`）驱动非空清单路径，保证映射代码 100% 覆盖 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 6430 语句，覆盖率 99%**（未覆盖 24 行）。

- 本任务新增 / 改动模块**100% 覆盖**：`app/print/base.py`、`app/print/null.py`、`app/print/__init__.py`、`app/schemas/print.py`、`app/api/print.py`、`app/core/exceptions.py`、`app/core/error_codes.py`、`app/api/deps.py`、`app/api/router.py`、`app/core/config.py`。
- 整体未覆盖 24 行均为既有代码占位分支（本任务未改动，随对应回补阶段）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实 PDF 渲染与模板定义来源（通用能力 / 表单定制阶段）、产物真实落存储与限时取址（文件管理阶段）、批量超阈值转后台任务（通用能力阶段，任务调度）、权限码与审计留痕（RBAC / 审计阶段）→ 见计划 §3.1 与需求 02-52 `> 注：` 块。
- Kiwi 平台登记：已完成（2026-09-20 于 mjbk Kiwi TCMS 登记用例 906；登记前实测平台最新号 905，印证「CI 自动导入会推进号段」）。
- 幂等复用与模板映射：占位幂等存储恒定首次、不缓存，复用语义经**替身注入**验证；模板映射分支经**替身来源**驱动。真实复用（Redis 去重 + 首次结果存取）与真实模板来源随对应阶段生效，无需改调用面。
- **处理偏差与遗留（2026-09-20 闭环）**：真实实现 / 模板来源 / 产物落存储 / 批量后台任务去向已在计划 §3.1 与需求 02-52 `> 注：` 块登记；消费方 `08_03_03` 与上游登记项 ①③ 见[实施记录](../实施/25_实施_01_打印模板与导出PDF基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
