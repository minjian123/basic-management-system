# 列表查询与偏好契约基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 20 列表查询与偏好契约基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-20 列表查询与偏好契约基座](../02_后端基座_04_跨阶段基座_20_列表查询与偏好契约基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_20_列表查询与偏好契约基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02-4-20 列表查询与偏好契约基座](../02_后端基座_04_跨阶段基座_20_列表查询与偏好契约基座.md) |
| 对应需求 | [02-47](../../../../../需求/02_需求_后端基座.md#r02-47) |
| 详细设计 | [20_详细设计_01_列表查询与偏好契约基座](../设计/20_详细设计_01_列表查询与偏好契约基座.md) |
| 实施记录 | [20 实施记录](../实施/20_实施_01_列表查询与偏好契约基座.md) |
| 测试日期 | 2026-09-20 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 781（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过（唯一失败为既有 `test_m3_closure.py`，非本次引入） |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 781 | 筛选操作符复用 | 单元 | `tests/schemas/test_filters.py::test_filter_operators_reuse_scope` | `FILTER_OPERATORS == SCOPE_OPERATORS`（11 种）；默认 `eq` |
| 781 | 筛选序列化 | 单元 | `…::test_filter_serialization` | 等值 / 多值逗号 / 区间 / `is_null`·`is_not_null`；`serialize_filters` 合并 |
| 781 | 列表筛选请求 | 单元 | `…::test_base_filter_query_params` | `to_query_params` 合并 keyword；空关键字不传 |
| 781 | 列表偏好协议 | 单元 | `tests/listing/test_listing.py::test_list_preference_protocol` | `build_list_pref_key`；`LIST_DENSITIES`；`ListPreference` 默认值 |
| 781 | 查询方案契约 | 单元 | `…::test_query_scheme_contract` | 继承 / 标识 / 枚举取值 / `QueryScheme.name` |
| 781 | 占位实现 | 单元 | `…::test_null_store_fixed_returns` | 空 / None / 原样 / False |
| 781 | 依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | 装配 `NullQuerySchemeStore`；提供者同一实例 |
| 781 | 占位路由 | 集成（ASGI） | `…::test_placeholder_routes` | 列表空 / 默认 None / 新建回显 / 详情 404 / 更新 / 删除 |
| 781 | 成功路径（内存存储） | 集成（ASGI） | `…::test_routes_with_in_memory_store` | 新建 / 列表 / 详情 / 默认解析 / 删除成功路径 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest --cov=app -q
591 passed, 8 skipped, 1 failed in 4.41s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
299 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ python3 scripts/tools/base-check/check-base.py .
[base-integrity] 通过：无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名。

$ python3 scripts/tools/base-check/check-backend-base.py .
[check-backend-base] 通过：继承链 / 错误码段位 / 迁移链全部对齐。
```

结果汇总：新增 Kiwi 781 用例 9 条全部通过；前置契约适配（`test_router_base.py` / `test_plugin_registration.py` 补 `query_scheme`）通过。**唯一失败为既有 `tests/crosscut/test_m3_closure.py`**（阶段三文档口径，改动前即失败，与本任务无关）。8 skipped 为真实 Redis 集成用例（环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| pyright strict `list[Unknown]` | `default_factory=list` 未带类型参数 + 裸 `list`/`tuple` isinstance 收窄 | `default_factory=list[X]` + `cast("list[object]", value)`（[实施记录](../实施/20_实施_01_列表查询与偏好契约基座.md)「问题与处置」） |
| 占位存储下路由成功分支未覆盖 | 占位恒 None | 内存测试存储经 `dependency_overrides` 校正 |
| 全量 1 项失败：`test_m3_closure.py` | 既有：阶段三文档缺「M3 验收门禁」节 | 超出本任务范围，登记为既有遗留 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 4904 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**100% 覆盖**：`app/schemas/filters.py`、`app/listing/base.py`、`app/listing/null.py`、`app/api/query_scheme.py`。
- 整体未覆盖 22 行均为既有代码占位分支（本任务未改动，随对应回补阶段）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：筛选真实执行（列表接口 / 表单定制阶段）、列表偏好与方案落库（通用能力阶段）、`query:scheme` 权限码（RBAC 阶段）、登录态真实鉴权（认证阶段）→ 见计划 §3.1 与需求 02-47 `> 注：` 块。
- Kiwi 平台登记：已完成（2026-09-20 于 mjbk Kiwi TCMS 登记用例 781）。
- 既有失败项：`test_m3_closure.py`（阶段三文档口径），本任务不改。
- **处理偏差与遗留（2026-09-20 闭环）**：真实实现 / 权限 / 鉴权去向已在计划 §3.1 与需求 02-47 `> 注：` 块登记；消费方见[实施记录](../实施/20_实施_01_列表查询与偏好契约基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
