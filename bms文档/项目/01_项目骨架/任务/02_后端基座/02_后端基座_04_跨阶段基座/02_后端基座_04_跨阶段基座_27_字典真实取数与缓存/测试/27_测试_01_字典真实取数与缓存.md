# 27 测试 · 字典真实取数与缓存

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 嵌套子任务 27（需求 02-56）· 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-27 字典真实取数与缓存](../02_后端基座_04_跨阶段基座_27_字典真实取数与缓存.md) › 测试记录　|　[← 实施记录](../实施/27_实施_01_字典真实取数与缓存.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | 02-4-27 字典真实取数与缓存（[任务](../02_后端基座_04_跨阶段基座_27_字典真实取数与缓存.md)） |
| 对应需求 | [02-56](../../../../../需求/02_需求_后端基座.md#r02-56) |
| 详细设计 | [27_详细设计_01_字典真实取数与缓存.md](../设计/27_详细设计_01_字典真实取数与缓存.md) |
| 实施记录 | [27_实施_01_字典真实取数与缓存.md](../实施/27_实施_01_字典真实取数与缓存.md) |
| 测试日期 | 2026-09-21 |
| 测试人 | minjian |
| 测试环境 | Ubuntu 开发机 · pytest 9（asyncio auto）· SQLite（临时文件库 + Alembic 迁移）· fakeredis · httpx ASGITransport · uvicorn（联调） |
| Kiwi 用例 | 963（分类「平台骨架」/ P2 / CONFIRMED / 标签「自动化」；`@pytest.mark.kiwi_id(963)` 标注于 `tests/dict/test_dict_real.py`、`tests/listing/test_query_scheme_store.py`、`tests/fieldtype/test_local_registry.py`） |
| 结论 | 全部通过（全量 741 通过 / 8 跳过；覆盖率 95.04%；真实服务联调通过） |

## 2. 测试范围与用例 <a id="scope"></a>

| 层次 | 文件 | 覆盖范围 |
| --- | --- | --- |
| 字典真实实现 | `tests/dict/test_dict_real.py` | 迁移建表与索引（七表 + 命名索引 + 唯一约束）/ 种子幂等 / `by_type`（版本三态 / 关键字 / 按值子集 / 探针截断 / 级联父值 / locale 回退 / 停用不可见）/ 缓存版本比对与失效（改条目后展示同步）/ `batch`（多类型合并 + 版本一致返 `None`）/ 翻译（子集回填 + 未命中不占位）/ 写路径（CRUD + 类型与编码冲突）/ 高级查询（attrs / 属性条件引擎 / 分页 / 非法字段与操作符拒绝）/ HTTP 端点（取数 / 版本一致 / 批量 / 属性 / 提供者 / items 与 business / 未注册 provider 404 / en-US / 写接口与版本递增）/ 装配解析（provider 名与实现类型）/ Redis 字典缓存（fakeredis：类型 key / 版本 `INCR` / 子集 / 失效 / 锁与释放） |
| 查询方案存储 | `tests/listing/test_query_scheme_store.py` | 落库 CRUD（新建 / 更新 / 读取 / 软删除）/ 三级默认解析（个人 > 平台）/ 个人方案 owner 过滤（他人不可见） |
| 字段类型注册表 | `tests/fieldtype/test_local_registry.py` | 内建类型注册（含 `dict` / `dict_multi`）/ 校验（字符串 / 数组 / 空值放行）/ `render_metadata` 与前端语义键对齐 / 列类型 / 未知类型 `NotFoundError` |
| 既有占位契约（回归） | `tests/dict/test_dict.py` | 三契约 / 数据契约 / 常量 / 错误码 / 空实现 / 占位路由——**未改动即通过** |
| 既有能力快照（同步） | `tests/crosscut/test_capability_cleanup.py`、`tests/api/test_plugins.py`、`tests/core/test_base_inheritance.py` | cache 域实现清单含 `memory` / `redis`；新类均继承基类（用例内注明 02-4-27 扩展） |

## 3. 执行记录与结果 <a id="run"></a>

| 命令 | 结果 |
| --- | --- |
| `uv run pytest -q` | 741 passed, 8 skipped（10.8s） |
| `uv run pytest -q --cov=app --cov-branch --cov-fail-under=70` | 覆盖率 **95.04%**（8397 语句 / 303 未覆盖；门禁 70% 通过） |
| `uv run ruff check .` | 通过 |
| `uv run ruff format --check .` | 通过 |
| `uv run pyright` | 0 errors, 0 warnings（strict） |
| 迁移 + 种子（真实文件库） | `uv run alembic upgrade head` → `bms_tenant_demo.db` 七表；`uv run python -m ops.seed_dict` 首次 25 行、重复 0 行 |
| 真实服务联调（uvicorn + dev 配置 + curl） | 见下「接口联调记录」 |

**接口联调记录**（真实服务 `127.0.0.1:8000`，dev 配置：`dict_source` / `dict_translator` = `sql`、`dict_cache_region` = `memory`、`query_scheme_store` = `sql`、字段类型 / 查询提供者 = `local`）：

| 接口 | 实测 |
| --- | --- |
| `GET /api/v1/dicts/user_status` | `{version: 0, items: [启用/停用], total: 2}` |
| `GET /api/v1/dicts/user_status?version=0` | `items: null`（版本一致零传输） |
| `POST /api/v1/dicts/batch`（`user_status` / `biz_type`） | 两类型各自返回条目 |
| `GET /api/v1/dicts/region?parent_id=` / `?parent_id=zj` | 顶层 1 条（浙江省）/ 子级 1 条（杭州市） |
| `GET /api/v1/dicts/region/attrs` | `level`（number，操作符集齐备） |
| `GET /api/v1/dicts/query-providers` | `builtin`（含 `param_schema`） |
| `POST /api/v1/dicts/biz_type/advanced-query`（items） | 3 条（采购 / 销售 / 库存） |
| `POST /api/v1/dicts/biz_type/advanced-query`（business + `builtin` + keyword） | 1 条（采购） |
| `GET /api/v1/dicts/user_status`（`Accept-Language: en-US`） | `Enabled` / `Disabled` |
| 写路径：`POST /dicts/types` + `POST /dicts/types/{type}/items` | 新增成功；`GET /dicts/{type}` 版本递增（0 → 2）且条目同步 |
| 写路径：`DELETE /dicts/types/{id}` | 软删除后取数返回 `40102`（类型不存在） |

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置与落点 |
| --- | --- | --- |
| 1 | 全量测试时 HTTP 用例访问旧库（`no such table`） | 插件工厂实例跨应用复用（既有机制）；HTTP 用例改 `dependency_overrides` 绑定当前测试库 + 新增装配解析用例。详见[实施记录 §4](../实施/27_实施_01_字典真实取数与缓存.md#issues) 问题 5 |
| 2 | 过滤 / 探针请求误命中整类型缓存 | 缓存读写策略修正（`arelease_lock` / 未获锁重读限无过滤 / 带 `limit` 不读缓存）。详见实施记录 §4 问题 3 / 4；用例已覆盖 |
| 3 | SQLite 中唯一约束为自动索引（约束名不在 `sqlite_master.type='index'`） | 迁移用例改为「命名索引查 `index` + 唯一约束名查 DDL 文本」 |
| 4 | 租户参数导致的缓存键不一致（直调用例读缓存传 `None` 与实现侧 `demo` 不符） | 用例改按实现侧租户口径读取（`demo`） |

## 5. 覆盖率 <a id="coverage"></a>

- 全量：**95.04%**（分支覆盖；门禁 ≥ 70%）。
- 新增模块：`app/dict/*`、`app/cache/{memory,redis}.py`、`app/listing/{models,store}.py`、`app/fieldtype/local.py`、`app/query/local.py`、`alembic/*` 均经用例覆盖（迁移建表 / 回退、种子幂等、取数过滤分支、缓存命中与失效、翻译子集、写路径冲突、条件引擎白名单与方言分支、HTTP 端点）。
- 未覆盖项：Redis 真实连接分支（`fakeredis` 覆盖语义；真实联调归阶段二窗口）、达梦 / MySQL / PostgreSQL 方言分支（编写口径，真库实测归阶段二窗口）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 三库真库执行与达梦方言实测、真实 Redis 集成用例（`BMS_TEST_REDIS_URL`）→ **阶段二 后端基座窗口**。
- 字典管理页面 / 权限码 / 日志审计 / 回收站 / 导入导出 → **阶段八 通用能力·字典模块**。
- 前端消费方联调（`06_06` 字典字段）→ 前端任务实施 / 测试记录登记。
- 详细设计偏差 3 项（`plugin_name` 口径 / 缓存读写策略 / `arelease_lock`）已回写设计 §18 实施对齐记录。

> 本文档依《[文档生成规范](../../../../../../../规范/文档生成规范.md)》编写
