# 通知渠道基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 04 通知渠道基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-4 通知渠道基座](../02_后端基座_04_跨阶段基座_04_通知渠道基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_04_通知渠道基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [04 通知渠道基座](../02_后端基座_04_跨阶段基座_04_通知渠道基座.md) |
| 对应需求 | [02-20](../../../../../需求/02_需求_后端基座.md#r02-20) |
| 详细设计 | [04_详细设计_01_通知渠道基座](../设计/04_详细设计_01_通知渠道基座.md) |
| 实施记录 | [04 实施记录](../实施/04_实施_01_通知渠道基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 50（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 50 | 契约继承与能力域标识 | 单元 | `tests/notify/test_notify.py::test_inheritance_and_key` | `NullNotifier → BaseNotifier → BaseCapability → BaseObject`；`placeholder` / `describe()`；`key == "notifier"` |
| 50 | 渠道枚举 | 单元 | `…::test_channel_enum` | `NotifyChannel` 三值（inbox / email / sms）；`StrEnum` 字符串比较 |
| 50 | 数据契约默认值与不可变 | 单元 | `…::test_data_contracts_defaults_and_frozen` | `NotificationMessage`（title 空 / biz_* None）/ `SendResult`（message_id / detail None）；frozen（`FrozenInstanceError`） |
| 50 | 占位发送固定成功 | 单元 | `…::test_null_send_fixed_success` | `SendResult`（`delivered is True`、`message_id == NULL_MESSAGE_ID`） |
| 50 | 通知依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullNotifier`；路由经 `Depends(get_notifier)` 取到同一实例 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
296 passed, 2 skipped in 1.00s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
174 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 50 用例 5 条全部通过；既有用例无回归（装配通知器后接口冒烟仍全绿）；全量 296 passed，2 skipped（真实 Redis 集成用例，环境未配置，随 04_02 重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| `ruff` `E501`（notify / deps docstring 超长） | 补「通知」后超 120 列 | 拆行 / 精简（详见[实施记录](../实施/04_实施_01_通知渠道基座.md)「问题与处置」） |
| `ruff format` 1 文件待格式化 | 新增用例多行调用换行 | `ruff format` 规整 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 2802 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**均 100% 覆盖**：`app/notify/base.py`（43 语句）、`app/notify/__init__.py`。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实渠道（站内信落库 + 实时推送、SMTP、短信）、模板与发送记录、渠道偏好过滤、公告与已读回执、短信降级 → **通知公告阶段 / 事件总线 / 运维**（计划「后续阶段待办」表「通知渠道真实实现」行）。
- Kiwi 平台登记：已完成（2026-09-14 于 mjbk Kiwi TCMS 登记用例 50）。
- **处理偏差与遗留（2026-09-14 闭环）**：真实渠道 / 模板 / 偏好过滤 / 公告回执 / 降级用例去向已在计划 §3.1「通知渠道真实实现」行与需求 02-20 `> 注：` 块登记；消费方标注见[实施记录](../实施/04_实施_01_通知渠道基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
