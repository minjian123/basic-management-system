# 全文检索基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 03 全文检索基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-3 全文检索基座](../02_后端基座_04_跨阶段基座_03_全文检索基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_03_全文检索基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [03 全文检索基座](../02_后端基座_04_跨阶段基座_03_全文检索基座.md) |
| 对应需求 | [02-19](../../../../../需求/02_需求_后端基座.md#r02-19) |
| 详细设计 | [03_详细设计_01_全文检索基座](../设计/03_详细设计_01_全文检索基座.md) |
| 实施记录 | [03 实施记录](../实施/03_实施_01_全文检索基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 49（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 49 | 契约继承与能力域标识 | 单元 | `tests/search/test_search.py::test_inheritance_and_key` | `NullSearchIndex → BaseSearchIndex → BaseCapability → BaseObject`；`placeholder` / `describe()`；`key == "search_index"` |
| 49 | 常量清单 | 单元 | `…::test_constants` | `SEARCH_INDEX_PREFIX == "bms-"`；`DEFAULT_SEARCH_SIZE == 20` |
| 49 | 数据契约默认值与不可变 | 单元 | `…::test_data_contracts_defaults_and_frozen` | `SearchQuery`（offset 0 / size 20 / filters None）/ `SearchHit`（highlight None）/ `SearchResult`（total 0）；frozen（`FrozenInstanceError`） |
| 49 | 占位写入 / 删除空操作 | 单元 | `…::test_null_index_and_delete_noop` | `index(document)` / `delete(index, doc_id)` 返回 None、不抛错 |
| 49 | 占位 search 固定命中 | 单元 | `…::test_null_search_fixed_hit` | 单条命中（id == `NULL_SEARCH_HIT_ID`、`total == 1`） |
| 49 | 检索依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullSearchIndex`；路由经 `Depends(get_search_index)` 取到同一实例 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
291 passed, 2 skipped in 0.99s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
171 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 49 用例 6 条全部通过；既有用例无回归（装配检索索引后接口冒烟仍全绿）；全量 291 passed，2 skipped（真实 Redis 集成用例，环境未配置，随 04_02 重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| `ruff` `E501`（deps / search docstring 超长） | 补「全文检索」后超 120 列 | 拆行 / 精简（详见[实施记录](../实施/03_实施_01_全文检索基座.md)「问题与处置」） |
| `SearchDocument.fields` 默认值 | 空映射默认易掩盖漏填 | 改为必填字段 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 2756 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**均 100% 覆盖**：`app/search/base.py`（54 语句）、`app/search/__init__.py`。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实 ES（索引 / 映射 / DSL / 高亮 / 别名 / 集群）、同步管线与重建、ES 故障降级、文件内容文本解析、租户隔离与数据权限过滤落地 → **全文检索阶段（十六）/ 文件管理阶段 / RBAC 阶段**（计划「后续阶段待办」表「全文检索真实实现」行）。
- Kiwi 平台登记：已完成（2026-09-14 于 mjbk Kiwi TCMS 登记用例 49）。
- **处理偏差与遗留（2026-09-14 闭环）**：真实 ES / 同步管线 / 重建 / 降级 / 租户权限用例去向已在计划 §3.1「全文检索真实实现」行与需求 02-19 `> 注：` 块登记；消费方标注见[实施记录](../实施/03_实施_01_全文检索基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
