# 审计哈希链基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 11 审计哈希链基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-11 审计哈希链基座](../02_后端基座_04_跨阶段基座_11_审计哈希链基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_11_审计哈希链基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [11 审计哈希链基座](../02_后端基座_04_跨阶段基座_11_审计哈希链基座.md) |
| 对应需求 | [02-34](../../../../../需求/02_需求_后端基座.md#r02-34) |
| 详细设计 | [11_详细设计_01_审计哈希链基座](../设计/11_详细设计_01_审计哈希链基座.md) |
| 实施记录 | [11 实施记录](../实施/11_实施_01_审计哈希链基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 57（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 57 | 契约继承与能力域标识 | 单元 | `tests/audit/test_hashchain.py::test_inheritance_and_key` | `NullHashChain → BaseHashChain → BaseCapability → BaseObject`；`placeholder` / `describe()`；`key == "hash_chain"` |
| 57 | 常量清单 | 单元 | `…::test_constants` | `GENESIS_HASH` 为 64 位零 hex；`HASH_ALGORITHM == "sha256"` |
| 57 | 数据契约默认值与不可变 | 单元 | `…::test_data_contracts_defaults_and_frozen` | `ChainVerifyResult.broken_index` None；`HashChainEntry` 字段；frozen |
| 57 | 占位 compute 固定返回 | 单元 | `…::test_null_compute_fixed` | `compute` 返回固定占位哈希 |
| 57 | 占位 verify 恒定通过 | 单元 | `…::test_null_verify_always_valid` | `ChainVerifyResult(valid=True, broken_index=None)`（含空链） |
| 57 | 哈希链依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullHashChain`；路由经 `Depends(get_hash_chain)` 取到同一实例 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
338 passed, 2 skipped in 1.07s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
198 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 57 用例 6 条全部通过；既有用例无回归（装配哈希链后接口冒烟仍全绿）；全量 338 passed，2 skipped（真实 Redis 集成用例，环境未配置，随 04_02 重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| `ruff` `E501`（hashchain docstring 超长） | 契约说明行偏长 | 精简（详见[实施记录](../实施/11_实施_01_审计哈希链基座.md)「问题与处置」） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 3184 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**均 100% 覆盖**：`app/audit/hashchain.py`（34 语句）。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实 SHA256 链计算 / 跨月衔接 / 捕获落库 / 有序消费 / 巡检 / 归档前校验 → **审计哈希链阶段 / 事件总线 / 归档阶段**（计划「后续阶段待办」表「审计哈希链真实实现」行）。
- Kiwi 平台登记：已完成（2026-09-14 于 mjbk Kiwi TCMS 登记用例 57）。
- **处理偏差与遗留（2026-09-14 闭环）**：真实链计算 / 跨月衔接 / 捕获 / 巡检用例去向已在计划 §3.1「审计哈希链真实实现」行与需求 02-34 `> 注：` 块登记；消费方见[实施记录](../实施/11_实施_01_审计哈希链基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
