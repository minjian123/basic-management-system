# LLM 适配基座测试记录

> 项目骨架 · 02 后端基座 · 04 跨阶段基座 · 02 LLM适配基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-4-2 LLM适配基座](../02_后端基座_04_跨阶段基座_02_LLM适配基座.md) › 01 测试　|　[← 父任务](../02_后端基座_04_跨阶段基座_02_LLM适配基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02 LLM适配基座](../02_后端基座_04_跨阶段基座_02_LLM适配基座.md) |
| 对应需求 | [02-18](../../../../../需求/02_需求_后端基座.md#r02-18) |
| 详细设计 | [02_详细设计_01_LLM适配基座](../设计/02_详细设计_01_LLM适配基座.md) |
| 实施记录 | [02 实施记录](../实施/02_实施_01_LLM适配基座.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 48（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 48 | 契约继承与能力域标识 | 单元 | `tests/llm/test_llm.py::test_inheritance_and_key` | `NullLlmProvider → BaseLlmProvider → BaseCapability → BaseObject`；`placeholder` / `describe()`；`key == "llm_provider"` |
| 48 | 常量清单 | 单元 | `…::test_constants` | `LLM_PROVIDER_TYPES` 三项无重复；`NULL_EMBEDDING_DIM == 1024` |
| 48 | 数据契约默认值与不可变 | 单元 | `…::test_data_contracts_defaults_and_frozen` | `ChatMessage`（role 默认 `user`）/ 三结果契约默认值正确；frozen（`FrozenInstanceError`） |
| 48 | 占位 chat 固定返回 | 单元 | `…::test_null_chat_fixed` | `ChatResult`（content == `NULL_CHAT_REPLY`；`model` / `provider_key` 回显） |
| 48 | 占位 embedding 零向量 | 单元 | `…::test_null_embedding_zero_vectors` | 向量条数 == `len(texts)`、每条维度 == `NULL_EMBEDDING_DIM` 且全 0.0；`model` / `provider_key` 回显 |
| 48 | 占位 ocr 固定返回 | 单元 | `…::test_null_ocr_fixed` | `OcrResult`（text == `NULL_OCR_TEXT`；`model` / `provider_key` 回显） |
| 48 | 依赖解析 | 集成（ASGI） | `…::test_dependency_provider_resolves` | `create_app()` 装配 `NullLlmProvider`；路由经 `Depends(get_llm_provider)` 取到同一实例 |

回归范围：全量用例（含接口冒烟）。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
285 passed, 2 skipped in 0.96s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
168 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 Kiwi 48 用例 7 条全部通过；既有用例无回归（装配 LLM Provider 后接口冒烟仍全绿）；全量 285 passed，2 skipped（真实 Redis 集成用例，环境未配置，随 04_02 重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| `ruff` `I001`（deps 导入未排序） | `app.llm` 应排在 `app.lock` 之前 | `ruff check --fix` 规整（详见[实施记录](../实施/02_实施_01_LLM适配基座.md)「问题与处置」） |
| `ChatMessage` 字段序 | dataclass 必填字段须在默认值字段之前 | 改为 `content` / `role`，设计同步回写 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 2699 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**均 100% 覆盖**：`app/llm/base.py`（59 语句）、`app/llm/__init__.py`。
- 未覆盖 3 行均为既有代码占位分支：`app/repositories/base_memory_repository.py:25,30`、`app/repositories/base_repository.py:109`（本任务未改动，随落库阶段回补）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实 Provider（外部 API / 私有化端点、`sys_ai_provider`、Secret、超时重试熔断）、RAG 管线与八项 AI 能力、`ai_chat_log` 审计与安全闸门 → **AI 阶段（十七）**（计划「后续阶段待办」表「LLM 适配真实实现」行）。
- Kiwi 平台登记：已完成（2026-09-14 于 mjbk Kiwi TCMS 登记用例 48）。
- **处理偏差与遗留（2026-09-14 闭环）**：真实 Provider / RAG / 审计 / 安全闸门用例去向已在计划 §3.1「LLM 适配真实实现」行与需求 02-18 `> 注：` 块登记；消费方标注见[实施记录](../实施/02_实施_01_LLM适配基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
