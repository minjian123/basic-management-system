# 请求 / 响应基类测试记录

> 项目骨架 · 02 后端基座 · 02 模块基类 · 03 请求 / 响应基类 BaseSchema · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-2-3 请求 / 响应基类 BaseSchema](../02_后端基座_02_模块基类_03_请求响应基类.md) › 01 测试　|　[← 父任务](../02_后端基座_02_模块基类_03_请求响应基类.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [03 请求 / 响应基类 BaseSchema](../02_后端基座_02_模块基类_03_请求响应基类.md) |
| 对应需求 | [02-7](../../../../../需求/02_需求_后端基座.md#r02-7) |
| 详细设计 | [03_详细设计_01_请求响应基类](../设计/03_详细设计_01_请求响应基类.md) |
| 实施记录 | [03 实施记录](../实施/03_实施_01_请求响应基类.md) |
| 测试日期 | 2026-09-13 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；Pydantic 2.13.5） |
| Kiwi 用例 | 14（分型序列化）、13（配置 / 序列化 / 分页契约） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 14 | 分型序列化 | 单元 | `tests/core/test_base_object.py::test_public_fields_declared_first` | Pydantic `to_dict == model_dump()`（含计算字段 `plus`）；普通对象取 `__dict__` 公开项 |
| 14 | dataclass 序列化（既有） | 单元 | `…::test_to_dict_reflects_public_fields_recursively` 等 | 声明字段、递归嵌套、稳定序、降级 str |
| 13 | 公共配置（既有） | 单元 | `tests/schemas/test_base_schema.py` | 去空白；`from_attributes` |
| 13 | 序列化 | 单元 | `…::test_schema_to_dict_matches_model_dump` | `to_dict == model_dump()`；`to_json` 稳定序 |
| 13 | 页码分页请求 | 单元 | `…::test_page_query_defaults_and_bounds` | 默认 `page=1`/`size=20`；`page<1`、`size>200` 校验报错 |
| 13 | 分页响应 / 游标 | 单元 | `…::test_page_and_cursor_response_contracts` | `BasePageResponse` 组装与序列化；`BaseCursorResponse`（`list/next_cursor/has_more`）；`BaseCursorQuery.cursor` 默认 None |
| 13 | 继承链（既有 + 新增） | 单元 | `…::test_demo_schemas_inherit_base_schema` / `test_pagination_inherits_base_schema` | demo 模型 → `BaseSchema`；分页基类 → `BaseSchema` |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
87 passed, 2 skipped in 0.38s

$ uv run ruff check .
All checks passed!

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增分型序列化与分页契约用例通过；全量 87 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 无 | — | 用例一次通过 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 932 语句，覆盖率 100%**（任务级快照；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 真实 Redis 集成用例（2 条）需 `BMS_TEST_REDIS_URL`，本机未配置即跳过，随 04_02（三库集成与流水线收口）重验证层启用。
- `ApiResponse` 及分页 `data` 装配的测试归 02-3-1。

> 本文档依《文档生成规范》编写
