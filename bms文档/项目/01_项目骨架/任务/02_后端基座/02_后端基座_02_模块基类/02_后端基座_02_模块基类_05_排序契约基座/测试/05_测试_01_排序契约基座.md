# 排序契约基座测试记录

> 项目骨架 · 02 后端基座 · 02 模块基类 · 05 排序契约基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-2-5 排序契约基座](../02_后端基座_02_模块基类_05_排序契约基座.md) › 01 测试　|　[← 父任务](../02_后端基座_02_模块基类_05_排序契约基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [05 排序契约基座](../02_后端基座_02_模块基类_05_排序契约基座.md) |
| 对应需求 | [02-40](../../../../../需求/02_需求_后端基座.md#r02-40) |
| 详细设计 | [05_详细设计_01_排序契约基座](../设计/05_详细设计_01_排序契约基座.md) |
| 实施记录 | [05 实施记录](../实施/05_实施_01_排序契约基座.md) |
| 测试日期 | 2026-09-13 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv） |
| Kiwi 用例 | 29（本任务新增，已登记）、11、12、13（回归） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 29 | 排序规格解析 | 单元 | `tests/schemas/test_sorting.py::test_parse_splits_fields_and_maps_directions` | `order_by` 逗号分隔；方向按位置对应，缺位回退 `desc` |
| 29 | 空值 / 空白 / 重复字段 | 单元 | `…::test_parse_handles_empty_blank_and_duplicate_fields` | 空串 / None → 空列表；空白项忽略；重复字段保留首次 |
| 29 | 方向容错 | 单元 | `…::test_parse_normalizes_direction_and_ignores_extra_values` | 大小写归一、非法方向回退 `desc`、方向数组超长忽略 |
| 29 | 白名单过滤 | 单元 | `…::test_specs_filters_by_whitelist` | 白名单外字段忽略；`whitelist=None` 不过滤；空白名单全忽略 |
| 29 | 契约默认值与约束 | 单元 | `…::test_sort_spec_and_query_defaults` | 默认方向 `desc`；请求默认空；非法方向被 Pydantic 拒绝 |
| 29 | 继承链 | 单元 | `…::test_inheritance_chain` | `SortSpec` / `BaseSortQuery` → `BaseSchema`；分页请求 → 排序请求 |
| 29 | 内存基线单字段排序 | 单元 | `tests/repositories/test_base_repository.py::test_memory_list_sorts_single_field_both_directions` | 升序 / 降序结果正确 |
| 29 | 内存基线多键排序 | 单元 | `…::test_memory_list_sorts_multi_key_and_keeps_default_order` | 主次键生效；不传 `sort` 保持 ID 升序（行为回归） |
| 29 | 空值位置 | 单元 | `…::test_memory_list_handles_none_values_by_direction` | 升序末尾 / 降序首位 |
| 29 | 类型混合兜底 | 单元 | `…::test_memory_sort_tolerates_mixed_types` | 分桶比较不抛 `TypeError`，结果确定（数值 → 字符串 → 空值） |
| 29 | 白名单默认与调用覆盖 | 单元 | `…::test_sort_whitelist_default_and_call_override` | 未声明字段被忽略；`specs(whitelist=...)` 覆盖生效 |
| 29 | 分页承载排序 | 单元 | `…::test_pagination_queries_carry_sort` | `list_page` / `list_cursor` 返回有序结果 |
| 29 | DB 侧占位 | 单元 | `…::test_db_repository_sort_placeholder` | `list(sort=...)` 抛 `NotImplementedError`；`_apply_sort` 占位原样返回语句 |
| 13 | 分页请求默认值（适配） | 单元 | `tests/schemas/test_base_schema.py::test_page_query_defaults_and_bounds` | `model_dump()` 含 `order_by` / `order` 默认 `None` |
| 11 / 12 | 仓储 / 服务基线回归 | 单元 | `tests/repositories/test_base_repository.py`、`tests/services/test_base_service.py` | 既有契约 / 内存 / DB 骨架与不存在语义行为不变 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
183 passed, 2 skipped in 0.52s

$ uv run ruff check .
All checks passed!

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增排序契约用例（Kiwi 29，13 条）全部通过；内存基线不传 `sort` 的默认顺序、DB 骨架占位行为与既有用例回归一致；2 条跳过为真实 Redis 集成用例（本机未配置 `BMS_TEST_REDIS_URL`，随 04_02 重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| Kiwi 29 用例一度未登记 | Kiwi TCMS `admin` 口令失效（本地资源文档与 mjbk `~/deploy/.env` 取值在 Web 与 JSON-RPC 两条路径均登录失败） | 经确认后重置口令并补齐登记：平台 **29 号＝排序契约基座**（本任务）、**28 号＝模块注册表**（补原 `kiwi_id(28)` 悬空，02-5-3） |
| 分页默认值断言失配 | 分页请求继承排序契约后 `model_dump()` 增加排序键 | 适配断言（Kiwi 13），已通过 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 1912 语句，未覆盖 3，覆盖率 99%**（任务级快照；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- **Kiwi 29 登记已完成**：本任务用例在平台登记为 **29 号「排序契约基座（规格解析 / 白名单 / 内存排序 / DB 占位）」**（产品「BMS 基础管理系统」、分类「平台骨架」、优先级 P2、状态 CONFIRMED、自动化标记；notes 含断言范围与执行结果），与代码 `@pytest.mark.kiwi_id(29)` 及详细设计第 9 节清单一致。登记过程中一并补齐 **28 号「模块注册表」**（原代码 `kiwi_id(28)` 悬空，02-5-3）。
- **Kiwi 编号对位已处置（2026-09-13）**：平台 19 ~ 27 号归 01 域前端与依赖锁定（apps/desktop 默认页 / 前端基础类 / 公共组合式 / Axios / Python 3.14 矩阵），02 域 9 个主题顺延补建 **30 ~ 38 号**用例并对位改号（19→30 ORM 模型基类、20→31 异常体系、21→32 配置基座、22→33 安全基座、23→34 日志基座、24→35 跨阶段基座、25→36 数据访问底座、26→37 能力域中间层、27→38 多租户数据拓扑）；代码 `kiwi_id` 标记（14 个测试文件）与 02 域任务文档引用（149 处，含设计 / 实施 / 测试记录）已同步改号并核对无残留，口径见《[测试规范](../../../../../../../规范/测试规范.md)》「用例管理（Kiwi TCMS）」节。
- **未覆盖项**：DB 侧真实 `ORDER BY` 与白名单防注入、排序字段索引配合、四库 `NULLS` 口径、分页限深与游标键 —— 随列表接口（基础能力）阶段回补，归口见详细设计第 12 节与计划「后续阶段待办」。

> 本文档依《文档生成规范》编写
