# 02-2-4 ORM 模型基类 BaseModel 与表规范

> 项目骨架 · 02 后端基座 · 02 模块基类 · 嵌套子任务 04

[文档首页](../../../../../../文档首页.md) › [02 模块基类](../02_后端基座_02_模块基类.md) › 04 ORM 模型基类 BaseModel 与表规范　|　[← 父任务](../02_后端基座_02_模块基类.md)

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02-2-4 |
| 父任务 | [02 模块基类](../02_后端基座_02_模块基类.md) |
| 对应需求 | [02-8](../../../../需求/02_需求_后端基座.md#r02-8) |
| 工时（重估） | 5h |
| 依赖 | 01_03（工程骨架：`app/models/base.py` 基座位置） |
| 负责人 | minjian |
| 状态 | 已完成 |
| 完成日期 | 2026-09-13 |

## 2. 任务内容 <a id="content"></a>

1. `app/models/base.py` 的 `BaseModel` 字段（每表必备，ORM 层定义一次）：
   - 主键 `id`：雪花 ID（yitter/IdGenerator，BIGINT 跨库类型），生成器封装于 `app/core/id.py`（`generate_id()`）
   - 审计：`created_at` / `created_by` / `updated_at` / `updated_by`——ORM 事件自动填充（insert/update 监听），业务代码禁止手动赋值
   - 软删除：`deleted_at`（NULL=未删）；乐观锁：`version`（更新时比对，冲突返回明确错误码）
2. 四库类型映射表（SQLite / MySQL / PostgreSQL / 达梦 DM8）写入模型定义注释；时间统一 UTC 存储、金额 `NUMERIC(20,4)`、字符串 VARCHAR（长度显式）、禁用方言专用类型（JSON / ENUM / SERIAL）
3. 软删除机制：查询默认过滤 `deleted_at IS NULL`（repository 基类统一注入）；唯一约束统一建为 `(唯一字段, deleted_at)` 复合唯一索引；删除写时间戳释放唯一键、恢复清 NULL
4. 索引与表级规范：排序 / 逻辑外键 / 数据权限过滤字段必须建索引，命名 `idx_字段` / `uq_字段`，每表索引不超过 5 个；表名单数 snake_case、平台域前缀 `sys_`/`wf_`/`rpt_`/`ai_`、每表与字段 COMMENT 必填
5. **分层与扩展**：`BaseModel` 为 L0（公共字段与约定）；多租户强制过滤 / 数据范围注入 / 审计捕获等横切按「插中间层」新增（如 `BaseTenantModel` / `BaseScopedModel`），**不在 `BaseModel` 堆叠横切逻辑**

## 3. 完成标准 <a id="accept"></a>

`BaseModel` 字段与四库类型映射定义落地、表规范写入模型注释；**建表与四库迁移验证按接口占位处理**（不连真库、不跑迁移）；`pytest`、`ruff`、`pyright` 全绿。

## 4. 参考文档 <a id="ref"></a>

- [04_详细设计_01_ORM模型基类与表规范](设计/04_详细设计_01_ORM模型基类与表规范.md)（本任务详细设计，已定稿）
- [04 实施记录](实施/04_实施_01_ORM模型基类与表规范.md)（BaseObject 去 ABC、雪花 ID、BaseModel 落地与验证）
- [04 测试记录](测试/04_测试_01_ORM模型基类与表规范.md)（字段 / 审计 / 乐观锁 / 软删除 / 雪花 用例与覆盖率）
- [需求 02-8](../../../../需求/02_需求_后端基座.md#r02-8)
- 《架构设计 · 数据架构》「数据规范」节
- 《数据库开发规范》第 2/3/4 节
- 《命名规范》「数据库命名」节

> 本文档依《文档生成规范》编写
