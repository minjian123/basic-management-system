# ORM 模型基类与表规范测试记录

> 项目骨架 · 02 后端基座 · 02 模块基类 · 04 ORM 模型基类 BaseModel 与表规范 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-2-4 ORM 模型基类与表规范](../02_后端基座_02_模块基类_04_ORM模型基类与表规范.md) › 01 测试　|　[← 父任务](../02_后端基座_02_模块基类_04_ORM模型基类与表规范.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [04 ORM 模型基类与表规范](../02_后端基座_02_模块基类_04_ORM模型基类与表规范.md) |
| 对应需求 | [02-8](../../../../../需求/02_需求_后端基座.md#r02-8) |
| 详细设计 | [04_详细设计_01_ORM模型基类与表规范](../设计/04_详细设计_01_ORM模型基类与表规范.md) |
| 实施记录 | [04 实施记录](../实施/04_实施_01_ORM模型基类与表规范.md) |
| 测试日期 | 2026-09-13 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；SQLAlchemy 2.0.52；SQLite 临时库） |
| Kiwi 用例 | 19 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 19 | 字段与默认值 | 单元 | `tests/models/test_base_model.py::test_fields_and_snowflake_id` | 字段齐备；`id` 为雪花值；审计 / 软删除 / 版本默认；`to_dict` 排除 `_sa_instance_state` |
| 19 | 审计自动填充 | 单元 | `…::test_audit_context_and_update` | 插入填 `created_at/updated_at` 与用户上下文；更新刷新 `updated_at/updated_by`、保留 `created_at` |
| 19 | 乐观锁 | 单元 | `…::test_optimistic_lock_and_stale` | 更新后 `version` +1；并发旧版本更新抛 `StaleDataError` |
| 19 | 软删除与复合唯一 | 单元 | `…::test_soft_delete_frees_unique` | 删除写 `deleted_at` 释放唯一键；删除行同键可共存 |
| 19 | 雪花唯一 / 单调 | 单元 | `…::test_snowflake_unique_and_monotonic` | 1000 个 ID 唯一且单调递增 |
| 19 | 序列用尽 / 回拨 | 单元 | `…::test_snowflake_seq_wrap` / `test_snowflake_clock_rollback` | 同毫秒 4096 序列用尽等待下一毫秒；小幅回拨追平、大幅回拨抛错 |
| 19 | WorkerId 与配置 | 单元 | `…::test_configure_and_invalid_worker` / `test_initial_worker_id_from_env` | 边界 WorkerId（0/1023）合法、越界报错；环境变量解析 / 回退 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
96 passed, 2 skipped in 0.49s

$ uv run ruff check .
All checks passed!

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 7 条 Kiwi 19 用例通过；全量 96 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 雪花回拨用例初版断言为负 ID | 假时钟取值早于自定义 epoch | 假时钟改用 epoch 之后的时间戳，断言恢复 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 1012 语句，覆盖率 100%**（任务级快照；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 真实 Redis 集成用例（2 条）需 `BMS_TEST_REDIS_URL`，本机未配置即跳过，随 04_02（三库集成与流水线收口）重验证层启用。
- 四库真库建表 / 迁移 / 达梦实测、软删除过滤、乐观锁错误码、WorkerId 自动注册的测试归对应后续阶段。

> 本文档依《文档生成规范》编写
