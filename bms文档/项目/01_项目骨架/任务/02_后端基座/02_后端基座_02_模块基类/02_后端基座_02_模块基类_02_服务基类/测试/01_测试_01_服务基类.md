# 服务基类测试记录

> 项目骨架 · 02 后端基座 · 02 模块基类 · 02 服务基类 BaseService · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-2-2 服务基类 BaseService](../02_后端基座_02_模块基类_02_服务基类.md) › 01 测试　|　[← 父任务](../02_后端基座_02_模块基类_02_服务基类.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02 服务基类 BaseService](../02_后端基座_02_模块基类_02_服务基类.md) |
| 对应需求 | [02-6](../../../../../需求/02_需求_后端基座.md#r02-6) |
| 详细设计 | [01_详细设计_01_服务基类](../设计/01_详细设计_01_服务基类.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_服务基类.md) |
| 测试日期 | 2026-09-13 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv） |
| Kiwi 用例 | 12（新增断言并入既有用例，不新增编号） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 12 | 事务钩子包裹范围 | 单元 | `tests/services/test_base_service.py::test_transaction_hook_wraps_writes_only` | `create` / `update` / `delete` 进入事务（计数 +1）；`list` / `get` / `exists` / `count` 不进入；缺失分支在事务内抛 `NotFoundError` |
| 12 | CRUD 委派（既有） | 单元 | `…::test_create_list_exists_count_delegate` | `create` / `list` / `exists` / `count` 行为正确 |
| 12 | 不存在语义（既有 3 条） | 单元 | `…::test_get_*` / `test_update_*` / `test_delete_*` | 命中正常、缺失抛 `NotFoundError` |
| 12 | 继承（既有） | 单元 | `…::test_demo_service_inherits_base` | `issubclass(DemoService, BaseService)` |

- 事务钩子用 spy 子类 `SpyService` 覆写 `_transaction` 计数，仅在测试内使用，不改业务代码。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
75 passed, 2 skipped in 0.36s

$ uv run ruff check .
All checks passed!

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增 1 条 Kiwi 12 用例通过；全量 75 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 无 | — | 用例一次通过 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 870 语句，覆盖率 100%**（任务级快照；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 真实 Redis 集成用例（2 条）需 `BMS_TEST_REDIS_URL`，本机未配置即跳过，随 04_02（三库集成与流水线收口）重验证层启用。
- `_transaction()` 真实实现（会话事务）的测试归 02-5-1 数据访问底座。

> 本文档依《文档生成规范》编写
