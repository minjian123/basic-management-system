# 多租户数据拓扑测试记录

> 项目骨架 · 02 后端基座 · 05 机制底座 · 02 多租户数据拓扑 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-5-2 多租户数据拓扑](../02_后端基座_05_机制底座_02_多租户数据拓扑.md) › 01 测试　|　[← 父任务](../02_后端基座_05_机制底座_02_多租户数据拓扑.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02 多租户数据拓扑](../02_后端基座_05_机制底座_02_多租户数据拓扑.md) |
| 对应需求 | [02-15](../../../../../需求/02_需求_后端基座.md#r02-15) |
| 详细设计 | [02_详细设计_01_多租户数据拓扑](../设计/02_详细设计_01_多租户数据拓扑.md) |
| 实施记录 | [02 实施记录](../实施/02_实施_01_多租户数据拓扑.md) |
| 测试日期 | 2026-09-13 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv） |
| Kiwi 用例 | 27 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 27 | 解析链 / 回落 | 单元 | `tests/db/test_tenant.py::test_resolve_chain_and_fallback` | 子域名 / 请求头 / token 命中 demo；无来源回落 |
| 27 | 未知租户 | 单元 | `…::test_resolve_unknown_tenant` | 404 / 80001；属 `OpenTenantError` 段位 |
| 27 | 豁免路径 / 依赖 | 接口 | `…::test_is_exempt_path`、`…::test_get_tenant_dependency` | 豁免放行 None；命中解析；未知 404 |
| 27 | `sys_tenant` 模型 | 单元 | `…::test_sys_tenant_declared_and_persistable` | 建表 / 字段 / 写入 |
| 27 | 引擎注册表 | 单元 | `tests/db/test_engine_registry.py` | 平台常驻、租户懒加载、LRU / 闲置回收、释放、连接预算 |
| 27 | 迁移 / 初始化脚本 | 单元 | `tests/ops/test_tenant_ops.py` | `--dry-run` 固定库清单 / 步骤；占位分支 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
161 passed, 2 skipped in 0.69s

$ uv run ruff check .
All checks passed!

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ uv run python ops/migrate_tenants.py --target all --db mysql --dry-run
[迁移] mysql → bms_platform（dry-run）
[迁移] mysql → bms_tenant_demo（dry-run）
[迁移] mysql → bms_archive（dry-run）
```

结果汇总：多租户相关用例通过；全量 161 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 无 | — | 用例一次通过（含工厂 `drop` 回写） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 1697 语句，覆盖率 100%**（任务级快照；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 真实建库 / 落表 / 解析并发隔离 / LRU 实测随落库阶段；真实 Alembic 迁移与种子归落库阶段（认证 / RBAC）。
- 真实 Redis 集成用例（2 条）需 `BMS_TEST_REDIS_URL`，本机未配置即跳过，随 04_02 重验证层启用。

> 本文档依《文档生成规范》编写
