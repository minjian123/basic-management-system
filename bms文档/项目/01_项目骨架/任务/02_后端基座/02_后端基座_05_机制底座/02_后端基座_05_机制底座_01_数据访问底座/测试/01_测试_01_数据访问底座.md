# 数据访问底座测试记录

> 项目骨架 · 02 后端基座 · 05 机制底座 · 01 数据访问底座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-5-1 数据访问底座](../02_后端基座_05_机制底座_01_数据访问底座.md) › 01 测试　|　[← 父任务](../02_后端基座_05_机制底座_01_数据访问底座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 数据访问底座](../02_后端基座_05_机制底座_01_数据访问底座.md) |
| 对应需求 | [02-14](../../../../../需求/02_需求_后端基座.md#r02-14) |
| 详细设计 | [01_详细设计_01_数据访问底座](../设计/01_详细设计_01_数据访问底座.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_数据访问底座.md) |
| 测试日期 | 2026-09-13 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv） |
| Kiwi 用例 | 11、12、25 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 25 | 引擎工厂 | 单元 | `tests/db/test_data_access.py::test_engine_factory_creates_and_caches` | 创建 / 缓存、平台 / 归档 / 租户分支、SQLite 与服务器方言、释放 |
| 25 | 会话工厂 / get_db | 单元 | `…::test_session_factory_and_get_db_dependency` | 会话可执行 `SELECT 1`；`get_db` 依赖经 ASGI 可用 |
| 25 | 只读标记 / 绑定 | 单元 | `…::test_read_only_context_marker` | 设置 / 读取 / 复位；绑定钩子读上下文 |
| 25 | 乐观锁转译 | 单元 | `…::test_version_guard_translates_conflict` | `StaleDataError` → `ConcurrentConflictError` |
| 25 | 分页 | 单元 | `…::test_pagination_page_and_cursor` | 页码切片与游标推进 / 终止 |
| 12 | 事务边界（异步） | 单元 | `tests/services/test_base_transactional_service.py` | 写操作进入 UoW、读不进、缺失分支在事务内抛错 |
| 12/25 | 工作单元 | 单元 | `tests/db/test_unit_of_work.py` | 空实现无副作用；`DbUnitOfWork` 会话事务开关 |
| 11/12 | 仓储 / 服务异步基线 | 单元 | `tests/repositories/test_base_repository.py`、`tests/services/test_base_service.py` | 既有行为异步化后一致 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest -q
133 passed, 2 skipped in 0.61s

$ uv run ruff check .
All checks passed!

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：新增数据访问用例通过；既有 repo / service / uow / api 用例异步化后全绿；全量 133 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 类型 / lint 告警 | 异步上下文注解与未用导入 | 实施记录 §4 已记；改 `AsyncGenerator` / `[object]`、删导入 |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 1458 语句，覆盖率 100%**（任务级快照；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 真实四库连通 / 连接池 / 软删除 SQL / 读写分离真实路由的集成测试随落库阶段。
- 真实 Redis 集成用例（2 条）需 `BMS_TEST_REDIS_URL`，本机未配置即跳过，随 04_02 重验证层启用。

> 本文档依《文档生成规范》编写
