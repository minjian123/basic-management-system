# 02-5-1 数据访问底座

> 项目骨架 · 02 后端基座 · 05 机制底座 · 嵌套子任务 01

[文档首页](../../../../../../文档首页.md) › [05 机制底座](../02_后端基座_05_机制底座.md) › 01 数据访问底座　|　[← 父任务](../02_后端基座_05_机制底座.md)

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02-5-1 |
| 父任务 | [05 机制底座](../02_后端基座_05_机制底座.md) |
| 对应需求 | [02-14](../../../../需求/02_需求_后端基座.md#r02-14) |
| 工时（重估） | 3h |
| 依赖 | 02-2-1（数据访问基类）、02-2-4（ORM 模型基类）、03-2（配置基座） |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. 异步引擎工厂接口：按数据源创建 / 缓存 `AsyncEngine` 的签名（连接池参数、超时、`pool_pre_ping`），**占位实现不真实建连**
2. AsyncSession 工厂接口：`async_sessionmaker` 形态与请求级会话依赖注入签名；事务边界约定（`with session.begin()`）；实现 `DbUnitOfWork`（替换 `NullUnitOfWork`，经 [02-2-2 服务基类 BaseService](../../02_后端基座_02_模块基类/02_后端基座_02_模块基类_02_服务基类/02_后端基座_02_模块基类_02_服务基类.md) 的 `BaseTransactionalService` 统一进入事务）；禁止异步会话跨请求共享
3. 四库方言接口：MySQL（aiomysql）/ PostgreSQL（psycopg 3）/ 达梦 DM8（dmPython，同步驱动经 `asyncio.to_thread` 封装例外口径）/ SQLite（aiosqlite）；URL 取值经配置基座读取接口
4. 基类异步切换：`BaseRepository[T]` / `BaseDbRepository[T]` / `BaseService[T]` / `BaseTransactionalService[T]` 异步签名与分页 / 软删除过滤回填基类（只加基类，模块类不改）；**分页执行**（页码 `limit/offset`、游标 `cursor/limit`）在 repository / service 落地，复用 [02-2-3 请求 / 响应基类](../../02_后端基座_02_模块基类/02_后端基座_02_模块基类_03_请求响应基类/02_后端基座_02_模块基类_03_请求响应基类.md) 的分页查询契约；填 [02-2-1 数据访问基类 BaseRepository](../../02_后端基座_02_模块基类/02_后端基座_02_模块基类_01_数据访问基类/02_后端基座_02_模块基类_01_数据访问基类.md) 的 `BaseDbRepository` 占位实现；同步内存基线保留为测试替身
5. 读写分离占位：读写路由接口（按上下文标记选引擎）填 `BaseRepository._resolve_binding` 预留钩子，本阶段主从同源

## 3. 完成标准 <a id="accept"></a>

数据访问接口 / 抽象声明就位、依赖注入可解析、应用可启动不报错、占位返回固定 / 批量数据可支撑上层链路；基类异步签名一致；`pytest`、`ruff`、`pyright` 全绿。**不连真库、不跑迁移**。

## 4. 参考文档 <a id="ref"></a>

- [需求 02-14](../../../../需求/02_需求_后端基座.md#r02-14)
- [02-2-1 数据访问基类 BaseRepository](../../02_后端基座_02_模块基类/02_后端基座_02_模块基类_01_数据访问基类/02_后端基座_02_模块基类_01_数据访问基类.md)（承接其 `BaseDbRepository` 骨架与路由钩子）
- 《架构设计 · 数据访问与分片》
- 《后端开发规范》「异步与并发」节
- 《数据库开发规范》「迁移」节

> 本文档依《文档生成规范》编写
