# 02-5-3 模块注册表

> 项目骨架 · 02 后端基座 · 05 机制底座 · 嵌套子任务 03

[文档首页](../../../../../../文档首页.md) › [05 机制底座](../02_后端基座_05_机制底座.md) › 03 模块注册表　|　[← 父任务](../02_后端基座_05_机制底座.md)

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02-5-3 |
| 父任务 | [05 机制底座](../02_后端基座_05_机制底座.md) |
| 对应需求 | [02-16](../../../../需求/02_需求_后端基座.md#r02-16) |
| 工时（重估） | 1h |
| 依赖 | 02-2-4（ORM 模型基类）、03-1（异常体系与统一响应） |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. `sys_module` 表**声明**（平台库，`module_key` / `name` / `table_prefix` / `errcode_segment` / `event_domain` / `status` + 基类审计字段，四要素 `(字段, deleted_at)` 复合唯一）；多语言模块名附表 `sys_module_i18n`（module_id + locale 联合主键 + name）
2. 占位数据：平台域固定 4 行（`sys` / `wf` / `rpt` / `ai`）以固定值返回；业务模块不预置，由产品仓库交付时登记
3. 注册方式约定：运行时**无写接口**；新增模块 = Alembic 迁移 revision 原子落地
4. 唯一性校验（对注册清单）：`table_prefix` / `errcode_segment` / `event_domain` 三要素唯一 + 格式校验；启动校验入口（冲突 / 非法 → 启动失败 + CRITICAL 明细）+ CI 校验入口
5. 只读接口契约：`GET /api/v1/modules` 返回注册清单（平台域 4 行固定数据，`?status` 筛选，软删除过滤，归属平台接口豁免租户解析）；本阶段暂不鉴权；**无任何写接口**

## 3. 完成标准 <a id="accept"></a>

`sys_module` 与 `sys_module_i18n` 声明齐备、占位返回平台域 4 行固定数据、唯一性校验对合法 / 冲突清单判定正确、`GET /api/v1/modules` 契约正确（`?status` 筛选、POST 404）；依赖注入可解析、应用可启动、无写接口暴露；`pytest`、`ruff`、`pyright` 全绿。**不建表**。

## 4. 参考文档 <a id="ref"></a>

- [需求 02-16](../../../../需求/02_需求_后端基座.md#r02-16)
- 《架构设计 · 模块注册》第 2/3 节
- 《项目规划说明》「模块注册要素」至「错误码段规则与注册」节
- 《命名规范》「数据库命名」节、《API接口规范》第 2/3 节

> 本文档依《文档生成规范》编写
