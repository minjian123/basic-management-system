# 接口路由基座测试记录

> 项目骨架 · 02 后端基座 · 06 接口层基座 · 01 接口路由基座 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-6-1 接口路由基座](../02_后端基座_06_接口层基座_01_接口路由基座.md) › 01 测试　|　[← 父任务](../02_后端基座_06_接口层基座_01_接口路由基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02-6-1 接口路由基座](../02_后端基座_06_接口层基座_01_接口路由基座.md) |
| 对应需求 | [02-55](../../../../需求/02_需求_后端基座.md#r02-55) |
| 详细设计 | [01_详细设计_01_接口路由基座](../设计/01_详细设计_01_接口路由基座.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_接口路由基座.md) |
| 测试日期 | 2026-09-20 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；ASGI 内存客户端，免启服务器） |
| Kiwi 用例 | 779（平台：分类「平台骨架」、P2、CONFIRMED、标签「自动化」） |
| 结论 | 通过（唯一失败为既有 `test_m3_closure.py`，非本次引入） |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 779 | 契约继承与标识 | 单元 | `tests/api/test_router_base.py::test_contract_inheritance_and_identity` | `BaseRouter → APIRouter` 且 `→ BaseObject`；`RouterRegistry → BaseObject`；`key` / `describe()` |
| 779 | 默认错误响应 | 单元 | `…::test_default_responses_merge_and_override` | `BaseRouter()` 含 `DEFAULT_RESPONSES`；调用方覆盖同名状态码；`default_responses=False` 豁免 |
| 779 | 注册与唯一性 | 单元 | `…::test_registry_register_uniqueness_and_order` | 同 `key` 拒重（`ConflictError`）；`keys` / `routers` 保序；`get` 未命中 None |
| 779 | 统一挂载 | 集成（ASGI） | `…::test_unified_mount_and_prefix` | 应用默认登记表挂 `/api/v1`；自定义注册表经 `mount` 挂指定前缀 |
| 779 | 参数绑定 | 集成（ASGI） | `…::test_parameter_binding_factories` | `page_query` / `sort_query` / `cursor_query` 解析并回带请求契约 |
| 779 | 统一响应与异常 | 集成（ASGI） | `…::test_unified_response_and_error` | `ApiResponse` 包裹；不存在转 404 统一响应（10002） |
| 779 | 鉴权占位 | 单元 + 集成（ASGI） | `…::test_auth_placeholder_dependency` | `require_auth` 恒定放行、可作路由级依赖 |

回归范围：全量用例（含接口冒烟）；`/docs` 与 `/openapi.json` 可访问。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest --cov=app -q
574 passed, 8 skipped, 1 failed in 3.70s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
286 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ python3 scripts/tools/base-check/check-base.py .
[base-integrity] 通过：无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名。

$ python3 scripts/tools/base-check/check-backend-base.py .
[check-backend-base] 通过：继承链 / 错误码段位 / 迁移链全部对齐。
```

结果汇总：新增 Kiwi 779 用例 7 条全部通过；既有用例无回归（4 个模块路由改基座后接口冒烟路径不变）。**唯一失败为既有 `tests/crosscut/test_m3_closure.py::test_gate_table_has_conclusions_and_evidence`**（阶段三「后端插件化」需求总览缺「M3 验收门禁」节，改动前即失败，与本任务无关；见[实施记录](../实施/01_实施_01_接口路由基座.md)「问题与处置」）。8 skipped 为真实 Redis 集成用例（环境未配置，随重验证层启用）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 全量 1 项失败：`test_m3_closure.py` | 既有：阶段三文档缺「M3 验收门禁」节 | 超出本任务范围，登记为既有遗留（见[实施记录](../实施/01_实施_01_接口路由基座.md)第 6 节），不在本任务修复 |
| `BaseRouter(default_responses=...)` 形参误入 `**kwargs` | 仅作类属性、未作构造形参 | `__init__` 增补形参（详见[实施记录](../实施/01_实施_01_接口路由基座.md)「问题与处置」） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 4658 语句，覆盖率 99%**。

- 本任务新增 / 改动模块**100% 覆盖**：`app/api/base.py`（61 语句）。
- 整体未覆盖 22 行均为既有代码占位分支（本任务未改动，随对应回补阶段）。任务级快照；门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项（已登记闭环，随对应阶段补用例）：真实登录态 / 权限校验（认证 / RBAC 阶段）、筛选契约 `FilterSpec` / `BaseFilterQuery`（02-4-20）、开放接口 `/api/open` 与版本化 → 见计划 §3.1 与需求 02-55 `> 注：` 块。
- Kiwi 平台登记：已完成（2026-09-20 于 mjbk Kiwi TCMS 登记用例 779）。
- 既有失败项：`test_m3_closure.py`（阶段三文档口径），本任务不改；留待阶段三文档收口对齐。
- **处理偏差与遗留（2026-09-20 闭环）**：真实鉴权 / 筛选契约 / 开放接口去向已在计划 §3.1 与需求 02-55 `> 注：` 块登记；消费方见[实施记录](../实施/01_实施_01_接口路由基座.md)「处理偏差与遗留」。
- 结论：本任务遗留已全部登记去向，偏差与遗留处理完成。

> 本文档依《文档生成规范》编写
