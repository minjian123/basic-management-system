# M1 验收门禁核对实施记录

> 项目骨架 · 04 CI 与阶段验收 · 03 M1 验收门禁核对 · 实施记录

[文档首页](../../../../../../文档首页.md) › [03 M1 验收门禁核对](../04_CI与阶段验收_03_M1验收门禁核对.md) › 实施　|　[← 父任务](../04_CI与阶段验收_03_M1验收门禁核对.md)

## 1. 实施信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [03 M1 验收门禁核对](../04_CI与阶段验收_03_M1验收门禁核对.md) |
| 对应需求 | [04-3](../../../../需求/04_需求_CI与阶段验收.md#r04-3) |
| 详细设计 | [03_详细设计_01_M1验收门禁核对](../设计/03_详细设计_01_M1验收门禁核对.md) |
| 实施日期 | 2026-09-15 |
| 实施人 | minjian |
| 实施环境 | 开发机（Ubuntu；Python 3.14 / uv；Node 22；端口 8000 / 5173 / 5174）；mjbk（Redis 实例、Kiwi TCMS 容器 `bms-kiwi`、GitLab Runner） |
| Kiwi 用例 | 68（M1 验收门禁核对：门禁 10 项证据 / 起服务与探针四态 / 状态三处一致 / 脚本硬规则 / 交叉审核留痕） |
| 提交 | `41f39a3`（`chore(ci)`：一致性核对脚本 + `base-integrity` 接入）；文档收口随本任务文档提交 |
| 结论 | 完成（门禁 9 项达标、1 项进行中） |

## 2. 实施概览 <a id="overview"></a>

```mermaid
flowchart LR
    A[Kiwi 68 先登记] --> B[新增 check-status.py<br/>硬 8 + 软 3]
    B --> C[挂入 base-integrity<br/>不扩触发面]
    C --> D[起三端服务<br/>8000 / 5173 / 5174]
    D --> E[探针四态与统一响应<br/>200 / 200 / 503 / code=10002]
    E --> F[10 项证据采集与对位]
    F --> G[门禁表扩为 5 列 + 结论]
    G --> H[需求 / 任务 / 计划三处回写]
    H --> I[第二 AI 交叉审核<br/>6 项发现全处置]
    I --> J[本地门禁全绿 → 推送]
```

结果小结：M1 门禁 10 个验收点逐项有命令、有输出、有索引——**9 项达标、1 项进行中**（第 10 项「验收留痕齐备」待 04_04）；阶段一 54 条需求状态与完成日期按需求 / 任务 / 计划三处回写（53 条已完成）；新增状态一致性核对脚本并常挂 CI；交叉审核发现 6 项，其中 2 项为**真实缺陷**（CI 潜伏项、历史记录数字矛盾）已当场修复，4 项为口径与证据修正并已回写设计。

## 3. 实施过程 <a id="process"></a>

1. **Kiwi 先登记**：经 mjbk 容器内 Django shell 登记本任务 1 条用例并取得编号 **68**（产品「BMS 基础管理系统」/ 分类「平台骨架」/ P2 / CONFIRMED / 标签「自动化」/ `is_automated=True`），题面含门禁结论齐备、探针四态、脚本硬规则、三处一致、交叉审核留痕五项断言。
2. **一致性核对脚本**：新增 `scripts/tools/check-docs/check-status.py`（Python 3.14 标准库；硬规则 8 条阻断、软提示 3 条告警、`--strict` 可升级）；`scripts/README.md` 工具链清单登记；`.gitlab-ci.yml` 的 `base-integrity` job 追加一行调用（**不新增 job、不扩触发面**，避免破坏「纯业务文档推送不建流水线」口径）。
3. **起三端服务**：后端 `uv run uvicorn app.main:create_app --factory --host 127.0.0.1 --port 8000`（长任务以 `scripts/tools/bg/bg-run.py` 后台化，日志落 `~/.bg/`）；前端 `npm run dev` 起 5173 与 5174。
4. **探针四态与统一响应**：`/docs` 200、`/` 200（`{"code":0,…}`）、`/healthz` 200；`/readyz` 依赖可达 → 200（`checks.redis.ok=true`、`database.ok=true`）、默认本地（无 Redis）与 `BMS_REDIS__URL=redis://127.0.0.1:6399/0` → 503（`redis.ok=false`、`database.ok=true`）；`/api/v1/demos/999999` → 404 `{"code":10002,…}`、`/api/v1/demos` → 200 `{"code":0,"data":[]}`、参数校验 → `{"code":10001,…}`；确认无临时 404 处理器残留。
5. **配置 / 日志实证**：`BMS_SERVER__PORT=not_a_number` → `ConfigError：配置校验失败：server.port：Input should be a valid integer…`、退出码 1；`BMS_LOG__FORMAT=json` → 单行 JSON（`event` / `level` / `logger` / `ts`）。
6. **基座对位**：`grep -rn '^class Base[A-Za-z0-9_]' backend/app` = 63（`^class Base` 为 64，含 ORM 声明基）、`^class Null` = 38；与《架构设计 · 后端基础类体系》§4 基类清单逐名对位（63/63），§4 引用的 65 个文件路径中 64 个存在，缺 `core/plugin.py`（插件化中间层，架构登记为随能力回补）；六类契约目录 `app/{cache,scope,sharding,events,audit,tasks}` 齐备。
7. **本地门禁**：`ruff check` 通过、`ruff format --check` 218 文件已格式化、`pyright` 0 errors、`pytest -q --cov=app --cov-branch --cov-fail-under=70` = **417 passed / 7 skipped**、覆盖率 **99.75%**；`uv run python -m ops.check_modules` = 校验通过（4 项）。
8. **门禁回写**：《需求总览》§4 门禁表由 4 列扩为 **5 列**（新增「证据索引」），10 行结论逐项落地（9 达标 + 1 进行中）；§4 结论句同步为 53/54。
9. **状态回写**：需求 04-3 元信息行 + `> 注：` 核对结论块 → 总览 §3 总清单 04-3 行、§5 域地图（03 域 2/1 → 3/0、04 域 3/1）→ 任务文档与域总览 → 计划（头部计数 17/1、§1 排期对象与工时 127h/2h、§2 章节区间与 04_03 行、§3 删 04_03 行与剩余合计 2h、§4 甘特、§5 门禁路径与留痕行）。
10. **规范与设计同步**：《需求文档规范》§3 补门禁表列口径（5 列 + 结论取值）、§6 检查清单补一条；设计回写 3 处实测修正（启动命令、第 6 项核对手法、脚本范围与 S3 细化）。
11. **交叉审核**：以「第二 AI 独立复核 + 人工异常项处理」替代评审会（单人期），6 项发现全部处置（见 [§4](#issues)）。
12. **提交推送**：脚本 + CI 接入提交 `41f39a3`；文档收口随本任务文档提交；推送后盯流水线全绿（第 3 项证据回填）。

## 4. 问题与处置 <a id="issues"></a>

**实施期问题**：

| # | 问题 / 现象 | 原因 | 处置与落点 |
| --- | --- | --- | --- |
| 1 | 设计初稿启动命令 `uv run uvicorn app.main:app` 报 `Error loading ASGI app. Attribute "app" not found in module "app.main"` | 后端为应用工厂形态（`app/main.py` 仅导出 `create_app()`，无模块级 `app`） | 改为 `uv run uvicorn app.main:create_app --factory`；设计 §4.1 第 1 项 / §9 / §14 第 8 行已回写 |
| 2 | 第 6 项初稿以「未匹配路由」探测统一响应，实测返回框架默认 `{"detail":"Not Found"}` | 未匹配路由由 Starlette 默认处理器处理，不属统一响应口径（02-9 口径为业务异常路径） | 改用 `/api/v1/demos/999999` → `code=10002`；设计 §4.1 第 6 项 / §13 / §14 第 9 行已回写 |
| 3 | 脚本初版对阶段 0 准备期误报 49 项硬规则 | 准备期文档为早期口径（完成日期含时间戳、状态有「框架完成」等扩展取值、域总览子任务清单列序不同） | 默认范围收为阶段一，`--stage` / `--all` 显式纳入历史阶段；设计 §5.1 / §14 第 10 行已回写 |
| 4 | S3 软规则误报 02_02 / 02_03 / 02_05「缺 实施/ 记录」 | 父任务（整合口径）的记录落在嵌套子任务目录 | 规则改为「本目录或任一子目录存在 `实施/`」；设计 §5.2 已回写 |
| 5 | H7 工时误报 6 项（如 `21h（重估：自有 3h + …）` vs `21h`） | 任务信息表单元格带说明文字 | 改为取前导 `\d+h` 后比对；设计 §5.2 已回写 |
| 6 | 脚本首跑报《需求总览》§5 中 03 域计数 `2 / 1` 过时 | 03-3 于 2026-09-14 完成后未同步域地图 | 已修正为 `3 / 0 已完成`（脚本价值的实证） |
| 7 | 本地起服务生成 `backend/bms_platform.db`，且未被 `.gitignore` 覆盖（易误入库） | 仓库忽略规则只含 Django 默认 `db.sqlite3` | 已补 `.gitignore` 规则 `backend/bms_*.db`（含 journal）；本产物不入库 |

**交叉审核结论与人工异常项处理**（第二 AI 独立复核，只读核查，逐条挑战证据）：

| # | 审核发现 | 性质 | 处置与落点 |
| --- | --- | --- | --- |
| a | 04-3 实施 / 测试记录在审核时点尚未落盘，而需求 / 任务 / 计划 / 门禁表已引其为证据源（链接悬空） | 时序性缺证（审核早于记录） | 本记录与 [03_测试_01_M1验收门禁核对](../测试/03_测试_01_M1验收门禁核对.md) 即为补证，引用链接随之闭合；审核意见闭环 |
| b | 第 4 / 9 项计数口径矛盾：`grep '^class Base'` 字面为 **64**（含 ORM `class Base(DeclarativeBase)`），文档写 63 | 事实性口径错误 | 统一为 `^class Base[A-Za-z0-9_]` = **63**，并注明 64 的差异来源；门禁表第 4 / 9 行、设计 §4.1 第 4 / 9 项、需求 04-3 注块已修正 |
| c | 第 9 项证据错配：以 02_03 / 02_04（Kiwi 39 ~ 61）支撑 02-17 ~ 02-41，遗漏 **02-40 排序契约**（交付于 02_02 嵌套 05） | 证据链未闭合 | 门禁表第 9 行与设计 §4.1 第 9 项补「02-40 排序契约交付于 02_02（嵌套 05）」 |
| d | **潜伏缺陷**：`.gitlab-ci.yml` 的 `swagger-snapshot` job 用 `from app.main import app`，与「仅应用工厂」口径冲突，重验证层激活后必然报错 | 代码 / 配置缺陷 | 当场修复为 `from app.main import create_app; create_app().openapi()`（该 job 仍以 `deploy/ci/verify-enabled` 守门未激活；2026-09-15 改分档 `verify/contract` 并激活） |
| e | `02-5-1`（数据访问底座）测试记录内自相矛盾：命令输出 `139 passed, 2 skipped`，汇总写「全量 133 passed」 | 历史记录数字矛盾 | 按命令输出校正汇总为 139 passed, 2 skipped |
| f | 运行态证据（流水线全绿、417 passed / 覆盖率 99.75%、脚本 373 项核对（收口后 374 项）、§4 路径 65/64 计数）无法静态验证 | 证据形态限制 | 由本记录 §5 与测试记录 §3 承载命令与实测输出，流水线结论由 GitLab 数据承载（编号见 §5 第 3 项）；可选：后续把 `pytest` 摘要落入测试记录 |

**审核通过项**：起服务命令与端口口径、健康检查响应结构与真实探针、CI 守卫与 job 清单、机制底座占位口径、异常体系处理器与错误码、配置校验与 JSON 日志、六类契约与计划 §3.1 登记、Null 占位实现计数、第 10 行时点口径与 04_04 存在性、需求 / 任务 / 计划三处状态与数值对齐——均成立。

## 5. 验证结果 <a id="verify"></a>

### 5.1 门禁 10 项证据索引（全表）

| # | 验收点 | 核对命令 / 手法 | 实测结果 | 结论 | 对应需求 |
| --- | --- | --- | --- | --- | --- |
| 1 | 本地起服务 | `uv run uvicorn app.main:create_app --factory --host 127.0.0.1 --port 8000`；`npm run dev`（frontend / frontend-mobile） | 后端起来（`Application startup complete.`）、`/docs` 200；5173 / 5174 各 200（标题「BMS 基础管理系统」） | 达标 | 01-1 ~ 01-6、03-1 |
| 2 | 健康检查正常 | `curl /healthz`、`curl /readyz`（依赖可达 / 不可达两态） | `/healthz` 200 `{"status":"ok"}`；`/readyz` 200（`redis.ok=true`、`database.ok=true`）/ 503（`redis.ok=false`、`database.ok=true`）；启动未完成态 503 见 03-3 记录（Kiwi 64 / 65） | 达标 | 03-3 |
| 3 | CI 流水线激活通过 | GitLab API 取 main 流水线状态与 job 清单 | 流水线 170（04-01）/ 174（04-02）全绿；收口推送 **流水线 176**（`34936ed5`）**12 job 全绿、44 秒**（含 `base-integrity` 内的文档状态核对、`backend-typecheck`、`backend-modules`、`integration-plan`）；三库真库 job 与缺陷链路未出现 | 达标 | 04-1、04-2 |
| 4 | 基座继承链就位 | `grep -rn '^class Base[A-Za-z0-9_]' backend/app`（63）与架构 §4 清单对位 | 63/63 逐名一致；§4 路径 65 个中 64 存在（缺 `core/plugin.py`，架构登记随能力回补） | 达标 | 02-1 ~ 02-8 |
| 5 | 机制底座接口齐备（占位可跑） | 应用启动（DI 可解析）+ `uv run pytest tests/db tests/ops -q` | 启动成功；用例全绿；占位返回固定 / 批量数据（02_05 三嵌套记录） | 达标（占位口径） | 02-14 ~ 02-16 |
| 6 | 异常体系生效 | `curl /api/v1/demos/999999`、`/api/v1/demos`、参数校验；`grep` 临时 404 处理器 | 404 `{"code":10002,…}`、200 `{"code":0,…}`、`{"code":10001,…}`；无临时 404 处理器残留 | 达标 | 02-9 |
| 7 | 配置 / 安全 / 日志结构就位 | `BMS_SERVER__PORT=not_a_number` 启动；`BMS_LOG__FORMAT=json` 启动 | `ConfigError：配置校验失败：server.port…` + 退出码 1；单行 JSON 日志（`event` / `level` / `logger` / `ts`） | 达标 | 02-10、02-11、02-12 |
| 8 | 跨阶段基座登记齐备 | `ls backend/app/`（六类）+ 架构节点 + 计划 §3.1 | `cache` / `scope` / `sharding` / `events` / `audit` / `tasks` 六目录齐备；计划 §3.1 六类回补窗口逐条 | 达标 | 02-13 |
| 9 | 补充扩展基座接口占位齐备 | `^class Base[A-Za-z0-9_]` 63 / `^class Null` 38；全量 pytest；占位判据 | 63 / 38；417 passed / 7 skipped、覆盖率 99.75%；真实依赖未装配 | 达标（占位口径） | 02-17 ~ 02-41 |
| 10 | 验收留痕齐备 | 本任务留痕 + 04_04 交付物 | 门禁表 5 列 10 行结论与证据索引已落 + 本记录 §5；报告与 README 待 04_04 | **进行中**（04_04 交付后转达标） | 04-3、04-4 |

### 5.2 本地门禁与自检

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 静态检查 | `uv run ruff check .` / `uv run ruff format --check .` / `uv run pyright` | 通过 / 218 文件已格式化 / 0 errors |
| 测试与覆盖率 | `uv run pytest -q --cov=app --cov-branch --cov-fail-under=70` | 417 passed、7 skipped；覆盖率 99.75%（门禁 ≥ 70%） |
| 模块注册清单 | `uv run python -m ops.check_modules` | 校验通过（4 项） |
| 文档状态一致性 | `python3 scripts/tools/check-docs/check-status.py` | 374 项核对、硬规则 0 不合规（软提示仅「实施记录尚未落盘」，本记录即闭环） |
| 基座自检 | `python3 scripts/tools/base-check/check-base.py` | 断链 0 / 跨层 0 / 措辞 0 / 清单 146 = 146 / 编号引用 0（记录落盘后复跑） |
| 流水线 | 流水线 **176**（`34936ed5`，收口推送） | **success**，12 job 全绿、时长 44 秒：`ci-base-build`、`gate-smoke`、`base-integrity`（已含 `check-status.py`）、`backend-lint`、`backend-typecheck`、`backend-modules`、`backend-test`、`frontend-check`、`frontend-mobile-check`、`integration-plan`、`frontend-build`、`frontend-mobile-build`；重验证层真库 job 与缺陷链路按 `verify-enabled` 守门未出现 |

> 流水线结论由 GitLab 承载：项目 `bms/bms` → Pipelines → 176。

## 6. 偏差与遗留 <a id="deviations"></a>

- **设计偏差（5 处，均已回写设计）**：§4.1 第 1 项启动命令改为应用工厂形态；§4.1 第 6 项核对手法改用业务异常路径；§5.1 默认核对阶段一（`--stage` / `--all` 显式纳入历史阶段）；§5.2 S3 收窄为「已完成任务留痕记录齐备」并容许父任务由嵌套子任务承担；§5.2 H7 工时取前导 `\d+h`。§14 第 8 ~ 10 行已登记。
- **交叉审核引入的修复（2 项真实缺陷）**：`.gitlab-ci.yml` 的 `swagger-snapshot` job 导入写法改为应用工厂（潜伏项，未激活即已修）；`02-5-1` 测试记录汇总数字校正为 139 passed。
- **第 10 项时点口径（登记，非遗留）**：04_03 记「进行中」，04_04 交付测试报告与 README 时回写「达标」并补证据（[设计 §6.4](../设计/03_详细设计_01_M1验收门禁核对.md#timing)）。
- **无新增遗留**：机制类真实实现、跨阶段基座回补、三库真库集成与达梦方言实测、Allure→Kiwi 接线、核心模块覆盖率 ≥ 80%、重验证层激活前置——均沿用既有登记（计划 §3.1），本任务未产生新待办。
- **可选改进（已登记）**：把 `pytest` 摘要与流水线 job 清单随记录归档，减少「运行态证据需再跑一次」的成本（本任务已按 §5.2 记录本次输出）。

> 本文档依《文档生成规范》编写
