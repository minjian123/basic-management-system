# 04 CI 与阶段验收

> 项目骨架父任务 · 「04 CI 与阶段验收」（4 个子任务）

[文档首页](../../../../文档首页.md) › 项目骨架任务 › 04 CI 与阶段验收　|　[← 03 后端基础能力](../03_后端基础能力/03_后端基础能力.md)

## 1. 域说明 <a id="intro"></a>

- **覆盖范围**：CI 流水线激活（main 冒烟层 / 重验证层 / exists 激活 / 覆盖率门禁 / CI 基础镜像）、三库集成与流水线收口（接口占位：测试库流程脚本 / 用例骨架 / 开关守卫）、M1 验收门禁核对（骨架三项 + 基座六项 + 验收留痕）、阶段测试报告与 README。
- **依据**：[需求 · CI 与阶段验收](../../需求/04_需求_CI与阶段验收.md)（需求 04-1 ~ 04-4）、《总体项目规划》「阶段内容与完成标准」节、「监控与报告」节、《开发部署规划》「开发工作流与 CI」节、《测试规范》「覆盖率要求」节、《架构设计 · 测试与质量》「CI 流水线」节。
- **与前后域的关系**：本域依赖 02 域（机制底座 02-14 ~ 02-16 提供数据访问 / 多租户拓扑 / 模块注册占位）与 03 域（配置 / 日志 / 健康检查就绪后冒烟层才有真实内容可跑）；本域收口后阶段一进入终态，阶段验收门禁放行方可进入阶段四（前端组件库）。
- **接口占位口径**：三库集成在本阶段只做「脚本占位 + 用例骨架 + 开关守卫」，不连真库、不建库、不跑迁移；真实三库集成随后续 后端基座激活。

## 2. 子任务清单 <a id="list"></a>

| 编号 | 任务 | 工时（重估） | 状态 | 完成日期 | 任务文件 |
| --- | --- | --- | --- | --- | --- |
| 01 | CI 流水线激活 | 3h | 已完成 | 2026-09-14 | [01](04_CI与阶段验收_01_CI流水线激活/04_CI与阶段验收_01_CI流水线激活.md) |
| 02 | 三库集成与流水线收口（占位） | 1h | 已完成 | 2026-09-15 | [02](04_CI与阶段验收_02_三库集成与流水线收口/04_CI与阶段验收_02_三库集成与流水线收口.md) |
| 03 | M1 验收门禁核对 | 2h | 已完成 | 2026-09-15 | [03](04_CI与阶段验收_03_M1验收门禁核对/04_CI与阶段验收_03_M1验收门禁核对.md) |
| 04 | 测试报告与 README | 2h | 已完成 | 2026-09-15 | [04](04_CI与阶段验收_04_测试报告与README/04_CI与阶段验收_04_测试报告与README.md) |

## 3. 工时与依赖 <a id="effort"></a>

- **工时合计**：8h（3+1+2+2），与《[项目骨架计划](../../计划/01_计划_项目骨架.md)》「已完成任务」表 04 域各行一致（域内 4/4 已完成，全部工时已交付）。
- **关键依赖**：01 → `03_02`、`03_03`（日志与健康检查就绪后冒烟层才有真实内容）、`01_02`、`01_03`（工程骨架与分层目录）；02 → `02_05`（机制底座：数据访问 / 多租户拓扑 / 模块注册占位）、01；03 → 阶段一全部域（01 ~ 04）；04 → 03。
- **风险**：重验证层部分 job 未就绪（E2E 用例、Trivy 外网不可达、三库集成）——以分档 `deploy/ci/verify/*` 守卫（P1 + P2 已开，Allure 导入已于 2026-09-15 接线），其余逐档修复就绪后开启；CI 基础镜像 push 报 `blob unknown`——按 best-effort 降级（单 runner 本机镜像可用），恢复强制推送待 registry 排查；前端覆盖率门禁口径（全量 `src/**` 采集与阈值）需在 04_01 定案。
- **状态说明**：需求 04-1 已「已完成」（2026-09-14，Kiwi 66）——冒烟层补齐 `backend-typecheck`（pyright）、`backend-modules`（模块注册清单校验）、`mobile-check`（ESLint + Vitest），前端双端接入 Vitest v8 coverage（阈值 70% 阻断）；`needs` 并行编排后流水线全绿（170，总时长 49 秒）；CI 基础镜像标签口径改为「三份锁文件 + 两份 Dockerfile」哈希，后端镜像装 `libatomic1` 且 apt 走清华源。遗留已登记：Allure→Kiwi 接线（**已于 2026-09-15 接线**，分档开关 `verify/report` 开启，见 04-01 实施 02；原登记见计划 §3.1）、核心模块覆盖率 ≥ 80%（随认证 / RBAC 阶段）、重验证层激活前置（E2E 待 `tests/e2e` 用例、三库随后续 后端基座、Trivy 待外网）。需求 04-2 已于 2026-09-15 完成（Kiwi 67）：新增 `backend/ops/test_db.py`（`plan` / `create` / `migrate` / `drop` + `--engine`，占位＝默认计划模式）、常跑 job `integration-plan`（三方言计划校验，无需真库）、拓扑路由集成骨架（5 条，1 条纯清单常绿），三库真库 job 换 `ci-backend:py314` 并在注释登记激活前置；流水线 174 全绿 50 秒。遗留：真实建库 / 迁移 / 真库用例与达梦方言实测随后续 后端基座（激活前置见计划 §3.1）。需求 04-3 已于 2026-09-15 完成（Kiwi 68）：阶段验收门禁 10 个验收点 9 项达标、1 项进行中（第 10 项待 04-4 交付报告与 README 后转达标），逐项结论与证据索引落《需求总览》§4（门禁表扩为 5 列）；实测证据含起三端服务（后端 8000 + 前端 5173 / 5174）、`/healthz` 200、`/readyz` 200 / 503 双态、`code=10002` 统一响应、配置启动校验退出码 1、JSON 日志、基类 63 个与架构 §4 逐名一致；新增 `scripts/tools/check-docs/check-status.py`（状态一致性核对，挂 `base-integrity`），据此修正 03 域地图计数过时；三类文档状态已回写（53 / 54）。需求 04-4 已于 2026-09-15 完成（Kiwi 69）——阶段测试报告落 `bms文档/项目/01_项目骨架/01_测试报告_项目骨架.md`（四章节 + 复盘 + 附录取证；自动化用例 460、Kiwi 69 条、后端覆盖率 99.75%、前端双端 100%、阶段一缺陷 Issue 0 条、工期偏差 -13 天），新增治理脚本 `scripts/tools/governance/collect_metrics.py`（三项度量 + 用例统计）与 `review_stage.py`（复盘清单 8 项），根与三工程 README 全面同步；**阶段验收门禁第 10 行转「达标」、阶段一 54/54 闭环**（域 04 由 3/4 结为 4/4，全部 8h 交付）。

> **收口后补登（2026-09-16）**：模块边界护栏（批 1）已落地——`backend/tests/crosscut/test_module_boundary.py`，AST 静态扫描三条断言（扫描覆盖自检 ≥100 文件防假绿；`core/` 不依赖 `api/` `services/`、`repositories/` 不依赖 `services/`；跨包不 import 下划线私有模块），随 pytest 自动执行、无需改流水线配置；Kiwi 688（分类「平台骨架」/ P2 / CONFIRMED / 标签「自动化」）。依据《后端开发规范》「依赖方向」节与「模块边界与数据所有权」节；批 2（ORM 关系与 SQL 表前缀扫描）待批 1 稳定后补齐。本项属阶段一收口后补登，不新增子任务、不调整工时与计划。

> 本文档依《文档生成规范》编写
