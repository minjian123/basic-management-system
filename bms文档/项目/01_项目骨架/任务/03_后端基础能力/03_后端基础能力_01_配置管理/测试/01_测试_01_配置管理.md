# 配置管理测试记录

> 项目骨架 · 03 后端基础能力 · 01 配置管理 · 测试记录

[文档首页](../../../../../../文档首页.md) › [03-1 配置管理](../03_后端基础能力_01_配置管理.md) › 测试　|　[← 父任务](../03_后端基础能力_01_配置管理.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 配置管理](../03_后端基础能力_01_配置管理.md) |
| 对应需求 | [03-1](../../../../需求/03_需求_后端基础能力.md#r03-1) |
| 详细设计 | [01_详细设计_01_配置管理](../设计/01_详细设计_01_配置管理.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_配置管理.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv） |
| Kiwi 用例 | 32（结构回归）+ 62（真实加载） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 32 | 默认值与分区 | 单元 | `tests/core/test_config.py::test_defaults_and_sections` | 八分区齐备、默认值正确 |
| 32 | 嵌套默认不共享 | 单元 | `…::test_nested_defaults_not_shared` | `allow_origins` / `pool` / `replicas` 不共享可变默认 |
| 32 | 读取接口单例 | 单元 | `…::test_get_settings_singleton` | `get_settings()` 单例；`cache_clear()` 后重载 |
| 32 | 取值校验 | 单元 | `…::test_env_validation` | `app.env` 合法三值通过；非法值 `ValidationError` |
| 32 | 公共基配置 | 单元 | `…::test_base_settings_config` | `extra="forbid"`、继承 `BaseSchema` 公共配置 |
| 32 | 序列化 | 单元 | `…::test_serialization` | `to_dict()` 含八分区；`to_json` 稳定序 |
| 62 | 缺省环境 | 单元 | `…::test_default_env_is_dev` | 缺省 `dev`；dev 覆盖（DEBUG/console/debug/CORS） |
| 62 | test 环境 | 单元 | `…::test_test_env_overlay` | `app.env=test`、INFO/json、CORS 空、debug false |
| 62 | prod 环境 | 单元 | `…::test_prod_env_overlay` | `app.env=prod`、WARNING/json |
| 62 | 环境回退 `[app].env` | 单元 | `…::test_env_selector_fallback_to_app_env` | 无 `BMS_ENV` 时取 `[app].env` 并回写 |
| 62 | `.env` 环境选择 | 单元 | `…::test_env_selector_from_dotenv` | `BMS_ENV` 可写在 `backend/.env` |
| 62 | 环境回退 dev | 单元 | `…::test_env_defaults_when_no_config` / `…_when_app_not_dict` | 无配置 / `[app]` 非表时回落 dev |
| 62 | 环境变量覆盖 | 单元 | `…::test_env_variable_override` | 完整 URL 覆盖 + `BMS_SERVER__PORT` 覆盖 |
| 62 | 密码分字段注入 | 单元 | `…::test_password_field_injection` | `resolved_url()` 合成密码；无密码时原样 |
| 62 | `BMS_ENV` 非法 | 单元 | `…::test_invalid_bms_env_fails` | `ConfigError` 含 `BMS_ENV` |
| 62 | 缺必填键 | 单元 | `…::test_missing_required_key_fails` | `ConfigError` 含 `database.platform.url` |
| 62 | 未知 / 拼错键 | 单元 | `…::test_unknown_key_fails` | `ConfigError` 含 `levle` |
| 62 | 类型 / 取值非法 | 单元 | `…::test_invalid_value_fails` / `…::test_invalid_log_level_fails` | `ConfigError` 含 `server.port` / `log.level` |
| 62 | prod 密钥校验 | 单元 | `…::test_prod_requires_secret_key` | `validate_startup` 抛 `ConfigError` |
| 62 | WorkerId 接线 | 单元 | `…::test_worker_id_wiring` | 配置 `worker_id=7` 后雪花 ID 正常产出 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest --cov=app -q
374 passed, 2 skipped in 1.31s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
209 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ python3 scripts/tools/base-check/check-base.py
[base-integrity] 通过：基座链接自洽、无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名。
```

结果汇总：配置管理用例 20 条通过（Kiwi 32 结构回归 6 条 + Kiwi 62 真实加载 14 条）；全量 374 passed，2 skipped（真实 Redis 集成用例，环境未配置）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 失败路径用例先期断言类型不符 | 直接调 `Settings()` 抛 `ValidationError`，入口应经 `load_settings()` 转 `ConfigError` | 失败路径用例改调 `load_settings()` |
| pyright 私有函数跨模块使用告警 | 测试直连 `config._resolve_environment` | 两处加 `# pyright: ignore[reportPrivateUsage]`（覆盖环境回退边界） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 3435 语句，覆盖率 99%**；本次重写模块 `app/core/config.py` **131 语句，覆盖率 100%**（任务级快照；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- Kiwi 32 结构回归保留；真实加载路径统一归 **Kiwi 62**（新登记编号）。
- `ConfigError` 纳入统一异常体系（`BizError`，`40001`）后，四类失败路径与 prod 校验用例断言不变（异常类型仍为 `ConfigError`）。
- 真实 Redis / 三库集成用例（2 条）需外部环境，本机未配置即跳过，随 04_02 重验证层启用。
- prod `security.secret_key` 非空校验归设计 §13 遗留项，随认证阶段复核（已在计划 §3.1「安全原语真实实现与依赖注入」行登记）。
- **处理偏差与遗留（2026-09-14 闭环）**：真实 Redis 集成 2 条跳过去向已在计划 §3.1「Redis 集成用例」行登记（随 04_02 重验证层执行）；WorkerId 双名已按用户拍板闭环（移除 `BMS_WORKER_ID` 旧名兜底，Kiwi 30 用例调整，见[实施记录](../实施/01_实施_01_配置管理.md)「处理偏差与遗留」）；prod 密钥形态复核归口同见实施记录。
- **结论**：测试遗留已全部登记去向或闭环。

> 本文档依《文档生成规范》编写
