# 日志体系测试记录

> 项目骨架 · 03 后端基础能力 · 02 日志体系 · 测试记录

[文档首页](../../../../../../文档首页.md) › [03-2 日志体系](../03_后端基础能力_02_日志体系.md) › 测试　|　[← 父任务](../03_后端基础能力_02_日志体系.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02 日志体系](../03_后端基础能力_02_日志体系.md) |
| 对应需求 | [03-2](../../../../需求/03_需求_后端基础能力.md#r03-2) |
| 详细设计 | [01_详细设计_01_日志体系](../设计/01_详细设计_01_日志体系.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_日志体系.md) |
| 测试日期 | 2026-09-14 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv；structlog 26.1.0；httpx ASGITransport） |
| Kiwi 用例 | 63（日志体系）+ 34（日志基座回归）+ 44（链路中间件回归） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 34 | 门面 / 初始化 / stdout / bind（回归） | 单元 | `tests/core/test_logging.py` | 继承链、幂等与级别、stdout 输出、`bind` 隔离 |
| 63 | JSON 渲染 | 单元 | `…::test_json_render_single_line_and_fields` | 物理单行、`ts` 含偏移、level / logger 齐备、中文不转义 |
| 63 | 异常单行堆栈 | 单元 | `…::test_json_exception_stack_is_single_line` | 异常记录物理单行、`exception` 含类型与消息 |
| 63 | console 渲染 | 单元 | `…::test_console_render` | 可读输出含事件与字段 |
| 63 | 上下文注入 | 单元 | `…::test_context_fields_injected` | `trace_id` / `request_id` / `tenant` / `user_id` / `client_ip` 自动注入 |
| 63 | 显式字段优先 | 单元 | `…::test_explicit_field_wins_over_context` | 调用点字段覆盖上下文值 |
| 63 | 脱敏（键名 / 后缀 / 嵌套） | 单元 | `…::test_redaction_sensitive_keys` | 原值不出现、掩码 `***`、嵌套命中 |
| 63 | 脱敏（连接串正则） | 单元 | `…::test_redaction_connection_string` | `scheme://user:pass@` 密码掩码、用户名保留 |
| 63 | 限深防护 | 单元 | `…::test_redaction_depth_limit_keeps_value` | 达到限深原样返回 |
| 63 | stdlib 统一 / access 关闭 | 单元 | `…::test_stdlib_logger_rendered_and_access_disabled` | `sqlalchemy.engine` 走 JSON 渲染；`uvicorn.access` disabled |
| 63 | TraceId 兜底 | 单元 | `tests/api/test_middleware.py::test_falls_back_to_request_id` | 上下文有 request_id 时 trace_id 取之 |
| 63 | 访问日志 + 兜底 | 接口 | `tests/api/test_request_logging.py::test_request_log_and_request_id_fallback` | INFO `request`、字段齐备、`trace_id == request_id`、响应头回写 |
| 63 | 入站链路透传 | 接口 | `…::test_inbound_trace_id_echoed` | `X-Trace-Id: abc` 透传回写、`request_id` 独立 |
| 63 | 排除路径 | 接口 | `…::test_excluded_paths_not_logged` | `/healthz` `/readyz` `/docs` `/openapi.json` 零日志行 |
| 63 | 来源 IP | 接口 | `…::test_client_ip_prefers_forwarded_for` | `X-Forwarded-For` 首值优先 |
| 63 | 非 HTTP 直通 | 单元 | `…::test_non_http_scope_passthrough` | lifespan 直通、不进上下文 |
| 63 | 慢请求 | 接口 | `…::test_slow_request_warning` | 超阈值整行 WARNING（`slow_request`） |
| 63 | 未捕获异常 | 接口 | `…::test_uncaught_exception_logged_with_context` | 500 不回显堆栈；ERROR 行含堆栈与 trace_id；`X-Request-Id` 与日志同值 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest --cov=app -q
393 passed, 2 skipped in 1.37s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
211 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations

$ python3 scripts/tools/base-check/check-base.py
[base-integrity] 通过：基座链接自洽、无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名。
```

结果汇总：日志体系用例 19 条通过（Kiwi 63 新增 17 条 + Kiwi 34 / 44 回归）；全量 393 passed，2 skipped（真实 Redis 集成用例，环境未配置，随 04_02 重验证层启用）。

本地起服务补充实测（非自动化）：dev console 与 `BMS_ENV=test` JSON 两种形态下单行输出、探针与 `/docs` 无访问行、`X-Trace-Id` 透传与回写、无入站头 `trace_id == request_id`、`ts` 含 `+08:00`、uvicorn.error 统一渲染且无 access 重复行。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 异常用例中 httpx 重抛未处理异常 | `ServerErrorMiddleware` 发送 500 后重新抛出 | 用例 `raise_app_exceptions=False`，断言 500 响应与日志 |
| 慢请求用例难以稳定触发 | 真实耗时不可控 | 以 `slow_request_ms=0` 构造必然越阈 |
| 时间戳无偏移 | `TimeStamper(utc=False)` 产朴素本地时间 | 断言含偏移；实现改自定义处理器（设计 §14 #13 回写） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app -q`：**TOTAL 3590 语句，覆盖率 99%**；本次改动模块 **100%**——`app/core/logging.py` 112 语句、`app/api/middleware.py` 74 语句、`app/api/errors.py` 48 语句、`app/core/context.py` 52 语句（任务级快照；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- Kiwi 34（日志基座接入点）与 Kiwi 44（链路中间件）结构 / 契约回归保留；日志体系新增路径统一归 **Kiwi 63**。
- 真实 Redis 集成用例（2 条）需 `BMS_TEST_REDIS_URL`，本机未配置即跳过，随 04_02（三库集成与流水线收口）重验证层启用。
- **处理偏差与遗留（2026-09-14 闭环）**：`ts` 时区偏差已回写设计；otel 链路上报 / `span_id` 关联、`tenant` / `user` 启用、body 脱敏过滤器均已在计划 §3.1 与需求 03-2 `> 注：` 块登记去向（详见[实施记录](../实施/01_实施_01_日志体系.md)「偏差与遗留」）。
- **结论**：测试遗留已全部登记去向或闭环。

> 本文档依《文档生成规范》编写
