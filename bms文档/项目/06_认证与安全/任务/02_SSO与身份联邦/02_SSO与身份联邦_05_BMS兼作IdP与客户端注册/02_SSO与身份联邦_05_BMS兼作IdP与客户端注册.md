# 05 BMS 兼作 IdP 与客户端注册

> 认证与安全 · 02 SSO 与身份联邦 · 子任务 05（需求 02-5）

[文档首页](../../../../../文档首页.md) › [02 SSO 与身份联邦](../02_SSO与身份联邦.md) › 05 BMS 兼作 IdP 与客户端注册　|　[← 父任务](../02_SSO与身份联邦.md)

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 05 |
| 父任务 | [02 SSO 与身份联邦](../02_SSO与身份联邦.md) |
| 对应需求 | [02-5](../../../需求/02_需求_SSO与身份联邦.md#r02-5) |
| 工时（重估） | 24h |
| 依赖 | 01_02（用户令牌密钥体系）、01_03（会话与登录）；阶段二 `07_02`（服务 JWT 与 JWKS） |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. **Provider 内核**：OIDC Discovery（`/.well-known/openid-configuration`）、JWKS、授权码流程（`/authorize` / `/token` / `/userinfo`）、ID Token 签发（复用用户令牌密钥体系，`kid` 与 `aud` 口径区分）、支持 PKCE。
2. **客户端注册（最小）**：`sys_client` 表文件先设计后落库（`client_id` / `client_secret` 哈希 / `redirect_uris` / `grant_types` / scope），提供内部最小注册与管理接口（创建 / 查询 / 启停 / 重置凭据；服务 JWT 或平台超管口径）；**管理页面归阶段十**。
3. **令牌与撤销**：授权码单次有效短 TTL；客户端凭据独立撤销；签发审计经操作日志占位（`sys_open_log` 与开放接口鉴权归阶段十）。
4. **并存边界**：BMS 兼作 IdP 与「BMS 作为外部 IdP 客户端」两向能力分列，不互相占用契约（用户令牌 / ID Token 的 `aud` 区分）。
5. **联调演示**：以测试客户端完成一次授权码流程 E2E（授权 → 换码 → userinfo），作为 M6「BMS 作 IdP 通过」证据。
6. **用例**：非法 `redirect_uri` / 未知 client / 授权码重放 / PKCE 缺失分支。

## 3. 完成标准 <a id="accept"></a>

Discovery / JWKS 可访问且字段合规；测试客户端授权码流程 E2E 通过（PKCE、授权码单次、scope 校验）；`sys_client` 注册 / 停用 / 重置经最小接口可用且落库；错误分支用例齐全；`sys_client` 表文件 ↔ 登记 ↔ 迁移零漂移。

## 4. 参考文档 <a id="ref"></a>

- [需求 02-5](../../../需求/02_需求_SSO与身份联邦.md#r02-5)
- 《[概要设计 · 身份认证SSO](../../../../../设计/概要设计/26_概要设计_身份认证SSO.md)》
- 《[概要设计 · 开放接口管理](../../../../../设计/概要设计/20_概要设计_开放接口管理.md)》（管理页面与调用审计归阶段十）
- 《[架构设计 · 接口与集成](../../../../../设计/架构设计/09_架构设计_接口与集成.md)》
- 《[后端基类清单](../../../../../后端基类清单.md)》「开放接口服务端」条目

> 交付物：详细设计、实施记录、测试记录（随任务开工建立，落本目录 `设计/`、`实施/`、`测试/`）。
