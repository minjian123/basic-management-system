# 对象存储接入验证测试记录

> 后端插件化 · 04 首个能力与阶段验收 · 01 对象存储接入验证 · 测试记录

[文档首页](../../../../../../文档首页.md) › [01 对象存储接入验证](../04_首个能力与阶段验收_01_对象存储接入验证.md) › 01 测试　|　[← 父任务](../04_首个能力与阶段验收_01_对象存储接入验证.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 对象存储接入验证](../04_首个能力与阶段验收_01_对象存储接入验证.md) |
| 对应需求 | [04-1](../../../../需求/04_需求_首个能力与阶段验收.md#r04-1) |
| 详细设计 | [01_详细设计_01_对象存储接入验证](../设计/01_详细设计_01_对象存储接入验证.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_对象存储接入验证.md) |
| 测试日期 | 2026-09-15 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14.4 / uv 0.12.7） |
| Kiwi 用例 | 661（嵌套 04-1-1 记 662） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

自动化文件：`backend/tests/storage/test_storage.py`（本任务用例标注 `@pytest.mark.kiwi_id(661)`；04-1-1 用例见 [04-1-1 测试记录](../04_首个能力与阶段验收_01_对象存储接入验证_01_minio实现与双实现纳管/测试/01_测试_01_minio实现与双实现纳管.md)）；另回归清单 / 装配既有用例（缺省断言适配）。

| Kiwi | 用例（函数） | 类型 | 断言要点 |
| --- | --- | --- | --- |
| 661 | `test_local_storage_real_read_write` | 集成 | 原子写 / 回读一致 / `exists` / `delete` 幂等 / 未命中 `NotFoundError` / `presign file://` / 键安全拒 `..` 与绝对路径 |
| 661 | `test_storage_provider_switch_zero_change` | 集成 | `provider=minio` 切换后既有路由（消费方未改）与 `resolve_plugin` 同实例（`MinioObjectStorage`，不建连） |
| 661 | `test_dual_implementations_enlisted` | 集成 | 装配快照 `object_storage` = local / minio / null；清单 `local active` + `minio` / `null` registered |
| 47（回归） | 既有契约 / 依赖解析用例 | 单元 / 集成 | 缺省装配改 `LocalObjectStorage` 后断言适配（探针写入 `tmp_path`） |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest tests/storage -q
14 passed

$ uv run pytest --cov=app --cov-branch -q
544 passed, 7 skipped

$ uv run ruff check . && uv run ruff format --check . && uv run pyright
All checks passed! / 274 files already formatted / 0 errors, 0 warnings, 0 informations

$ uv run python -m ops.check_plugins
[插件装配] 校验通过（36 项）   # 退出码 0
```

结果汇总：`local` 真实读写 / 切换零改动 / 双实现纳管全部通过；全量 **544 passed、7 skipped**；无外部服务依赖（MinIO 走假客户端）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 缺省断言迁移与探针落盘 | 缺省 `local` 切换 + 工厂按注册表缓存 | 用例改隔离注册表 + `tmp_path` 注入（实施记录 §4 已记） |

## 5. 覆盖率 <a id="coverage"></a>

- `app/storage/local.py` / `app/storage/minio.py` 核心路径由本套件覆盖（MinIO 走假客户端，真实探活不计）；全量快照 TOTAL 99%。
- 关键模块 `app/core/provider.py` / 插件机制模块维持 100%（03 收口口径）。

## 6. 偏差与遗留 <a id="deviations"></a>

- MinIO 真实连通（服务可达 / 桶策略）随后续文件管理实测。
- `[minio].bucket` 键位清理随后续版本（当前注记废弃）。

> 本文档依《文档生成规范》编写
