# 能力域基类改继承测试记录

> 后端插件化 · 02 能力域改造 · 01 能力域基类改继承 · 测试记录

[文档首页](../../../../../../文档首页.md) › [01 能力域基类改继承](../02_能力域改造_01_能力域基类改继承.md) › 01 测试　|　[← 父任务](../02_能力域改造_01_能力域基类改继承.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 能力域基类改继承](../02_能力域改造_01_能力域基类改继承.md) |
| 对应需求 | [02-1](../../../../需求/02_需求_能力域改造.md#r02-1) |
| 详细设计 | [01_详细设计_01_能力域基类改继承](../设计/01_详细设计_01_能力域基类改继承.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_能力域基类改继承.md) |
| 测试日期 | 2026-09-15 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14.4 / uv 0.12.7） |
| Kiwi 用例 | 531 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

自动化文件：`backend/tests/crosscut/test_plugin_registration.py`（7 条，均标注 `@pytest.mark.kiwi_id(531)`）：

| Kiwi | 用例（函数） | 类型 | 断言要点 |
| --- | --- | --- | --- |
| 531 | `test_ports_declared_and_abstract` | 单元 | 40 端口清单一致；均抽象；`plugin_key == key`；三属性自身声明（`null` / `0.1.0`） |
| 531 | `test_null_defaults_registered` | 单元 | 36 个有缺省实现的端口 `(plugin_key, null)` 登记为对应 `NullXxx`；4 个无实现端口不登记 |
| 531 | `test_abstract_ports_not_registered` | 单元 | 快照实现不含任何端口类 |
| 531 | `test_unnamed_concrete_subclass_not_registered` | 单元 | `HealthCheckRegistry` 不登记（继承申明不生效） |
| 531 | `test_inherited_declaration_does_not_register` | 单元 | 继承申明不登记 / 自身声明登记 / 非字符串声明安全跳过 |
| 531 | `test_event_worker_location_and_abstraction` | 单元 | `BaseEventWorker` 落 `events/base.py`；发布 / 消费抽象不登记 |
| 531 | `test_app_starts` | 冒烟 | `create_app` + lifespan 进出正常（行为不变） |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest tests/crosscut/test_plugin_registration.py -q
7 passed in 0.04s

$ uv run pytest --cov=app --cov-branch --cov-report=term-missing -q
app/core/plugin.py     151      0     52      0   100%
TOTAL                 3977      4    444      7    99%
449 passed, 7 skipped in 1.70s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
222 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：登记语义用例全部通过；全量 449 passed、7 skipped（真实 Redis / 真库等环境依赖用例）；`app/core/plugin.py` 覆盖率 100%。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 首轮 Null 登记用例失败（快照键数与预期不符） | `audit` / `cache` / `task` / `event` 无 `NullXxx` | 回写设计后按「36 有实现 / 4 无实现」口径调整期望（实施记录 §4 已记） |

## 5. 覆盖率 <a id="coverage"></a>

- `app/core/plugin.py`：**100%**（151 语句 / 52 分支，缺失 0）。
- 全量快照：TOTAL 99%（3977 语句 / 4 缺失；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 4 个无缺省实现端口的 `null` 登记与用例随对应能力阶段补（设计已注记）。
- 提供者改造（02-2）/ `null.py` 迁移（02-3）/ 装配接线（01_02）用例随各任务补登 Kiwi。

> 本文档依《文档生成规范》编写
