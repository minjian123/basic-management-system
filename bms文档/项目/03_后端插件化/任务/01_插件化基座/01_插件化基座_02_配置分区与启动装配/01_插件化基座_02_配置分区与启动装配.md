# 02 配置分区与启动装配

> 后端插件化 · 01 插件化基座 · 子任务 02（含嵌套子任务 01）

[文档首页](../../../../../文档首页.md) › [01 插件化基座](../01_插件化基座.md) › 02 配置分区与启动装配　|　[← 父任务](../01_插件化基座.md)

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02 |
| 父任务 | [01 插件化基座](../01_插件化基座.md) |
| 对应需求 | [01-2](../../../需求/01_需求_插件化基座.md#r01-2) |
| 工时（重估） | 5h（自有 3h + 嵌套子任务 2h） |
| 依赖 | 01（中间层与注册表） |
| 负责人 | minjian |
| 状态 | 已完成 |
| 完成日期 | 2026-09-15 |

## 2. 子任务清单 <a id="list"></a>

| 编号 | 任务 | 工时（重估） | 状态 | 完成日期 | 任务文件 |
| --- | --- | --- | --- | --- | --- |
| 01 | 启动校验与 CI 离线校验 | 2h | 已完成 | 2026-09-15 | [01](01_插件化基座_02_配置分区与启动装配_01_启动校验与CI离线校验/01_插件化基座_02_配置分区与启动装配_01_启动校验与CI离线校验.md) |

父任务工时 = 自有 3h + 直属子任务 2h = 5h；子任务状态 / 完成日期随其实施进度回写本表。

## 3. 任务内容 <a id="content"></a>

1. `config.toml` 各能力分区按需补 `provider` / `options` 字段（仅被替换域添加）；`app/core/config.py` 建模支持（未知键 / 取值非法启动即报错，延续 [《后端基础能力需求》03-1（配置管理）](../../../../01_项目骨架/需求/03_需求_后端基础能力.md#r03-1) 口径）；密钥类只走 Secret / 环境变量，不入配置文件
2. 启动装配（`app/main.py` lifespan）：读取各能力 provider → 实例化并缓存 → 调 `setup()`；装配结果落 `app.state`，替代现有直连 `NullXxx()` 的装配段
3. 延迟导入：注册表登记实现类（或工厂），实例化时才导入具体实现模块——未选中实现不进启动路径
4. `aclose()` 经 `ResourceManager` 逆序统一回收
5. 启动日志：打印各能力当前 provider（不含 `options` 密钥），供排障核对
6. 单测：切换 provider 后装配结果随实现生效（消费方代码零改动）/ 未配置能力维持 `null` / 启动日志不含密钥
7. 嵌套子任务「启动校验与 CI 离线校验」（01-2-1）：provider 存在性 / 契约版本 / 依赖可用性校验与拒启、`ops/check_plugins.py` CI 离线校验（见子任务文档）

## 4. 完成标准 <a id="accept"></a>

切换某能力 provider 后装配随实现生效且消费方代码零改动；未配置能力维持解析到 `null`；启动日志可核对各能力 provider 且不含密钥；延迟导入生效（未选中实现不进启动路径）；非法 provider / 重名 / 契约版本不符启动被拒（含 CI 离线校验拦截）；`pytest` / `ruff` / `pyright` 全绿。

## 5. 参考文档 <a id="ref"></a>

- [01 详细设计](设计/01_详细设计_01_配置分区与启动装配.md)（本任务详细设计）
- [需求 01-2](../../../需求/01_需求_插件化基座.md#r01-2)
- 《架构设计 · 扩展点与插件化》「配置驱动装配」节
- [《后端基础能力需求》03-1（配置管理）](../../../../01_项目骨架/需求/03_需求_后端基础能力.md#r03-1)（`config.toml` 分区先例）
- 《后端开发规范》「后端基座体系（强制）」节

> 本文档依《文档生成规范》编写
