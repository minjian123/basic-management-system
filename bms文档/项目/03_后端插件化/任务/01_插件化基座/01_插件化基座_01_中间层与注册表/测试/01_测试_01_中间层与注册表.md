# 中间层与注册表测试记录

> 后端插件化 · 01 插件化基座 · 01 中间层与注册表 · 测试记录

[文档首页](../../../../../../文档首页.md) › [01 中间层与注册表](../01_插件化基座_01_中间层与注册表.md) › 01 测试　|　[← 父任务](../01_插件化基座_01_中间层与注册表.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 中间层与注册表](../01_插件化基座_01_中间层与注册表.md) |
| 对应需求 | [01-1](../../../../需求/01_需求_插件化基座.md#r01-1) |
| 详细设计 | [01_详细设计_01_中间层与注册表](../设计/01_详细设计_01_中间层与注册表.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_中间层与注册表.md) |
| 测试日期 | 2026-09-15 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14.4 / uv 0.12.7） |
| Kiwi 用例 | 529 |
| 结论 | 通过（自有内容）；嵌套 01-1-1 用例随其任务补 |

## 2. 测试范围与用例 <a id="scope"></a>

自动化文件：`backend/tests/core/test_plugin.py`（14 条，均标注 `@pytest.mark.kiwi_id(529)`）：

| Kiwi | 用例（函数） | 类型 | 断言要点 |
| --- | --- | --- | --- |
| 529 | `test_base_pluggable_contract` | 单元 | 字段默认 / `key` 回退 / `describe` 标识 / 继承 `BaseAsyncResource` |
| 529 | `test_abstract_port_not_registered` | 单元 | 抽象端口构建期剔除 |
| 529 | `test_null_registration_and_explicit_name` | 单元 | `Null` 缺省登记 `null`；显式名优先；占位后缀 |
| 529 | `test_unnamed_non_null_not_registered` | 单元 | 无名非 `Null` 实例化子类不登记 |
| 529 | `test_snapshot_readonly_and_lazy_build` | 单元 | 懒构建 / 排序稳定 / 快照写入抛 `TypeError` |
| 529 | `test_explicit_factory_registration` | 单元 | 零参工厂（组合路径）登记 |
| 529 | `test_duplicate_rejected` | 单元 | 同轨重名 `PluginError`（含双方类名、码位 40002） |
| 529 | `test_cross_track_duplicate_rejected` | 单元 | 跨轨重名即拒 |
| 529 | `test_invalid_contract_version_rejected` | 单元 | `1.0` 等非法格式构建期拒 |
| 529 | `test_empty_key_candidate_rejected` | 单元 | 候选键为空报错 |
| 529 | `test_explicit_registration_validation` | 单元 | 显式项键 / 名缺失、抽象类、不可调用凝聚明细 |
| 529 | `test_sealed_registry_rejects_register` | 单元 | 构建后登记拒（运行期只读） |
| 529 | `test_module_entrypoints` | 单元 | 默认实例入口：构建 / 快照 / 登记薄封装 |
| 529 | `test_lifecycle_defaults_and_resource_manager` | 单元 | 默认 `setup` / `aclose` 空实现；`ResourceManager` 逆序回收 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest tests/core/test_plugin.py -q
14 passed in 0.02s

$ uv run pytest --cov=app --cov-branch --cov-report=term-missing -q
app/core/plugin.py    109      0     34      0   100%
TOTAL                3781      3    426      7    99%
431 passed, 7 skipped in 1.67s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
220 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：插件机制用例全部通过；全量 431 passed、7 skipped（真实 Redis / 真库等环境依赖用例，与本次改动无关）；`app/core/plugin.py` 覆盖率 100%。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| Kiwi 平台 68/69 已被既有策展用例占用 | 编号以平台自增主键为准 | 顺延登记 529（先登记后编码；实施记录 §4 已记） |
| pyright strict 对测试未引用类 / 快照写入 cast 报错 | `reportUnusedClass`；嵌套 dict 类型 | 补断言引用类与键；内层快照 cast 为 `dict[str, object]` |

## 5. 覆盖率 <a id="coverage"></a>

- 新增模块 `app/core/plugin.py`：**100%**（109 语句 / 34 分支，缺失 0）——满足《AI开发规范》「新增模块覆盖率 100%」。
- 全量快照：TOTAL 99%（3781 语句 / 3 缺失；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 嵌套 01-1-1（解析 / 契约版本）、02（装配 / 延迟导入）、01-2-1（启动 / CI 离线校验）、03（清单接口）的用例随各任务补登 Kiwi。
- 契约测试基座（域三 03-1）将遍历 `plugin_registry_snapshot()`，本任务保持快照可枚举与只读。

> 本文档依《文档生成规范》编写
