# 解析器与契约版本测试记录

> 后端插件化 · 01 插件化基座 · 01 中间层与注册表 · 01-1-1 解析器与契约版本 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [01-1-1 解析器与契约版本](../01_插件化基座_01_中间层与注册表_01_解析器与契约版本.md) › 01 测试　|　[← 父任务](../01_插件化基座_01_中间层与注册表_01_解析器与契约版本.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01-1-1 解析器与契约版本](../01_插件化基座_01_中间层与注册表_01_解析器与契约版本.md) |
| 对应需求 | [01-1](../../../../../需求/01_需求_插件化基座.md#r01-1) |
| 详细设计 | [01_详细设计_01_解析器与契约版本](../设计/01_详细设计_01_解析器与契约版本.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_解析器与契约版本.md) |
| 测试日期 | 2026-09-15 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14.4 / uv 0.12.7） |
| Kiwi 用例 | 530 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

自动化文件：`backend/tests/core/test_plugin_resolve.py`（11 条，均标注 `@pytest.mark.kiwi_id(530)`）：

| Kiwi | 用例（函数） | 类型 | 断言要点 |
| --- | --- | --- | --- |
| 530 | `test_resolve_reuses_single_instance` | 单元 | 同 provider 同实例；不同键 / 实现名各自独立 |
| 530 | `test_resolve_null_fallback` | 单元 | `None` / 空串 → `null` 登记实现，同实例 |
| 530 | `test_resolve_unregistered_raises` | 单元 | 未注册 `PluginError`（键名 + 已注册实现名） |
| 530 | `test_resolve_factory_cached` | 单元 | 零参工厂仅调用一次，结果缓存 |
| 530 | `test_contract_version_compatible` | 单元 | 主版本一致（minor / patch 差异）放行 |
| 530 | `test_contract_version_mismatch_rejected_and_logged` | 单元 | 不匹配告警日志 + `PluginError`；不缓存失败实例 |
| 530 | `test_missing_contract_version_rejected` | 单元 | 结构化实现缺版本属性：传期望拒、未传跳过 |
| 530 | `test_invalid_expected_version_rejected` | 单元 | 期望版本非 `X.Y.Z` 即拒 |
| 530 | `test_invalid_actual_version_rejected` | 单元 | 实现版本非 `X.Y.Z`（工厂实例）解析期即拒 |
| 530 | `test_build_clears_instance_cache` | 单元 | 快照重建后解析得到新实例 |
| 530 | `test_module_entrypoint_resolve` | 单元 | 模块级 `resolve_plugin` 按同一语义工作 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest tests/core/test_plugin_resolve.py -q
11 passed in 0.02s

$ uv run pytest --cov=app --cov-branch --cov-report=term-missing -q
app/core/plugin.py    150      0     52      0   100%
TOTAL                3822      3    444      7    99%
442 passed, 7 skipped in 1.68s

$ uv run ruff check .
All checks passed!

$ uv run ruff format --check .
221 files already formatted

$ uv run pyright
0 errors, 0 warnings, 0 informations
```

结果汇总：解析与契约版本用例全部通过；全量 442 passed、7 skipped（真实 Redis / 真库等环境依赖用例）；`app/core/plugin.py` 覆盖率 100%。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 首轮覆盖率 99%（实现版本格式非法分支） | 构建期只校验候选类，工厂实例路径未覆盖 | 增补 `test_invalid_actual_version_rejected`；覆盖率回到 100% |

## 5. 覆盖率 <a id="coverage"></a>

- `app/core/plugin.py`：**100%**（150 语句 / 52 分支，缺失 0）。
- 全量快照：TOTAL 99%（3822 语句 / 3 缺失；门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 提供者改造 / 装配预热（实例缓存生效面）用例随域二 02-2 / 02 补登。
- 契约测试基座（域三 03-1）将遍历 `plugin_registry_snapshot()` 与 `resolve` 语义，本任务保持接口稳定。

> 本文档依《文档生成规范》编写
