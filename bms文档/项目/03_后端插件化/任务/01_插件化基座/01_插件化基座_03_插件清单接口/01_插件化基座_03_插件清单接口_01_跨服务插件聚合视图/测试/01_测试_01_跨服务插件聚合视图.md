# 跨服务插件聚合视图测试记录

> 后端插件化 · 01 插件化基座 · 03 插件清单接口 · 01-3-1 跨服务插件聚合视图 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [01-3-1 跨服务插件聚合视图](../01_插件化基座_03_插件清单接口_01_跨服务插件聚合视图.md) › 01 测试　|　[← 父任务](../01_插件化基座_03_插件清单接口_01_跨服务插件聚合视图.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01-3-1 跨服务插件聚合视图](../01_插件化基座_03_插件清单接口_01_跨服务插件聚合视图.md) |
| 对应需求 | [01-4](../../../../../需求/01_需求_插件化基座.md#r01-4) |
| 详细设计 | [01_详细设计_01_跨服务插件聚合视图](../设计/01_详细设计_01_跨服务插件聚合视图.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_跨服务插件聚合视图.md) |
| 测试日期 | 2026-09-26 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14.4 / uv 0.12.7） |
| Kiwi 用例 | 2190 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

自动化文件：`backend/services/platform/tests/api/test_plugins.py`（聚合护栏 4 条，均标注 `@pytest.mark.kiwi_id(2190)`）：

| Kiwi | 用例（函数） | 类型 | 断言要点 |
| --- | --- | --- | --- |
| 2190 | `test_aggregate_placeholder_contract` | 接口 | 200 + `code=0` + `data == {"placeholder": true, "services": []}`；未被 `/{plugin_key}` 吞掉 |
| 2190 | `test_aggregate_path_in_openapi` | 契约 | 应用可启动、`/api/v1/plugins/aggregate` 在 OpenAPI paths |
| 2190 | `test_aggregate_excludes_options_secrets` | 接口 | 自定义 `options` 敏感值不出现在响应 |
| 2190 | `test_aggregate_readonly_no_write_methods` | 接口 | POST / PUT / DELETE → 405 |

连带修复（范围外，用户确认）：`libs/bms_core/tests/crosscut/test_m2_closure.py` / `test_m3_closure.py`（Kiwi 2188 / 663 / 664）收口护栏改为结构自洽断言。

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest services/platform/tests/api/test_plugins.py -q
9 passed

$ uv run pytest libs/bms_core/tests/crosscut/test_m2_closure.py libs/bms_core/tests/crosscut/test_m3_closure.py -q
7 passed

$ uv run pytest -q
1349 passed, 37 skipped

$ uv run python -m ops.contract_snapshot check
[contract_snapshot] 通过：9 个启用服务快照与公开契约一致

$ uv run ruff check . && uv run ruff format --check . && uv run pyright
All checks passed! / 725 files already formatted / 0 errors, 0 warnings
```

结果汇总：聚合占位护栏用例与阶段收口护栏全部通过；全量回归通过；契约快照零漂移。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| M2 / M3 收口护栏 3 项失败 | 最近提交改阶段计划头 / 需求总览未同步护栏固定断言 | 改为结构自洽断言（实施记录 §4）；随本次全量验证后转绿 |

## 5. 覆盖率 <a id="coverage"></a>

- 新增聚合模型与占位路由均被护栏用例覆盖（`schemas/plugin.py` 新增类、`api/plugins.py` `/aggregate` 分支经固定返回用例直测）。
- 门禁阈值以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准（后端新增模块覆盖率 100%）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 真实取数与 `platform:manage` 鉴权用例随触发时 / 认证阶段补登 Kiwi；聚合契约（按服务嵌套）本次已冻结，触发实现按「只加不删」演进。
- 契约 baseline 不更新（兼容变更）；`platform.json` 快照已随本次提交刷新。

> 本文档依《文档生成规范》编写
