# 契约测试基座测试记录

> 后端插件化 · 03 契约与收敛提取 · 01 契约测试基座 · 测试记录

[文档首页](../../../../../../文档首页.md) › [01 契约测试基座](../03_契约与收敛提取_01_契约测试基座.md) › 01 测试　|　[← 父任务](../03_契约与收敛提取_01_契约测试基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 契约测试基座](../03_契约与收敛提取_01_契约测试基座.md) |
| 对应需求 | [03-1](../../../../需求/03_需求_契约与收敛提取.md#r03-1) |
| 详细设计 | [01_详细设计_01_契约测试基座](../设计/01_详细设计_01_契约测试基座.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_契约测试基座.md) |
| 测试日期 | 2026-09-15 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14.4 / uv 0.12.7） |
| Kiwi 用例 | 567（嵌套 03-1-1 记 568） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

自动化文件：`backend/tests/contracts/test_plugin_contracts.py`（模块级 `@pytest.mark.kiwi_id(567)`）+ `support.py` / `conftest.py`；域侧用例见 [03-1-1 测试记录](../03_契约与收敛提取_01_契约测试基座_01_域注册表与实现覆盖/测试/01_测试_01_域注册表与实现覆盖.md)。

| Kiwi | 用例（函数） | 类型 | 断言要点 |
| --- | --- | --- | --- |
| 567 | `test_snapshot_integrity` | 单元 | 快照键 = 装配清单（36）；条目枚举互证（37）；命名 / 版本 `X.Y.Z`；`null` 类继承 `BaseNullObject` |
| 567 | `test_duplicate_name_rejected` | 单元 | 隔离注册表内同 `(plugin_key, plugin_name)` 两实现 → `PluginError`（重名） |
| 567 | `test_resolve_semantics` | 单元 | 空 provider → `null` 实例且缓存复用；未注册 provider → `PluginError` |
| 567 | `test_class_impl_contract` | 单元（参数化 35） | 零参实例化；`setup`×2 / `aclose`×2 幂等可重入；`describe` 含键名；版本格式；`null` 校验 |
| 567 | `test_factory_entries_read_only` | 单元 | 2 个工厂条目（`health_check_registry:local` / `masking:null`）端口齐备；不实例化 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest tests/contracts -q
57 passed

$ uv run pytest --cov=app --cov-branch -q
531 passed, 7 skipped

$ uv run pytest --cov=app --cov-branch --cov-report=term-missing -q  # 关键模块口径
app/core/plugin.py     162   0  58   0  100%
app/core/registry.py    22   0   2   0  100%
app/core/assembly.py   120   0  12   0  100%
app/health/registry.py   8   0   0   0  100%

$ uv run ruff check . && uv run ruff format --check . && uv run pyright
All checks passed! / 270 files already formatted / 0 errors, 0 warnings, 0 informations

$ uv run python -m ops.check_plugins
[插件装配] 校验通过（36 项）   # 退出码 0
```

结果汇总：快照完整性 / 语义抽查 / 35 类实现遍历 / 工厂只读全部通过；域侧 18 条用例（详见 03-1-1 记录）通过；全量 **531 passed、7 skipped**；关键模块覆盖率 **100%**。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 首跑 `test_snapshot_integrity` 失败 | `masking:null` 为工厂条目而非类 | 断言限定类实现（实施记录 §4 已记，设计同步） |
| `ruff` B009（`getattr` 常量属性） | 风格规则 | 改 `isinstance` 分支 + `cast` 直取属性 |
| `pyright` 未使用函数 / 类 | 私有 fixture 命名与测试类引用 | fixture 去下划线；测试类断言引用对齐既有用例样式 |

## 5. 覆盖率 <a id="coverage"></a>

- `app/core/plugin.py` / `registry.py` / `assembly.py` / `app/health/registry.py`：**100%**。
- 契约套件自身经 `pytest` 全量执行；全量快照 TOTAL 99%（4300 语句 / 5 缺失，门禁阈值以《测试规范》与流水线为准）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 测试编号与设计预期一致（567 / 568），无编号偏差。
- 对象存储双实现自动纳入断言随 04-1；3 域唯一性开关随 03-3；健康真实探针连通随后续。

> 本文档依《文档生成规范》编写
