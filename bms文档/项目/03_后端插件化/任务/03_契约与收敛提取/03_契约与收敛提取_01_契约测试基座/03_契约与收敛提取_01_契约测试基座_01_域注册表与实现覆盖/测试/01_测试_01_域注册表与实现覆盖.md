# 域注册表与实现覆盖测试记录

> 后端插件化 · 03 契约与收敛提取 · 01 契约测试基座 · 03-1-1 域注册表与实现覆盖 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [03-1-1 域注册表与实现覆盖](../03_契约与收敛提取_01_契约测试基座_01_域注册表与实现覆盖.md) › 01 测试　|　[← 父任务](../03_契约与收敛提取_01_契约测试基座_01_域注册表与实现覆盖.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [03-1-1 域注册表与实现覆盖](../03_契约与收敛提取_01_契约测试基座_01_域注册表与实现覆盖.md) |
| 对应需求 | [03-1](../../../../../需求/03_需求_契约与收敛提取.md#r03-1) |
| 详细设计 | [01_详细设计_01_域注册表与实现覆盖](../设计/01_详细设计_01_域注册表与实现覆盖.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_域注册表与实现覆盖.md) |
| 父任务测试记录 | [01 测试记录](../../测试/01_测试_01_契约测试基座.md) |
| 测试日期 | 2026-09-15 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14.4 / uv 0.12.7） |
| Kiwi 用例 | 568 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

自动化文件：`backend/tests/contracts/test_domain_registry_contracts.py`（模块级 `@pytest.mark.kiwi_id(568)`）+ `support.py` 测试注册表 / 注册项替身（`MemoryFieldTypeRegistry` / `MemoryQueryProviderRegistry` / `MemoryDashboardCardRegistry` / `NamedCheck` 等）。

| Kiwi | 用例（函数） | 类型 | 断言要点 |
| --- | --- | --- | --- |
| 568 | `test_null_registry_variants_empty` | 单元（参数化 4） | 4 域 `Null` 变体：`keys()` 空、`get` 未命中 `None` |
| 568 | `test_registry_contract_uniqueness` | 单元 | `uniqueness=True` 域（health）重复登记 → `ConflictError`；开关数 = 1 |
| 568 | `test_null_fieldtype_semantics` | 单元 | 校验恒过（空元组）/ 列类型恒 `NULL_COLUMN_TYPE` |
| 568 | `test_null_query_semantics` | 单元 | 恒空结果（`rows=()` / `total=0`） |
| 568 | `test_null_dashboard_metadata_semantics` | 单元 | 元数据恒空映射 |
| 568 | `test_null_dashboard_fetch_semantics` | 单元 | 取数恒空映射 |
| 568 | `test_null_health_semantics` | 单元 | 登记 no-op（`keys()` 空）+ 空集聚合就绪（不探依赖） |
| 568 | `test_memory_fieldtype_aggregation_not_found` | 单元 | 命中委托 / 未命中 `NotFoundError`（校验与列类型两路径） |
| 568 | `test_memory_query_aggregation_not_found` | 单元 | 命中委托 / 未命中 `NotFoundError` |
| 568 | `test_memory_dashboard_aggregation_not_found` | 单元 | 元数据命中委托 / 未命中 `NotFoundError` |
| 568 | `test_memory_dashboard_fetch_not_found` | 单元 | 取数未命中 `NotFoundError` |
| 568 | `test_health_aggregation_templates` | 单元 | 保序收集（`ready` / `down`）+ 异常类名兜底（`ConnectionError`） |
| 568 | `test_health_aggregation_timeout` | 单元 | 单项超时兜底（`TimeoutError`） |
| 568 | `test_registered_items_auto_enumerated` | 单元 | 三域替身登记 1 项后 `keys()` / `get()` 即含该项 |
| 568 | `test_health_local_registration_enumerated` | 集成 | `resolve_plugin` 解析 `local` 后枚举 `redis` / `database` |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run pytest tests/contracts/test_domain_registry_contracts.py -q
18 passed

$ uv run pytest tests/contracts -q
57 passed

$ uv run pytest --cov=app --cov-branch -q
531 passed, 7 skipped

$ uv run ruff check . && uv run ruff format --check . && uv run pyright
All checks passed! / 270 files already formatted / 0 errors, 0 warnings, 0 informations
```

结果汇总：域侧 18 条用例全部通过（含 4 个变体参数）；健康 `local` 注册项枚举为 `("redis", "database")`；未执行真实探活，不依赖外部服务。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 唯一性断言首跑未触发 | 契约工厂指向 `Null` 注册表（no-op 正确） | 改真实 `HealthCheckRegistry`（实施记录 §4 已记） |

## 5. 覆盖率 <a id="coverage"></a>

- 域注册表基类 / `Null` 实现路径由本套件与既有域单测共同覆盖；全量快照 TOTAL 99%（4300 语句 / 5 缺失）。
- `app/core/plugin.py` / `registry.py` / `assembly.py` / `app/health/registry.py`：**100%**。

## 6. 偏差与遗留 <a id="deviations"></a>

- 3 域唯一性断言随 03-3 收敛后把 `uniqueness` 翻转 `True`（补登对应用例口径，Kiwi 随 03-3）。
- 健康真实探针（redis / database 连通）聚合实测随后续运维。
- **后续变更（03-3，2026-09-15）**：3 域 `Null` 登记由 no-op 改**真实**、唯一性断言改**独立清单**（Kiwi 655）；本记录中「Null 登记空操作 / `keys` 仍空」相关断言已随 03-3 调整（见 [03-3 测试记录](../../../03_契约与收敛提取_03_BaseProviderRegistry收敛/测试/01_测试_01_BaseProviderRegistry收敛.md)）。

> 本文档依《文档生成规范》编写
