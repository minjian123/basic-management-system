# 后端基座与服务化地基阶段测试报告

> BMS 基础管理系统 · 阶段二（后端基座与服务化地基 · M2 后端基座与服务化地基可用）· 阶段末测试报告

[文档首页](../../文档首页.md) › [后端基座与服务化地基需求总览](需求/00_需求_后端基座与服务化地基.md) › 测试报告　|　[需求 10-1](需求/10_需求_阶段验收.md#r10-1)　[10-1 任务](任务/10_阶段验收/10_阶段验收_01_阶段验收门禁核对与测试报告/10_阶段验收_01_阶段验收门禁核对与测试报告.md)

| 项 | 值 |
| --- | --- |
| 报告日期 | 2026-09-24 |
| 阶段范围 | 阶段二 31 条需求（域一 后端基座真实实现 5 / 域二 多服务骨架与仓库 3 / 域三 服务目录与契约登记 3 / 域四 API 网关与边缘 3 / 域五 服务间通信与一致性 4 / 域六 数据拓扑升级 4 / 域七 认证与服务身份 3 / 域八 可观测性栈 2 / 域九 多服务 CI-CD 与契约门禁 3 / 域十 阶段验收 1） |
| 阶段任务 | 31 项任务全部完成；工时 316h（实际 2026-09-22 ~ 2026-09-24） |
| 数据来源 | 本地全量 `uv run pytest`（收口当日快照）、GitLab 流水线（项目 `bms/bms`）、Kiwi TCMS 平台 |
| 编制方式 | 命令采集 + 文档断言用例（`backend/libs/bms_core/tests/crosscut/test_m2_closure.py`，Kiwi 2188）+ 人工汇总 |
| 依据 | 《[测试规范](../../规范/测试规范.md)》§12、《[项目管理规范](../../规范/项目管理规范.md)》、《[后端基座与服务化地基计划](计划/01_计划_后端基座与服务化地基.md)》 |

## 1. 用例执行统计 <a id="run"></a>

| 层次 | 工具 | 用例数 | 结果 | 说明 |
| --- | --- | --- | --- | --- |
| 后端全量（单元 / 接口 / 集成） | pytest（SQLite 轻量库） | 1378 收集（**1341 passed / 37 skipped**；其中本任务新增 4 条收口断言用例，收口基线为 1337 passed） | 通过率 100% | 跳过 37 条为需真实外部环境的集成用例（三库真库 / 达梦方言 / 真实 Redis / Keycloak / MinIO），未配置对应环境变量即跳过 |
| 契约 / 事件 / 边界专项 | pytest `tests/{ops,events,boundary}` | **68 passed** | 通过 | 契约快照与基线、oasdiff 门禁、Schemathesis 冒烟护栏、事件契约 23 条与零漂移、边界硬校验与越界度量 |
| 数据访问 / 迁移 / 平台运维专项 | pytest `tests/{ops,boundary,db}` | **268 passed** | 通过 | 库键与库名派生、租户隔离与批量迁移、表归属对账、连接预算、排序契约 DB 侧 |
| 前端三包（core / vue / ui-ep） | Vitest | 159 文件 / **2086 条**（core 96 文件 1180 条 · vue 2 文件 5 条 · ui-ep 61 文件 901 条） | 通过 | 本阶段前端无业务页面改动（仅 CI 与模块构建链路），跑既有基线 |
| E2E（浏览器主流程） | Playwright | **未激活** | — | 前置为完整登录链路（阶段六）与前端全页面（阶段十五） |
| 文档断言（收口） | pytest `test_m2_closure.py` | 4 passed | 通过 | 门禁表 14 行结论齐备 / 计划收口状态自洽 / 报告归档六节 / 任务与域总览一致（Kiwi 2188，含于全量口径） |

**Kiwi 用例（本阶段新增 34 个策展编号）**：

| 域 | Kiwi 编号 | 覆盖 |
| --- | --- | --- |
| 域一 后端基座真实实现 | 984 / 1019 / 1050 / 1078 / 1156 | 数据访问底座 / 多租户拓扑与租户解析 / 异步仓储与 `BaseModel` / 迁移分链与排序 DB 侧 / 三库真库与达梦实测 |
| 域二 多服务骨架与仓库 | 1204 / 1205 / 1206 | monorepo 工程与共享基座库 / 服务运行与健康检查 / 单体重构为分布式 |
| 域三 服务目录与契约登记 | 2162 / 2163 / 2164 | 服务目录与注册要素登记 / 启动与 CI 唯一性校验 / 只读接口 |
| 域四 API 网关与边缘 | 2165 / 2166 / 2167 | 网关接入与声明式配置 / 边缘认证与请求净化 / 限流与服务发现 |
| 域五 服务间通信与一致性 | 2168 ~ 2175 | 契约快照与同步调用 / 边界硬校验与运行时守卫 / Outbox 与幂等消费 / 事件契约与 Saga 基座 |
| 域六 数据拓扑升级 | 2176 / 2177 / 2178 / 2187 | 平台层表归属与按服务拆分 / 每服务每租户建库与路由 / 多服务多库迁移与连接预算 / 租户解析形态健壮性 |
| 域七 认证与服务身份 | 2179 / 2180 / 2181 | OIDC IdP 接入 / 双类 JWT / 网关认证接线与服务身份 |
| 域八 可观测性栈 | 2182 / 2183 | 可观测栈接入 / 按服务归因与健康就绪 |
| 域九 多服务 CI-CD 与契约门禁 | 2184 / 2185 / 2186 | 父-子流水线 / 不可变镜像与回滚 / 契约门禁 |
| 域十 阶段验收 | 2188 | 本任务：门禁核对 / 报告归档 / 状态一致 |

**CI 侧执行**：阶段内在 main 分支推送 **流水线 #307 ~ #368 共 57 条**（success 38 / failed 18 / canceled 1，另有 1 条 renovate 分支流水线不在统计内）。失败条目全部落在**实施期中间推送**（域二 ~ 域四、域六、域九；失败岗位为 `backend-test` / `module-build` / `ci-base-build` / `integration-dm8` / `backend-lint` / `base-integrity` / `contract-gate` / `contract-smoke`），均在**同一任务内修复后转绿**（缺陷与处置见 §2；各条取证见 [附录 A](#appendix)）。**阶段收口末条流水线 #368（`d3d6eddc`）success**。

## 2. 缺陷统计 <a id="defect"></a>

| 项 | 数量 | 说明 |
| --- | --- | --- |
| 新增缺陷（自动捕获，本阶段窗口） | **18**（本任务关闭 16 + 09_01 期间已关 2） | `defect-auto` 标签自动单，对应本阶段失败流水线 #307 ~ #358；**本任务逐条附「失败岗位 + 后续转绿流水线」评论后关闭 16 条**（2026-09-24），另 2 条（iid 41 / 42，流水线 #349 / #351 前端镜像构建失败）已于 2026-09-24 00:54 / 01:03 先行关闭 |
| 开放缺陷（自动捕获，历史阶段） | 27 → **0** | 对应流水线 #248 ~ #306（2026-09-17 ~ 2026-09-21，属阶段一 / 阶段四 / 阶段五期间）；**已于 2026-09-24 收口后经用户确认按同一口径批量核销**（评论 + 关闭，附人工定级与后续转绿流水线），核销后项目工作项 `opened=0` / `closed=47` |
| 新增缺陷（人工 / UAT） | 0 | 阶段内无人工上报缺陷（UAT 未进入） |
| 过程问题处置 | 若干 | 均在任务实施记录 §4 / §6 留痕，非产品缺陷 |

**本阶段 16 条自动单的处置口径**（逐条评论 + 关闭，依据后续转绿流水线）：

| 失败岗位 | 条数 | 处置说明 |
| --- | --- | --- |
| `integration-dm8` | 5 | 达梦实例**建连阶段**报 `dmPython DatabaseError [CODE:-70089] Encryption module failed to load`（达梦服务端加密模块 / 客户端环境问题，非本仓代码）；该岗位于 2026-09-22 置 `when: never` 临时关闭（计划 §7 第 8 项），达梦口径改由 01_05 达梦真库实测与 06_02 三库真库演练承载 |
| `contract-gate` / `contract-smoke` | 4 | 契约门禁实施期（09_03）基线入库与冒烟口径调整过程中的拦截与失败，随基线入库与调用方式修正后转绿（流水线 #359） |
| `backend-test` / `module-build` / `ci-base-build` / `backend-lint` / `base-integrity` | 7 | 域二 ~ 域四 / 域六 实施期迭代推送（工程装配、基础镜像构建上下文、Kiwi 导入参数、CI 门禁分层等），修复后转绿 |

**历史 27 条（#248 ~ #306）的补核销（2026-09-24 收口后，经用户逐项确认）**：处理方式 / 定级口径 / 文档登记落点 / `defect-capture` dump 缺口归口 4 项均取推荐项后按同一口径批量核销；失败岗位分布 = `desktop-build` 14、`base-integrity` 6、`backend-test` 4、`ci-base-build` 2、`desktop-check` 2、`swagger-snapshot` 1；人工定级 = 门禁类（`base-integrity` / `backend-test` / `desktop-check`）**P2**、构建与快照类（`ci-base-build` / `desktop-build` / `swagger-snapshot`）**P3**；逐条附「后续首次转绿流水线与提交」复验证据（检索口径 GitLab API 项目 2 `/pipelines/{id}/jobs`）。

**代表性过程问题**（详见各任务实施记录）：BuildKit attestation 推送报 `blob unknown`（09_01，构建加 `--provenance=false`）；服务镜像缺 `alembic.ini` / `config*.toml`（09_01）；Trivy DB 国内镜像与缓存（09_01）；`ops.migrate_tenants` 等对 `SysTenant` 改惰性导入、`compose run` 变量须 `-e` 注入（09_02）；dev 环境租户解析把 IP 字面量当子域名致 7 处 5xx（06_04，修复前后 `7 failures` → `462 passed`）；达梦 `DROP NOT NULL` 与 ops 同步方言路径（06_02）；观测组件镜像坏副本换版本（08_01）。

## 3. 覆盖率 <a id="coverage"></a>

| 口径 | 数值 | 说明 |
| --- | --- | --- |
| 全量语句覆盖 | **95%**（14106 语句 / 524 缺失） | 收口当日 `uv run pytest -q --cov=bms_core --cov=bms_platform … --cov-branch --cov-report=term-missing` |
| 分支覆盖 | **93%**（2106 分支 / 139 部分） | 同上 |
| 共享基座库 `bms_core` | **96.7%** 语句 / **93.6%** 分支（289 文件） | 数据访问 / 多租户 / 能力域 / 边界 / Outbox / 事件 / 观测 等基座实现 |
| 服务侧 `services/*` | **92.4%** 语句 / **91.7%** 分支（126 文件） | 9 个启用服务的应用层与只读接口（服务级 CI 门槛 ≥ 70%） |
| 未覆盖口径 | 524 行 | 真实依赖执行体（健康探针 / 迁移 CLI / ASGI 入口 / 达梦同步分支）与异常分支；真实链路用例在 CI / 真机侧执行或按环境变量跳过 |

## 4. 风险与遗留项 <a id="risk"></a>

以[计划 §7「后续阶段待办」](计划/01_计划_后端基座与服务化地基.md#followup)为唯一台账，37 项逐条给去向（`已闭环` 者附闭环落点）：

| # | 项 | 影响 | 去向 |
| --- | --- | --- | --- |
| 1 | 达梦复合唯一 `(唯一字段, deleted_at)` 多 NULL 语义差异兜底 | 未删行不能共存 | 信创适配阶段（定案前不改平台统一口径与表结构） |
| 2 | 达梦 `JSON` 列经 dmPython 读回字符串的适配 | 直接下标访问 JSON 字段需处理 | 按影响面后续（业务模块接入时评估） |
| 3 | `ops/init_tenant.py` 达梦种子路径需经同步门面 | 达梦下种子不可用 | 租户管理阶段 / 达梦生产化 |
| 4 | 真实主从复制与只读副本谓词 | 读写分离仅验证路由决策 | 后续（读写分离真实化） |
| 5 | 游标排序键类型范围扩展（`Decimal` 等） | 部分类型不可用游标 | 按需 |
| 6 | 骨架表（`sys_task` / `sys_notification` / `sys_icon` / `ai_chat_log` 等）迁移 | 表未落库 | 各所属阶段 |
| 7 | 分片表按月预创建与分片路由真实落地 | 分片未真实生效 | 任务调度阶段 |
| 8 | CI `integration-dm8` 临时关闭（达梦环境加密模块） | 达梦集成不在 CI 常跑 | 达梦环境修复（运维 / 达梦生产化） |
| 9 | 平台 / 能力域模型与落库实现迁出 `bms_core` | 平台库共享 | **已闭环（06_03 / 06_02）** |
| 10 | 发件箱三表保留期清理与归档、跨租户死信看板、按库投递编排 | 数据堆积 / 运维视图缺 | 任务调度 / 归档阶段；06_02 |
| 11 | 服务级事件契约与订阅声明（跨仓库消费方） | 订阅无法自动校验 | 消费方实现阶段 |
| 12 | 事件清单文档与契约注册表对账脚本 | 依赖人工对账 | 后续（事件批量落地） |
| 13 | topic 命名域与已登记事件域对齐 | 命名待统一 | 阶段十 M10 集成消息 |
| 14 | 开发库自动建表不写 Alembic 版本号 | 新增迁移会撞「表已存在」 | 01_04 后续 / 运维 |
| 15 | `sys_tenant.db_key` 物理删列 | 遗留可空废弃列 | 后续数据库治理任务 |
| 16 | 服务目录快照入基线 + `catalog` 降级指标化 | — | **已闭环（09_03 / 08_02）** |
| 17 | 其余服务基础设施表迁移脚本 | 事件链不完整 | M10 事件总线真实投递 / 该服务首次发事件 |
| 18 | 三库真库建库迁移演练 | — | **已完成（06_02，2026-09-23）** |
| 19 | 06_02 Kiwi 用例登记 | — | **已完成（2178）** |
| 20 | 分链形态变更后既有开发 / CI 临时库须重建 | 老库不可用 | 开发 / 运维（文档已就位） |
| 21 | 完整登录链路、用户 claims 与 `X-User-Id`、JIT 建号 | 登录链路不完整 | 阶段六 |
| 22 | RBAC 权限码细粒度校验 | 权限未接入路由 | 阶段七 |
| 23 | 认证内部端点独立鉴权与 `forward-auth` 平移 ext_authz、mTLS | 内部端点依赖内网 | K8s / 运维阶段 |
| 24 | 票据校验缓存与网关服务 JWT 缓存优化 | 性能开销 | 性能阶段 |
| 25 | 可观测组件镜像国内加速经验 | 拉取易失败 | 运维（文档与工具已就位） |
| 26 | Prometheus 抓取目标切 Compose DNS | — | **已闭环（09_02：`up` 9/9、`probe_success` 18/18）** |
| 27 | `prometheus-client` 多进程模式、prod 采样率与 Tempo 保留期 | 多副本下指标口径 | 性能 / 运维阶段 |
| 28 | 真实 Celery 运行时链路埋点实测 | 埋点未实测 | 任务调度阶段 |
| 29 | CI 推送阶段度量（发布成功率 / 契约破坏数） | — | **已闭环（09_01 / 09_03）** |
| 30 | 业务链路 SLO 规则实体与容量水位监控 | 业务 SLO 未落地 | 对应业务阶段与阶段十四压测 |
| 31 | 告警内部接口格式转换与短信通道 | 通知通道未接 | 通知阶段 |
| 32 | dev 环境 5xx 另立任务修复 | — | **已闭环（06_04）** |
| 33 | 共享基座变更下契约冒烟覆盖口径（`rules:changes` 只认服务路径） | 共享变更不拉服务子流水线 | 09 域后续（CI 容量 / runner 并发评估） |
| 34 | 开放自动缺陷单 27 条（#248 ~ #306，阶段二之前） | 缺陷库存在陈旧自动单，影响后续缺陷甄别 | **已闭环（2026-09-24 收口后经确认批量核销：评论 + 关闭，附定级与转绿证据）**；「修复即流转关闭」流程缺口转第 36 项 |
| 35 | 测试资产仓《Kiwi 用例台账》§2 缺 984 / 1204 / 1205 / 1206 四条登记 | 台账与平台编号对账不全 | 测试资产仓台账补齐（随下次用例批次维护） |
| 36 | 缺陷单「修复即流转关闭」治理脚本（扫描后续流水线转绿 → 关闭建议 / 半自动关闭） | 自动单未随修复流转关闭 | 缺陷治理（阶段收口窗口 / 运维） |
| 37 | `defect-capture` 现场 dump 缺口（`mysqldump … Unknown database 'bms_test_mysql'` 后跳过，2026-09-24 流水线 #358 日志实证） | 缺陷现场缺数据库 dump | CI 与测试库治理（运维窗口） |

台账核销说明：第 9 / 16 / 18 / 19 / 26 / 29 / 32 项已随本阶段任务闭环；**第 34 项已于 2026-09-24 收口后批量核销闭环**（新增第 36 / 37 项承接治理脚本与上报缺口）；其余项均转后续阶段或运维，去向明确、无「无归口」条目。

## 5. 复盘与经验教训 <a id="retro"></a>

| # | 经验 / 教训（已发生的事实） | 已固化的改进项 |
| --- | --- | --- |
| 1 | **三库实测先行**：01_05 / 06_02 在阶段早期做真库演练，提前暴露达梦 `DROP NOT NULL`、复合唯一多 NULL、`JSON` 读回字符串等差异，未拖到阶段末 | 方言结论「先真库实测 → 回写方言特性文档并标实测 / 待验」；`ops/test_db.py` 三库对象常备 |
| 2 | **契约冒烟从「非阻断」到「阻断」抓出真实缺陷**：06_04 用同口径 Schemathesis 复现 dev 环境 7 处 5xx（IP 字面量被当租户子域名），修复后 462 passed；护栏用例（platform 侧零 5xx）在旧代码下必红 | 冒烟 `allow_failure` 摘除（阻断）+ 缺陷复现固化为回归用例（Kiwi 2187） |
| 3 | **「改一点推一次等整条流水线」是慢循环**：阶段内 57 条流水线中 18 条失败，多为可在本地提前发现的静态 / 结构问题 | 固化 `scripts/tools/preflight/check-preflight.py --fast`（本地跑 CI 关键门禁，秒级） |
| 4 | **分档开关 + 父子流水线让重验证层可控**：`deploy/ci/verify/{contract,contract-gate,db,defect,image}` 按需开启，服务子流水线只按服务路径放行 | 档位即开关文件（删档即关）；文档不写死计数 |
| 5 | **自动缺陷单「报了不关」**：43 条自动单堆积（本任务关闭阶段内 16 条，历史 27 条仍在），缺陷库噪声影响甄别 | 本任务按《测试规范》§9 逐条附修复依据关闭；**历史 27 条已于收口后同日经确认批量核销（项目工作项开放数归零）**；缺口（自动单未随修复流转关闭）转计划 §7 第 36 项治理脚本 |
| 6 | **甘特与剩余表口径漂移长期挂着**：S2 软提示 5 项（08_01 / 08_02 / 09_01 ~ 09_03 已完成节点仍在甘特）自 09_03 起未清 | 本任务按「甘特不含已完成节点」收口为里程碑态；后续任务完成时同步清理甘特节点 |
| 7 | **校验脚本与实际写法脱节导致 S1 静默失效**：`check-status` 的 `EFFORT_RE` 只认 `阶段合计 **316**h`，而本项目全部阶段计划实际写作 `**316h**` / `57h` → **工时子校验在所有阶段从未触发**；收口核对时经真实验证发现 | 修正则（兼容三种写法）+ 本阶段工时行收口为可校验形态；修正后阶段一 / 二 / 四 / 五 均 0 软提示，并暴露出**阶段三头部 57h ↔ 明细 61h** 的历史不一致（已同步修正计划与阶段报告为 61h / 12 项） |
| 7b | **文档断链易错点（相对层级）**：本任务 `实施/`、`测试/` 记录初稿对阶段级文档误用 3 层 `../`（应为 4 层） | 提交前必跑 `check-links`，本任务已按提示修正并复跑通过 |
| 8 | **验收类任务需独立复核**：本任务加做一轮「第二 AI 独立复核 + 人工处理异常项」，逐条挑战证据链 | 保留为阶段验收固定动作（发现项落实施记录 §4） |

## 附录 A 数据采集命令与原始输出 <a id="appendix"></a>

```bash
# 1) 全量执行与覆盖率（收口当日快照，backend/）
$ uv run pytest -q --cov=bms_core --cov=bms_platform --cov=bms_identity --cov=bms_tenant \
    --cov=bms_org --cov=bms_file --cov=bms_notification --cov=bms_search --cov=bms_ai \
    --cov=bms_report --cov-branch --cov-report=term-missing
TOTAL                                       14106    524   2106    139    95%
1337 passed, 37 skipped in 30.47s   # 收口基线（新增 4 条收口断言用例后：1341 passed, 37 skipped in 23.55s）
# 跳过原因（摘）：未配置 BMS_TEST_DB_URL / BMS_TEST_TENANT_DB_URL（三库真库 / 达梦方言 / 四库连通 /
#   三库拓扑）、BMS_TEST_REDIS_URL（真实 Redis）、IDP_TEST_*（Keycloak）、BMS_TEST_MINIO_*（MinIO）

$ uv run pytest libs/bms_core/tests/ops libs/bms_core/tests/boundary libs/bms_core/tests/db -q
268 passed, 1 warning in 6.33s
$ uv run pytest libs/bms_core/tests/ops/test_contract_gate.py libs/bms_core/tests/ops/test_contract_smoke.py \
    libs/bms_core/tests/ops/test_contract_snapshot.py libs/bms_core/tests/ops/test_contract_snapshot.py \
    libs/bms_core/tests/events libs/bms_core/tests/boundary -q
68 passed in 3.71s

# 2) 静态检查
$ uv run ruff check . && uv run ruff format --check . && uv run pyright
All checks passed!
723 files already formatted
0 errors, 0 warnings, 0 informations

# 3) 前端三包门禁（仓库根）
$ pnpm run check
core：Test Files 96 passed (96) / Tests 1180 passed (1180)
vue ：Test Files  2 passed  (2) / Tests    5 passed   (5)
ui-ep：Test Files 61 passed (61) / Tests  901 passed (901)

# 4) 文档与工程自检（仓库根）
$ python3 scripts/tools/check-docs/check-status.py --stage 02_后端基座与服务化地基
检查 344 项：硬规则不合规 0，软提示 0
$ python3 scripts/tools/base-check/check-base.py
[base-integrity] 通过：无跨层引用、无措辞残留、清单与磁盘一致、跨文档引用均写章节名、数据库设计登记一致。
$ python3 scripts/tools/base-check/check-links.py
[check-links] 通过：全库相对链接无断链、无失效锚点。
$ python3 scripts/tools/base-check/check-backend-base.py
[check-backend-base] 通过：继承链 / 错误码段位 / 迁移链全部对齐。
$ python3 scripts/tools/preflight/check-preflight.py --fast
================ preflight 结论 ================
全部通过
#   含 check-status（硬 0 / 软 0）、gateway_config check（服务目录零漂移）、
#   contract_snapshot check（9 个启用服务快照一致）、event_contracts check（契约 23 条 / 订阅 0 条、零漂移）

# 5) 流水线（GitLab bms/bms，main）
#   窗口 #307 ~ #368 共 57 条：success 38 / failed 18 / canceled 1（含实施期迭代推送）
#   失败条目（18）均为实施期中间推送，全部在同任务内修复后转绿：
#     #307 backend-test      #309 #310 module-build     #317 ~ #324 integration-dm8 / ci-base-build / backend-test
#     #340 backend-lint+base-integrity                  #349 #351 frontend-image
#     #355 contract-gate     #356 ~ #358 contract-smoke
#   收口末条：#368（d3d6eddc，06_04 回填）success

# 6) 缺陷查询（GitLab API，项目 2）
$ curl -s --header "PRIVATE-TOKEN: ***" ".../projects/2/issues?labels=defect-auto&state=opened&per_page=100"
开放缺陷：27（阶段二之前 #248 ~ #306；阶段内 #307 ~ #358 共 16 条已附修复依据评论并关闭，2026-09-24）
```

> 本报告依《文档生成规范》与《测试规范》「测试报告」节编写 · 数据快照 2026-09-24
