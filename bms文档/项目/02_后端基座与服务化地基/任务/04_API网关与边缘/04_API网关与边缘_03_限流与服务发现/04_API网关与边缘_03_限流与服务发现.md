# 03 限流与服务发现

> 后端基座与服务化地基 · 04_API网关与边缘 · 子任务 03

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 03 |
| 父任务 | [04_API网关与边缘](../04_API网关与边缘.md) |
| 对应需求 | [04-3](../../../需求/04_需求_API网关与边缘.md#r04-3) |
| 工时（重估） | 6h |
| 依赖 | 04_02 |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. 边缘限流（共享 Redis 多副本一致计数，登录限流优先）
2. 服务发现 DNS（地址收敛配置层、禁硬编码 IP）
3. 边缘观测（访问日志 / 指标出口）与灰度预留

## 3. 完成标准 <a id="accept"></a>

共享限流多副本一致；服务间按名寻址无硬编码 IP；边缘日志 / 指标可见；校验通过。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · API 网关与边缘》「边缘功能」节；《架构设计 · 部署与运维》「集群设计要点」节

> **前置契约（已交付）**：04_01（2026-09-23）已交付 APISIX standalone 声明式接入与**服务发现按名寻址**——`deploy/gateway/apisix.yaml` 由服务目录生成、CI 零漂移校验；上游节点为 `{service_key}:8000`（Compose DNS，禁硬编码 IP），外部路径 `/api/{service_key}/v1/...`。**本任务接线点**：限流插件经 `bms_core/services/gateway_catalog.py::ROUTE_PLUGINS` 按服务合并（`limit-count` + 共享 Redis 多副本一致）；边缘观测出口与灰度路由经 `global_rules` / `plugin_metadata` 扩展；规则声明式入 Git 并随流水线发布，运行时热加载（文件替换即 1s 内生效）。
