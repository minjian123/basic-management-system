# 前端网关接入改造测试记录

> 后端基座与服务化地基 · 04 API网关与边缘 · 01 网关接入与声明式配置 · 嵌套子任务 01 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [04-1-1 前端网关接入改造](../04_API网关与边缘_01_网关接入与声明式配置_01_前端网关接入改造.md) › 测试　|　[详细设计](../设计/01_详细设计_01_前端网关接入改造.md) · [实施记录](../实施/01_实施_01_前端网关接入改造.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [04-1-1 前端网关接入改造](../04_API网关与边缘_01_网关接入与声明式配置_01_前端网关接入改造.md) |
| 对应需求 | [04-4](../../../../../需求/04_需求_API网关与边缘.md#r04-4) |
| 详细设计 | [01_详细设计_01_前端网关接入改造](../设计/01_详细设计_01_前端网关接入改造.md) |
| 测试日期 | 2026-09-26 |
| 测试人 | minjian |
| 测试环境 | 开发机（Node v24.20.0 / pnpm 11.7.0 / Vitest 5；契约快照 9 份） |
| Kiwi 用例 | **2191**（平台骨架 / P2 / CONFIRMED / 自动化；登记输入 `test/scripts/kiwi/cases/2026-09-26_阶段二04-1-1_前端网关接入改造.json`，编号平台回读） |
| 自动化文件 | `frontend/packages/core/tests/service-endpoint.spec.ts`、`route-guard.spec.ts`；`frontend/apps/desktop/tests/api.spec.ts`、`api-http.spec.ts`、`api-endpoints.spec.ts`、`auth-guard.spec.ts`；`frontend/packages/api-types/tests/exports.spec.ts`（均 `kiwi_id: 2191`） |

## 2. 自动化用例 <a id="cases"></a>

用例先登记 Kiwi TCMS 取得编号 **2191**，再编码；一任务一条策展用例、多断言共用同一 `kiwi_id`。

| 用例 / 文件 | Kiwi | 断言要点 | 结果 |
| --- | --- | --- | --- |
| `core/tests/service-endpoint.spec.ts` | 2191 | `SERVICE_KEYS` 9 个且顺序与目录一致；`isServiceKey` 正反例；`servicePrefix` 逐服务；未登记服务键抛 `BaseError(10001)`；`serviceUrl` 空 / 首部斜杠 / 重复斜杠 / 查询串归一 | 通过（8 例） |
| `core/tests/route-guard.spec.ts` | 2191 | 持令牌放行；无令牌受保护路径返登录路径；白名单精确与子路径放行、前缀不误伤；自定义白名单 / 登录路径 | 通过（5 例） |
| `desktop/tests/api.spec.ts` | 2191 | `get/post/put/del(service, path)` 组装为 `/api/{service}/v1/...`；写方法带幂等键；`apiUrl` 组装；统一响应解包与业务码错误；令牌内存存储 | 通过（7 例） |
| `desktop/tests/api-http.spec.ts` | 2191 | baseURL 缺省 `/`；请求拦截器按令牌注入 `Authorization`；成功解包；幂等键入头；业务码非 0 抛 `ApiError`；401 → `SessionExpiredError` + `onUnauthorized`；非 axios 错误 → `onError` + `ApiError(10001)`；`installHttpAdapter` 注入核心适配器 | 通过（9 例） |
| `desktop/tests/api-endpoints.spec.ts` | 2191 | 字典→platform / 组织→org / 验证码→identity / 文件→file / 检索→search / 通知→notification；AI 流式端点→ai；`authHeaders` 两态；端点选项 headers 动态取令牌 | 通过（4 例） |
| `desktop/tests/auth-guard.spec.ts` | 2191 | `isAuthGuardEnabled` 缺省关 / `=on` 开；关闭时不注册钩子；启用时按令牌判定（无令牌→`/login`、公开→放行、持令牌→放行）；`LoginView` 占位渲染 | 通过（5 例） |
| `api-types/tests/exports.spec.ts` | 2191 | index 命名空间集合 == 核心 `SERVICE_KEYS`；9 份服务类型文件存在且带「勿手改」生成头 | 通过（2 例） |
| `pnpm run api-types:gen:check` | 2191 | 重新生成 9 份类型与仓库产物逐字节零差异（CI `api-types-check` 同口径） | 通过 |
| 既有全量回归 | — | 核心 98 文件 / 1193 例；ui-ep 61 / 901；宿主 20 / 121；demo / sample 模块 lint / typecheck / test / build / budget / guard:isolation 全绿 | 通过 |

## 3. 覆盖率与门禁 <a id="coverage"></a>

| 项 | 结果 |
| --- | --- |
| 核心（`pnpm --filter @bms/core run test`） | 98 文件 / **1193 例通过** |
| api-types（`pnpm run api-types:check`） | typecheck / lint / test 通过（1 文件 / 2 例） |
| 宿主（`test:cov`） | 20 文件 / **121 例通过**；整体覆盖率达标（阈值 70%）；新增 `router/guard.ts`、`views/LoginView.vue` 100% |
| 宿主构建与体积（`build` / `budget`） | 构建通过；首屏合计 195.8 KB（预算 260 KB）、最大单文件 98.8 KB（预算 150 KB） |
| 模块工程门禁 | demo / sample 全绿（体积预算与产物面隔离零违规） |
| 文档与基座校验 | `check-status --stage 02_后端基座与服务化地基` 硬 0（软提示见实施记录）；`check-base` / `check-links` / `check-preflight --fast` 全绿 |

## 4. 验收映射 <a id="accept"></a>

| 完成标准（需求 04-4） | 证据 |
| --- | --- |
| 前端请求一律经「域 → 服务前缀」映射按服务段寻址 | 核心 `service-endpoint` 用例 + 宿主 `apiUrl` / `get/post/put/del` URL 断言（`/api/{service}/v1/...`） |
| 能力源按服务分流 | `api-endpoints` 用例逐能力源 endpoint 断言 + dev 页接线（DictCheck / CaptchaCheck） |
| 类型由契约快照生成、无手写漂移 | `@bms/api-types` 生成 9 服务类型入 Git；`gen:check` 零漂移；CI `api-types-check` 独立 job 阻断 |
| 登录与各能力源请求联通 | 骨架：401 钩子 + 守卫判定 + 登录占位页用例就位；真实登录链路归阶段六（详细设计 §8）；能力源可按服务段端点直连各服务 |
| 用例覆盖、校验通过 | Kiwi 2191 + 前端各包门禁（lint / typecheck / test / build / budget）+ 基座与文档校验全绿 |

## 5. 问题与处置 <a id="issues"></a>

| # | 问题 | 发现阶段 | 处置 |
| --- | --- | --- | --- |
| 1 | 令牌护栏拦截登录页硬编码色值 | `desktop-check`（`guard-tokens.spec.ts`） | 去色值回退，仅消费 `--bms-*` 令牌 |
| 2 | `generate.mjs` 的 node 全局被 eslint `no-undef` 拦截 | `api-types:check` | eslint 为 `scripts/**/*.mjs` 单独声明 node 全局 |
| 3 | 外置依赖态下 pnpm run 触发重装判定 | 实施（跑单测） | 单测用包内二进制跑；预检改 `node` 直跑生成器 |

## 6. 偏差与遗留 <a id="deviations"></a>

- 同[实施记录](../实施/01_实施_01_前端网关接入改造.md) §7（守卫落点 `router/guard.ts`、vite 函数式配置已回写设计；真实登录 / RBAC / 模块 `api` 注入 / 移动端 / 代理端口各自归口）。
- 真实登录链路与守卫默认开启归阶段六；届时本任务的核心映射、请求层与端点解析均直接复用，不返工。

> 本文档依《[文档生成规范](../../../../../../../规范/文档生成规范.md)》编写
