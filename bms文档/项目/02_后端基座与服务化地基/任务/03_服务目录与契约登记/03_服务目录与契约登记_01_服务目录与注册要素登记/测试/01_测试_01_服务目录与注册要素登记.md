# 服务目录与注册要素登记测试记录

> 后端基座与服务化地基 · 03 服务目录与契约登记 · 01 服务目录与注册要素登记 · 测试记录

[文档首页](../../../../../../文档首页.md) › [01 服务目录与注册要素登记](../03_服务目录与契约登记_01_服务目录与注册要素登记.md) › 测试　|　[详细设计](../设计/01_详细设计_01_服务目录与注册要素登记.md) · [实施记录](../实施/01_实施_01_服务目录与注册要素登记.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 服务目录与注册要素登记](../03_服务目录与契约登记_01_服务目录与注册要素登记.md) |
| 对应需求 | [03-1](../../../../需求/03_需求_服务目录与契约登记.md#r03-1) |
| 详细设计 | [01_详细设计_01_服务目录与注册要素登记](../设计/01_详细设计_01_服务目录与注册要素登记.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_服务目录与注册要素登记.md) |
| 测试日期 | 2026-09-22 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv）；SQLite 真库（临时库 + 开发平台库）；迁移零漂移离线比对 |
| Kiwi 用例 | 2162 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 2162 | 服务目录清单结构 | 单元 | `libs/bms_core/tests/services/test_module_registry.py::test_service_catalog_shape` | `SERVICE_CATALOG` 16 行（平台服务 10 + 业务模块 6）；服务键集合；分组 / 状态筛选；平台域视图；版本初值 |
| 28 | 清单校验（合法 / 重复 / 格式） | 单元 | `…::test_validate_accepts_legal_registry` / `test_validate_detects_duplicates` / `test_validate_detects_bad_format` | 四要素唯一；`module_key`/`service_key`/`table_prefix`/`event_domain`/`errcode_segment` 重复检出；非法格式 / 分组 / 批次 / semver / 产品维度逐项检出 |
| 2162 | 模型与迁移 | 单元 | `…::test_models_declared_and_persistable` + `tests/alembic/test_alembic_drift.py`（1078） | 新字段可建表 / 落库；`errcode_segment`/`service_key`/`product_key` 可空；DB 唯一仅 3 组（段位无唯一）；0002 平台链迁移零漂移 |
| 2162 | 种子 upsert 幂等 | 单元 | `libs/bms_core/tests/ops/test_module_ops.py::test_seed_module_upsert_idempotent` / `test_seed_module_cli` | 首建 16 行；重复 0/0；既有行补齐 `service_key`；dry-run / CLI 输出 |
| 2162 | 只读仓储 | 单元（SQLite 真库） | `libs/bms_core/tests/repositories/test_module_repository.py` | `list_catalog` 按分组 / 状态 / 服务维度过滤；`get_by_key` 命中与缺失；软删后不可见 |
| — | 回归适配 | 单元 | `tests/alembic/test_alembic_chains.py` / `tests/ops/test_migration_ops.py` / `platform` 的 `test_modules` / `test_type_mapping` | 平台 head = 0002；服务目录接口 16 行；段位不建唯一 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run ruff check .                # All checks passed!
$ uv run ruff format --check .       # 580 files already formatted
$ uv run pyright                     # 0 errors, 0 warnings
$ uv run pytest -q                   # 921 passed, 36 skipped
$ uv run pytest libs/bms_core/tests --cov=bms_core.services.module_registry \
      --cov=bms_core.repositories.module_repository --cov=bms_core.models.platform   # 均 100%
$ uv run alembic -n alembic:platform -x url="sqlite+aiosqlite:///./bms_platform.db" upgrade head
$ uv run python -m ops.seed_module   # 新增 12 行 / 更新 4 行（重复执行 0 / 0）
$ uv run python -m ops.check_modules # 校验通过（16 项）
$ python3 scripts/tools/base-check/check-backend-base.py   # 通过
$ python3 scripts/tools/base-check/check-base.py           # 通过（174 文档一致）
$ python3 scripts/tools/base-check/check-links.py          # 断链 0 / 失效锚点 0
```

结果汇总：本任务新增 / 适配用例全绿（Kiwi 2162 标注 6 处、既有 28 / 1050 / 1078 语义适配后保持通过）；全量 921 passed / 36 skipped；新增 / 修改模块覆盖率 100%；开发平台库迁移与种子实测幂等（16 行）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 平台链 head 断言失败 | 新增 0002 后 head 由 0001 变 0002 | 适配 `test_alembic_chains` / `test_migration_ops`（见实施记录 §4） |
| 段位复合唯一 DDL 断言失败 | 段位去唯一为设计口径 | `test_type_mapping` 改断言 3 组唯一 + 段位无唯一 |
| 服务目录接口断言 4 行失败 | 清单由平台域 4 行扩为服务目录 16 行 | 适配 `test_modules` 期望值 |
| 迁移分支标签冲突 | 0002 重复声明 `platform` | `branch_labels=None`（见实施记录 §4） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest libs/bms_core/tests --cov=…`：`bms_core/services/module_registry.py` **100%**、`bms_core/repositories/module_repository.py` **100%**、`bms_core/models/platform.py` **100%**；本任务新增 / 修改代码无未覆盖分支。

## 6. 偏差与遗留 <a id="deviations"></a>

- 启动 / CI 接库唯一性与契约版本兼容校验（03_02）、只读 HTTP 接口（03_03）不在本任务；平台服务段位留空、错误码沿用平台五位段为开放项。
- 服务简称整改范围经确认作罢，本次服务行沿用微服务工程名；既有模块简称与《英文简称规范》未改动（详见实施记录 §7）。

> 本文档依《文档生成规范》编写
