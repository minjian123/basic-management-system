# 阶段验收门禁核对与测试报告实施记录

> 后端基座与服务化地基 · 10 阶段验收 · 01 阶段验收门禁核对与测试报告 · 实施记录

[文档首页](../../../../../../文档首页.md) › [01 阶段验收门禁核对与测试报告](../10_阶段验收_01_阶段验收门禁核对与测试报告.md) › 实施　|　[详细设计](../设计/01_详细设计_01_阶段验收门禁核对与测试报告.md) · [测试记录](../测试/01_测试_01_阶段验收门禁核对与测试报告.md)

## 1. 实施信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 阶段验收门禁核对与测试报告](../10_阶段验收_01_阶段验收门禁核对与测试报告.md) |
| 对应需求 | [10-1](../../../../需求/10_需求_阶段验收.md#r10-1) |
| 详细设计 | [01_详细设计_01_阶段验收门禁核对与测试报告](../设计/01_详细设计_01_阶段验收门禁核对与测试报告.md) |
| 实施日期 | 2026-09-24 |
| 实施人 | minjian |
| 实施环境 | 开发机（Ubuntu；Python 3.14 / uv；Node 22 / npm）；mjbk（GitLab CE + Runner、Kiwi TCMS 容器、三库与可观测栈）——mjbk 侧**只做证据查询**（GitLab API / Kiwi 登记），不重跑重负载实测（决策项 2） |
| Kiwi 用例 | **2188**（平台骨架 / P2 / CONFIRMED / 自动化；登记输入 `test/scripts/kiwi/cases/2026-09-24_阶段二10-01_阶段验收门禁核对与测试报告.json`，回读 `exports/2026-09-24_阶段二10-01_阶段验收门禁核对与测试报告_登记结果.json`） |
| 提交 | 设计 `09005064`；实施 / 测试记录与回写见 §6 |
| 结论 | 完成（阶段二 14 行门禁**全部达标**；[阶段测试报告](../../../../01_测试报告_后端基座与服务化地基.md)归档；三类文档状态一致；S2 软提示 5 → 0） |

## 2. 实施概览 <a id="overview"></a>

```mermaid
flowchart LR
    A["8 项待拍板逐项确认"] --> B["详细设计定稿并提交（09005064）"]
    B --> C["证据采集：本地全量门禁 + GitLab API + Kiwi"]
    C --> D["门禁 14 行逐条判定与证据索引"]
    D --> E["阶段测试报告撰写归档"]
    E --> F["缺陷单处置：阶段内 16 条评论 + 关闭"]
    F --> G["状态回写：计划 / 任务 / 域总览"]
    G --> H["Kiwi 2188 登记 → 收口断言用例"]
    H --> I["第二 AI 独立复核 + 异常项处置"]
    I --> J["全量验证全绿 → 提交"]
```

结果小结：阶段二 14 行门禁（完成标准 5 + 服务化地基 S1~S7 + 多服务一键拉起 / 独立库迁移 + 阶段验收留痕）逐条给出结论与证据索引，**全部达标**（4 项带限定并注明归口）；阶段测试报告（六节 + 附录）归档；计划 §1 计数与工时、§2 / §3、§4 甘特、§5 结论与里程碑对照、§7 台账全部回写；阶段内 16 条自动缺陷单附修复依据评论后关闭（历史 27 条登记遗留）；`check-status --stage 02_后端基座与服务化地基` 由「硬 0 / 软 5」转 **硬 0 / 软 0**。

## 3. 实施过程 <a id="process"></a>

1. **决策确认（8 项，先于动笔）**：排期窗口与收口日期、证据采集范围、门禁判定口径、门禁结论落点、甘特收口形态、Kiwi 用例条数与落点、计划工时口径、独立复核——逐项用 question 确认（推荐项在前、附差异说明），全部取推荐项（对齐记录见[设计 §10](../设计/01_详细设计_01_阶段验收门禁核对与测试报告.md#alignment)）。
2. **详细设计定稿并提交**：`09005064 docs(10_01): 阶段验收门禁核对与测试报告详细设计（门禁 14 行核对口径 + 报告结构 + 收口回写）`。
3. **证据采集（本地）**：`uv run pytest -q --cov=… --cov-branch --cov-report=term-missing`（**1341 passed / 37 skipped**（收口基线 1337 + 本任务 4 条收口断言用例）、TOTAL 95% / 分支 93%）、专项套件（`tests/{ops,boundary,db}` 268 passed；契约 / 事件 / 边界 68 passed）、`ruff check` / `ruff format --check`（723 文件）/ `pyright`（0 errors）、前端 `npm run check`（core 96 文件 1180 条、vue 2 / 5、ui-ep 61 / 901）。
4. **证据采集（mjbk 查询）**：GitLab API（项目 2）取阶段窗口 main 流水线 #307 ~ #368（57 条：success 38 / failed 18 / canceled 1）、失败条目的失败岗位与后续转绿流水线、`defect-auto` 开放缺陷单；Kiwi TCMS 平台回读各任务策展编号（本阶段新增 34 个）。
5. **门禁 14 行逐条判定**：按设计 §4 的核对手法与证据来源逐行核对（结论与索引见 [§5.1](#verify)），结论内联回写[计划 §5](../../../../计划/01_计划_后端基座与服务化地基.md#accept)，并新增「里程碑对照」表（M2 基线 10 周 ↔ 2026-09-24 实际达成）。
6. **阶段测试报告撰写归档**：`bms文档/项目/02_后端基座与服务化地基/01_测试报告_后端基座与服务化地基.md`（六节 + 附录 A：命令与原始输出、流水线区间、缺陷查询结果）。
7. **缺陷单处置**：阶段窗口内 16 条 `defect-auto` 自动单逐条 POST 评论（失败岗位 + 后续转绿流水线 + 达梦环境问题说明）后 `state_event=close`；历史 27 条**不越权处置**，登记为计划 §7 新增待办。
8. **状态回写**：计划表头计数（31 项 / 0 项）、§1 排期口径与工时（`已完成 316h；剩余 0h；阶段合计 **316h**`，S1 可校验形态）、§2 表题补完成区间并追加 `10_01` 行、§3 剩余表清空、§4 甘特精简为十域里程碑 + M2、§5 结论内联与里程碑对照、§7 新增第 34 / 35 项与核销注；任务文档状态 / 完成日期与收口注块；域总览子任务表状态。
9. **收口断言用例**：先登记 Kiwi（回读 **2188**），再写 `backend/libs/bms_core/tests/crosscut/test_m2_closure.py`（4 个断言面），与 `test_m3_closure.py` 同口径（文档断言 + 不写死易漂移计数）。
10. **独立复核与全量验证**：第二 AI 独立复核（见 §4）→ 全门禁复跑全绿（[§5.2](#verify)）→ 提交。

## 4. 问题与处置 <a id="issues"></a>

**实施期问题**：

| # | 问题 / 现象 | 原因 | 处置与落点 |
| --- | --- | --- | --- |
| 1 | `check-status --stage 02_…` 长期报 S2 软提示 5 项（甘特含已完成节点 08_01 / 08_02 / 09_01 ~ 09_03） | 09_03 收口遗留（任务完成时未同步清理甘特节点） | 本任务按「甘特不含已完成节点」精简为里程碑态，软提示清零（决策项 5）；遗留闭环记入计划 §7 核销注 |
| 2 | 计划 §1 工时行的 `**308h**` 加粗写法使 `check-status` 的 S1 工时校验**静默跳过** | 正则要求「已完成 Nh；剩余 Mh…阶段合计 \*\*Nh\*\*」形态 | 规范化为 `已完成 316h；剩余 0h；阶段合计 **316h**`（决策项 7）；已核对明细合计恰为 316h，S1 校验生效 |
| 3 | 阶段内 main 流水线 18 条 failed，直接写「全绿」会失真 | 失败均为实施期迭代推送（含契约门禁首次实证拦截） | 报告 §1 与附录 A 按「区间 + 状态分布 + 失败岗位 + 后续转绿」如实记录，并逐条落到缺陷单处置（§3 第 7 步） |
| 4 | 自动缺陷单 43 条长期开放（含阶段内 16 条） | 《测试规范》§9 关闭需人工，单人期未跟进 | 经决策项 9 确认：关闭阶段内 16 条（附修复依据），历史 27 条登记遗留（计划 §7 第 34 项）+ 登记流程缺口 |
| 5 | 达梦 CI 岗位 `integration-dm8` 为 `when: never`，与「三库真库集成通过」表述存在张力 | 达梦环境加密模块问题（计划 §7 第 8 项，非本仓代码） | 门禁第 5 行结论后加「**（限定：…）**」注明归口（决策项 3） |
| 6 | `check-status` 的 `EFFORT_RE` 只认 `阶段合计 **316**h`，与本项目**全部**阶段实际写法（`**316h**` / `57h`）不符 → S1 工时子校验在所有阶段计划上**从未触发** | 脚本正则与实际口径脱节（历史缺陷，10_01 门禁核对发现） | 经决策项 10 修正则（兼容三种写法）；修正后阶段一 / 二 / 四 / 五 均 0 软提示，**阶段三暴露头部 57h ↔ 明细 61h 不一致** → 经决策项 11 一并修正（计划 §1 与阶段测试报告 meta 的工时 / 任务数同步为 61h / 12 项） |

**第二 AI 独立复核（独立视角只读复核，逐条挑战证据链）**：

| # | 复核发现 | 性质 | 处置与落点 |
| --- | --- | --- | --- |
| a | 「阶段内流水线全绿」类表述与 57 条中 18 条 failed 的事实冲突 | 证据表述风险 | 报告 §1 / 附录 A 改为区间 + 状态分布 + 失败岗位逐条列示，并说明「均为实施期迭代、同任务内转绿」，避免夸大 |
| b | 「三库真库集成通过」若不加限定，会掩盖 `integration-dm8` 岗位关闭的事实 | 结论完备性 | 门禁第 5 行加限定与归口；报告 §2 单列 5 条达梦环境类自动单的处置说明 |
| c | Kiwi「本阶段新增 34 个编号」需与平台 / 台账对账，不能按「上号 +1」推算 | 事实性核对 | 以各任务测试记录 §1 与平台回读为准；核对中发现台账 §2 缺 984 / 1204 / 1205 / 1206 四条登记 → 登记计划 §7 第 35 项 |
| d | 计划 §7 台账 33 项若只写「见台账」则无核销结论 | 闭环完备性 | 报告 §4 逐条给「影响 + 去向」并给核销说明（7 项已闭环）；计划 §7 补核销注 |
| e | 收口断言用例若写死 31 / 316h，后续回写易误红 | 测试可维护性 | 用例改为结构自洽断言（头部计数 == 已完成表行数、剩余 0、工时行为可校验形态），不写死计数 |

**审核通过项**：门禁 14 行与《总体项目规划》阶段二行 + S1~S7 逐条对位无缺漏；计划 / 任务 / 域总览三处状态与工时一致（H5 / H6 / H7 / S1 全绿）；报告头部 meta 与需求总览 / 计划计数逐字一致；阶段内自动单关闭依据（失败岗位 + 后续转绿流水线）可从 GitLab API 复核；历史 27 条未擅自处置。

## 5. 验证结果 <a id="verify"></a>

### 5.1 门禁 14 行核对（核对手法与实测结果）

| # | 验收点 | 核对命令 / 手法 | 实测结果 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | 数据访问真实落库 | 全量 `uv run pytest -q` + `bms_core/db/` 落地物核对 | 1341 passed / 37 skipped；引擎 / 会话工厂 / UoW / 仓储齐备；覆盖 96.7% | 达标 |
| 2 | 三库拓扑可路由、租户隔离与批量迁移正确 | 库键 / 库名派生用例 + `ops` 编排用例 + 06_02 真库演练结论 | 三库演练 9 / 9 / 13 例通过并清理（06_02 §3.2）；`ops` 268 passed | 达标 |
| 3 | 模块注册接库校验生效、`BaseModel` 落库与排序 DB 侧生效 | `SERVICE_CATALOG` 计数 + 启动校验用例 + 排序 / keyset 用例 | 16 行（9 启用）；`CatalogError` 40003 拒启；`BaseModel` 四库映射与 NULL 恒末位 | 达标 |
| 4 | 基类异步切换完成 | `BaseDbRepository` 异步 CRUD 用例 | `tests/db` 全绿（含于 268 passed） | 达标 |
| 5 | 三库真库集成与达梦方言实测通过 | 01_05 达梦四项实测 + 06_02 三库演练 + CI `verify/db` 档 | 达梦结论标「实测」入方言特性文档；**（限定：`integration-dm8` 岗位 `when: never`，计划 §7 第 8 项）** | 达标（限定） |
| 6 | S1 服务边界与契约 | 契约基线目录 + 预检 `contract_snapshot check` + oasdiff / Schemathesis 护栏用例 | `deploy/contracts/baseline/` 9 份；快照零漂移；契约专项 68 passed | 达标 |
| 7 | S2 数据所有权 | `TABLE_OWNERSHIP` 计数与对账用例 + 边界硬校验用例 + `keys.py` 库键契约 | 39 条归属登记、双向对账通过；每服务库名 `bms_{service}` 派生 | 达标 |
| 8 | S3 事件与一致性 | 事件契约快照 + Outbox / 幂等 / Saga 用例 + 预检 `event_contracts check` | 契约 23 条 / 订阅 0 条、零漂移；**（限定：RocketMQ 真实投递归阶段十）** | 达标（限定） |
| 9 | S4 服务身份与认证 | 网关 / 双类 JWT 用例 + 07_03 mjbk 六项实测清单 | 入站只信服务 JWT、伪造头被拒、`X-User-Subject` 注入；**（限定：完整登录链路归阶段六 / 七）** | 达标（限定） |
| 10 | S5 运行时无状态与外部化 | 每服务 `/healthz` `/readyz` 用例 + 单体重构后装配点核对 | 9 服务探针用例全绿；会话 / 缓存 / 文件 / 任务外部化装配点齐备 | 达标 |
| 11 | S6 分布式可观测 | 跨服务同 trace 用例 + 指标 / 日志配置核对 + 双服务冒烟 | trace 贯通、指标端点与 Grafana 面板就位；**（限定：真实 Celery 埋点归任务调度阶段）** | 达标（限定） |
| 12 | S7 度量与门禁 | 档位文件 + CI job 定义 + 度量口径核对 + 发布 / 回滚实测 | `deploy/ci/verify/{contract,contract-gate,db,defect,image}` 已开；两类度量口径与通道在位 | 达标 |
| 13 | 多服务本地一键拉起、每服务独立库迁移验证通过 | `deploy/compose/services.yml` + bootstrap + `up` / `probe_success` 实测 | 9 服务编排（Compose DNS）；`up{job="bms-services"}` 9/9、`probe_success` 18/18 | 达标 |
| 14 | 阶段验收留痕 | 本表 + 报告 + 状态三处 + 收口断言用例 | 计划 §5 结论齐备；报告归档；`check-status` 硬 0 / 软 0；Kiwi 2188 用例通过 | 达标 |

### 5.2 本地门禁与自检

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 静态检查 | `uv run ruff check .` / `uv run ruff format --check .` / `uv run pyright` | 通过 / 723 文件已格式化 / 0 errors |
| 测试与覆盖率 | `uv run pytest -q --cov=… --cov-branch --cov-report=term-missing` | 1341 passed、37 skipped；TOTAL 95%（分支 93%）、`bms_core` 96.7% / 93.6% |
| 收口断言用例 | `uv run pytest libs/bms_core/tests/crosscut/test_m2_closure.py -q` | 4 passed（Kiwi 2188） |
| 前端门禁 | `npm run check`（仓库根） | core 1180 / vue 5 / ui-ep 901 全通过（159 文件） |
| 文档状态一致性 | `python3 scripts/tools/check-docs/check-status.py --stage 02_后端基座与服务化地基` | 检查 349 项，**硬 0 / 软 0** |
| 基座自检 | `check-base.py` / `check-links.py` / `check-backend-base.py` | 三者全通过（无断链、无跨层、清单与磁盘一致、继承链与迁移链对齐） |
| 本地预检 | `scripts/tools/preflight/check-preflight.py --fast` | 全部通过（含 `contract_snapshot` / `event_contracts` / `gateway_config` 零漂移） |
| 真实流水线 | 推送后父-子流水线（收口提交） | 见 §6（收口推送流水线号） |

## 6. 偏差与遗留 <a id="deviations"></a>

- **设计偏差（2 处）**：① 计划 §7 未新增「甘特 S2 闭环」待办行，改为在 §7 加核销注（待办表只放未闭环事项）；② 收口断言用例的 S 门禁标签断言按计划表实际写法改为 `**服务化地基 S{1..7} …**`（设计 §7 原写 `S1 ~ S7` 泛化表述）。两处已在实施中就地修正并落本记录。
- **重负载未重跑（口径，非偏差）**：三库真库 / 契约冒烟 / 服务发布与回滚按决策项 2 复用已归档证据；如后续需要当日快照，可单独发起。
- **新增遗留（2 项，已登记计划 §7）**：第 34 项（开放自动缺陷单 27 条 + 「修复即流转关闭」流程缺口）、第 35 项（测试资产仓台账缺 984 / 1204 / 1205 / 1206 四条登记）。
- **遗留闭环（1 项）**：09_03 收口遗留的甘特 S2 软提示（5 项）随本任务甘特精简清零。
- **提交清单**（各仓库独立提交）：bms 仓——`docs(10_01)`（设计 / 报告 / 计划 / 任务 / 域总览）、`test(10_01)`（收口断言用例）；测试资产仓——Kiwi 登记输入与回读、台账批次追加。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
