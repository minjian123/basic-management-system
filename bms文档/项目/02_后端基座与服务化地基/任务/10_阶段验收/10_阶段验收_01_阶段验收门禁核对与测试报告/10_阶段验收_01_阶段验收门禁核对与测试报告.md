# 01 阶段验收门禁核对与测试报告

> 后端基座与服务化地基 · 10_阶段验收 · 子任务 01

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 01 |
| 父任务 | [10_阶段验收](../10_阶段验收.md) |
| 对应需求 | [10-1](../../../需求/10_需求_阶段验收.md#r10-1) |
| 工时（重估） | 8h |
| 依赖 | 01_05、02_03、03_02、04_03、05_04、06_02、07_03、08_02、09_03 |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. 逐条核对阶段二完成标准与服务化地基门禁 S1~S7，给出结论与证据索引
2. 按《测试规范》产出阶段测试报告并归档
3. 三类文档状态一致、遗留项登记归口
4. 门禁结论与证据落计划文档与测试报告

## 3. 完成标准 <a id="accept"></a>

阶段二门禁逐条有结论与证据、S1~S7 逐条判定；阶段测试报告归档；三类文档状态一致；遗留项登记归口；校验通过。

## 4. 参考文档 <a id="ref"></a>

- 《总体项目规划》「阶段内容与完成标准」「验收与收尾」节；《微服务演进规划》「服务化地基要求与门禁」节

> **前置契约（已交付 · 05_04，2026-09-23）**：事件契约与 Saga 基座已就位——事件契约注册表与兼容校验（`bms_core/events/contracts.py`）、平台默认事件契约 23 条（`bms_core/events/platform_events.py`）与快照 `deploy/events/contracts.json`（CLI `backend/ops/event_contracts.py`，预检 / CI 零漂移）；`sys_outbox.event_version` 列（平台链 `0004` / 租户链 `0003`）与签发校验模式 `[event].contract_mode`；Saga 三态协同式执行器 `BaseSagaExecutor` / `ChoreographySagaExecutor`（`[saga]`，缺省 `choreography`）与审计链分条键 `build_chain_key`；存量事件名已统一为「已登记事件域」前缀，强一致场景口径见《后端开发规范》§10。**S3（事件与一致性）门禁**按上述产物逐条核对。

> **前置契约（已交付 · 07_03，2026-09-23）**：网关认证接线与服务身份已就位——网关 `forward-auth` 转认证服务内部端点（`/api/v1/auth/introspect`）按 `aud=api` 全校验用户 JWT、判公开路径并换发短时网关服务 JWT（`aud=service`）；后端 `[edge].provider = service_jwt`（`ServiceJwtEdgeTrust`，入站服务 JWT 本地 JWKS 验签、只信有效服务 JWT）；`require_auth` 按 `[edge].require_gateway_identity` 门控真实可信身份；身份头新增 `X-User-Subject`；限流维度扩用户主体 / 租户。mjbk 实测通过（无凭证 401 / 公开路径 200 / 身份头注入 / 用户 token 覆盖 / 伪造头剥除 / 服务 JWT 验签）。**S4（服务身份与认证）门禁**按上述产物逐条核对。

> **前置契约（已交付 · 09_01 ~ 09_03，2026-09-24）**：多服务 CI-CD 与契约门禁已就位——父-子流水线（父按变更路径 trigger 服务子流水线，`strategy: depend`；公共模板 `deploy/ci/templates/backend-service.yml`；镜像 `bms-{service}:$CI_COMMIT_SHORT_SHA`）、不可变镜像与一键回滚（SHA + tag 追加 semver、禁 latest、留存清理；`scripts/tools/deploy/release.py` + 版本台账；健康门禁 `/healthz` + `/readyz` + 网关路由，失败自动回滚；发布清单 artifact）、**契约门禁（每服务独立基线 `deploy/contracts/baseline/<service>.json`；oasdiff `breaking` 拦截与中文清单；Schemathesis 打真实服务冒烟；破坏数 `bms_contract_breaking_total{service}` 推 Pushgateway）**。**S7（度量与门禁）门禁**按上述产物逐条核对：契约破坏数、服务独立发布成功率两类度量的口径 / 通道 / 门禁均已在位。
