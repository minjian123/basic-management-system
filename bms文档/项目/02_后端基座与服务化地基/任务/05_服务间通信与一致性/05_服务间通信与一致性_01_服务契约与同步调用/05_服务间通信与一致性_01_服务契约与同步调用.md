# 01 服务契约与同步调用

> 后端基座与服务化地基 · 05_服务间通信与一致性 · 子任务 01

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 01 |
| 父任务 | [05_服务间通信与一致性](../05_服务间通信与一致性.md) |
| 对应需求 | [05-1](../../../需求/05_需求_服务间通信与一致性.md#r05-1) |
| 工时（重估） | 10h |
| 依赖 | 02_01 |
| 负责人 | minjian |
| 状态 | 已完成 |
| 完成日期 | 2026-09-23 |

## 2. 任务内容 <a id="content"></a>

1. 每服务唯一公开契约（OpenAPI），禁止访问内部实现
2. 服务间调用封装（显式超时 / 重试 / 熔断 / 限流）
3. 契约快照入 CI
4. 跨服务读经契约 DTO 或本地只读投影

## 3. 完成标准 <a id="accept"></a>

服务间调用经公开契约且韧性可证；契约快照可导出；禁止内部实现访问可校验；pytest / ruff / pyright 全绿。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 服务间通信与分布式一致性》「服务边界与契约」「同步调用」节

> **前置契约（已交付 · 02_01，2026-09-22）**：工作区与共享基座库已就位（`libs/bms_core` + `services/<服务>`，包 `bms_core` / `bms_<服务>`）；服务化语义基座（服务契约 / 服务间调用 / Outbox）预留承载于共享基座库。本任务在该骨架上落服务公开契约与同步调用。
