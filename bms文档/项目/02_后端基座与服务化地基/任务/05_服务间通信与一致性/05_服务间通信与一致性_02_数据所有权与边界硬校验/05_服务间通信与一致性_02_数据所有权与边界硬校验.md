# 02 数据所有权与边界硬校验

> 后端基座与服务化地基 · 05_服务间通信与一致性 · 子任务 02

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02 |
| 父任务 | [05_服务间通信与一致性](../05_服务间通信与一致性.md) |
| 对应需求 | [05-2](../../../需求/05_需求_服务间通信与一致性.md#r05-2) |
| 工时（重估） | 8h |
| 依赖 | 05_01 |
| 负责人 | minjian |
| 状态 | 已完成 |
| 完成日期 | 2026-09-23 |

## 2. 任务内容 <a id="content"></a>

1. 数据所有权（写方唯一；跨服务读经契约 / 事件或只读投影）
2. 边界硬校验 CI（禁跨服务 import 内部实现、禁跨服务库访问）
3. 读侧出口（只读投影 / 数据查询提供者）与例外登记
4. 边界越界数 / 跨库访问数纳入度量

## 3. 完成标准 <a id="accept"></a>

边界硬校验可拦截越界（CI 失败）；读侧出口用例通过；所有权纪律可度量；pytest / ruff / pyright 全绿。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 数据架构》「数据规范」节；《后端开发规范》「模块边界与数据所有权」节

> **前置契约（已交付 · 05_01，2026-09-23）**：跨服务读出口两条合法出口已就位——契约 DTO 基类 `ServiceDto`（`bms_core/schemas/service.py`）+ 只读投影沿用数据查询提供者 `BaseQueryProvider`（`bms_core/query/`）；服务间调用基座 `BaseServiceClient`（插件键 `service_client`，公开契约路径 `/api/v1` 前缀 + 目标服务登记启用护栏）与公开契约快照（`bms_core/services/service_contract.py` + `ops/contract_snapshot.py` + `deploy/contracts/*.json`）可供本任务复用。本任务在此之上落**跨服务 import / 跨库访问硬校验与边界度量**。
