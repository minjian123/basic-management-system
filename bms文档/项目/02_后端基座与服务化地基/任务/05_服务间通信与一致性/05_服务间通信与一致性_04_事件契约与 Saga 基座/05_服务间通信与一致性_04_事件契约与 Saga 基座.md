# 04 事件契约与 Saga 基座

> 后端基座与服务化地基 · 05_服务间通信与一致性 · 子任务 04

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 04 |
| 父任务 | [05_服务间通信与一致性](../05_服务间通信与一致性.md) |
| 对应需求 | [05-4](../../../需求/05_需求_服务间通信与一致性.md#r05-4) |
| 工时（重估） | 8h |
| 依赖 | 05_03 |
| 负责人 | minjian |
| 状态 | 已完成 |
| 完成日期 | 2026-09-23 |

## 2. 任务内容 <a id="content"></a>

1. 事件契约版本（只增不删、破坏性升版本、消费兼容）
2. Saga / 补偿基座语义（本地事务 + 事件 + 幂等补偿）
3. 强一致场景口径（审计链按服务×租户分条、财务单一账务服务、排除 2PC/XA）

## 3. 完成标准 <a id="accept"></a>

事件契约版本规则落地（兼容用例）；Saga / 补偿基座语义就位可复用；强一致场景口径与实现一致；pytest / ruff / pyright 全绿。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 服务间通信与分布式一致性》「跨服务一致性（Saga / 补偿）」「强一致场景处理」节

> **前置契约（已交付 · 05_03，2026-09-23）**：事务性发件箱与幂等消费基座已就位——`bms_core/outbox/`（`BaseOutboxStore` / `SqlOutboxStore`、`BaseOutboxDispatcher` / `PollOutboxDispatcher`、`ProcessedEventStore`）；`sys_outbox` / `sys_event_consumed` / `sys_event_dead_letter` 三表双链（平台链 `0003_sys_outbox` / 租户链 `0002_sys_outbox`）；`EventEnvelope` 已扩 `event_id` / `occurred_at` / `aggregate_key`；错误码 `10009` `OUTBOX_DELIVERY`；CLI `ops/outbox.py`（dispatch / replay）与死信看板接口 `/api/v1/outbox/dead-letters`。本任务的事件契约版本规则与 Saga / 补偿基座**复用**上述投递与幂等能力（补偿动作自身幂等经 `ProcessedEventStore` / 业务幂等键承载），不改其契约。
