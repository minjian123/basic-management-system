# 03 BaseRepository 异步与 BaseModel 落库

> 后端基座与服务化地基 · 01_后端基座真实实现 · 子任务 03

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 03 |
| 父任务 | [01_后端基座真实实现](../01_后端基座真实实现.md) |
| 对应需求 | [01-3](../../../需求/01_需求_后端基座真实实现.md#r01-3) |
| 工时（重估） | 10h |
| 依赖 | 01_01 |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. BaseRepository 异步化与 DB 实现（CRUD / 分页 / 存在性）
2. BaseModel 落库（雪花 ID / 软删除 / 审计字段 / 乐观锁）
3. 四库类型映射与软删除复合唯一索引口径验证
4. sys_module 表与平台域种子落库

## 3. 完成标准 <a id="accept"></a>

异步 CRUD 与分页用例通过；雪花 ID / 软删除 / 审计 / 乐观锁用例通过；四库类型映射验证通过；pytest / ruff / pyright 全绿。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 后端基础类体系》「各层基类与模块约定」节；《后端基类清单》

> **前置契约（已交付）**：01_01 数据访问底座（2026-09-22）已交付请求级会话与工作单元（`get_db`（按只读标记选引擎）/ `get_read_db` / `get_write_db` / `get_uow`（绑定主库会话））、读写绑定 `WRITE_BINDING="default"` / `READ_BINDING="replica"`、`DbUnitOfWork` 事务边界、`PrimaryHealth` 降级。本任务落 `BaseDbRepository` 真实 CRUD / 软删除 SQL / 乐观锁转译时经会话依赖接入，**不需改 01_01**。详细契约见 [01_01 详细设计](../01_后端基座真实实现_01_数据访问底座落库/设计/01_详细设计_01_数据访问底座落库.md)。
