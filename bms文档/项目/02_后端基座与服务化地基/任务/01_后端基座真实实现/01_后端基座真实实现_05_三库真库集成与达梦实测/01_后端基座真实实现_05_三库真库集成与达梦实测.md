# 05 三库真库集成与达梦实测

> 后端基座与服务化地基 · 01_后端基座真实实现 · 子任务 05

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 05 |
| 父任务 | [01_后端基座真实实现](../01_后端基座真实实现.md) |
| 对应需求 | [01-5](../../../需求/01_需求_后端基座真实实现.md#r01-5) |
| 工时（重估） | 10h |
| 依赖 | 01_04 |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. 三库真库集成用例（多数据源 / 读写分离 / 分片 / 迁移 / 租户隔离）
2. 达梦方言实测（复合唯一 NULL 语义 / 迁移 DDL / schema 前缀大小写 / 类型映射）
3. CI 三库 job（verify/db 档）激活与凭据配齐

## 3. 完成标准 <a id="accept"></a>

三库真库集成用例全绿；达梦方言实测结论登记；CI verify/db 档通过；pytest / ruff / pyright 全绿。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 测试与质量》「测试金字塔」节；《部署发布规范》

> **前置契约（已交付）**：01_02 多租户数据拓扑（2026-09-22）已交付租户源真库查询（`TenantSource`）、租户库模板解析（`url_template`）、双租户 SQLite 物理隔离用例与集成骨架 env 守卫（`tests/integration/test_tenant_routing_integration.py` 的 `test_env_guarded_engine_routing` 为三库扩展点）。本任务真库集成时在其上扩展：三库租户库连通与达梦租户库（同步引擎路径）实测、跨租户隔离真库断言与 CI `verify/db` 档激活。
>
> **前置契约（已交付）**：01_03（2026-09-22）已交付四库类型映射与软删除复合唯一索引 `(唯一字段, deleted_at)` 的**离线 DDL 断言登记表**（`tests/models/test_type_mapping.py`，含布尔 / JSON 与达梦 `dmSQLAlchemy` 方言）与 SQLite NULL 语义验证；`sys_module` 平台表已落库（种子幂等）。本任务三库真库实测时**直接复用**该登记表作对照基线，达梦真库的复合唯一 NULL 语义 / 迁移 DDL / schema 前缀大小写 / 类型映射逐项实测结论回写该表与结论记录。
