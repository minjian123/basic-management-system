# 数据访问底座落库测试记录

> 后端基座与服务化地基 · 01 后端基座真实实现 · 01 数据访问底座落库 · 测试记录

[文档首页](../../../../../../文档首页.md) › [01 数据访问底座落库](../01_后端基座真实实现_01_数据访问底座落库.md) › 01 测试　|　[详细设计](../设计/01_详细设计_01_数据访问底座落库.md) · [实施记录](../实施/01_实施_01_数据访问底座落库.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 数据访问底座落库](../01_后端基座真实实现_01_数据访问底座落库.md) |
| 对应需求 | [01-1](../../../../需求/01_需求_后端基座真实实现.md#r01-1) |
| 详细设计 | [01_详细设计_01_数据访问底座落库](../设计/01_详细设计_01_数据访问底座落库.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_01_数据访问底座落库.md) |
| 测试日期 | 2026-09-22 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv）；SQLite 真连，三库集成用例待 env |
| Kiwi 用例 | 984 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 984 | 四库方言识别 | 单元 | `tests/db/test_engine_routing.py::test_four_dialect_names` | MySQL / PostgreSQL / 达梦 / SQLite 方言名 |
| 984 | 主 / 副本绑定与轮询 | 单元 | `…::test_write_and_read_binding_round_robin` | 写主引擎缓存；只读轮询副本；`replicas` 可读 |
| 984 | 无副本回落主 | 单元 | `…::test_read_falls_back_to_write_without_replicas` | 无副本只读回落主引擎 |
| 984 | 达梦同步方言 | 单元 | `…::test_dm_sync_only_dialect` | 异步 `create` 抛 `ConfigError`；`create_sync` 可建并释放 |
| 984 | 请求上下文选引擎 | 单元 | `…::test_request_context_selects_engine` | `get_db` 按标记选；`get_read_db` / `get_write_db` 强制；`get_primary_health` |
| 984 | 只读方法判定 | 单元 | `tests/db/test_readonly_middleware.py::test_is_read_method` | GET / HEAD / OPTIONS 只读，其余写，大小写不敏感 |
| 984 | 只读中间件 | 单元 | `…::test_middleware_sets_and_resets_marker` | 请求内设标记、结束复位 |
| 984 | 中间件非 HTTP 直通 | 单元 | `…::test_middleware_passthrough_non_http` | lifespan 作用域直通不设标记 |
| 984 | 主库降级 / 恢复 | 单元 | `tests/db/test_degradation.py::test_mark_probe_and_recover` | 探测成功清标记、失败置标记、`aclose` 清空 |
| 984 | 降级写被拒 | 单元 | `…::test_degraded_write_rejected` | 503 / 10006 |
| 984 | 降级写恢复 | 单元 | `…::test_degraded_write_recovers` | 探测成功恢复 200 并清标记 |
| 984 | 连接类异常转译 | 单元 | `…::test_operational_error_translated_and_degraded` | `OperationalError` → 503 / 10006 且标记降级 |
| 984 | 池参数按服务与超时 | 单元 | `tests/db/test_pool_config.py::test_pool_params_and_service_override` | 服务覆盖池参数、`connect_args`、密码合成 |
| 984 | 池默认与 SQLite | 单元 | `…::test_pool_defaults_and_sqlite_no_pool` | 默认池参数；SQLite 不带池参数 |
| 984 | 连接预算告警 | 单元 | `…::test_pool_budget_warnings` | 超限出告警；`max_connections=0` 跳过 |
| 984 | 事务提交 / 回滚 | 单元 | `tests/db/test_unit_of_work.py::test_db_unit_of_work_commit_and_rollback` | `begin` 提交生效、`rollback` 回滚 |
| 984 | 配置新键与覆盖解析 | 单元 | `tests/core/test_config.py::test_data_access_keys_and_effective_pool` | `service` / `max_connections` / `connect_timeout` / `effective_pool` |
| 984 | 四库连通 | 集成 | `tests/integration/test_db_connectivity_integration.py` | env 守卫（`BMS_TEST_DB_URL`），未配置跳过；用例名含方言标识 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run ruff check .                # All checks passed!
$ uv run ruff format --check .       # 381 files already formatted
$ uv run pyright                     # 0 errors, 0 warnings, 0 informations
$ uv run pytest -q                   # 759 passed, 11 skipped
$ uv run pytest --cov=app --cov-branch --cov-report=term   # TOTAL 95.09%
$ uv run python -m ops.check_modules # 校验通过（4 项）
```

结果汇总：本任务新增 / 修改用例全绿，预存在 6 条失败已一并修复；全量 759 passed / 11 skipped。SQLite 真连、读写选引擎、主库降级、连接池与事务用例均通过；三库连通集成用例因未配置 `BMS_TEST_DB_URL` 跳过（归 01-05）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 预存在 6 条失败 | `config.dev.toml` 字典 / 查询方案 provider 与占位用例断言不符（02-4-27 域） | 经用户确认后修复：占位用例模块加 autouse 夹具固定 `null` provider，全绿 |
| 只读绑定断言不一致 | `READ_BINDING` 语义由占位改真实 | 更新既有断言为 `replica` |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app --cov-branch --cov-report=term`：**TOTAL 8556 语句，覆盖率 95.09%**（门禁阈值 70%，达标；任务级快照）。本任务数据访问模块：`db/engine.py` 98%、`db/health.py` 100%、`db/routing.py` 100%、`db/session.py` 100%、`db/registry.py` 98%、`db/unit_of_work.py` 100%、`core/config.py` 100%（引擎 `drop` 防御分支与 `connect_args` 反分支为不可达分支）。

## 6. 偏差与遗留 <a id="deviations"></a>

- **预存在失败已修复**：`tests/dict/test_dict.py`、`tests/fieldtype/test_fieldtype.py`、`tests/listing/test_listing.py`、`tests/query/test_query.py` 共 6 条（CI `main` 流水线 #306 曾红，02-4-27 域），经用户确认后本任务以最小改动修复。
- 三库真库连通与达梦方言实测、CI `verify/db` 档归任务 05；达梦异步适配器归任务 05。
- 租户库键与引擎注册表 LRU 实测归任务 02；`BaseDbRepository` 真实 CRUD 归任务 03。

> 本文档依《文档生成规范》编写
