# 02 多租户数据拓扑落库与实测

> 后端基座与服务化地基 · 01_后端基座真实实现 · 子任务 02

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02 |
| 父任务 | [01_后端基座真实实现](../01_后端基座真实实现.md) |
| 对应需求 | [01-2](../../../需求/01_需求_后端基座真实实现.md#r01-2) |
| 工时（重估） | 10h |
| 依赖 | 01_01 |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. 租户解析全局中间件（子域名 / X-Tenant-ID / token 三级解析链 + 豁免路径）
2. 平台库表与 sys_tenant 落库
3. 引擎注册表懒加载与 LRU 回收、活跃引擎上限
4. 租户隔离实测（跨租户访问拒绝、查询强制 tenant 过滤）

## 3. 完成标准 <a id="accept"></a>

三级解析链与引擎懒加载 / 回收用例通过；跨租户访问拒绝用例通过；pytest / ruff / pyright 全绿。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 多租户路由》「租户解析链」「引擎注册表与生命周期」「隔离边界」节

> **前置契约（已交付）**：01_01 数据访问底座（2026-09-22）已交付引擎主 / 副本多绑定（`EngineFactory.create(db_key, read_only=…)`）、`EngineRegistry.get(db_key, *, read_only=False)`、请求级会话 `get_db`（按只读标记选引擎）/ `get_read_db` / `get_write_db` / `get_uow`（绑定主库会话）、只读标记 `ReadOnlyMiddleware` 与绑定 `READ_BINDING="replica"`、`PrimaryHealth` 降级与连接池按服务 / worker。本任务接租户时**复用**：租户解析结果经 `db_key` 传入注册表与工厂，**不需改 01_01 契约**；`EngineRegistry` 的租户库键解析、LRU 实测与跨实例锁归本任务。详细契约见 [01_01 详细设计](../01_后端基座真实实现_01_数据访问底座落库/设计/01_详细设计_01_数据访问底座落库.md)。
