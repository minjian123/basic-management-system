# 多租户数据拓扑落库与实测测试记录

> 后端基座与服务化地基 · 01 后端基座真实实现 · 02 多租户数据拓扑落库与实测 · 测试记录

[文档首页](../../../../../../文档首页.md) › [02 多租户数据拓扑落库与实测](../01_后端基座真实实现_02_多租户数据拓扑落库与实测.md) › 01 测试　|　[详细设计](../设计/01_详细设计_02_多租户数据拓扑落库与实测.md) · [实施记录](../实施/01_实施_02_多租户数据拓扑落库与实测.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02 多租户数据拓扑落库与实测](../01_后端基座真实实现_02_多租户数据拓扑落库与实测.md) |
| 对应需求 | [01-2](../../../../需求/01_需求_后端基座真实实现.md#r01-2) |
| 详细设计 | [01_详细设计_02_多租户数据拓扑落库与实测](../设计/01_详细设计_02_多租户数据拓扑落库与实测.md) |
| 实施记录 | [01 实施记录](../实施/01_实施_02_多租户数据拓扑落库与实测.md) |
| 测试日期 | 2026-09-22 |
| 测试人 | minjian |
| 测试环境 | 开发机（Ubuntu，Python 3.14 / uv）；SQLite 真库（临时平台库 + 双租户库），三库用例 env 守卫（归 01_05） |
| Kiwi 用例 | 1019 |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi | 用例 | 类型 | 自动化文件 | 断言要点 |
| --- | --- | --- | --- | --- |
| 1019 | 数据源键往返与非法键 | 单元 | `tests/db/test_tenant.py::test_tenant_db_key_round_trip` | `tenant_{code}` 派生 / 反解；非租户键 / 空编码 `ConfigError` |
| 1019 | 主机名与豁免路径 | 单元 | `…::test_tenant_hostname_and_exempt` | 完整主机名（去端口 / ≥三段）与豁免精确匹配、可覆盖 |
| 1019 | 解析链优先级与来源类型 | 单元 | `…::test_resolve_chain_priority_and_kinds` | 子域名（按 domain）→ 请求头（按 code）→ token 位（按 code）；豁免返回 None |
| 1019 | 回落与拒绝 | 单元 | `…::test_resolve_fallback_and_rejection` | 无来源经源取 demo / 源缺失内置兜底 / 源无 demo 回落 / 关闭回落 404 / 未知租户 404 |
| 1019 | 上下文取值 | 单元 | `…::test_current_tenant_context_prefers_full_context` | 完整上下文优先（含主键）；仅编码时派生库键；无上下文回落演示租户 |
| 1019 | 租户源按编码 / 域名命中 | 单元 | `tests/db/test_tenant_source.py::test_lookup_by_code_and_domain` | 真库查询命中；上下文携带主键 / 状态 / 域名 |
| 1019 | 未知与软删除拒绝 | 单元 | `…::test_unknown_and_soft_deleted_rejected` | 404 / 80001（未知 / 软删除 / 未知域名） |
| 1019 | 停用租户强制回收 | 单元 | `…::test_suspended_tenant_released_and_rejected` | 403 / 80002；引擎从活跃清单移除 |
| 1019 | 缓存命中与失效 | 单元 | `…::test_cache_hit_and_invalidate` | 缓存命中减少平台库查询；`invalidate` 后重查 |
| 1019 | 无缓存直查 | 单元 | `…::test_no_cache_direct_query` | null 缓存直查不阻断；空失效空操作 |
| 1019 | 异步缓存方法优先 | 单元 | `…::test_async_cache_methods_preferred` | `aget` / `aset` / `adelete` 调用；按域名单键失效 |
| 1019 | 中间件链优先级与上下文 | 单元 | `tests/db/test_tenant_middleware.py::test_chain_priority_and_context` | 无来源回落、头命中、子域名优先；请求态 + 上下文注入与结束复位 |
| 1019 | token 租户位 | 单元 | `…::test_token_tenant_scope_state` | 外层认证替身写请求态 → 解析链第三级生效 |
| 1019 | 未知与停用拒绝 | 单元 | `…::test_unknown_and_suspended_rejected` | 就地 404 / 80001、403 / 80002，不进入下游 |
| 1019 | 豁免路径 | 单元 | `…::test_exempt_path_skips_resolution` | 豁免与文档路径不解析、不设上下文 |
| 1019 | 关闭回落与无源兜底 | 单元 | `…::test_no_fallback_and_no_source` | prod 口径 404；无源 demo 兜底、未知 404 |
| 1019 | 依赖读请求态 | 单元 | `…::test_tenant_dependency_reads_state` | `get_tenant` 取中间件解析结果 |
| 1019 | 依赖就地解析 | 单元 | `…::test_tenant_dependency_without_middleware` | 无中间件装配时经源 / 内置兜底解析 |
| 1019 | 非 HTTP 直通 | 单元 | `…::test_non_http_passthrough` | lifespan 作用域不解析 |
| 1019 | 租户库模板解析 | 单元 | `tests/db/test_tenant_routing.py::test_tenant_url_template_resolution` | `{tenant}` / `{database}` 格式化；空模板回落单库；非法键 / 非法占位 `ConfigError` |
| 1019 | 会话按租户路由 | 单元 | `…::test_session_routes_by_tenant_db_key` | `get_db` / `get_read_db` / `get_write_db` / `get_uow` 取租户库引擎；豁免回落平台库；未知 404 |
| 1019 | 跨租户物理隔离 | 单元 | `…::test_cross_tenant_physical_isolation` | 双租户独立库：写 A 读 B 为空、引擎实例分离 |
| 1019 | 租户过滤钩子 | 单元 | `…::test_tenant_filter_condition_injection` | 条件次序（软删除 → 租户）；上下文主键驱动；未启用 / 无主键不注入 |
| 1019 | 平台常驻与懒加载 | 单元 | `tests/db/test_engine_registry.py::test_platform_resident_and_tenant_lazy` | 平台引擎复用；租户懒加载 |
| 1019 | LRU 与闲置清扫 | 单元 | `…::test_lru_and_idle_eviction` | 活跃上限逐出最久未用；闲置阈值访问清扫 |
| 1019 | 释放与锁键 | 单元 | `…::test_release_and_lock_reuse` | 租户可回收 / 平台不回收；锁键 `bms:global:lock:engine:{db_key}` |
| 1019 | 跨实例创建锁 | 单元 | `…::test_cross_instance_lock_used_on_creation` | 创建窗口 `hold` 调用与释放；已存在不重复取锁 |
| 1019 | 连接预算 | 单元 | `…::test_connection_budget`、`…::test_tenant_pool_budget_warnings` | 单库口径边界；活跃引擎数超限告警、`max_connections=0` 跳过 |
| 1019 | 种子幂等与 CLI | 单元 | `tests/ops/test_tenant_ops.py::test_seed_tenant_resolve_url` / `…::test_seed_tenant_idempotent` / `…::test_seed_tenant_cli` | URL 解析优先序；建表 + demo/acme 幂等；dry-run 输出 |
| 1019 | 配置键 | 单元 | `tests/core/test_config.py::test_tenant_keys_and_url_template` | `[tenant]` 默认与覆盖、`url_template` 键 |
| 1019 | 双租户路由与隔离 | 集成 | `tests/integration/test_tenant_routing_integration.py` | 平台常驻 / 租户引擎模板解析 / 物理隔离 / 回收与预算；三库用例 env 守卫（跳过，归 01_05） |

## 3. 执行记录与结果 <a id="run"></a>

```bash
$ uv run ruff check .                # All checks passed!
$ uv run ruff format --check .       # 386 files already formatted
$ uv run pyright                     # 0 errors, 0 warnings, 0 informations
$ uv run pytest -q                   # 787 passed, 8 skipped
$ uv run pytest --cov=app --cov-branch --cov-report=term   # TOTAL 95%
$ uv run python -m ops.seed_tenant   # 新增 2 行（幂等；重复执行输出 0）
$ uv run python -m ops.check_modules # 校验通过（4 项）
$ python3 scripts/tools/base-check/check-backend-base.py   # 通过
$ python3 scripts/tools/base-check/check-base.py           # 通过
$ python3 scripts/tools/base-check/check-links.py          # 断链 0 / 失效锚点 0
```

结果汇总：本任务新增 / 适配用例全绿（Kiwi 1019 标注 39 处）；全量 787 passed / 8 skipped（较 01_01 快照 759 / 11 净增 28 条通过、跳过减少 3 条——三库集成骨架由占位跳过转为真实执行）。租户解析链、租户源取数与缓存、停用回收、引擎注册表生命周期与跨实例锁、会话租户路由、租户过滤钩子与双租户物理隔离用例均通过；三库真库用例因未配置 `BMS_TEST_DB_URL` 跳过（归 01_05）。

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 原因 | 处置与落点 |
| --- | --- | --- |
| 直构应用用例平台库无 `sys_tenant` | 迁移归 01_04，测试平台库未建表 | `conftest` autouse 平台库夹具（临时库 + 种子）；`test_missing_required_key_fails` 就地清除平台库环境覆盖 |
| 租户缓存跨用例残留 | 缓存 Region 进程级单例 | `client` 夹具前后清空；新增异步缓存替身覆盖异步方法分支 |
| 插件清单 cache provider 断言 | dev 覆盖启用 memory 缓存 | 断言按 dev 生效值就地适配 |
| 降级探测替身签名 | 探测随库键传参 | 替身补 `db_key` 参数（就地适配） |

## 5. 覆盖率 <a id="coverage"></a>

`uv run pytest --cov=app --cov-branch --cov-report=term`：**TOTAL 8798 语句，覆盖率 95%**（门禁阈值 70%，达标；任务级快照）。本任务新增 / 修改模块：`db/tenant.py` 100%、`db/tenant_source.py` 100%、`db/registry.py` 99%、`db/session.py` 100%、`repositories/base_scoped_repository.py` 100%、`core/config.py` 100%、`api/middleware.py` 99%（残留分支为预存在的请求日志 / 防御分支）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 三库真库连通与达梦方言实测、CI `verify/db` 档归任务 01_05；`BaseDbRepository` 真实 CRUD 与租户条件 SQL 翻译归 01_03；Alembic 迁移与 SQLite 全量自动建表归 01_04；按服务建库归 06_01。
- 开放项：租户库只读副本与副本密码差异；`expire_at` 到期判定（租户管理阶段）；缓存失效跨实例即时联动；子域名生产泛域名与缓存 TTL 调优。

> 本文档依《文档生成规范》编写
