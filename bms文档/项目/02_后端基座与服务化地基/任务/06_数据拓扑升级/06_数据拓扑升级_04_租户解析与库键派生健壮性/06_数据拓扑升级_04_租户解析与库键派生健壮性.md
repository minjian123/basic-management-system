# 04 租户解析与库键派生健壮性

> 后端基座与服务化地基 · 06_数据拓扑升级 · 子任务 04

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 04 |
| 父任务 | [06_数据拓扑升级](../06_数据拓扑升级.md) |
| 对应需求 | [06-4](../../../需求/06_需求_数据拓扑升级.md#r06-4) |
| 工时（重估） | 6h |
| 依赖 | 06_01、06_03（租户解析链与租户源）、09_03（契约冒烟） |
| 负责人 | minjian |
| 状态 | 已完成 |
| 完成日期 | 2026-09-24 |

## 2. 任务内容 <a id="content"></a>

1. 主机名判定：IP 字面量（IPv4 / IPv6）与 `localhost` 不入子域名解析；其余按「≥ 三段且各标签形态合法」判定
2. 来源值形态校验：`X-Tenant-ID`、token 租户位与子域名映射值先过编码 / 域名形态校验，形态非法视为未命中
3. 兜底收口：租户源兜底仅在「来源值可作租户编码」时构造上下文，否则按 4xx 返回（不冒泡 5xx）
4. 门禁转阻断：dev 环境 platform 公开契约零 5xx 后，`contract-smoke-<服务>` 由非阻断改为阻断
5. 回归护栏：新增租户解析形态用例与 platform 侧「dev 环境（Host 为 IP）全 GET 端点零 5xx」回归用例

## 3. 完成标准 <a id="accept"></a>

IP 字面量 / `localhost` 主机名不入租户解析；形态非法的来源值不触发 5xx（dev / test 回落演示租户、prod 返 4xx）；dev 环境 platform 公开契约全 GET 端点零 5xx（真实流水线 `contract-smoke-platform` 以阻断 job 实证）；`contract-smoke-<服务>` 为阻断 job；pytest / ruff / pyright 全绿。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 多租户路由》「租户解析链」「三库命名与建库标准」节
- 《契约门禁与契约测试使用说明》；GitLab Issue #47（缺陷账）

> **立项背景（2026-09-24，补充需求 06-4，GitLab Issue #47）**：09_03 契约冒烟在 `BMS_ENV=dev`/SQLite 下命中 platform **7 处 5xx**（`/api/v1/dicts/{dict_type}`、`/api/v1/dicts/{dict_type}/attrs`、`/api/v1/outbox/dead-letters`、`/api/v1/outbox/dead-letters/{dead_letter_id}`、`/api/v1/query-schemes`、`/api/v1/query-schemes/default`、`/api/v1/query-schemes/{scheme_id}`），原归口记为「dev 依赖缺口 / 未迁移表」。本任务复现后**更正根因**：租户解析把 IP 字面量主机名（`Host: 127.0.0.1:8000`，恰好四段）当带子域名的租户域名，并在租户注册契约不可达时把该值原样当租户编码，派生库名 `bms_platform_127.0.0.1` 形态非法 → `ConfigError` 冒泡 500。经逐项确认后按「顺延编号 + 挂现有父任务 + 修复与转阻断同一闭环」立项。
>
> **进度（2026-09-24，已交付收口）**：来源形态判定（IP / 本机名 / 域名 / 编码）与解析链收口、租户源兜底收口、`contract-smoke-<服务>` 转阻断（CI + 档位 / 规范 / 规划 / 部署说明）均已完成；护栏用例 5 例（Kiwi 2187）与 platform 侧「dev 环境全 GET 端点零 5xx」回归用例就位（旧代码下必红）。同口径 Schemathesis 由修复前 `7 failures` 转 **`462 passed` / exit 0**；全量 `pytest` 1337 passed / 37 skipped、ruff / format / pyright / 基座 / 边界 / 状态 / 链接 / 预检全绿。文档：[详细设计](设计/01_详细设计_04_租户解析与库键派生健壮性.md) · [实施记录](实施/01_实施_04_租户解析与库键派生健壮性.md) · [测试记录](测试/01_测试_04_租户解析与库键派生健壮性.md)。**待办**：真实流水线 `contract-smoke-platform`（阻断）验收 + GitLab Issue #47 评论关闭（随推送执行）。
