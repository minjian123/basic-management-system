# 租户解析与库键派生健壮性测试记录

> 后端基座与服务化地基 · 06 数据拓扑升级 · 04 租户解析与库键派生健壮性 · 测试记录

[文档首页](../../../../../../文档首页.md) › [04 租户解析与库键派生健壮性](../06_数据拓扑升级_04_租户解析与库键派生健壮性.md) › 测试　|　[详细设计](../设计/01_详细设计_04_租户解析与库键派生健壮性.md) · [实施记录](../实施/01_实施_04_租户解析与库键派生健壮性.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [04 租户解析与库键派生健壮性](../06_数据拓扑升级_04_租户解析与库键派生健壮性.md) |
| 对应需求 | [06-4](../../../../需求/06_需求_数据拓扑升级.md#r06-4) |
| 详细设计 | [01_详细设计_04_租户解析与库键派生健壮性](../设计/01_详细设计_04_租户解析与库键派生健壮性.md) |
| 实施记录 | [01_实施_04_租户解析与库键派生健壮性](../实施/01_实施_04_租户解析与库键派生健壮性.md) |
| 测试日期 | 2026-09-24 |
| 测试人 | minjian |
| 测试环境 | 开发机（pytest / ruff / pyright / 基座与边界校验 / 预检 / CI Lint）；同口径冒烟：本机 `BMS_ENV=dev` + SQLite 真实服务 + `uvx --from schemathesis==4.28.0`（CI 用同版本固定 tag 容器） |
| Kiwi 用例 | **2187**（平台骨架 / P2 / CONFIRMED / 自动化；登记输入 `test/scripts/kiwi/cases/2026-09-24_阶段二06-04_租户解析与库键派生健壮性.json`，编号平台回读） |
| 结论 | 通过（新增 5 例；全量 1337 passed / 37 skipped；同口径冒烟由 7 failures 转 462 passed） |

## 2. 测试范围与用例 <a id="scope"></a>

用例先登记 Kiwi TCMS 取得编号 **2187**，再编码；一任务一条策展用例、多断言共用同一 `kiwi_id`。既有用例文件的新增用例沿用**文件原 Kiwi 编号**（`test_tenant.py` / `test_tenant_remote.py` 为 1019 / 2176），不改既有标注。

| 用例 / 文件 | Kiwi | 断言要点 | 结果 |
| --- | --- | --- | --- |
| `libs/bms_core/tests/db/test_tenant.py::test_tenant_hostname_and_exempt` | 1019 | 合法三段域名照常返回；`127.0.0.1:8000` / `192.168.0.107` / `[::1]:8000` / `localhost:8000` / `localhost.localdomain` / `demo..bms.example.com` / `-demo.bms.example.com` / `demo_bms_example_com` 一律 `None`；豁免路径判定不变 | 通过 |
| `…::test_source_value_shape_validation` | 1019 | `is_tenant_code`（合法 / 含点 / 数字开头 / 连字符开头 / 空 / 超长 65）、`is_tenant_domain`（多段 / 二段 / 单段 / 空段）、`is_local_hostname`（IPv4 / IPv6 / localhost / 空 / 域名） | 通过 |
| `…::test_malformed_sources_treated_as_absent` | 1019 | IP 主机名 + 非法 `X-Tenant-ID`：dev 回落演示租户且**未送入租户源取数**（仅兜底取 demo）；关闭回落（prod 口径）抛 `TenantNotFoundError` 且不取数 | 通过 |
| `libs/bms_core/tests/db/test_tenant_remote.py::test_remote_source_fallback_rejects_non_code_values` | 2176 | 契约不可达 + 兜底：`by_domain("demo.bms.example.com")` 与 `by_code("127.0.0.1")` 均抛 `TenantNotFoundError`（不构造非法库键） | 通过 |
| `services/platform/tests/api/test_dev_contract_endpoints.py::test_dev_contract_get_endpoints_have_no_server_error` | 2187 | 启用每服务每租户 URL 模板 + `Host: 127.0.0.1:8000`：契约全部 19 个 GET 操作（排除 `/healthz` `/readyz` `/metrics`）无一 5xx | 通过（**旧代码下必红，报 7 处 500**） |
| `…::test_ip_host_resolves_to_demo_tenant` | 2187 | 同条件下解析结果为演示租户（`tenant_code=demo`、`db_key=tenant_demo`） | 通过 |
| `libs/bms_core/tests/ops/test_contract_gate.py::test_ci_contract_jobs_and_switch` | 2186 | 9 个 `contract-smoke-<服务>` job 均**无** `allow_failure`（原断言 `is True` 已更新）；`contract-gate` 亦无 | 通过 |
| 既有全量回归 | — | `cd backend && uv run pytest -q` | **1337 passed / 37 skipped** |

## 3. 执行记录与结果 <a id="run"></a>

| 验证项 | 命令 / 操作 | 实测结果 |
| --- | --- | --- |
| 定向用例 | `uv run pytest libs/bms_core/tests/db/test_tenant.py libs/bms_core/tests/db/test_tenant_remote.py libs/bms_core/tests/db/test_tenant_middleware.py libs/bms_core/tests/db/test_tenant_routing.py -q` | `26 passed` |
| platform 侧用例 | `cd services/platform && uv run pytest tests/api/test_dev_contract_endpoints.py -q` | `2 passed` |
| 护栏反向验证 | `git stash push` 还原 `db/tenant.py` / `db/tenant_remote.py` 后跑 platform 用例 | **2 failed**，明细为 7 处 `500 {"code":40001,"message":"库名形态非法：bms_platform_127.0.0.1"}`（与流水线 #362 一致）→ 护栏有效 |
| 同口径冒烟（修复前） | `BMS_ENV=dev` + SQLite 起 platform 真实服务（8010），`uvx --from schemathesis==4.28.0 schemathesis run deploy/contracts/platform.json --url http://127.0.0.1:8010 --max-examples 1 --checks not_a_server_error --suppress-health-check all --include-method GET --include-method HEAD --exclude-path /healthz --exclude-path /readyz --exclude-path /metrics` | `Selected 19/46`、`Server error: 7`、`256 generated, 7 found 7 unique failures`、`exit 1` |
| 同口径冒烟（修复后） | 同命令打修复后服务（8013，全新 SQLite 库） | `Selected 19/46`、`462 generated, 462 passed`、`3 warnings`（无 5xx）、`exit 0` |
| 全量用例 | `uv run pytest -q` | `1337 passed, 37 skipped in 23.78s` |
| 静态检查 | `uv run ruff check .` / `uv run ruff format --check .` / `uv run pyright` | `All checks passed!` / `723 files already formatted` / `0 errors, 0 warnings` |
| 基座 / 边界 / 状态 / 链接 | `check-base.py` / `check-backend-base.py` / `check-service-boundaries.py` / `check-status.py --stage 02_后端基座与服务化地基` / `check-links.py` | 全通过；状态核对 350 项硬规则 0 不合规（5 项软提示为 09_03 收口遗留的甘特节点，非本任务引入） |
| 本地预检 | `scripts/tools/preflight/check-preflight.py --fast` | 通过（含 `.gitlab-ci.yml` Lint） |
| 真实流水线 | `contract-smoke-platform`（**阻断**）随本次推送运行 | 待推送后验收（推送按两级指令独立确认，见 §6） |

验收映射：

| 完成标准（需求 06-4） | 证据 |
| --- | --- |
| IP 字面量 / `localhost` 主机名不入租户解析 | `test_tenant_hostname_and_exempt` + platform 侧解析断言 + 同口径冒烟修复后零 5xx |
| 形态非法来源值不触发 5xx（dev 回落 / prod 4xx） | `test_malformed_sources_treated_as_absent` + `test_remote_source_fallback_rejects_non_code_values` |
| dev 环境 platform 全 GET 端点零 5xx（真实流水线实证） | platform 侧端点用例（旧代码下必红）+ 同口径冒烟 462 passed；真实流水线待推送后验收 |
| `contract-smoke-<服务>` 为阻断 job | `test_ci_contract_jobs_and_switch`（无 `allow_failure`）+ 档位说明 / 规范 / 规划 / 部署说明同步 |
| 校验通过 | 全量 pytest / ruff / format / pyright / 基座 / 边界 / 状态 / 链接 / 预检全绿 |

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 发现阶段 | 处置 |
| --- | --- | --- | --- |
| 1 | 首版 platform 用例在旧代码下也通过（未复现缺陷） | 用例反向验证 | 测试包 conftest 固定置空每服务每租户 URL 模板、库名不由库键派生；改用例自带 fixture 打开租户模板复现 CI 条件（见实施记录 §4 第 2 项） |
| 2 | `ruff format` 要求重排 `tenant_remote.py` 新增语句 | 静态检查 | 就地格式化后复检通过 |
| 3 | 计划软提示 S2 5 项（甘特含已完成节点） | 状态核对 | 属 09_03 收口遗留，按「不扩范围」不处理，登记实施记录 §6 第 3 项 |

## 5. 覆盖率 <a id="coverage"></a>

- 全量后端用例 **1337 passed / 37 skipped**；新增用例 5 个（tenant 形态 2 + 远程兜底 1 + platform 契约端点 2），Kiwi 2187 一条策展用例。
- 改动模块分支覆盖率：`bms_core/db/tenant.py` **100%**、`bms_core/db/tenant_remote.py` **91%**（`uv run pytest libs/bms_core/tests/db --cov=bms_core.db.tenant --cov=bms_core.db.tenant_remote --cov=bms_core.db.tenant_registry --cov-branch`）；平台服务包整体 70%（CI 服务级门槛，本任务未改动服务实现）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 同[实施记录](../实施/01_实施_04_租户解析与库键派生健壮性.md) §6（共享基座冒烟覆盖口径、其余 8 服务 CI 实证、甘特软提示、`*.db` 仓库卫生、契约基线、认证链路共 6 项归口）。
- 真实流水线验收（阻断 job 通过 + Issue #47 评论关闭）需推送后执行：推送按两级指令独立确认（《AI开发规范》「提交与推送」）。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
