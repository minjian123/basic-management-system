# 03 平台层表归属与按服务拆分

> 后端基座与服务化地基 · 06_数据拓扑升级 · 子任务 03

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 03 |
| 父任务 | [06_数据拓扑升级](../06_数据拓扑升级.md) |
| 对应需求 | [06-3](../../../需求/06_需求_数据拓扑升级.md#r06-3) |
| 工时（重估） | 20h（2026-09-23 范围调整：全部余量并入 06_02，工时按「已交付 + 移交」重划 40h → 20h；交付量见文末注块） |
| 依赖 | 01_02、05_01、05_02 |
| 负责人 | minjian |
| 状态 | 进行中 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. 平台层表（`sys_*`）逐表服务归属定案并登记（服务目录 / 数据库设计总览 / 表文件三处一致），静态硬校验由「共享前缀放行」收窄为「表级归属」
2. `sys_tenant` 与 `sys_module` / `sys_module_i18n` 的模型与落库实现迁出共享基座库 `bms_core`，按归属落到所属服务
3. 跨服务读出口：租户上下文以网关注入身份头为主 + 服务侧缓存 + 版本键失效，未命中经服务契约取数（超时与降级口径与同步调用一致）
4. 迁移链由「数据源」扩为「服务 × 数据源」，各服务库可分别取链、独立迁移

## 3. 完成标准 <a id="accept"></a>

平台层每张表有唯一服务归属且校验生效（未登记 / 跨服务引用被拒）；`bms_core` 内无平台层表直读；租户解析与停用 / 改名失效即时生效（不直读平台库）；迁移链按「服务 × 数据源」可取、可分链执行；pytest / ruff / pyright 全绿。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 数据架构》「数据分布」「数据规范」节；《架构设计 · 多租户路由》「三库命名与建库标准」节
- 《微服务演进规划》「服务化地基要求与门禁」（S1 / S2）节

> **立项背景（2026-09-23 专项决策）**：平台库共享问题自 02_01 起三次顺延（02_01 开放项 → 02_03 归 03_01 → 03_01 归 06_需求 → 05_02 归独立任务），导致「每服务独立库 × 每租户」在平台库维度无法落地。经专项讨论定案路线「**表归属 → 逐表迁出 → 拆库**」：本任务承担前两步与迁移链按服务分链（**不含**平台服务库的最终拆分，那由 01 承接）。
>
> **前置契约（已交付）**：01_02 多租户数据拓扑（租户库键 / `url_template` 解析、引擎注册表懒加载与回收）、05_01 服务契约与同步调用（`service_client` 能力域与 `ServiceDto`，跨服务读出口的实现基座）、05_02 数据所有权与边界硬校验（静态规则 6/7、运行时守卫与例外白名单、`DataOwnershipError` 10008）。本任务在其上做「归属收窄 + 实现迁出」，**不改**三者的对外契约（若确需变更，按契约变更登记并标注消费方）。
>
> **进度（2026-09-23，第一轮已交付，任务未收口）**：表归属登记与派生、静态 / 运行时校验收窄、`SysTenant` 迁出与租户注册只读契约、租户源装配点（`local` / `remote`）已落地；余量（建表与种子、快照出口接线、迁移链按服务分链、`SysModule` 迁出）见[实施记录 §6](实施/03_实施_01_平台层表归属与按服务拆分.md#deviations)。文档：[详细设计](设计/01_详细设计_03_平台层表归属与按服务拆分.md) · [实施记录](实施/03_实施_01_平台层表归属与按服务拆分.md) · [测试记录](测试/01_测试_03_平台层表归属与按服务拆分.md)（Kiwi 用例 2176；全量 1122 通过 / 36 跳过）。
>
> **范围移交（2026-09-23，06_01 承接）**：本任务遗留 2（**服务目录快照只读契约 + 启动接库校验改经契约 + `CatalogHealthCheck` 非必需项降级**）已由 [06_01 每服务每租户建库与路由](../06_数据拓扑升级_01_每服务每租户建库与路由/06_数据拓扑升级_01_每服务每租户建库与路由.md) 交付（其工时由 12h 上调至 20h），本任务收口时不再重复；**迁移编号变更**：本设计规划的 `sys_table_ownership` 迁移由平台链 `0005` 顺延为 `0006` —— **分链落地后编号按链内独立重排，实际落为 `platform:platform/0005_sys_table_ownership`**（`tenant:platform/0001_sys_tenant` 合并了原 `0005_sys_tenant_db_key_nullable` 语义）。
>
> **余量全部移交 06_02（2026-09-23 决策确认）**：本任务全部余量——遗留 1（`sys_table_ownership` 建表 / 种子 / 对账）、遗留 3（字典跨服务读出口 `HttpDictSource` / `HttpDictTranslator`）、遗留 4（迁移链按服务分链 + 移除过渡豁免）、遗留 5（`SysModule` / `SysModuleI18n` 迁出）——**并入 [06_02 多服务多库迁移与连接预算](../06_数据拓扑升级_02_多服务多库迁移与连接预算/06_数据拓扑升级_02_多服务多库迁移与连接预算.md) 实施**（本任务工时按「已交付 + 移交」重划 40h → 20h，域内合计不变）；06_02 交付后本任务一并收口。**已由 06_02 第一轮交付**：遗留 1、遗留 4、遗留 5（分链机制见其详细设计 §4.1~§4.5）。**仍未交付**（随 06_02 收口轮）：遗留 3 字典读出口、三库真库演练、Kiwi 用例与文档回写。
