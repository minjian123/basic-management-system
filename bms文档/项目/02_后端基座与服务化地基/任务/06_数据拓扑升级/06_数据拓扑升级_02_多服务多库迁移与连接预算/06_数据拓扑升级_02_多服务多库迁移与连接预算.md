# 02 多服务多库迁移与连接预算

> 后端基座与服务化地基 · 06_数据拓扑升级 · 子任务 02

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02 |
| 父任务 | [06_数据拓扑升级](../06_数据拓扑升级.md) |
| 对应需求 | [06-2](../../../需求/06_需求_数据拓扑升级.md#r06-2) |
| 工时（重估） | 28h（重估上调：含承接 06_03 全部余量 20h；交付量见文末注块） |
| 依赖 | 06_01、01_04 |
| 负责人 | minjian |
| 状态 | 进行中 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. Alembic 按服务 × 数据源维护迁移（链名 `{service}:{datasource}`、版本目录两级、表集由归属登记派生、段名 = 链名）
2. 批量迁移遍历「服务 × 数据源 × 租户」幂等执行；开通（新服务 / 新租户）自动执行对应迁移
3. 连接预算按服务核算、活跃租户懒加载与回收
4. 库数量增长可观测
5. **承接 06_03 全部余量**（2026-09-23 决策）：表归属登记落库与对账（`sys_table_ownership` 表文件 + 总览登记 + 迁移 + 幂等种子 + 双向对账）、分链与过渡豁免移除、`SysModule` / `SysModuleI18n` 迁出共享基座库、字典跨服务读出口（`HttpDictSource` / `HttpDictTranslator`）

## 3. 完成标准 <a id="accept"></a>

迁移链按「服务 × 数据源」可取、可分链执行且表集由归属登记派生；多服务多库批量迁移幂等且可演练（三库真库建库 → 迁移 → 校验 → 清理）；开通即迁移可一键完成；连接预算按服务生效；库数量可观测；平台层表归属登记落库并与清单双向对账一致、`bms_core` 内无平台层表直读；pytest / ruff / pyright 全绿。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 数据架构》「迁移策略」节；《架构设计 · 性能与安全》「部署规模与连接规划」节

> **前置契约（已交付，2026-09-23）**：[06_01 每服务每租户建库与路由](../06_数据拓扑升级_01_每服务每租户建库与路由/06_数据拓扑升级_01_每服务每租户建库与路由.md) 已交付本任务可直接消费的三项能力——① **库键与库名单一来源**（`bms_core/db/keys.py`：相对键 / 全限定键双形态、`bms_{service}` / `bms_{service}_{code}` 库名派生、越界拒绝 10008 与运维豁免），批量迁移与连接预算据此遍历「服务 × 租户」与取库；② **按服务建库入口** `ops/provision_tenant.py`（平台服务库 + 服务租户库，幂等，不含迁移），本任务承接其后的迁移编排与演练；③ **单链迁移可按库键定向执行**（`-x db_key=<库键>`，`chain_url` 键参数对全链生效）。

> **范围承接（2026-09-23 决策确认）**：06_03 全部余量并入本任务（并入清单取任务文档与实施记录的并集：`sys_table_ownership` 建表 / 种子 / 对账、字典跨服务读出口、分链与过渡豁免移除、`SysModule` / `SysModuleI18n` 迁出），**本任务交付后 06_03 一并收口**（其设计 / 实施 / 测试记录作为本任务前序轮次引用）；本任务工时 8h → **28h**（含承接余量 20h），详细设计见[设计/01_详细设计_02_多服务多库迁移与连接预算.md](设计/01_详细设计_02_多服务多库迁移与连接预算.md)。

> **进度（2026-09-23，第一轮已交付，任务未收口）**：① **分链机制**已落地——链名 `{service}:{datasource}`、段名 = 链名（缺省段 `[alembic]` = `platform:tenant`）、版本目录 `versions/{service}/{datasource}/`（存量脚本按归属拆分归位，`0001_sys_tenant_module` 拆为 `platform:platform/0001_sys_module` 与 `tenant:platform/0001_sys_tenant`，`0005_sys_tenant_db_key_nullable` 语义并入首建）、表集由归属登记派生 ∩ 已有模型、模型按服务 `MODEL_MODULES` 动态导入、`db/bootstrap.py` 只建「本服务 + 有脚本链」；② **模型迁出**——`SysModule` / `SysModuleI18n` / `ModuleRepository` 迁至 `bms_platform`，`bms_core` 的 `catalog` 取数改「权威读取器注册点」模式；③ **归属登记落库**——`sys_table_ownership` 表文件 + 总览登记 + 平台服务链迁移 `0005_sys_table_ownership` + `ops/seed_tables.py` / `ops/check_tables.py`（离线断言 + 接库对账）+ `platform` 服务启动期接库对账；④ **批量迁移编排**矩阵化（服务 × 数据源 × 租户 + 库数量统计）、`provision_tenant --migrate`；⑤ **连接预算按服务核算**（`pool_budget_rows` + `workers_by_service` / `max_connections_by_service` + `ops/check_budget.py`）；⑥ **库数量可观测**（`bms_db_count` + `db/inventory.py` + 引擎记账）。门禁：pytest 1186 passed / pyright 0 / ruff 干净 / 基座与边界校验通过。**余量**：字典跨服务读出口（`HttpDictSource` / `HttpDictTranslator`）、三库真库建库迁移演练、Kiwi 用例登记与新增用例补齐、架构 / 基类清单 / 记录等文档回写（见计划「偏差与遗留」列）。
