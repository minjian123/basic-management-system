# 02 服务运行与健康检查

> 后端基座与服务化地基 · 02_多服务骨架与仓库 · 子任务 02

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02 |
| 父任务 | [02_多服务骨架与仓库](../02_多服务骨架与仓库.md) |
| 对应需求 | [02-2](../../../需求/02_需求_多服务骨架与仓库.md#r02-2) |
| 工时（重估） | 6h |
| 依赖 | 02_01 |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. 每服务独立启动（配置分层加载 / 结构化日志 / 服务名与版本标识）
2. 每服务 /healthz /readyz 与依赖就绪聚合
3. 优雅停机（SIGTERM 摘流）

## 3. 完成标准 <a id="accept"></a>

每服务可独立启动并暴露探针；就绪失败返回 503；优雅停机用例通过；pytest / ruff / pyright 全绿。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 可观测性》「健康检查与优雅停机」节

> **前置契约（已交付 · 02_01，2026-09-22）**：工作区骨架已就位——服务工程 `services/<服务名>/`（包 `bms_<服务名>`，src 布局）+ 共享基座库 `libs/bms_core`（包 `bms_core`）；接口层基座（路由基类 / 异常处理器 / 公共中间件 / 健康探针 `/healthz` `/readyz` / 依赖提供者）在 `bms_core.api`，服务入口与应用工厂在 `services/platform/src/bms_platform/main.py`；新增服务经 `python scripts/new_service.py <服务名>` 生成同构骨架。本任务在每个服务入口上补配置分层 / 结构化日志 / 服务名与版本标识 / 探针聚合与优雅停机。
