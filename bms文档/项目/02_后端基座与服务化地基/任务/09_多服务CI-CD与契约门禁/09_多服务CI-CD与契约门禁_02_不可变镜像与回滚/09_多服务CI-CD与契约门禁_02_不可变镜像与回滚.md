# 02 不可变镜像与回滚

> 后端基座与服务化地基 · 09_多服务CI-CD与契约门禁 · 子任务 02

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02 |
| 父任务 | [09_多服务CI-CD与契约门禁](../09_多服务CI-CD与契约门禁.md) |
| 对应需求 | [09-2](../../../需求/09_需求_多服务CI-CD与契约门禁.md#r09-2) |
| 工时（重估） | 6h |
| 依赖 | 09_01 |
| 负责人 | minjian |
| 状态 | 已完成 |
| 完成日期 | 2026-09-24 |

## 2. 任务内容 <a id="content"></a>

1. 镜像按 commit SHA / 语义化版本打标签，禁 latest 作生产引用
2. 一键回滚（切换上一 tag，迁移向前修复优先）
3. 健康门禁（/readyz 与冒烟回归判定）
4. 发布留痕（检查单关键项 CI 采集）

## 3. 完成标准 <a id="accept"></a>

镜像标签不可变可追溯；回滚可一键完成；健康门禁生效；发布留痕可查；校验通过。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 部署与运维》「版本与发布管理」节
- [详细设计](设计/01_详细设计_02_不可变镜像与回滚.md) · [实施记录](实施/01_实施_02_不可变镜像与回滚.md) · [测试记录](测试/01_测试_02_不可变镜像与回滚.md) · [服务编排与发布部署使用说明](../../../../../资料/开发服务器/linux/服务编排与发布部署使用说明.md)
- Kiwi 用例 **2185**

> **前置契约（已交付 · 08_02，2026-09-24）**：健康门禁的观测侧已就位——blackbox 探针（`probe_success`，探测每服务 `/healthz` `/readyz`，仅 200 成功）与 `BmsProbeFailing` 告警规则；发布后经 `/readyz` 判定的健康门禁可直接复用（探针目标随服务容器化切 `{service_key}:8000` Compose DNS）。**本任务接线点**：发布结果按 `bms_release_total{service,result}` 口径推送 Pushgateway（通道与口径已交付，见《可观测性栈部署使用说明》「告警与阶段度量」节）。

> **前置契约（已交付 · 09_01，2026-09-24）**：服务可独立构建与发布链路已就位——服务子流水线 `service-build` / `service-release` 产出并推送 `bms-{service}:$CI_COMMIT_SHORT_SHA`（构建 `--provenance=false`；镜像含 `alembic.ini` / `config*.toml`，入口 `python -m bms_{service}`；`deploy/ci/verify/image` 档守卫）；发布度量已真实推送（`bms_release_total{service="platform",result="success"}` 流水线实证）；`resource_group: bms-release-$SERVICE` 串行与 release `interruptible: false` 已就位。**本任务接线点**：Compose 服务编排（`{service_key}:8000`）与 Prometheus / blackbox 目标切换、镜像不可变标签（语义化版本 / 禁 latest / 留存）、一键回滚、健康门禁（`/readyz` + 冒烟）、发布留痕在其上落地；镜像运行时依赖外部库（容器冒烟以迁移库挂载验证 `/healthz` 200、`/readyz` 503 缺 Redis 符合预期），迁移与编排随本任务定案；`deploy/boundaries` 例外白名单等仓库根相对资产在容器内的落点同随编排定案。
