# 03 契约门禁

> 后端基座与服务化地基 · 09_多服务CI-CD与契约门禁 · 子任务 03

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 03 |
| 父任务 | [09_多服务CI-CD与契约门禁](../09_多服务CI-CD与契约门禁.md) |
| 对应需求 | [09-3](../../../需求/09_需求_多服务CI-CD与契约门禁.md#r09-3) |
| 工时（重估） | 8h |
| 依赖 | 05_01 |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. 每服务导出 OpenAPI 契约快照并维护独立基线
2. oasdiff breaking 破坏性变更拦截（只加不删）
3. Schemathesis 依据 OpenAPI 生成用例打真实服务
4. 门禁挂 MR / main，基线更新走评审

## 3. 完成标准 <a id="accept"></a>

破坏性契约变更被 CI 拦截并给出清单；Schemathesis 冒烟可运行；基线更新受评审约束；校验通过。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 测试与质量》「CI 流水线」节

> **前置契约（已交付 · 05_01，2026-09-23）**：每服务公开契约（OpenAPI）快照已就位——`deploy/contracts/<service>.json`（9 个启用服务，`info.version` = 服务 `CONTRACT_VERSION`），生成 / 零漂移校验 CLI `backend/ops/contract_snapshot.py`（`export` / `check`），CI `swagger-snapshot` job 已导出并归档 `deploy/contracts/*.json`。本任务在此基础上落 **oasdiff 破坏性变更门禁与 Schemathesis 契约测试**（消费本快照）。

> **前置契约（已交付 · 08_02，2026-09-24）**：契约破坏数的采集通道已就位——`bms_contract_breaking_total{service}` 口径（登记于 `METRIC_NAMES`）与 Pushgateway 通道（Prometheus 抓取 + Grafana「阶段度量」面板）。**本任务接线点**：oasdiff 门禁产出的破坏性变更数按该口径推送（命令模板见《可观测性栈部署使用说明》「告警与阶段度量」节）；快照接口 `GET /api/v1/modules/snapshot` 入契约快照与 oasdiff 基线（06_01 登记落点，08_02 已闭环指标化侧）。
