# 父-子流水线测试记录

> 后端基座与服务化地基 · 09 多服务CI-CD与契约门禁 · 01 父-子流水线 · 测试记录

[文档首页](../../../../../../文档首页.md) › [01 父-子流水线](../09_多服务CI-CD与契约门禁_01_父-子流水线.md) › 01 测试　|　[详细设计](../设计/01_详细设计_01_父-子流水线.md) · [实施记录](../实施/01_实施_01_父-子流水线.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 父-子流水线](../09_多服务CI-CD与契约门禁_01_父-子流水线.md) |
| 对应需求 | [09-1](../../../../需求/09_需求_多服务CI-CD与契约门禁.md#r09-1) |
| 详细设计 | [01_详细设计_01_父-子流水线](../设计/01_详细设计_01_父-子流水线.md) |
| 测试日期 | 2026-09-24 |
| 测试人 | minjian |
| 测试环境 | 开发机（pytest / ruff / pyright / 基座校验 / CI Lint）；mjbk（真实镜像构建、容器冒烟、Trivy 扫描、真实流水线 349 / 351 / 352） |
| Kiwi 用例 | **2184**（平台骨架 / P2 / CONFIRMED / 自动化；登记输入 `test/scripts/kiwi/cases/2026-09-24_阶段二09-01_父子流水线.json`，编号平台回读） |
| 自动化文件 | `backend/libs/bms_core/tests/ops/test_ci_pipeline.py`、`backend/libs/bms_core/tests/core/test_service_catalog_startup.py`（均 `@pytest.mark.kiwi_id(2184)`） |

## 2. 自动化用例 <a id="cases"></a>

用例先登记 Kiwi TCMS 取得编号 **2184**，再编码；一任务一条策展用例、多条断言共用同一 `kiwi_id`。

| 用例 / 文件 | Kiwi | 断言要点 | 结果 |
| --- | --- | --- | --- |
| `libs/bms_core/tests/ops/test_ci_pipeline.py::test_trigger_jobs_cover_enabled_services` | 2184 | trigger job 的 `SERVICE` 集合 == 服务目录 `enabled_service_keys()`；每 job include 公共模板、`strategy: depend`、规则含 `backend/services/{svc}/**/*` 与 MR / `compare_to` 兜底 | 通过 |
| `…::test_parent_backend_test_rules_are_shared_paths_only` | 2184 | `backend-test` 规则含共享 / 工作区路径且不含 `backend/services`；`backend-image` / `container-scanning` 已移除；`stages` 含 `trigger`；前端镜像档守卫 + 脚本内前置判定 | 通过 |
| `…::test_service_template_structure` | 2184 | 子模板 `workflow` 只放行 `parent_pipeline`；三阶段；工程级测试（`services/$SERVICE`）+ `--cov=bms_$SERVICE --cov-fail-under=70` | 通过 |
| `…::test_service_build_and_release_jobs` | 2184 | build：`--build-arg SERVICE` + `bms-$SERVICE:$CI_COMMIT_SHORT_SHA` + `--provenance=false` + 镜像档守卫；release：push + `aquasec/trivy:0.74.0`（无 `:latest`）+ `TRIVY_DB_REPOSITORY` 国内镜像 + `trivy-cache` + `resource_group: bms-release-$SERVICE` + `interruptible: false` + `CI_JOB_STATUS` / `bms_release_total` / `PUSHGATEWAY_URL` | 通过 |
| `…::test_template_constants_and_image_assets` | 2184 | 模板 `REGISTRY_IMAGE_PREFIX` 与父流水线同值；镜像档开关存在；Dockerfile 含 `ARG SERVICE` / 目标服务 COPY / `python -m bms_${SERVICE}` / `uv sync --frozen` | 通过 |
| `libs/bms_core/tests/core/test_service_catalog_startup.py`（改） | 2184 | 工程级 / 子集运行自足（显式登记 platform 目录读取器）：原 9 条 `CatalogError` 失败归零；文件子集 11 passed、工程级 749 passed | 通过 |
| 既有全量回归 | — | `uv run pytest -q`（工作区根） | **1300 passed / 37 skipped**，覆盖率 **95.16%** |

## 3. 真实构建与扫描冒烟（mjbk） <a id="smoke"></a>

| 验证项 | 操作 | 实测结果 |
| --- | --- | --- |
| 镜像构建 | `docker build --provenance=false --build-arg SERVICE=platform -f Dockerfile .`（上下文 `backend/`） | 成功；两层 `uv sync` 缓存生效；非目标服务源码不入镜像 |
| 容器启动 | `docker run -p 18001:8000 -v <迁移库>:/opt/bms/bms_platform.db` | `/healthz` → 200 `{"status":"ok","service":"platform","version":"0.1.0"}`；容器 `healthy`（HEALTHCHECK 生效） |
| 就绪探针 | `curl /readyz` | 503（`redis` 不可达、`database` / `catalog` ok）——容器环境无 Redis，符合预期（依赖外部化归 09_02） |
| 镜像扫描 | `trivy image --severity CRITICAL,HIGH --ignore-unfixed`（0.74.0，国内 DB 镜像） | **0 条**（Debian 13.7；pip 已移除，无 vendored msgpack/setuptools 洞） |
| Registry 推送 | `docker push`（login 后） | 成功（`--provenance=false` 后无 `blob unknown`） |
| 阶段度量推送 | `docker:27-cli`（busybox wget）推 Pushgateway | `PUSH_OK`；Prometheus 查询可见；组删除 `DELETE /metrics/job/bms-ci` 返回 202 清空样例 |

## 4. 真实流水线验收 <a id="pipeline"></a>

| 流水线 | 提交 | 观察结果 |
| --- | --- | --- |
| 349 | `07947c1b`（feat + docs + platform 服务包注释） | **`trigger-platform` → 子流水线 350 全绿**：`service-test` / `service-build` / `service-release` 均 success；仅 platform 一条 bridge（其余 8 服务未触发）→「改动某服务仅触发其子流水线」达成；Registry 落 `bms-platform:07947c1b`；Pushgateway 落 `bms_release_total{service="platform",result="success"} 1`。父流水线因 `frontend-image`（镜像档开启但前端 Dockerfile 未就绪）失败 → 生成自动缺陷 Issue #41（已归因关闭） |
| 351 | `1ffca061`（前端镜像补 `exists` 双路径） | `frontend-image` 仍运行（实测 `rules:exists` 多模式为「任一命中」，不能做 AND）→ 失败；Issue #42（已归因关闭） |
| 352 | `01e8fe0a`（改脚本内 `test -f` 前置判定） | **全绿**：`frontend-image` 输出「Dockerfile 未就绪：跳过前端镜像构建」后 success；`backend-test` success（共享路径变更跑全量）；**bridges = 0（共享 / 工作区 / CI 配置变更不触发任何服务子流水线）**；`defect-capture` skipped |

验收映射：

| 完成标准（需求 09-1） | 证据 |
| --- | --- |
| 改动某服务仅触发其子流水线 | 流水线 349：仅 `trigger-platform` 一条 bridge，子流水线 350 三 job 全绿；流水线 352：bridges=0（非服务路径变更不触发服务子流水线）；护栏用例断言规则集合与路径 |
| 服务可独立构建与发布 | 子流水线 `service-build` / `service-release` 成功；Registry `bms-platform:07947c1b`；发布度量 success；mjbk 真实构建 + 容器 `/healthz` 200 + Trivy 0 高危 |
| 公共模板复用且无重复 YAML | 单一模板 `deploy/ci/templates/backend-service.yml` 被 9 个 trigger 引用（护栏用例断言）；常量与父流水线同源断言 |
| 校验通过 | 全量 pytest / 工程级 10 root / ruff / format / pyright / 基座 / 边界 / 状态 / preflight / CI Lint 全绿 |

## 5. 问题与处置 <a id="issues"></a>

| # | 问题 | 发现阶段 | 处置 |
| --- | --- | --- | --- |
| 1 | core 测试子集运行 9 条失败（platform 目录读取器未登记） | 设计前置核查 | 用例自足登记（幂等），子集 / 工程级全绿（见实施记录 §4） |
| 2 | `docker push` 报 `blob unknown` | mjbk 冒烟 | 构建加 `--provenance=false`（BuildKit attestation 清单不被 Registry 接受），实测修复 |
| 3 | 镜像启动缺 `alembic.ini` / `config*.toml` | mjbk 冒烟 | Dockerfile 补 COPY；容器 `/healthz` 200 |
| 4 | 首扫 45 条 CRITICAL/HIGH（base 镜像自带） | mjbk 冒烟 | apt 安全更新 + 运行时移除 pip；复扫 0 条 |
| 5 | Trivy DB 默认源不可达 / 国内镜像选型 | mjbk 冒烟 | `ghcr.nju.edu.cn/aquasecurity/trivy-db:2` + 缓存卷 |
| 6 | `frontend-image` 空跑失败（两次） | 真实流水线 349 / 351 | 脚本内前置判定跳过；352 全绿；两条自动缺陷单归因关闭 |
| 7 | 自动缺陷单（流水线失败的副作用） | 349 / 351 | Issue #41 / #42 评论归因并关闭（配置缺口、非产品缺陷） |

## 6. 覆盖率与统计 <a id="coverage"></a>

- 全量后端覆盖率 **95.16%**（门禁 ≥70%）；服务级覆盖率实测 77%~94%（notification 77 / search 81 / tenant 84 / org 85 / file 89 / report 90 / ai 91 / identity 91 / platform 94），服务子流水线 `--cov-fail-under=70` 有余量。
- 本任务新增 / 修改用例文件 2 个（护栏 5 条断言 + 隔离修复），Kiwi 2184 一条策展用例。

## 7. 偏差与遗留 <a id="deviations"></a>

- 同[实施记录](../实施/01_实施_01_父-子流水线.md) §7（09_02 / 09_03 / 运维阶段归口 9 项）。
- 流水线 349 / 351 的失败均为配置缺口（前端镜像前置），已在 `fix(09_01)` 修复并以 352 复验；对应自动缺陷单已关闭并留痕。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
