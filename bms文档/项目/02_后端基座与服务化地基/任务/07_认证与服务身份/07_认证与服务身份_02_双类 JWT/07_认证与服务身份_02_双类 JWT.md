# 02 双类 JWT

> 后端基座与服务化地基 · 07_认证与服务身份 · 子任务 02

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02 |
| 父任务 | [07_认证与服务身份](../07_认证与服务身份.md) |
| 对应需求 | [07-2](../../../需求/07_需求_认证与服务身份.md#r07-2) |
| 工时（重估） | 8h |
| 依赖 | 07_01 |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. 用户 JWT（aud=api）与服务 JWT（aud=service，短时 + scope + 服务标识）
2. RS256 / ES256 签名与 JWKS 分发、校验 exp / iss / aud
3. aud 隔离（用户 token 调内部服务被拒）、外部 token 不透传

## 3. 完成标准 <a id="accept"></a>

双类 JWT 可签发与校验；aud 区分生效；密钥轮换可并行验证；pytest / ruff / pyright 全绿。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 认证与会话》「服务身份与服务间认证」节

> **前置契约（已交付 · 07_01，2026-09-23）**：自托管 OIDC IdP（Keycloak 26.7.4）与 BMS 的 OIDC 客户端已就位——IdP issuer 形如 `http://<idp-host>:8090/realms/bms`（Discovery `<issuer>/.well-known/openid-configuration`、JWKS `<issuer>/protocol/openid-connect/certs`，realm `bms` + 客户端 `bms-backend` 含服务账号）；`bms_core/idp/` 交付真实 `OidcIdentityProvider`（Discovery / 授权 / 换码 / userinfo + `verify_token` 经 JWKS 验签：仅 RS256/ES256、校验 `exp`/`iss`/`aud`）、`IdentityClaims` 数据契约（`subject` / `issuer` / `audience` / `expires_at` / `payload`）与 JWKS 内核 `idp/jwks.py`（`JwksCache` + `verify_jwt`）、`[identity_provider]` 配置节。**本任务接线点**：双类 JWT（`aud=api` / `aud=service`）签发与 JWKS **分发**可复用 `idp/jwks.py` 的验签助手与 JWKS 缓存；realm 客户端 `bms-backend` 已启服务账号，补 audience protocol mapper / scope 即得 `aud` 口径。
