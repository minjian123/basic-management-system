# 02 按服务归因与健康就绪

> 后端基座与服务化地基 · 08_可观测性栈 · 子任务 02

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 02 |
| 父任务 | [08_可观测性栈](../08_可观测性栈.md) |
| 对应需求 | [08-2](../../../需求/08_需求_可观测性栈.md#r08-2) |
| 工时（重估） | 6h |
| 依赖 | 08_01、02_02 |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. span / 指标 / 日志带 service.name 与 version 资源标签，跨服务调用链可定位
2. 核心链路 SLO 告警阈值锚点
3. 每服务探针与阶段度量（发布成功率 / 契约破坏数 / 越界数）

## 3. 完成标准 <a id="accept"></a>

跨服务调用链可按服务定位；每服务探针可用；关键度量可采集；校验通过。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 可观测性》「链路追踪」「健康检查与优雅停机」节

> **前置契约（已交付 · 02_02，2026-09-22）**：服务运行时基座已就位——服务身份（名称 / 版本，包声明 + `[app].service` 覆盖）经 `core/service.py` 解析并注入结构化日志上下文（每条日志携带 `service` / `service_version`），并以 `service` / `version` 字段进入 `/healthz` `/readyz` 探针响应；`app.state.service_identity`（`ServiceIdentity`）与 `ServiceRuntime` 可作按服务归因的身份源，优雅停机摘流（`app.state.draining`）已就绪。本任务据此为 OTel 资源标签（`service.name` / `service.version`）、指标 / 日志归因与 SLO 阈值锚点复用同一身份源；`minio` 检查项另有归口（文件管理阶段）。

> **前置契约（已交付 · 08_01，2026-09-23）**：可观测栈接入已就位——**指标**（`PrometheusMetrics` 默认启用，每服务 `/metrics` 采集端点，`bms_request_total` / `bms_request_duration_seconds` / `bms_dependency_up` 带 `service` 标签）；**链路**（`OtelTracer` 默认启用 + `tracing/setup.py` 全局 provider：资源标签 `service.name` / `service.version`、采样 `parentbased_traceidratio`、OTLP/HTTP → otel-collector → Tempo，W3C 传播；OTel 为 trace id 事实源，日志与 Tempo 一致）；**编排**（otel-collector / Tempo / Prometheus / Grafana / Loki / Alloy 一键拉起，保留期 15d / 30d / 72h，Grafana 三数据源 + 总览面板 provisioning）；**日志**（Alloy → Loki，`service` / `level` 标签 + `trace_id` / `request_id` 结构化元数据）；**边缘**（APISIX 指标 / 访问日志入栈）。**本任务接线点**：SLO 阈值锚点与告警（Alertmanager）、阶段度量（发布成功率 / 契约破坏数 / 越界数）、探针细化与按服务归因的最终验收——直接复用上述真实实现与数据源，不重复接入。**标注重叠**：资源标签 `service.name` / `service.version` 已随 08_01 真实实现落地（OTLP 必需的归因基础），本任务负责复核验收与在其上做 SLO / 度量。
