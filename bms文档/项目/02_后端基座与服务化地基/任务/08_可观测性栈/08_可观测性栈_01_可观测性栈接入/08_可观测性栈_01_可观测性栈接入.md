# 01 可观测性栈接入

> 后端基座与服务化地基 · 08_可观测性栈 · 子任务 01

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 01 |
| 父任务 | [08_可观测性栈](../08_可观测性栈.md) |
| 对应需求 | [08-1](../../../需求/08_需求_可观测性栈.md#r08-1) |
| 工时（重估） | 10h |
| 依赖 | 02_01 |
| 负责人 | minjian |
| 状态 | 已完成 |
| 完成日期 | 2026-09-23 |

## 2. 任务内容 <a id="content"></a>

1. OTel SDK + otel-collector 自动埋点（FastAPI / SQLAlchemy / Redis / Celery）
2. Tempo 接入（OTLP + 保留期 / TTL）
3. Prometheus 采集每服务 /metrics、Grafana 统一展示
4. Loki 结构化日志采集（trace_id / request_id / service）

## 3. 完成标准 <a id="accept"></a>

Trace 可查、指标可查、日志可按服务检索；保留期配置就位；本地一键拉起含可观测栈；校验通过。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 可观测性》「指标采集」「链路追踪」节

> **前置契约（已交付 · 02_01，2026-09-22）**：工作区与服务工程骨架已就位（`libs/bms_core` + `services/platform`，包 `bms_core` / `bms_platform`），每服务独立工程可独立构建；可观测能力域契约（`bms_core.metrics` / `bms_core.tracing`）随共享库复用。本任务据接入 OTel + Tempo + Prometheus + Loki 并按服务归因。

> **前置契约（已交付 · 04_03，2026-09-23）**：边缘观测出口已就位——APISIX 指标端点 `http://<mjbk-IP>:<GATEWAY_METRICS_PORT:-9091>/apisix/prometheus/metrics`（`deploy/gateway/config.yaml` 的 `plugin_attr.prometheus.export_addr`，路由已挂 `prometheus: {}` 插件）与访问日志出口（`nginx_config.http` 输出 stdout，随 `docker logs bms-apisix` 可见）。**本任务接线点**：Prometheus 增加抓取该端点（指标前缀 `apisix_`）、Loki / 日志栈采集 APISIX 访问日志；边缘指标并入 Grafana 展示与告警（真实采集 / 展示 / 告警归本任务）。

> **完成情况（2026-09-23）**：已交付——详见[详细设计](设计/01_详细设计_01_可观测性栈接入.md)、[实施记录](实施/01_实施_01_可观测性栈接入.md)、[测试记录](测试/01_测试_01_可观测性栈接入.md)。Kiwi 用例 2182；本机门禁全绿（pytest 1282 / pyright 0 / ruff 干净 / 基座与边界校验全绿）；mjbk 真实冒烟（指标可抓 / Trace 可查 / 日志可按服务检索 / APISIX 入栈 / 一键拉起）。
