# 01 可观测性栈接入

> 后端基座与服务化地基 · 08_可观测性栈 · 子任务 01

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 01 |
| 父任务 | [08_可观测性栈](../08_可观测性栈.md) |
| 对应需求 | [08-1](../../../需求/08_需求_可观测性栈.md#r08-1) |
| 工时（重估） | 10h |
| 依赖 | 02_01 |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. OTel SDK + otel-collector 自动埋点（FastAPI / SQLAlchemy / Redis / Celery）
2. Tempo 接入（OTLP + 保留期 / TTL）
3. Prometheus 采集每服务 /metrics、Grafana 统一展示
4. Loki 结构化日志采集（trace_id / request_id / service）

## 3. 完成标准 <a id="accept"></a>

Trace 可查、指标可查、日志可按服务检索；保留期配置就位；本地一键拉起含可观测栈；校验通过。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 可观测性》「指标采集」「链路追踪」节

> **前置契约（已交付 · 02_01，2026-09-22）**：工作区与服务工程骨架已就位（`libs/bms_core` + `services/platform`，包 `bms_core` / `bms_platform`），每服务独立工程可独立构建；可观测能力域契约（`bms_core.metrics` / `bms_core.tracing`）随共享库复用。本任务据接入 OTel + Tempo + Prometheus + Loki 并按服务归因。
