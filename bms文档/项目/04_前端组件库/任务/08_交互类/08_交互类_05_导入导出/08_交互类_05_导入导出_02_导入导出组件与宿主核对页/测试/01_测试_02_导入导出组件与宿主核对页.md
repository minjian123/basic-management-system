# 01 测试 · 导入导出组件与宿主核对页

> 前端组件库 · 08 交互类 · 05 导入导出 · 嵌套子任务 02（需求 08-5）· 测试记录

[文档首页](../../../../../../../文档首页.md) › [08 交互类](../../../08_交互类.md) › [05 导入导出](../../08_交互类_05_导入导出.md) › 测试记录　|　[← 实施记录](../实施/01_实施_02_导入导出组件与宿主核对页.md)

## 1. 信息 <a id="info"></a>

| 项 | 值 |
| --- | --- |
| 任务 | 08-5-2 导入导出组件与宿主核对页（[任务](../08_交互类_05_导入导出_02_导入导出组件与宿主核对页.md)） |
| 依据 | [详细设计](../设计/01_详细设计_01_导入导出组件与宿主核对页.md) + [实施记录](../实施/01_实施_02_导入导出组件与宿主核对页.md) |
| Kiwi 用例 | 770（分类「平台骨架」/ P2 / CONFIRMED / 标签「自动化」；用例文件首行 `// kiwi_id: 770`） |
| 测试日期 | 2026-09-19 |
| 用例规模 | 新增 1 个用例文件 22 条；`ui-ep` 全量 39 文件 / 402 用例 |

## 2. 用例与验收映射 <a id="cases"></a>

| 验收点（详细设计 §5） | 用例 |
| --- | --- |
| 三投影薄适配（就绪 / 阶段 / 进度 / 结果 / 取消 / 复位） | `ui-ep/tests/interaction-import-export.spec.ts`（`useBaseFileDownload` / `useBaseImportFlow` / `useBaseExportFlow` 各 2 ~ 4 条） |
| 导入占位零请求、文件校验不请求、幂等键内容派生与重试复用 | 同上（导入流投影） |
| 上传 / 解析中禁止关闭与取消复位 | 同上（导入流投影 + `ImportDialog`） |
| `ImportDialog` 就绪态三步渲染、文件校验、提交事件与幂等键、内部编排 | 同上 +（**复用**）`interaction-placeholder-2.spec.ts` 既有冻结断言（未改动即通过） |
| 结果与错误行报告、分页、下载错误明细上抛 | 同上（`ImportErrorReport` 直接挂载 + `ImportDialog` 内置件） |
| `ExportButton` 就绪态载荷透传、禁用口径、内部编排与结果上抛 | 同上 +（**复用**）`interaction-placeholder-2.spec.ts` 既有冻结断言 |
| 脱敏提示、超阈值后台提示与进度件取消 / 重试 | 同上（`ExportButton` + `ExportProgress` 状态矩阵） |
| 无数据不预先禁用（点击零请求并由核心写提示） | 同上（`ExportButton` 无数据与明文权限收窄条） |
| 下载触发工具（blob / URL 通路、无目标不触发） | 同上（jsdom 以 `URL.createObjectURL` 桩断言） |
| 分包边界（两个懒加载件解析与 `data-subpackage`） | 同上（`vi.dynamicImportSettled()` + 属性断言） |
| 组件挂链护栏（值引入基类投影） | `ui-ep/tests/guard-component-inheritance.spec.ts`（自动覆盖新增件与三投影） |
| 宿主机对页十二项自检 | chromium headless `--dump-dom`（**12 / 12**） |
| `vue-tsc`、ESLint、Vitest 全通过 | 根 `npm run check` + 宿主 `vue-tsc -b` / `lint` / `test:cov` / `build` / `budget` |
| 文档链接与基座校验 | `check-links.py`、`check-base.py` |

## 3. 执行结果 <a id="result"></a>

| 门禁 | 结果 |
| --- | --- |
| core `tsc` / `eslint` / `vitest` | 通过；44 文件 / 362 用例全绿 |
| `@bms/vue` | 通过；2 文件 / 5 用例 |
| `@bms/ui-ep` `vue-tsc` / `eslint` / `vitest` | 通过；39 文件 / 402 用例全绿（本次新增 22 条） |
| 宿主 `vue-tsc -b` / `lint` | 通过 |
| 宿主 `test:cov` | 通过；35 用例 + 语句覆盖 86.79%（行 87.61%） |
| 宿主 `build` / `budget` | 通过；合计 121.1 KB / 260 KB、最大单文件 102.3 KB / 150 KB（无分包退化告警） |
| 既有 `interaction-placeholder-2.spec.ts`（`08_01_02` 冻结契约） | 13 / 13 通过（未改动断言） |
| 开发态核对页（08_05_02，chromium `--dump-dom`） | **12 / 12** 通过 |
| `check-links.py` | 通过（断链 0、失效锚点 0） |
| `check-base.py` | 通过（四项均为 0） |

## 4. 问题与偏差 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 既有冻结断言「就绪态点击导出必上抛 `export`」与设计「无数据禁用」冲突（既有用例未传 `total`） | 按铁律取「对外契约冻结优先」：入口不做无数据禁用，点击后核心写提示且零请求；设计 §3.6 已回写，用例补「零请求 + 提示」断言 |
| 2 | 首版 `imported` 事件未上抛（件内 `inline` 被写成 computed 却按函数调用） | 改为函数判定并补断言（`imported` / `done` / `update:visible`） |
| 3 | `exported` / `queued` 结果对象含核心补写的 `message` | 断言改 `toMatchObject`（核心补提示属既定语义，不断言全等） |
| 4 | 下载明细断言依赖内置报告件挂载 | 改以 `jobs.execute` 注入驱动至结果步后断言 `ImportErrorReport` 挂载与 `download` 上抛 |

## 5. 覆盖率与遗留 <a id="cov"></a>

- 新增代码均有用例覆盖：3 套投影、1 个下载触发工具、4 件组件（含两个懒加载分包件解析）与 1 个错误报告件；组件挂链护栏自动覆盖新增件与投影。
- **真实接口联调**：模板下载 / 执行导入 / 错误明细下载 / 列表导出 / 后台任务轮询未就绪；注入路径与占位路径均已覆盖，真实联调用例随阶段八补测。
- **事件与注入双轨**：宿主同时监听事件并注入 `jobs` 会重复执行（二选一），属既定设计口径，登记为使用约束。
- **移动端实现**：`ui-vant` 侧未覆盖，两套契约套件与件层形态已就位供复用。
- **上游回写**：三处上游文档回写随 `08-5-1` 遗留一并完成，本子任务只回写《组件设计 01 导入导出》。

> 本文档依《[文档生成规范](../../../../../../../规范/文档生成规范.md)》编写
