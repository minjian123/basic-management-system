# 01 测试 · 报表设计器

> 前端组件库 · 08 交互类 · 09 报表设计器与大屏设计器与播放 · 嵌套子任务 01（需求 08-9）· 测试记录

[文档首页](../../../../../../../文档首页.md) › [08 交互类](../../../08_交互类.md) › [09 报表设计器与大屏设计器与播放](../../08_交互类_09_报表设计器与大屏设计器与播放.md) › 测试记录　|　[← 实施记录](../实施/01_实施_01_报表设计器.md)　[详细设计 →](../设计/01_详细设计_01_报表设计器.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | 08-9-1 报表设计器（[任务](../08_交互类_09_报表设计器与大屏设计器与播放_01_报表设计器.md)） |
| 对应需求 | [08-9](../../../../../需求/08_需求_交互类.md#r08-9) |
| 依据 | [详细设计](../设计/01_详细设计_01_报表设计器.md)（定稿 `760cf1bb`）+ [实施记录](../实施/01_实施_01_报表设计器.md) |
| 测试日期 | 2026-09-19 |
| 测试人 | minjian |
| 测试环境 | Ubuntu 开发机 · Vitest 5（`core` 纯 TS / `ui-ep` jsdom · Vue Test Utils）· chromium headless（开发态核对页） |
| Kiwi 用例 | 776（分类「平台骨架」/ P2 / CONFIRMED / 标签「自动化」） |
| 结论 | 全部用例通过；完成标准达标（三区 / 数据集与映射 / 网格 / 预览 / 保存发布 / 分包、核对页 12/12） |

## 2. 用例清单与结果 <a id="cases"></a>

| 用例文件 | 覆盖范围 | 条数 | 结果 |
| --- | --- | --- | --- |
| `core/tests/domain-report-layout.spec.ts` | 尺寸与图表项归一（未知类型回落 / 脏字段不抛错 / 无标识丢弃 / 标识递增）；结构操作（新增自动落位 / 删除幂等 / 移动缩放夹取 / 居中 / 更新 / 复制 / 重叠检测 / 选中查找）；字段与选项（停用不可选 / 配置生成 / 标题解析）；校验（空 / 不存在 / 停用 / 缺映射 / 有效）与序列化 / 脏比对 | 17 | 通过 |
| `core/tests/report-designer.spec.ts` | 契约套件 `describeReportDesignerContract`（11 条）；能力身份与依赖图；数据集选择与切换（停用不可选 / 切换报表清空 / 失效重置）；拖拽广播与保存发布（未注入仍落点 / 发布依赖保存 / 另存不改编码 / 失败保留脏标记 / needsBlock 与 discard） | 14 | 通过 |
| `ui-ep/tests/interaction-report-designer.spec.ts` | 投影 `useBaseReportDesigner`（占位零请求 / 注入处理函数取数与增删）；`DatasetPanel`（字段与预览 / 停用提示与预览触发）；`ChartConfigPanel`（空态 / 选中标记 / 类型切换）；`ReportCanvas`（空态与分包标记 / 删除事件）；`ReportGrid`（网格项与 gridstack 分包标记）；`ReportDesigner`（占位 / 三区与事件上抛 / 注入驱动保存发布 / 脏拦截） | 11 | 通过 |
| `ui-ep/tests/interaction-placeholder-3.spec.ts`（既有，未改动） | `08_01_03` 冻结契约回归（占位降级 / 就绪三区与事件透传 / 分包断言） | 14 | 通过 |
| `core/tests/guard-manifest.spec.ts` / `guard-jsdoc.spec.ts` / `guard-core-framework-agnostic.spec.ts` | 能力登记对账（新增 `report-designer` 键）/ 注释齐备 / 核心框架无关 | — | 通过 |
| `ui-ep/tests/guard-component-inheritance.spec.ts` / 宿主 `guard-module-style.spec.ts` | 组件挂链（五件均实质挂链）/ 样式作用域 | — | 通过 |

## 3. 验收映射 <a id="mapping"></a>

| 验收点（需求 08-9 / 子任务 08-9-1） | 用例 | 结果 |
| --- | --- | --- |
| 数据集选择与字段映射（元数据驱动） | `domain-report-layout.spec.ts`（字段 / 选项 / 配置生成）+ `DatasetPanel` / `ChartConfigPanel` 用例 + 核对页 ②③④ | 通过 |
| 图表类型与配置面板、实时渲染 | `ChartConfigPanel` 用例 + 报告画布复用 `ChartRenderer` + 核对页 ⑥⑦⑧ | 通过 |
| 12 列网格布局（增删 / 拖拽 / 缩放 / 选中联动） | `domain-report-layout.spec.ts`（结构操作与夹取）+ `ReportGrid` 用例 + 核对页 ⑨⑩⑪ | 通过 |
| 数据预览与保存发布 | `report-designer.spec.ts`（预览 / 保存 / 发布 / 另存）+ 核对页 ⑤⑫ | 通过 |
| 分包（gridstack / ECharts 独立分包、首屏不加载） | 分包标记用例 + 宿主 `build` / `budget` | 通过 |
| Vitest 覆盖配置序列化分支 | `domain-report-layout.spec.ts`（序列化 / 等价比对） | 通过 |
| `vue-tsc`、ESLint、Vitest 全通过 | 根 `pnpm run check` + 宿主 `vue-tsc -b` / `lint` / `test` | 通过 |

## 4. 覆盖率与门禁 <a id="coverage"></a>

| 命令 | 结果 |
| --- | --- |
| `pnpm run check`（core + vue + ui-ep） | 58 + 2 + 46 文件 / 689 + 5 + 566 用例全绿 |
| 宿主 `pnpm exec vue-tsc -b` / `pnpm run lint` | 通过 |
| 宿主 `pnpm run test` | 通过（35 用例） |
| 宿主 `pnpm run build` / `pnpm run budget` | 通过（首屏入口块 119.5 KB / 260 KB；最大单文件 104.1 KB / 150 KB） |
| 开发态核对页（chromium headless `--dump-dom`） | 12 / 12 自检项通过 |
| `check-links.py` | 通过（断链 0 / 失效锚点 0） |

新增模块覆盖率：`core/src/domain/report-layout.ts`、`core/src/capabilities/report-designer.ts`、`core/testing/report-designer.ts`、`ui-ep/src/composables/useBaseReportDesigner.ts` 与五件组件的分支均由上述用例覆盖（含占位零请求、只读不动作、网格夹取、脏拦截、保存失败保留、分包边界）。

## 5. 问题与偏差 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 核对页首轮 10/12（数据预览未接线、脏基线未建立） | 按实施记录第 4 节第 5 项处置（补预览接线 + ⑫ 调整顺序），复跑 12/12 |
| 2 | gridstack 在 jsdom 下不可用 | 采用「动态 import + try/catch 降级」，单测断言分包标记与网格项；真实行为由核对页验证 |
| 3 | 冻结契约同步可见性与分包懒加载冲突 | 数据集列表内联同步渲染、详情面板独立分包（根 `data-test` 区分），冻结用例未改动即通过 |

## 6. 遗留 <a id="leftover"></a>

- **真实接口联调**（报表定义取数 / 保存 / 发布 / 另存 / 数据集取数预览）随报表 BI 窗口；处理函数未注入即占位零请求。
- **预览结果分发细化**：本期按数据集匹配分发，后续可细化为按图表项取数。
- **大屏设计器与播放**（`08-9-2`）随后单独设计与提交。
- **宿主真实页面**「报表设计器页」与移动端实现待后续任务。

> 本文档依《[文档生成规范](../../../../../../../规范/文档生成规范.md)》编写
