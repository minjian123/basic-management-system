# 01 测试 · 表单设计器

> 前端组件库 · 08 交互类 · 06 表单设计器与表单渲染器 · 嵌套子任务 01（需求 08-6）· 测试记录

[文档首页](../../../../../../../文档首页.md) › [08 交互类](../../../08_交互类.md) › [06 表单设计器与表单渲染器](../../08_交互类_06_表单设计器与表单渲染器.md) › 测试记录　|　[← 实施记录](../实施/01_实施_01_表单设计器.md)　[详细设计 →](../设计/01_详细设计_01_表单设计器.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | 08-6-1 表单设计器（[任务](../08_交互类_06_表单设计器与表单渲染器_01_表单设计器.md)） |
| 对应需求 | [08-6](../../../../../需求/08_需求_交互类.md#r08-6) |
| 依据 | [详细设计](../设计/01_详细设计_01_表单设计器.md)（定稿 `b937bb37`）+ [实施记录](../实施/01_实施_01_表单设计器.md) |
| 测试日期 | 2026-09-19 |
| 测试人 | minjian |
| 测试环境 | Ubuntu 开发机 · Vitest 5（`core` 纯 TS / `ui-ep` jsdom · Vue Test Utils）· chromium headless（开发态核对页） |
| Kiwi 用例 | 771（分类「平台骨架」/ P2 / CONFIRMED / 标签「自动化」） |
| 结论 | 全部用例通过；完成标准达标（拖拽落点与发布分支覆盖、元数据契约一致、四件真实编排与分包可用、核对页 12/12） |

## 2. 用例清单与结果 <a id="cases"></a>

| 用例文件 | 覆盖范围 | 条数 | 结果 |
| --- | --- | --- | --- |
| `core/tests/domain-form-layout.spec.ts` | 布局归一与空布局（归一 / 失败引用不抛错 / 分组 / 字段引用与列数 / 键收集与计数）；结构操作（插入幂等与定位 / 自动建分区 / 移动与排序 / 索引夹取 / 移出与跨列 / 分区增删改 / 新键不冲突 / 标签与查询区明细区 / 选中归一）；层级解析与回退（只读矩阵 / 恢复目标 / 按层级解析 / 空布局不参与命中 / 生效布局 / 渲染输入同形）；失效引用与校验与脏比对；自建字段与可拖入（列名派生 / 类型白名单 / 选项集 / DDL 文案 / 可拖入判定） | 28 | 通过 |
| `core/tests/form-designer.spec.ts` | 契约套件两套驱动（元数据契约 11 条 + 设计器编排 13 条）；能力身份与依赖图；拖拽协作（广播 `drop` 载荷 / 未注入仍落点）；取数与组合能力（先元数据再快照 / 失败置失败态）；保存发布与恢复（写回层级清脏 / 校验拦截 / 恢复失败保留本地）；权限与提示协作 | 35 | 通过 |
| `ui-ep/tests/interaction-form-designer.spec.ts` | 投影薄适配（占位零请求 / 取数基线 / 结构与脏同步 / 平台层级只读 / 保存发布恢复与 DDL 重试）；拖拽适配（同区下移索引减一 / 跨区不修正 / 四阶段载荷）；四件真实实现（三区渲染与分包 / 字段拖入与「已使用」/ 平台只读禁用 / 注入 `jobs` 驱动编排与自建字段 / 不可拖入置灰 / 脏拦截上抛）＋ 属性面板（选中联动 / 分区属性 / 只读禁用）＋ 自建字段对话框（列名预览 / 校验拦截 / 选项集必填 / DDL 状态与重试） | 17 | 通过 |
| `ui-ep/tests/interaction-placeholder-2.spec.ts`（既有，未改动） | `08_01_02` 冻结契约回归（占位降级 / 就绪态三区与载荷透传 / 平台层级只读） | 19 | 通过 |
| `core/tests/guard-manifest.spec.ts` / `guard-jsdoc.spec.ts` / `guard-core-framework-agnostic.spec.ts` | 能力登记对账（新增 `form-designer` 键）/ 注释齐备 / 核心框架无关 | — | 通过 |
| `ui-ep/tests/guard-component-inheritance.spec.ts` | 组件挂链（四件均实质挂链） | — | 通过 |

## 3. 验收映射 <a id="mapping"></a>

| 验收点（需求 08-6 / 子任务 08-6-1） | 用例 | 结果 |
| --- | --- | --- |
| 三区拖拽（字段调板 → 画布分区、分区内排序、跨分区、查询区 / 明细区排序） | `interaction-form-designer.spec.ts`（拖入 / 落点 / 排序载荷）+ 核对页 ③④⑤ | 通过 |
| 布局配置（分区列数与跨列、字段属性、自建字段、查询区与明细区） | `domain-form-layout.spec.ts`、`FieldPropertyPanel` / `ExtFieldDialog` 用例 + 核对页 ⑥⑦⑧ | 通过 |
| 三级层级与保存发布（发布产出与渲染器共享元数据契约） | `form-designer.spec.ts`（层级 / 保存 / 发布 / 恢复）+ 契约套件 + 核对页 ⑩⑪⑫ | 通过 |
| 元数据契约与渲染器一致 | `describeFormLayoutContract`「渲染输入与设计产出同形」+ `domain-form-layout.spec.ts` | 通过 |
| 字段类型来源经统一提供者注册表（未注册不可拖入） | `domain-form-layout.spec.ts`（可拖入判定）+ `interaction-form-designer.spec.ts`（置灰） | 通过 |
| Vitest 覆盖拖拽落点与发布分支 | 上述核心 + ui-ep 用例 | 通过 |
| `vue-tsc`、ESLint、Vitest 全通过 | 根 `npm run check` + 宿主 `vue-tsc -b` / `lint` | 通过 |

## 4. 覆盖率与门禁 <a id="coverage"></a>

| 命令 | 结果 |
| --- | --- |
| `npm run check`（core + vue + ui-ep） | 46 + 2 + 40 文件 / 425 + 5 + 419 用例全绿 |
| 宿主 `npx vue-tsc -b` / `npm run lint` | 通过 |
| 宿主 `npm run test:cov` | 通过（语句覆盖 86.79%、行覆盖 87.61%、35 用例） |
| 宿主 `npm run build` / `npm run budget` | 通过（合计 121.9 KB / 260 KB；最大单文件 103.0 KB / 150 KB；无分包退化告警） |
| 开发态核对页（chromium headless `--dump-dom`） | 12 / 12 自检项通过 |
| `check-links.py` / `check-base.py` | 通过（断链 0 / 失效锚点 0；跨层引用 / 措辞残留 / 清单一致 / 跨文档编号引用均为 0） |

新增模块覆盖率：`core/src/domain/form-layout.ts`、`core/src/capabilities/form-designer.ts`、`core/testing/form-layout.ts`、`ui-ep/src/composables/useBaseFormDesigner.ts`、`ui-ep/src/utils/dragSortable.ts` 与四件组件的分支均由上述用例覆盖（含占位零请求、只读不动作、失败与重试、防重复、分包边界）。

## 5. 问题与偏差 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 核对页首轮 4/12（读不到设计器实例 + 新建字段未并入调板） | 按实施记录第 4 节第 6 项处置（件补 `defineExpose` + 新建字段并入调板），复跑 10/12 |
| 2 | 核对页二轮 10/12（步骤 ⑤ 新增空分区触发校验拦截保存） | 属**设计正确行为**；核对页自检顺序调整为「先验证校验拦截 → 删空分区 → 保存与发布 → 恢复默认」，复跑 12/12 |
| 3 | 既有冻结用例要求 `data-dragging` 属性 | 件内保留拖拽广播并补该属性（对外契约冻结优先），既有用例未改动即通过 |

## 6. 遗留 <a id="leftover"></a>

- **真实接口联调**（布局取数 / 保存 / 发布事件 / 恢复默认 / 自建字段 DDL 与重试）随阶段八；处理函数未注入即占位零请求。
- **分组二级组织完整交互**随 `08-6-2` 渲染侧口径细化（数据面已就位）。
- **字典高级查询界面完整配置交互**随字典字段节点对齐。
- **字段级权限叠加（`visible` / `editable`）运行时渲染**随 `08-6-2`（本任务已交付布局侧语义与元数据面；`08_04` 遗留已登记移交）。
- **宿主真实页面**「表单布局管理」与移动端实现待后续任务。

> 本文档依《[文档生成规范](../../../../../../../规范/文档生成规范.md)》编写
