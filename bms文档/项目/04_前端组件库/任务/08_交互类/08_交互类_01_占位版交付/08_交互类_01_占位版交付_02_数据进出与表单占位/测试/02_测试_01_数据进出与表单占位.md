# 01 测试 · 数据进出与表单占位

> 前端组件库 · 08 交互类 · 01 占位版交付 · 嵌套子任务 02（需求 08-1）· 测试记录

[文档首页](../../../../../../../文档首页.md) › [08 交互类](../../../08_交互类.md) › [01 占位版交付](../../08_交互类_01_占位版交付.md) › 测试记录　|　[← 任务](../08_交互类_01_占位版交付_02_数据进出与表单占位.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 数据进出与表单占位](../08_交互类_01_占位版交付_02_数据进出与表单占位.md) |
| 对应需求 | [08-1](../../../../../需求/08_需求_交互类.md#r08-1) |
| 详细设计 | [02 详细设计](../设计/02_详细设计_01_数据进出与表单占位.md) |
| 实施记录 | [02 实施](../实施/02_实施_01_数据进出与表单占位.md) |
| 测试日期 | 2026-09-18 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1 / jsdom） |
| Kiwi 用例 | 761（分类「平台骨架」/ P2 / CONFIRMED / 标签「自动化」；代码 `// kiwi_id: 761`） |
| 结论 | 通过（ui-ep 237 用例全绿；占位契约套件通过） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例组 | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 占位契约 | 契约套件 | `ui-ep/tests/interaction-placeholder-2.spec.ts`（复用 `describePlaceholderInteractionContract`） | 未就绪 → 降级 / 禁用 / 零请求（含 `load()`）；就绪 → 不再降级 / 禁用 |
| 能力投影 | 单元 | 同上 | 上传引擎（未注入占位 / 注入进度 / 取消）、异步任务（状态流转与退避）、拖拽（阶段与订阅）、元数据（加载与版本）、语言上下文（切换与格式） |
| 导入对话框 | 单元 | 同上 | 占位降级；分包主体；模板下载；文件类型 / 大小校验；幂等键生成与提交事件 |
| 导出触发 | 单元 | 同上 | 占位禁用与降级；分包进度；导出载荷透传；选中导出未选行禁用 |
| 表单设计器 | 单元 | 同上 | 占位降级；三区 / 字段拖入 / 脏数据；画布分包；选中联动属性；平台层级只读 |
| 表单渲染器 | 单元 | 同上 | 占位降级；主体分包与三态；`defineExpose` 校验 / 取数 / 设值；查看态提交禁用 |
| 国际化文案编辑器 | 单元 | 同上 | 占位降级；语言清单与启停；网格分包；单元格编辑与删除事件透传 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
npm run check                                    # 根：core + vue + ui-ep
python3 scripts/tools/base-check/check-links.py
python3 scripts/tools/base-check/check-base.py
```

```text
> ui-ep  32 files / 237 tests passed（+19）
> core   29 files / 160 tests；vue 2 files / 5 tests
> links  断链 0 / 失效锚点 0
> base   基座校验通过
```

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | `FormMode` 与既有导出同名 | 渲染器三态改名 `FormRenderMode` |
| 2 | 异步分包件等待 | 用例用 `vi.dynamicImportSettled()` + `flushPromises()` |
| 3 | 文件输入二次 `defineProperty` 报错 | 定义时显式 `configurable/writable` |

## 5. 覆盖率 <a id="coverage"></a>

- `ui-ep` 随根 `check` 执行；本任务新增 19 用例（237 − 218）；契约套件复用 `@bms/core/testing`，五件真实实现继续复用。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项：真实数据通路（`08_05` / `08_06` / `08_07`）、拖拽内核、导入执行与错误明细、异步导出任务轮询、i18n 缓存失效。
- 件数口径偏差：导入导出拆双件，件数 10 → 11，已同步父任务 / 域 / 计划。

> 本文档依《[文档生成规范](../../../../../../../规范/文档生成规范.md)》编写
