# 01 测试 · 配置与工作流占位

> 前端组件库 · 08 交互类 · 01 占位版交付 · 嵌套子任务 01（需求 08-1）· 测试记录

[文档首页](../../../../../../../文档首页.md) › [08 交互类](../../../08_交互类.md) › [01 占位版交付](../../08_交互类_01_占位版交付.md) › 测试记录　|　[← 任务](../08_交互类_01_占位版交付_01_配置与工作流占位.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 配置与工作流占位](../08_交互类_01_占位版交付_01_配置与工作流占位.md) |
| 对应需求 | [08-1](../../../../../需求/08_需求_交互类.md#r08-1) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_01_配置与工作流占位.md) |
| 实施记录 | [01 实施](../实施/01_实施_01_配置与工作流占位.md) |
| 测试日期 | 2026-09-18 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1 / jsdom） |
| Kiwi 用例 | 760（分类「平台骨架」/ P2 / CONFIRMED / 标签「自动化」；代码 `// kiwi_id: 760`） |
| 结论 | 通过（ui-ep 218 用例全绿；占位契约套件通过） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例组 | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 占位契约 | 契约套件 | `ui-ep/tests/interaction-placeholder.spec.ts`（`describePlaceholderInteractionContract`） | 未就绪 → 降级 / 禁用 / 零请求（含调用 `load()`）；就绪 → 不再降级 / 禁用 |
| 占位组合式 | 单元 | 同上 | 就绪前 `markLoaded` 不计数、就绪后计数；状态经 `BaseDataState` 投影（`empty → ready`） |
| 权限配置 | 单元 | 同上 | 占位降级；就绪树 / 推导只读 / 脏数据；页签切换；树勾选与字段矩阵编辑事件；数据范围 / 主体绑定渲染 |
| 审批流展示 | 单元 | 同上 | 占位降级；进度 / 会签 / 时间线渲染；同意 / 驳回 / 转办 / 撤回 / 评论与节点点击；无权限禁用 |
| 审批流只读图 | 单元 | 同上 | 按需异步分包解析（`data-subpackage=bpmn`）；无 XML 降级进度视图 |
| 流程建模器 | 单元 | 同上 | 占位降级；三区 / 调板 / 事件透传；画布异步分包；选中联动属性面板；只读禁用编辑、导出可用 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
npm run check                                    # 根：core + vue + ui-ep
python3 scripts/tools/base-check/check-links.py
python3 scripts/tools/base-check/check-base.py
```

```text
> ui-ep  31 files / 218 tests passed（+13）
> core   29 files / 160 tests；vue 2 files / 5 tests
> links  断链 0 / 失效锚点 0
> base   基座校验通过
```

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 异步分包件未在 `flushPromises()` 后解析 | 改用 `vi.dynamicImportSettled()` + `flushPromises()` |
| 2 | 画布 `select` 事件类型不匹配 | 外层按宽类型入参、收窄为 `ModelerElement` |

## 5. 覆盖率 <a id="coverage"></a>

- `ui-ep` 随根 `check` 执行；本任务新增 13 用例（218 − 205）；契约套件由 `@bms/core/testing` 提供，真实实现继续复用。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项：真实数据通路（`08_04` / `08_08`）、bpmn-js 渲染与引擎预解析、二次确认与幂等键。
- 无测试偏差；Kiwi 编号已回填（760）。

> 本文档依《[文档生成规范](../../../../../../../规范/文档生成规范.md)》编写
