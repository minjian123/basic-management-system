# 01 测试 · 授权编排基类与领域

> 前端组件库 · 08 交互类 · 04 权限配置 · 嵌套子任务 01（需求 08-4）· 测试记录

[文档首页](../../../../../../../文档首页.md) › [08 交互类](../../../08_交互类.md) › [04 权限配置](../../08_交互类_04_权限配置.md) › 测试记录　|　[← 实施记录](../实施/01_实施_01_授权编排基类与领域.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | 08-4-1 授权编排基类与领域（[任务](../08_交互类_04_权限配置_01_授权编排基类与领域.md)） |
| 对应需求 | [08-4](../../../../../需求/08_需求_交互类.md#r08-4) |
| 详细设计 | [01_详细设计_01_授权编排基类与领域.md](../设计/01_详细设计_01_授权编排基类与领域.md) |
| 实施记录 | [01_实施_01_授权编排基类与领域.md](../实施/01_实施_01_授权编排基类与领域.md) |
| 测试日期 | 2026-09-19 |
| 测试人 | minjian |
| 测试环境 | Ubuntu 开发机 · Vitest 5（jsdom 无关，纯 TS 用例） |
| Kiwi 用例 | 767（分类「平台骨架」/ P2 / CONFIRMED / 标签「自动化」；先登记后编码，用例文件首行 `// kiwi_id: 767`） |
| 结论 | 通过 |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi 用例 | 类型 | 自动化文件 |
| --- | --- | --- |
| 767 | 单元（领域纯函数 + 编排基类） | `frontend/packages/core/tests/domain-permission-config.spec.ts`（21 条）、`frontend/packages/core/tests/permission-config.spec.ts`（18 条） |

本任务范围：领域纯函数（权限树推导 / 三态与级联 / 字段权限 / 数据范围 / 主体绑定 / 载荷与脏基线 / 幂等键 / 错误码定位）与授权编排能力基类（占位语义、取数、四类授权写入、全量覆盖提交与上下文刷新、失败与撤销、生命周期）。件层与投影的验证归 `08-4-2`。

## 3. 执行记录与结果 <a id="run"></a>

| 命令 | 结果 |
| --- | --- |
| `npm run core:test` | 39 文件 / 296 用例通过（本次新增 39） |
| `npm run core:typecheck` | 通过 |
| `npm run core:lint` | 通过（`--max-warnings 0`） |
| `npx prettier --config frontend/apps/desktop/.prettierrc.json --check` | 通过 |

覆盖要点（对照详细设计 §5 验收映射）：

| 验收点 | 用例位置 |
| --- | --- |
| 扁平化 / 查找 / 可授予判定 | `domain-permission-config.spec.ts`「权限树推导」 |
| 隐含推导（菜单 → 表单 → 业务只读） | 同上（`applyPermissionCheck` + `deriveGranted`）+ 编排用例断言 |
| 三态与级联（半选 / 取消连带） | 同上（`resolveCheckState` + 级联断言） |
| 动作默认全无（菜单不联动动作） | 同上 |
| 挂接缺失不可授予 | 同上 + 编排用例 |
| 字段默认全开 / 收窄 / 归属校验 | 「字段权限与数据范围」 |
| 数据范围默认无 / 非空入选 | 同上 |
| 主体去重与上限 | 「主体绑定」 |
| 载荷排序稳定 / 业务不进载荷 | 「载荷与脏基线」 |
| 脏基线键与顺序无关 | 同上 + 编排用例「撤销回滚」 |
| 幂等键确定性 | 「载荷与脏基线」+ 编排用例「同内容同键」 |
| 错误码解析与页签定位 | 「错误码定位」 |
| 占位语义（降级 / 禁用 / 零请求） | 编排用例「占位语义与取数」 |
| 提交阶段推进与上下文刷新（注入 / 未注入 / 刷新失败） | 编排用例「全量覆盖提交与上下文刷新」 |
| 提交失败保留本地、错误码定位、重试 | 同上 |
| 防重复提交 | 同上 |
| 角色切换 / 撤销 / 复位 / 生命周期 | 编排用例「角色切换 / 撤销 / 复位」 |
| 能力登记与清单对账（护栏） | `guard-manifest.spec.ts`、`guard-jsdoc.spec.ts` |

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 用例初版按方法调用 `granted()`（实为访问器）致类型报错 | 统一改为属性访问 |
| 2 | 用例未注入权限上下文即断言 `dirty`，勾选被 `canGrant` 拒绝 | 补齐 `setCodes(['role:grant'])` 前置 |
| 3 | 树族勾选集合断言初版按 `size` 断言，未反映级联后的实际键集合 | 改为断言键集合（`['biz:user', 'form:user']`），与级联口径一致 |

## 5. 覆盖率 <a id="coverage"></a>

- 新增核心文件均有用例覆盖：`domain/permission-config.ts`（21 条覆盖全部导出函数与边界分支）、`capabilities/permission-config.ts`（18 条覆盖阶段机、占位、写入、提交与刷新、失败与撤销）。
- 覆盖率门禁口径以《[测试规范](../../../../../../../规范/测试规范.md)》与流水线为准；本任务只作任务内快照（`core` 全量 39 文件 / 296 用例通过）。
- 契约套件 `describePermissionConfigContract` 的驱动用例在 `08-4-2`（`ui-ep` 投影适配）落地。

## 6. 偏差与遗留 <a id="deviations"></a>

- **契约套件驱动**：本任务只交付套件定义，驱动用例随 `08-4-2` 落 `ui-ep`（遗留，归 `08-4-2`）。
- **真实接口联调**：取数 / 提交 / 权限码取数三项处理函数的注入路径已覆盖（含失败与刷新失败分支），真实联调用例随阶段七补测。
- **移动端**：`ui-vant` 侧实现未覆盖，随移动端任务补测（契约套件已就位供复用）。

> 本文档依《[文档生成规范](../../../../../../../规范/文档生成规范.md)》编写
