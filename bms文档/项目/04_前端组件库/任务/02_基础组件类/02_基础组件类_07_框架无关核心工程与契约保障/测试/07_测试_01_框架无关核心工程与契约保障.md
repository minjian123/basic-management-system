# 07 测试 · 框架无关核心工程与契约保障

> 前端组件库 · 02 基础组件类 · 子任务 07（需求 02-7）· 测试记录

[文档首页](../../../../../../文档首页.md) › [02 基础组件类](../../02_基础组件类.md) › [07 框架无关核心工程与契约保障](../02_基础组件类_07_框架无关核心工程与契约保障.md) › 测试记录　|　[← 任务](../02_基础组件类_07_框架无关核心工程与契约保障.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [07 框架无关核心工程与契约保障](../02_基础组件类_07_框架无关核心工程与契约保障.md) |
| 对应需求 | [02-7](../../../../需求/02_需求_基础组件类.md#r02-7) |
| 详细设计 | [07 详细设计](../设计/07_详细设计_01_框架无关核心工程与契约保障.md) |
| 实施记录 | [07 实施](../实施/07_实施_01_框架无关核心工程与契约保障.md) |
| 测试日期 | 2026-09-18 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1） |
| Kiwi 用例 | 待平台登记后回读回填（先写用例、编号后补） |
| 结论 | 通过（5 用例全绿；`npm run core:check` 全绿） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例 | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 核心框架无关护栏 · 源码零框架依赖 | 单元 | `frontend/packages/core/tests/guard-core-framework-agnostic.spec.ts` | `src/**/*.ts` 无 `vue` / `@vue/*` / `element-plus` / `vant` / `@bms/{vue,ui-ep,ui-vant}` / `apps/` import |
| 核心框架无关护栏 · 包依赖面为空 | 单元 | 同上 | `dependencies` / `peerDependencies` 为空 |
| 核心框架无关护栏 · fixture 拦截 | 单元 | 同上 | 违规样本必须被扫描器命中 |
| 错误码段位 · 平台段位取值 | 单元 | `frontend/packages/core/tests/error-codes.spec.ts` | `10001` / `10002` / `10003` / `10005` / `30001` |
| 错误码段位 · 前端保留子段 | 单元 | 同上 | `19001` 落 `19xxx`、保留子段判定 |
| 契约用例工厂入口 | 单元（冒烟） | 同上（经 `describeContract`） | `@bms/core/testing` 入口可调用并执行套件 |

> 用例先写代码（名称内未编 Kiwi 号）；登记后按平台回读编号回填本表与用例标注。

## 3. 执行记录与结果 <a id="run"></a>

命令（仓库根）：

```bash
npm run core:check
```

输出摘要：

```text
> core:typecheck  tsc --noEmit -p tsconfig.json        （通过）
> core:lint       eslint . --max-warnings 0           （零告警）
> core:test       vitest run
  ✓ tests/guard-core-framework-agnostic.spec.ts (3 tests)
  ✓ tests/error-codes.spec.ts (2 tests)
  Test Files  2 passed (2)
       Tests  5 passed (5)
```

## 4. 问题与处置 <a id="issues"></a>

- 无测试过程问题（首次全绿）。

## 5. 覆盖率 <a id="coverage"></a>

- 本期核心门禁为 `vitest run`（与既往核心口径一致，未挂 coverage provider）；覆盖率门禁（整体 ≥ 70%）随宿主 / 插件包（`vue` / `ui-ep` / `ui-vant` / `apps`）落地后接入 `test:cov` 执行（归口域 03+）。
- 当前源码面：`src/mechanisms/error-codes.ts`（被 `error-codes.spec.ts` 覆盖）、`src/index.ts`（导出）、`testing/index.ts`（经 `describeContract` 覆盖）。

## 6. 偏差与遗留 <a id="deviations"></a>

- **未覆盖项**：契约套件断言（待基类域）、清单对账护栏（待基类域）、渲染插件 / 宿主的覆盖率门禁（待各包落地）。
- **归口**：`02_03` / `02_04` / `02_05` 与后续需求域；Kiwi 编号回读后回填本记录与需求用例标注。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
