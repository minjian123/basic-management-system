# 框架无关核心重构 · 实施记录（三类残留全量对齐：术语 / CI 与镜像 / 设计节点目录）

> 前端组件库 · 02 基础组件类 · 02-7 框架无关核心重构 · 收尾批次 · 实施记录

[文档首页](../../../../../../文档首页.md) › [02-7 框架无关核心重构](../02_基础组件类_07_框架无关核心重构.md) › 01 实施　|　[← 任务文档](../02_基础组件类_07_框架无关核心重构.md)

## 1. 实施信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 阶段 | 收尾批次 · 三类残留全量对齐（微前端术语 / CI job 与镜像路径 / 设计节点目录；另含代码标识与坏串修复） |
| 实施日期 | 2026-09-17 |
| 实施人 | minjian（4 个并行子任务 + 主会话复核；代码与 CI 主会话执行） |
| 环境 | Linux 开发机（mjpc）；含代码改名、CI 配置、Kiwi 平台数据更新 |
| 实测工时 | ≈3h |
| 结论 | 三类残留清零：**微前端统一称「产品前端模块」**、**CI job 短名与镜像路径按 `frontend/apps/*` 对齐**、**设计节点 33 组目录 / 文件重命名**；`fragment-context` → `module-context` 代码全链同步；修复 3 类历史坏串；`core:check` / `check-base` / `check-links` / `check-status` 全通过 |

## 2. 交付物 <a id="deliverables"></a>

### 2.1 代码标识（`fragment-context` → `module-context`） <a id="code"></a>

| 项 | 内容 |
| --- | --- |
| 文件 | `frontend/packages/core/src/capabilities/fragment-context.ts` → `module-context.ts` |
| 符号 | `BaseFragmentContext` / `FragmentContextOptions` / `FragmentContextHandles` → `BaseModuleContext` / `ModuleContextOptions` / `ModuleContextHandles`；能力 key `fragment-context` → `module-context`（`capabilities/{index,manifest}.ts`） |
| 测试 | `frontend/packages/core/tests/contracts/{s2-behaviors,s2-capability-matrix}.spec.ts` 同步；`core:check` 11 文件 / 65 用例全绿 |

### 2.2 CI 与镜像 <a id="ci"></a>

| 项 | 旧 | 新 |
| --- | --- | --- |
| CI job | `frontend-check` / `frontend-build` / `frontend-mobile-check` / `frontend-mobile-build` | `desktop-check` / `desktop-build` / `mobile-check` / `mobile-build`（`core-check` / `frontend-image` 不变） |
| 镜像内路径 | `/opt/ci/frontend` / `/opt/ci/frontend-mobile` | `/opt/ci/frontend/apps/desktop` / `/opt/ci/frontend/apps/mobile`（`/opt/ci/workspace` 不变） |
| 落点 | `.gitlab-ci.yml`（job 名 / `needs` / 复制命令）、`deploy/ci/Dockerfile.frontend`、`deploy/ci/build-base.sh` | 同上；Dockerfile 变更触发 `ci-base-build` 自动重建镜像 |
| 文档回写 | 全库 90+ 处旧 job 名与旧镜像路径引用 | 新名（含 mermaid 节点标签与 needs 表） |

### 2.3 设计节点目录（33 组） <a id="nodes"></a>

| 类别 | 旧 | 新 |
| --- | --- | --- |
| 机制节点 | `01_机制基类/03_组件设计_片段机制/`（含 md / html） | `03_组件设计_能力机制/` |
| 类别目录 | `02_能力片段/` | `02_能力类/` |
| 能力节点（30） | `NN_组件设计_XX片段/`（含 `NN_组件设计_XX片段.md`、`NN_原型设计_XX片段.html`） | `NN_组件设计_XX能力/`（同名三件） |
| 上下文节点 | `31_组件设计_片段上下文片段/` | `31_组件设计_模块上下文能力/` |

- 全库引用（总纲 / 清单 / 文档首页 / 项目 04 / 布局设计 / 概要设计 / test 仓）同步；`check-links` 断链 0 / 失效锚点 0。

### 2.4 微前端术语（「片段」→「产品前端模块」） <a id="term"></a>

| 语境 | 旧 | 新 |
| --- | --- | --- |
| 运行时单元 | 片段 / 运行时片段 / 产品片段 / 微前端片段 | **产品前端模块** |
| 复合词 | 片段加载器 / 片段清单 / 片段治理 / 片段契约 / 片段注册 / 片段卸载 / 片段名 | 模块加载器 / 模块清单 / 模块治理 / 模块契约 / 模块注册 / 模块卸载 / 模块名 |
| 能力体系 | 能力片段 / 预置片段 / XX片段 / 片段层 / 片段机制 | 能力 / 预置能力 / XX能力 / 能力层 / 能力机制 |
| 保留 | 局部节选义（代码片段 / 样式片段 / 日志片段 / 成功片段 / 高亮片段 / 检索片段） | 不变 |

- 范围：规划 3 件、资料知识档案（微前端 8 篇 + 插件化 / 微服务 / LLM 适配 / 技术栈总览）、架构设计 11 件、概要设计、设计组件设计（含原型 html 可见文案）、规范、清单、文档首页、项目 04 / 05、test 仓模板。

### 2.5 Kiwi 平台与历史坏串 <a id="kiwi"></a>

| 项 | 内容 |
| --- | --- |
| 平台正文 | 按对齐后登记输入再次同步（含 case 708「模块上下文」）；历史用例 697「对应片段（useOptionSource…）」→「对应能力（option-source / user-display / editor-kernel / upload-engine 能力类）」；平台全库复扫 `fragment-context` / 微前端 `片段` 残留 0（仅余局部节选义 1 处） |
| 坏串修复 | ① `项目规划说明.md` 治理分级被历史替换破坏的 `片段` 恢复 `L2` / `L3`（7 处）；② 原型设计错误页 SVG path `域基类8` / `片段4` / `域基类7` 恢复 `L48` / `L24` / `L47`（3 处）；③ `N片段SQL` / `N能力片段SQL` → `NL2SQL`（架构 / 概要 / 知识档案 / 项目 01 等 20+ 处） |

## 3. 验证结果 <a id="verify"></a>

| 命令 / 项 | 结果 |
| --- | --- |
| `npm run core:check` | 11 文件 / 65 用例全绿 |
| `npm run check`（packages 全量） | 通过（core / vue / ui-ep / ui-vant） |
| `desktop-check` / `mobile-check` 等价本地命令 | `eslint` + `vue-tsc -b` + `vitest run` 通过 |
| `check-base.py` | 通过（跨层 0 / 措辞残留 0 / 清单与磁盘一致 / 编号引用 0） |
| `check-links.py` | 通过（断链 0 / 失效锚点 0） |
| `check-status.py` | 通过（374 项 0 不合规） |
| 残留复扫 | 微前端「片段」与旧 job 名 / 旧镜像路径 / 旧目录名 / `fragment-context` 清零；剩余「片段」仅局部节选义（模板 2、用例正文 1）与历史快照留痕（`exports/2026-09-16_策展用例快照.json`、`exports/2026-09-17_策展用例快照.json`，为当时平台状态导出，对齐后快照见 `_标题对齐后` 件） |
| 流水线 | **256 success（211 秒）**：`ci-base-build` 重建镜像（36.6s，新镜像内路径生效）+ `desktop-check` / `mobile-check` / `core-check` / `desktop-build` / `mobile-build` / 后端组全绿；新 job 名与镜像路径端到端验证通过 |

## 4. 偏差与遗留 <a id="deviation"></a>

| # | 项 | 归口 / 去向 |
| --- | --- | --- |
| 1 | 设计节点 03 机制节点内「原设计面（未落码）」事件名 `fragment:*` | 保留（未落码设计面，随实现或后续批次决定是否改 `capability:*`） |
| 2 | 历史快照导出件保留当时标题（`2026-09-16` / `2026-09-17` 两版） | 留痕；最新口径见 `2026-09-17_策展用例快照_标题对齐后.json` |
| 3 | 域 05+ 回补波、真机视觉核对批次 | 后续会话 |

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
