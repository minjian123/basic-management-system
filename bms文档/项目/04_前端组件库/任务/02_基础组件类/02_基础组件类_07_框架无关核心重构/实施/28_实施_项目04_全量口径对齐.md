# 框架无关核心重构 · 实施记录（项目 04 全量口径对齐）

> 前端组件库 · 02 基础组件类 · 02-7 框架无关核心重构 · 收尾批次 · 实施记录

[文档首页](../../../../../../文档首页.md) › [02-7 框架无关核心重构](../02_基础组件类_07_框架无关核心重构.md) › 01 实施　|　[← 任务文档](../02_基础组件类_07_框架无关核心重构.md)

## 1. 实施信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 阶段 | 收尾批次 · **项目 04 全量口径对齐**（含历史实施 / 测试记录；历史溯源统一走 git） |
| 实施日期 | 2026-09-17 |
| 实施人 | minjian（5 个并行子任务 + 主会话复核；Kiwi 平台操作主会话执行） |
| 环境 | Linux 开发机（mjpc）；文档为主，另含 Kiwi 平台用例标题改名（经 ssh Django shell） |
| 实测工时 | ≈3h |
| 结论 | 项目 04 下 **115 个文档全部对齐现行口径**（旧路径 / 旧术语 / 旧组合式名清零；微前端术语、CI job 名与镜像路径、设计节点目录名三类残留于同日全量对齐，见实施记录 29）；目录 / 文件重命名 3 组；Kiwi 平台 **15 条用例标题 + 43 条用例正文**改写并重导快照；`check-base` / `check-links` / `check-status` 全通过 |

## 2. 范围与重命名 <a id="scope"></a>

| 项 | 内容 |
| --- | --- |
| 文档口径对齐 | 任务 02（57）/ 任务 03（34）/ 任务 04（18）/ 需求（00 / 02 / 03 / 04 / 05 ~ 08 / 10）/ 计划 / 审计 / README，共 115 件；含各任务 `实施/` 与 `测试/` 历史记录 |
| 目录重命名 | `任务/02_基础组件类/02_基础组件类_03_能力机制与预置能力/`（含 4 个同名文件） |
| 文件重命名 | `审计/01_审计_前端基类与能力合规.md` |
| 引用修正 | 全库（bms + test）38 处引用随改名同步；`check-links` 无断链 / 无失效锚点 |

## 3. 映射表 <a id="mapping"></a>

| 类 | 旧 | 新 |
| --- | --- | --- |
| 目录 | `frontend-mobile/` · 裸 `apps/*` · 裸 `packages/*` | `frontend/apps/mobile/` · `frontend/apps/{desktop,mobile}` · `frontend/packages/*` |
| 代码落点 | 旧机制层 `src/base/`；旧能力层 `src/components/base/<域>/useXxx.ts`；`fragments.ts` | `frontend/packages/core/src/base/`；`frontend/packages/core/src/capabilities/<域>.ts`；`…/capabilities/manifest.ts` |
| 机制 API | `useCapabilityBase` / `usePlaceholderBase` / `useAsyncResourceBase` / `useRegistryBase` / `useSubscriptionBase` / `BaseRegistryItem` | `BaseCapability` / `BasePlaceholder` / `BaseAsyncResource` / `BaseRegistry` / `BaseSubscription` / `RegistryItemLike` |
| 域语义 | 旧双轨（`useInputBase` + `BaseInput.vue` 等 4 域） | 能力类 `BaseField` / `BaseInputControl` / `BaseValue` / `BaseDisplayControl` / `BaseTreeData` / `BaseEditorKernel`；域包装按需形态；字段上下文归字段壳能力 `BaseFieldShell` |
| 护栏与用例 | 旧四类 `tests/guard-*.spec.ts`、`tests/base-domain-*.spec.ts` | `frontend/packages/core/tests/guard-core-framework-agnostic.spec.ts` + `frontend/packages/core/tests/contracts/*.spec.ts`、`s2-behaviors.spec.ts`；宿主用例 `frontend/apps/{desktop,mobile}/tests/`；组件用例 `frontend/packages/{ui-ep,ui-vant}/tests/` |
| 术语 | 「双端同款」 | 「同接口多实现」 |

- **微前端语境（现行口径）**：产品前端模块 / 模块加载 / 模块清单 / 模块治理（阶段五）。
- 三类残留（微前端术语、CI job 名与镜像路径、设计节点目录名）已于同日**全量对齐**（见[实施记录 29](29_实施_三类残留全量对齐.md)）。

## 4. Kiwi 平台操作 <a id="kiwi"></a>

| 项 | 内容 |
| --- | --- |
| 新增脚本 | `test/scripts/kiwi/rename_cases.py`（批量改名标题）、`update_case_text.py`（按标题批量更新正文）；均走 Django shell + ssh 管道，支持 `--dry-run` 与回读 |
| 改名清单 | `test/scripts/kiwi/cases/2026-09-17_标题对齐改名.json`（15 条） |
| 平台标题 | **15 条**：698 / 699 / 700（`frontend-mobile` → `frontend/apps/mobile`、`frontend` → `frontend/apps/desktop`）、705 / 707 / 708 / 713、阶段一 19 / 20 / 21 / 22 / 23 / 24 / 25 / 26（旧名与「同款」→「同接口多实现」） |
| 平台正文 | 登记输入 JSON 22 个全量复扫并改写（13 个含旧标记文件 + 03-02-01）；平台对应用例正文更新 **43 条**（按标题匹配回读）；历史用例 66 单条修 `frontend-mobile` |
| 快照 | 重导出 `test/scripts/kiwi/exports/2026-09-17_策展用例快照_标题对齐后.json`（150 条，标题对外口径命中 0；平台正文全库复扫旧标记 0） |
| 登记输入改名 | `2026-09-16_阶段四02-03_能力机制与预置能力.json` |
| 台账同步 | `test/test文档/用例/Kiwi用例台账.md` 与探索性测试记录 `ET-2026-09-16-01.md` 同步现行口径 |

## 5. 验证结果 <a id="verify"></a>

| 命令 | 结果 |
| --- | --- |
| `check-base.py` | 通过（跨层引用 0 / 措辞残留 0 / 清单与磁盘一致 / 跨文档编号引用 0） |
| `check-links.py` | 通过（断链 0 / 失效锚点 0） |
| `check-status.py` | 通过（374 项 0 不合规） |
| 残留复扫 | 项目 04 旧标记清零（路径 / 机制 API / 域双轨 / 护栏用例 / 术语）；微前端语境、CI job 名与镜像路径、设计节点目录名三类残留随实施记录 29 全量对齐；Kiwi 登记输入文件名随该批改名 |
| 结构核对 | 各文档标题层级 / 锚点 / 表格列数 / 状态 / 日期 / 工时 / 编号未变（少量交付物树按现行落点折叠，列数不变） |

## 6. 偏差与遗留 <a id="deviation"></a>

| # | 项 | 归口 / 去向 |
| --- | --- | --- |
| 1 | 域 05+ 回补波（含域包装重建、字段上下文重建） | 后续会话（实施记录 25 / 26 同口径） |
| 2 | 设计侧目录名（`03_组件设计_能力机制`、`02_能力类/` 等 31 个节点目录）与组件设计正文术语 | 组件设计侧既有口径（S9 波 2 / 波 3 保留）；如后续统一重命名目录，另立批次 |
| 3 | 真机视觉核对批次 | 后续会话 |

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
