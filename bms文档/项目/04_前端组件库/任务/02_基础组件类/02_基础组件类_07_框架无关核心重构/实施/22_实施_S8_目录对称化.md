# 框架无关核心重构 · S8 实施记录（目录对称化：前端工程归位 `frontend/{apps,packages}`）

> 前端组件库 · 02 基础组件类 · 02-7 框架无关核心重构 · 收尾批次（S8）实施记录

[文档首页](../../../../../../文档首页.md) › [02-7 框架无关核心重构](../02_基础组件类_07_框架无关核心重构.md) › 01 实施　|　[← 任务文档](../02_基础组件类_07_框架无关核心重构.md)

## 1. 实施信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 阶段 | `02_07` 收尾批次 **S8 目录对称化**（2026-09-17 追加；用户提出：前端侧目录与 `backend/` 对称） |
| 实施日期 | 2026-09-17 |
| 实施人 | minjian |
| 环境 | Linux 开发机（mjpc）；Node v24.20.0；npm workspaces |
| 实测工时 | ≈1h |
| 结论 | 完成：前端工程归位 `frontend/{apps,packages}`（`frontend/apps/desktop` / `frontend/apps/mobile` / `frontend/packages/{core,vue,ui-ep,ui-vant}`），与 `backend/` 平级对称；CI / 镜像内路径 / 脚本 / 文档全链对齐，双端与四包门禁全绿 |

## 2. 交付物 <a id="deliverables"></a>

| 块 | 内容 |
| --- | --- |
| 结构迁移 | 前端工程归位 `frontend/{apps,packages}`（385 条 rename，git 历史保留） |
| 根配置 | `package.json` workspaces 为 `frontend/packages/*`；`npm install` 重建锁与 `node_modules/@bms/*` 链接（指向 `../../frontend/packages/*`）；包内 `tsconfig.json` extends 深度 `../../` → `../../../tsconfig.base.json`（4 包） |
| CI / 镜像 | `.gitlab-ci.yml`：job 规则 `exists` / 脚本路径 / symlink 目标全量切 `frontend/apps/*`、`frontend/packages/*`；workflow `changes.paths` 收敛为 `frontend/**/*`（含 apps + packages）；`Dockerfile.frontend`：`COPY frontend/packages/ /opt/ci/workspace/frontend/packages/` 与 `COPY frontend/apps/{desktop,mobile}/`（镜像内路径 `/opt/ci/frontend/apps/desktop`、`/opt/ci/frontend/apps/mobile` 保持）；`build-base.sh`：锁文件输入与上下文布局切 `frontend/*` |
| 脚本 / 配置 | `scripts/tools/governance/{review_stage.py,collect_metrics.py}` 路径对齐；`.vscode/extensions.json` / `.npmrc` 注释；`frontend/packages/core/tests/guard-core-framework-agnostic.spec.ts` 注释与 fixture 路径 |
| 文档（现行 / 权威） | bms `README.md`（命令 + 目录树重排为 `frontend/` 下 apps + packages）；双端 README（命令 / 相对深度 / backend 与 bms文档 链接）；《前端基类清单》§9 / §10；《前端开发规范》§3 / §2；《前端审计规范》；架构 02 / 03 / 05 / 08 / 10 / 32 / 33；概要 38；组件设计 01 / 09 基础类三节点；规划三件；04 前端组件库计划、02-07 任务文档与会话交接、03-2-1 任务与设计；test 仓台账路径对齐 |
| 不改 | 项目 04 文档已按现行口径全量对齐（2026-09-17，实施记录 28；历史经 git 追溯）；测试仓 `cases/` 登记输入与平台快照保留原文 |

## 3. 验证结果 <a id="verify"></a>

| 命令 | 结果 |
| --- | --- |
| 根 `npm run check` | core **11 / 65** + vue **4 / 13** + ui-ep **13 / 82** + ui-vant **16 / 82** 全绿（lint / typecheck / test） |
| `frontend/apps/desktop`：lint / vue-tsc / test / build / budget | 零告警 / 通过 / **14 文件 / 77 用例** / 通过 / **208.9 / 240 KB** |
| `frontend/apps/mobile`：lint / vue-tsc / test / build / budget | 零告警 / 通过 / **11 文件 / 63 用例** / 通过 / **128.9 / 140 KB** |
| 文档校验 | `check-base` / `check-links` / `check-status` 通过 |
| 旧路径残留 | 代码 / 配置 / 脚本零残留；项目 04 文档已按现行口径全量对齐（2026-09-17，实施记录 28；历史经 git 追溯） |

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 包内 tsconfig `extends: ../../tsconfig.base.json` 在迁移后指向 `frontend/tsconfig.base.json`（不存在） | 四包统一改 `../../../tsconfig.base.json`（apps 无此依赖，无需改） |
| 2 | 双端 vite / tsconfig 的 `../../packages/...` 别名 | **无需改**：apps 与 packages 同步下移一层，相对深度不变 |
| 3 | 双端 README 相对链接与 backend 命令路径 | 深度 +1 对齐（`../../../bms文档/...`、`cd ../../../backend`） |

## 5. 偏差与遗留 <a id="deviation"></a>

| # | 项 | 归口 / 去向 |
| --- | --- | --- |
| 1 | 项目 04 文档已按现行口径全量对齐（2026-09-17，实施记录 28；历史经 git 追溯） | 后续新记录一律写 `frontend/*` 现行路径 |
| 2 | 部分设计 / 材料文档的旧体系路径 | 域 05+ 回补波按新体系重建时随节点更新 |
| 3 | 镜像重建 | 推送后 `ci-base-build` 按新锁哈希重建（build-base 上下文与 Dockerfile 已对齐） |

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
