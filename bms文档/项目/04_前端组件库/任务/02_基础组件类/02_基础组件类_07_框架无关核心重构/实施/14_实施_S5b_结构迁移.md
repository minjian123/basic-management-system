# 框架无关核心重构 · S5b 实施记录：结构迁移（宿主归位 `frontend/apps/desktop/`）

> 前端组件库 · 02 基础组件类 · 02-7 框架无关核心重构 · S5 阶段（结构迁移）实施记录

[文档首页](../../../../../../文档首页.md) › [02-7 框架无关核心重构](../02_基础组件类_07_框架无关核心重构.md) › 01 实施　|　[← 任务文档](../02_基础组件类_07_框架无关核心重构.md)

## 1. 实施信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 阶段 | S5b 结构迁移（宿主归位 `frontend/apps/desktop/`；全仓引用全量对齐） |
| 实施日期 | 2026-09-17 |
| 实测工时 | ≈2.5h |
| 结论 | S5b 完成：桌面端工程归位 `frontend/apps/desktop/`（git 历史保留），CI / 镜像 / 脚本 / 文档全链引用对齐；工程与基座校验全绿 |

## 2. 交付物 <a id="deliverables"></a>

| 块 | 内容 |
| --- | --- |
| 目录迁移 | 宿主归位 `frontend/apps/desktop/`（跟踪文件与未跟踪产物（`node_modules` / `dist` / `coverage` 等）随目录重命名；git 历史完整保留，`blame` / `log --follow` 可追溯） |
| 引用全量更新 | 全仓引用统一为 `frontend/apps/desktop/`：`.gitlab-ci.yml`（触发面 / `exists` / 脚本路径）、`deploy/ci/*`、`scripts/`、根 `README.md` / `.npmrc` 与 `bms文档/` **全部文档**（含历史实施/测试记录——**口径**：文档以当前系统一致性为准，历史版本经 git 查询；HTML 资产 3 个一并更新） |
| 镜像内路径保留 | `/opt/ci/frontend/apps/desktop`（镜像内布局）与 job 名（`desktop-check` / `desktop-build`）、镜像名（`ci-frontend` / `bms-frontend`）**保持不变**；`deploy/ci/Dockerfile.frontend` 源路径改 `COPY frontend/apps/desktop/ /opt/ci/frontend/`，`build-base.sh` 上下文目录与 `Dockerfile.frontend` 名对齐 |
| 工程内深度修正 | `vite.config.ts` / `tsconfig.app.json`：`../packages` → `../../packages`（3 + 3 处）；`tests/helpers/guard/repo.ts` 清单路径 → `../../bms文档/…`（迁移使相对深度 +1） |
| 保留项 | 旧组件目录原位并存（待 S5c 审计重跑后归档）；`frontend/apps/mobile/` 不动（S4 归位） |

## 3. 验证结果 <a id="verify"></a>

| 命令 | 结果 |
| --- | --- |
| `frontend/apps/desktop: npm run lint` | 通过（`--max-warnings 0`） |
| `frontend/apps/desktop: npm test` | **56** 文件 / **347** 用例全绿（含迁移中断链的 guard 用例修复） |
| `frontend/apps/desktop: npm run build` | 通过（vue-tsc -b + vite build） |
| `frontend/apps/desktop: npm run budget` | 通过：合计 **213.9** KB（预算 240）/ 单文件 **91.2** KB（预算 120） |
| 根 `npm run check` | 全绿：core **41** + vue **3** + ui-ep **59** 用例 |
| `check-links.py` / `check-base.py` | 通过（链接自洽；清单 170 与磁盘一致、无跨层引用与措辞残留） |

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 处置 |
| --- | --- |
| 引用替换的**范围边界**：镜像内路径 `/opt/ci/frontend/apps/desktop` 被机械替换 | 逐类甄别后回改（镜像内路径与 job/镜像名保留；源路径与上下文目录更新）；`deploy/ci/Dockerfile.frontend` 目标路径回改 `/opt/ci/frontend/` |
| `build-base.sh` 的 `Dockerfile.frontend` 文件名被误替换 | 回改并同步上下文目录（`$frontend_ctx/frontend/apps/desktop`）与 `mkdir` 对齐 |
| 迁移致相对深度 +1，`guard` 用例读清单失败（1 失败 + 5 跳过） | `repo.ts` 清单路径改 `../../bms文档/…`；工程内 `../packages` 系列统一 +1 层 |

## 5. 回写清单 <a id="writeback"></a>

| 文档 | 回写点 |
| --- | --- |
| [任务文档](../02_基础组件类_07_框架无关核心重构.md) | 状态行（S5b 完成）与阶段记录追加 |
| [计划](../../../../计划/01_计划_前端组件库.md) | §3 表 `02_07` 行（S5b 结构迁移） |
