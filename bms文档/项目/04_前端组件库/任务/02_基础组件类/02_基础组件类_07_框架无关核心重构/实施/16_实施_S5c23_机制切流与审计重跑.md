# 框架无关核心重构 · S5c-2 / S5c-3 实施记录：机制切流与审计重跑

> 前端组件库 · 02 基础组件类 · 02-7 框架无关核心重构 · S5c 阶段实施记录

[文档首页](../../../../../../文档首页.md) › [02-7 框架无关核心重构](../02_基础组件类_07_框架无关核心重构.md) › 01 实施　|　[← 任务文档](../02_基础组件类_07_框架无关核心重构.md)

## 1. 实施信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 阶段 | S5c-2 机制切流（v-perm / 脚本判定 / 菜单路由链）+ S5c-3 审计重跑 |
| 实施日期 | 2026-09-17 |
| 实测工时 | ≈2h |
| 结论 | S5c 完成：权限判定全链经 `@bms/ui-ep` 注入点；审计重跑（清单 / 护栏 / 审计文档）完成，全链校验通过 |

## 2. 交付物 <a id="deliverables"></a>

| 块 | 内容 |
| --- | --- |
| 机制切流（S5c-2） | `directives/perm.ts`：判定改经 `checkPerm`（保留 `permVersion` 响应式重评与 `.all` / `.not` 语义）；`utils/perm.ts`：`hasPerm` / `canAccess` 走注入点；`router/menuRoutes.ts`：`nameComponent` / `resolveView` 切 `@bms/ui-ep`（宿主 `routeComponent` 仅保留 `resolveView` 供注入装配） |
| 用例适配 | `directive-perm.spec` / `permission.spec`：注入 `configurePermissionChecker`（接权限 store 语义） |
| 审计重跑（S5c-3） | 审计文档追加 §7（组件根 35/35、护栏 24 用例、宿主 234、插件 59、门禁与清单对账）；`前端基类清单` §9 / §10 更新（组件位置 → `packages/ui-ep/src/components/<域>/`、件数 34、「同接口多实现」口径）；域 03 设计文档 7 处路径引用更新 |

## 3. 验证结果 <a id="verify"></a>

| 命令 | 结果 |
| --- | --- |
| `apps/desktop: lint / test` | lint 通过；**33** 文件 / **234** 用例全绿 |
| 护栏四类（继承 / 依赖 / 清单 / 新增流程） | **24** 用例全绿 |
| 组件根覆盖 | `packages/ui-ep/src/components/**/*.vue` **35/35** |
| `check-links` / `check-base` | 通过（清单 170 一致、无措辞残留） |

## 4. 问题与处置 <a id="issues"></a>

| 问题 | 处置 |
| --- | --- |
| 判定切注入点后权限测试失配（指令 / 脚本 / 按钮三类） | 各 spec 注入 `configurePermissionChecker`（store 语义），组件与指令保持注入点口径 |
| 审计文档与清单的旧路径 / 件数口径 | 全量更新（旧件路径 0 处残留；件数 42 → 34，双端口径改「同接口多实现」） |

## 5. 回写清单 <a id="writeback"></a>

| 文档 | 回写点 |
| --- | --- |
| [任务文档](../02_基础组件类_07_框架无关核心重构.md) | 状态行（S5c 完成）与阶段记录追加 |
| [计划](../../../../计划/01_计划_前端组件库.md) | §3 表 `02_07` 行（S5c 完成；S5 收口→审计重跑待办更新） |
| [审计文档](../../../../审计/01_审计_前端基类与片段合规.md) | §7 S5c 重跑 |
