# 框架无关核心重构 · S9 实施记录（文档对齐 · 波 2：组件设计 02 基础组件类 44 节点）

> 前端组件库 · 02 基础组件类 · 02-7 框架无关核心重构 · 收尾批次（S9）实施记录

[文档首页](../../../../../../文档首页.md) › [02-7 框架无关核心重构](../02_基础组件类_07_框架无关核心重构.md) › 01 实施　|　[← 任务文档](../02_基础组件类_07_框架无关核心重构.md)

## 1. 实施信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 阶段 | `02_07` 收尾批次 **S9 文档对齐 · 波 2**（组件设计 `02_基础组件类` 逐节点重写实现章，用户确认深度） |
| 实施日期 | 2026-09-17 |
| 实施人 | minjian（4 个并行子任务分批执行 + 主会话统一复核） |
| 环境 | Linux 开发机（mjpc）；纯文档改动（不触代码） |
| 实测工时 | ≈2h |
| 结论 | 波 2 完成：**机制基类 8 + 能力 31 + 域基类 5 = 44 节点** md 实现口径全部改写为新体系；术语 / 落点 / 护栏 / 投影口径对齐；契约语义（Props / 事件 / 插槽 / 行为规则）未动；`check-base` / `check-links` / `check-status` 全通过 |

## 2. 交付物 <a id="deliverables"></a>

| 块 | 内容 |
| --- | --- |
| 机制基类（8 节点） | 根系 / 组件根（`BaseFrontend` / `BaseComponent` + `@bms/vue` 投影）、能力机制（`mechanisms/capability.ts` + `capabilities/manifest.ts` + `CapabilityRegistry`）、占位 / 异步资源 / 订阅（`BaseSubscription extends BaseAsyncResource`）/ 注册表（`BaseRegistry<T>` + `RegistryItemLike`，独立泛型不入继承链）/ 错误（`ErrorCodes` + 宿主 `ApiError`） |
| 能力（31 节点） | 每节点：类名 / key / depends / 投影（有则 `frontend/packages/vue/src/bindings/useXxx.ts`，无则「按需回补」）、落点 `frontend/packages/core/src/capabilities/<key>.ts`、实现口径注（类化 + 旧层删除历史）、场景与护栏口径 |
| 域基类（5 节点） | 域能力类化口径（输入域 `field` + `input-control` + `field-shell`/`field-perm`；展示域 `value` + `display-control`；树域 `tree-data`；编辑器域 `editor-kernel`；图表域规则已就位、实现随 `07_06`）；原 4 件域包装（`BaseInput` / `BaseDisplay` / `BaseTree` / `BaseEditor` + `useXxxBase`，真件）随旧层删除、回补波重建 |
| 索引同步 | 《[组件设计总纲](../../../../../../设计/组件设计/01_组件设计_总览.md)》下游 / 契约口径 / 目录树注释；[文档首页](../../../../../../文档首页.md) 44 条组件设计索引行描述（`BaseXxx（核心能力类）[+ 投影]` 口径） |
| 统一规则 | 「片段」→「能力」；`useCapabilityBase` 声明 → 继承 `BaseCapability` + `manifest.ts` 登记；旧 `src/base/*` / `src/components/base/*` / `useXxxBase` / `guard-*` → 新落点与现行护栏；「双端同款」→「同接口多实现」；**历史留痕**：真实存在过的旧件标「已删除（历史经 git 追溯）」，从未落码的按需包装标「按需形态（历史未落码）」（复核修正 18 文件措辞） |

## 3. 验证结果 <a id="verify"></a>

| 命令 | 结果 |
| --- | --- |
| 旧标记扫描（44 节点 + 总纲 + 文档首页） | `frontend-mobile` / `src/components/base` / `useCapabilityBase` / `双端同款` **0 命中**；`src/base/` 仅作为 `frontend/packages/core/src/base/` 子串出现 |
| `check-base.py` | 通过（跨层引用 0 / 措辞残留 0 / 清单与磁盘一致 170 / 跨文档编号引用 0） |
| `check-links.py` | 通过（断链 0 / 失效锚点 0；44 节点链接目标多重集与 HEAD 一致） |
| `check-status.py` | 通过（374 项 0 不合规） |
| 结构核对 | 44 节点章节编号 / 锚点 / mermaid 围栏未变；契约语义未动（仅实现口径与术语） |

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 子任务批次把从未落码的能力包装（如 `BaseOptionSource.vue` / `BaseUploader.vue` / `BaseTabsNav.vue` 等）写成「已删除（历史经 git 追溯）」 | 经 git 历史核对（真实存在的旧包装只有 4 件域包装 + 根 / 组件根包装），统一修正为「按需形态（历史未落码）」（18 文件） |
| 2 | 能力机制校验时机（挂载时 → 实例化时）、`v-plain` / `maskValue` 回补去向等细节推断 | 已按现行核心实现口径落文；如后续回补波调整，随对应任务同步 |
| 3 | 契约表与现行核心代码的命名差异（如 `assertDeps` / `list` / `mode` 等设计面 vs 代码面） | **保留契约语义**（本波只改实现口径）；差异登记为波 3 / 回补波对齐项（见遗留 1） |

## 5. 偏差与遗留 <a id="deviation"></a>

| # | 项 | 归口 / 去向 |
| --- | --- | --- |
| 1 | 组件设计契约与现行核心实现的少量命名差异（机制契约表个别字段 / 动态路由 `mode` / `fallback404` 等） | 按「设计与代码不一致先回写设计」口径，随域 05+ 回补波逐项对齐（或在回补波前立专项） |
| 2 | **S9 波 3**：其余类目节点（布局 / 容器 / 基础控件 / 字段 / 展示 / 交互 / 基础类约 50 节点）+ 布局设计 2 处 html + 组件设计原型 html 文案（35 个含「片段」文案）+ 需求 03 / 04 与资料扫尾 | 后续会话 |
| 3 | 能力节点原型 html（demo 文案中的「片段」口径） | 归波 3（与设计节点文案一并扫尾） |

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
