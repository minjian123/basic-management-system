# 框架无关核心重构 · S9 实施记录（文档对齐 · 波 3：其余类目节点与 html / 索引扫尾）

> 前端组件库 · 02 基础组件类 · 02-7 框架无关核心重构 · 收尾批次（S9）实施记录

[文档首页](../../../../../../文档首页.md) › [02-7 框架无关核心重构](../02_基础组件类_07_框架无关核心重构.md) › 01 实施　|　[← 任务文档](../02_基础组件类_07_框架无关核心重构.md)

## 1. 实施信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 阶段 | `02_07` 收尾批次 **S9 文档对齐 · 波 3**（其余组件设计类目 + 原型 html 文案 + 索引与零散扫尾） |
| 实施日期 | 2026-09-17 |
| 实施人 | minjian（5 个并行子任务分批执行 + 主会话统一复核） |
| 环境 | Linux 开发机（mjpc）；纯文档改动（不触代码） |
| 实测工时 | ≈3h |
| 结论 | 波 3 完成：**组件设计其余 76 节点**（布局 11 / 容器 9 / 基础控件 8 / 字段 12 / 展示 14 / 交互 19 / 基础类 3）实现口径重写 + **原型 html 50 个**文案对齐 + 布局设计 html 5 / 知识档案 20 / 需求 00 与 03/04 / 后端基类清单对账行等扫尾；`check-base` / `check-links` / `check-status` 全通过。**S9 三波全部收口** |

## 2. 交付物 <a id="deliverables"></a>

| 块 | 内容 |
| --- | --- |
| 组件设计节点（76 md） | 落点改仓库根全路径（PC `frontend/packages/ui-ep/src/components/<域>/`、移动端按需 `ui-vant`；宿主 `frontend/apps/*`）；能力引用改类名（`useContainer` 片段 → 能力 `container` 等）；已实现件写现状（侧边菜单 / 异常与空状态 4 件 / 容器 8 件 / 基础类 3 件），未实现件写目标落点与回补波口径；每节点补「实现口径（2026-09-17，新体系）」注 |
| 原型 html（50 个） | 可见文案「片段」→「能力」（约 167 处）、`useCapabilityBase` 声明句改「`BaseCapability` 子类 + `manifest.ts` 登记」、路径 token 对齐；**href / 文件名 / JS 标识与逻辑零改动**（链接自洽复跑通过） |
| 布局设计 / 知识档案 / 其他 | 布局设计 5 个 html（工程路径 / 令牌路径 / 双端口径）+ 原型设计 13 / 概要设计 18 / 架构 29 移动端口径；知识档案「前端」20 篇路径 token（保留导航锚点 `#apps/desktop`）；需求 00（范围外口径）与 03/04（注块落点与术语）；[后端基类清单](../../../../../../后端基类清单.md) 对账行 |
| 统一规则 | 「片段」→「能力」；「双端同款」→「同接口多实现（+ 契约套件同一套断言）」；路径一律 `frontend/...`；历史留痕：真件标「已删除（历史经 git 追溯）」、未落码件标「按需形态（历史未落码）」 |

## 3. 验证结果 <a id="verify"></a>

| 命令 | 结果 |
| --- | --- |
| `check-base.py` | 通过（跨层引用 0 / 措辞残留 0 / 清单与磁盘一致 170 / 跨文档编号引用 0） |
| `check-links.py` | 通过（断链 0 / 失效锚点 0；期间修复知识档案锚点误替换 20 处：`#frontend/apps/desktop` → `#apps/desktop`） |
| `check-status.py` | 通过（374 项 0 不合规） |
| 残留扫描 | 现行文档（排除历史实施 / 测试 / 已完成任务记录）旧标记清零：`frontend-mobile` / `apps/desktop`（裸路径）/ `src/components/base` / `useCapabilityBase` / `双端同款`（仅存「废止 / 替代」说明语境）；误替换扫描（`能力能力` / `片段片段` / `N能力`）0 命中 |

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 知识档案路径 token 替换时误伤导航锚点（`#apps/desktop` → `#frontend/apps/desktop`，20 篇） | `check-links` 失效锚点报错定位后全量回修；复跑通过 |
| 2 | 子任务对域 05+ 的能力映射（如 `useDictStore` → `option-source`、`useDateValue` → `locale`）属职责推断 | 已在文中以「归并 / 按需」措辞留活口，随域 05+ 回补波核对（实施记录 25 遗留 1 同口径） |
| 3 | 原型 html 标题中旧命名重复（如 `（useInteractive / useInteractive）`，应为 `BaseXxx / useXxx`） | 未动（不属本波规则，避免误伤）；列入收尾润色项 |
| 4 | 交互类节点「组件内组合式落点」在插件域 vs 宿主 `apps/*` 之间的取舍 | 按「组件与组合式同域（插件）」落定；组合式确属宿主应用态的（偏好 / 主题 / 租户 / 标签桥接）落宿主，已在节点注明 |

## 5. 偏差与遗留 <a id="deviation"></a>

| # | 项 | 归口 / 去向 |
| --- | --- | --- |
| 1 | 历史记录保留旧路径 / 旧术语（各任务 `实施 / 测试`、已完成任务主文档与设计、计划与审计的历史段、`项目/00 /01` 阶段一记录） | 历史留痕（git 追溯）；新记录一律现行口径 |
| 2 | 组件设计契约与实现的少量命名差异 + 域 05+ 能力映射推断 | 随域 05+ 回补波逐项对齐（实施记录 25 遗留 1） |
| 3 | 原型 html 标题命名重复等收尾润色 | 低优先，随域 05+ 或文档批次顺带 |
| 4 | 《[组件设计总纲](../../../../../../设计/组件设计/01_组件设计_总览.md)》路线图「类别 / 节点」列仍用目录名（含「能力片段 / 片段机制」目录名） | 目录名不变（索引与磁盘一致）；如后续统一重命名目录，另立批次 |

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
