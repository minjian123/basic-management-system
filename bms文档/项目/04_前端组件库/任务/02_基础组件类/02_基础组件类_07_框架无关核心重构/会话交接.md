# 02-7 框架无关核心重构 · 会话交接（2026-09-17）

> 用途：跨会话交接说明——新会话先读本文件即可掌握进度、下一步与关键约束。
> [文档首页](../../../../../文档首页.md) › [02-7 框架无关核心重构](02_基础组件类_07_框架无关核心重构.md) ｜ [任务文档](02_基础组件类_07_框架无关核心重构.md) · [实施记录](实施/)

## 1. 一句话现状 <a id="status"></a>

专项 `02_07` **S0 ~ S5 六阶段全链完成**（含 S4 `ui-vant` 与移动端全量收口；`npm run check` 与双端宿主门禁全绿）；
**未推送**（推送后台 CI 需首跑验证，含基础镜像重建）；下一步：域 05+ 任务基线、`apps/desktop` 旧基座 / 片段层收口（另立）、Kiwi 平台侧用例置 `DISABLED`（用户执行）。

## 2. 总进度 <a id="progress"></a>

| 范围 | 状态 |
| --- | --- |
| 阶段四域 02（88h）/ 03（62h）/ 04（25h） | **全部完成并审计**（域 03 收官 `03_05`、域 04 收官 `04_04`） |
| 专项 `02_07` S0 骨架 / S1 机制接线 / S2 能力类化 / S3 `ui-ep` / S5 切换收口 | 完成（详见任务文档与实施记录 01 ~ 16） |
| S4a 共享逻辑与契约机制 | **完成**（core `domain/` + `contracts/` + `@bms/core/testing` 契约用例工厂；`useVirtualRange` 迁 `@bms/vue`；ui-ep 改造） |
| S4b `ui-vant` 插件与契约套件 | **完成**（9 件迁入 + 注入实现 + 组件契约套件双实现全绿 + CI / 镜像扩展） |
| S4c 移动端宿主收口与结构迁移 | **完成**（`frontend-mobile/` → `apps/mobile/` + 全量切 `@bms/*` + 旧层 / 旧件 / 28 旧 spec 删除 + 台账 / 审计 / 文档回写） |

## 3. 本会话（2026-09-17）完成 <a id="session"></a>

- **S4a（实施记录 17，≈2h）**：容器共享逻辑单份下沉 `packages/core/src/domain/`（`scroll-position` / `size` / `virtual-range` 纯函数）；四个注入契约迁 `packages/core/src/contracts/`（`view-resolver` 泛型去 Vue）+ 共享工厂；`@bms/core/testing` 契约用例工厂（`describeConfirmContract` / `describePermissionContract`）；`useVirtualRange` 投影迁 `@bms/vue`；ui-ep 去本地双份并接入契约套件。
- **S4b（实施记录 18，≈3h）**：`packages/ui-vant` 建立（Vant 4）并迁入 9 件；注入实现（`confirm` Vant 对话框按需加载 / `permission` 共享工厂）；组件契约工厂（`@bms/core/testing/components`）+ Vue 挂载适配（`@bms/vue/testing`）；mobile 9 个容器 spec 随迁（Kiwi 742 ~ 750 原号）；CI `core-check` 切 workspace 根依赖、`ci-frontend` 镜像增 workspace 根 `npm ci`（含 vant）、`build-base.sh` 哈希输入加根锁文件。
- **S4c（实施记录 19，≈4h）**：`git mv frontend-mobile apps/mobile`（历史保留，镜像路径 / job 名保持）；宿主全量切 `@bms/*`（adapters / stores / api / directives / utils / dev 页）+ `@bms/vue` 补 `useAccess` / `useFrontendBase`；旧 `src/base`（12）/ `components`（31 片段 + 4 域包装件 + 9 件）/ 28 旧 spec 删除；CI / 镜像 / 脚本路径对齐；预算经确认放宽至 140 KB（实测 127.6）；审计 §8 重跑、清单 / 规范 / 计划 / 任务 / README 回写。
- **验证**：根 `npm run check` = core 60 / vue 11 / ui-ep 78 / ui-vant 64 全绿（ui-ep 与 ui-vant 跑同一契约套件）；`apps/mobile` lint / vue-tsc / test（10 文件 / 58 用例，覆盖率 87.97% / 75.53%）/ build / budget（127.6 / 140 KB）全通过；`apps/desktop` lint + 234 用例全绿（无回归）。
- **确认口径**：S4 启动 12 项 + 设计补 3 项 + 预算 1 项（均 question 逐项确认；见 S4 详细设计 §2 与实施记录 17 ~ 19）。

## 4. 下一步 <a id="next"></a>

| # | 事项 | 说明 |
| --- | --- | --- |
| 1 | **推送后 CI 验证** | `git push` → 首跑 `ci-base-build` 重建镜像（workspace 根依赖，耗时增加）→ `core-check` / `frontend-check` / `frontend-mobile-check` / 两个 build job 全绿；推送需用户明确指令，推送后 `gl_watch_pipeline` 跟盯 |
| 2 | **Kiwi 平台侧置 `DISABLED`** | 执行清单：apps 侧 724 / 725、726 ~ 730、731 ~ 736、737、740 / 741、742 ~ 750 的 apps 侧执行；mobile 侧旧层 698 ~ 722 与 724 ~ 741 中 mobile 执行部分（台账 §2.1 已登记口径，2026-09-17 增补）；平台操作由用户执行 |
| 3 | **`apps/desktop` 旧层收口（建议另立批次）** | 宿主内旧 `src/base` 与 `src/components/base` 片段层仍在消费（stores / api / menuRoutes / 4 域包装件）；按 S4c 同口径收口（`useFrontendBase` / `useAccess` 等投影已就位可供 desktop 复用） |
| 4 | **域 05+（数据展示 / 表单 / 字段）** | 任务基线未建；片段层 / 能力层为组合轨输入 |
| 5 | 移动端缺口清单批次 | 反馈件（`EmptyState` / `LoadingMask` / `SkeletonBlock` / `ErrorPage`）等移动端缺失件另立批次（S4 严格同构口径） |

## 5. 关键约束与惯例（勿忘） <a id="rules"></a>

- **执行纪律**：待拍板事项一律 question 逐项确认（推荐项在前）；提交分笔（代码 / 文档）；**推送需用户明确指令**；全程中文；动手前先读《AI开发规范》与相关设计。
- **架构口径**：「同接口多实现 + 契约测试同一套断言」（契约用例工厂 `@bms/core/testing`）；能力避让组件根通用名（`busy` / `inactive` / `pending` / `metaVersion` / `permVisible` 等）；「不注册不可用」（`BaseError 10002`）；`packages/core` 禁依赖 Vue / UI 库 / 插件 / 宿主（护栏）；`useXxx` 只存在 `packages/vue/bindings`。
- **路径（S4c 后）**：宿主 `apps/desktop` / `apps/mobile`；插件 `packages/{core,vue,ui-ep,ui-vant}`；镜像内路径 `/opt/ci/frontend`、`/opt/ci/frontend-mobile`、`/opt/ci/workspace` 与 job 名保持；依赖桥模式见 `.gitlab-ci.yml`。
- **预算**：desktop 109.3 / 120 KB（合计 240 KB）；mobile 127.6 / 140 KB（2026-09-17 经确认放宽，理由入实施记录 19）；`ui-vant` 的 Vant 对话框走按需加载。
- **服务器**：睡眠时先 `python3 scripts/tools/wol/wake_mjbk.py` 唤醒（凭据在 `deploy/.env`）。
- **提交前全套**：`apps/desktop` 与 `apps/mobile`（lint / test / build / budget）+ bms 根 `npm run check` + `check-links.py` + `check-base.py`；CI 推送后 `gl_watch_pipeline` 跟盯。

## 6. 遗留项 <a id="todo"></a>

| # | 遗留 | 说明 |
| --- | --- | --- |
| 1 | Kiwi 平台侧旧件用例置 `DISABLED` | 台账 §2.1 已登记（apps 侧 + mobile 侧旧层）；平台操作待用户执行 |
| 2 | `apps/desktop` 旧基座 / 片段层收口 | 建议另立批次（同 S4c 口径）；审计 §8 已登记 |
| 3 | 域 05+（数据展示 / 表单 / 字段） | 任务基线未建；片段层（`packages/core/src/capabilities` 31 能力）为组合轨输入 |
| 4 | 移动端缺失件（反馈件等） | 另立「移动端缺口清单」批次 |
| 5 | `scripts/tools/governance/review_stage.py` / `collect_metrics.py` 路径对齐 | 已随 S4c 更新（`apps/desktop` / `apps/mobile`） |
| 6 | 包级 ESLint 未覆盖（`ui-ep` / `ui-vant`） | 如需包级 lint 另立工程化批次 |

## 7. 关键文件索引 <a id="files"></a>

| 类别 | 位置 |
| --- | --- |
| 设计 | [S4 详细设计](设计/01_详细设计_S4_ui-vant插件与移动端收口.md) · [架构设计 · 前端组件体系](../../../../../设计/架构设计/05_架构设计_前端组件体系.md)（「框架无关核心与插件架构」节） |
| 任务 | [02-7 任务文档](02_基础组件类_07_框架无关核心重构.md)（状态 + 阶段记录）· [实施记录 01 ~ 19](实施/) |
| 审计 | [前端基类与片段合规 §7 / §8](../../../审计/01_审计_前端基类与片段合规.md) · [前端审计规范](../../../../../规范/前端审计规范.md) |
| 清单 / 规范 | [前端基类清单](../../../../../前端基类清单.md)（§9 / §10）· [前端开发规范](../../../../../规范/前端开发规范.md)（§3 新体系分层） |
| 计划 | [前端组件库计划](../../../计划/01_计划_前端组件库.md)（§3 `02_07` 行） |
| 核心包 | `packages/core/src/`（base / mechanisms / capabilities / domain / contracts）· `packages/core/testing/`（契约用例工厂） · `packages/vue/src/bindings/`（投影） |
| 插件 | `packages/ui-ep/src/`（PC 34 件 + 注入点）· `packages/ui-vant/src/`（移动端 9 件 + 注入点） |
| 宿主 | `apps/mobile/src/adapters/`（注入接线 + 宿主根系 sink）· `apps/desktop/src/adapters/` · 两侧 `vite.config.ts`（`@bms/*` alias + 分包） |
| CI / 部署 | `.gitlab-ci.yml`（`core-check` / `frontend-check` / `frontend-mobile-check` 等）· `deploy/ci/{Dockerfile.frontend,build-base.sh}` |
| 测试仓库 | `test/test文档/用例/Kiwi用例台账.md`（§2.1 迁移与下线） |

> 本文档依《[文档生成规范](../../../../../规范/文档生成规范.md)》编写 · 随进度滚动更新（下一批次启动时先读本文件）
