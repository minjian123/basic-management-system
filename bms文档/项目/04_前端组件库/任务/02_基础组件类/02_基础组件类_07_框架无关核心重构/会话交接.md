# 02-7 框架无关核心重构 · 会话交接（2026-09-17）

> 用途：跨会话交接说明——新会话先读本文件即可掌握进度、下一步与关键约束。
> [文档首页](../../../../../文档首页.md) › [02-7 框架无关核心重构](02_基础组件类_07_框架无关核心重构.md) ｜ [任务文档](02_基础组件类_07_框架无关核心重构.md) · [实施记录](实施/)

## 1. 一句话现状 <a id="status"></a>

专项 `02_07` **S0 ~ S5 六阶段 + 收尾批次 S6（desktop 旧层收口）全链完成**；双端宿主均全量切 `@bms/*`；
遗留处理进行中：**Kiwi 平台置 `DISABLED` 已执行（724 ~ 737 / 740 / 741，16 条）**、**03-2-1 移动端反馈件已完成（Kiwi 751 ~ 755）**；下一步：包级 ESLint；域 05+ 任务基线留待下轮。

## 2. 总进度 <a id="progress"></a>

| 范围 | 状态 |
| --- | --- |
| 阶段四域 02（88h）/ 03（62h）/ 04（25h） | **全部完成并审计**（域 03 收官 `03_05`、域 04 收官 `04_04`） |
| 专项 `02_07` S0 ~ S5 六阶段 + S6 收尾 | **完成**（实施记录 01 ~ 20） |
| 03-2-1 移动端反馈件（嵌套子任务） | **完成**（`@bms/ui-vant` 四件 + 宿主 `useFeedback`；契约套件同一套断言；Kiwi 751 ~ 755；实施 / 测试记录各 01） |
| Kiwi 平台旧件用例停用 | **已执行并回读**（724 ~ 737 / 740 / 741 = 16 条 `DISABLED`；快照 150 条留痕） |
| 包级 ESLint（`frontend/packages/*`） | **完成（2026-09-17）**：根统一装依赖 + 各包 `eslint.config.js`（`skip-formatting` 交 Prettier）+ 根 `lint` / `check` 串联；`guard` 正则转义修正；四包零告警（CI `core-check` 自动覆盖） |
| 目录对称化（S8，2026-09-17） | **完成**：`apps/` 与 `packages/` 收进 `frontend/`（与 `backend/` 平级）——`frontend/apps/{desktop,mobile}` + `frontend/packages/{core,vue,ui-ep,ui-vant}`；CI / 镜像内路径（/opt/ci/workspace/frontend/packages）/ 脚本 / 文档全链对齐；实施记录 22 |
| 文档对齐（S9，2026-09-17） | **波 1 完成**（实施记录 23）：权威口径层；**波 2 完成**（实施记录 25）：组件设计 `02_基础组件类` 44 节点（机制 8 + 能力 31 + 域基类 5）实现章重写 + 总纲 / 文档首页索引同步；**波 3 完成**（实施记录 26）：其余 76 节点 + 原型 html 50 + 布局设计 / 知识档案 / 需求 / 索引扫尾——**S9 三波全部收口** |
| CI 分层放行（S10，2026-09-17） | **完成**（实施记录 24）：job 级 `changes` 按域放行（后端组 / 前端组 / 镜像档），纯基座文档推送只跑 `gate-smoke` + `base-integrity`（秒级）；MR 维持全量 |

## 3. 本会话（2026-09-17）完成 <a id="session"></a>

- **S4a（实施记录 17，≈2h）**：容器共享逻辑单份下沉 `frontend/packages/core/src/domain/`（`scroll-position` / `size` / `virtual-range` 纯函数）；四个注入契约迁 `frontend/packages/core/src/contracts/`（`view-resolver` 泛型去 Vue）+ 共享工厂；`@bms/core/testing` 契约用例工厂（`describeConfirmContract` / `describePermissionContract`）；`useVirtualRange` 投影迁 `@bms/vue`；ui-ep 去本地双份并接入契约套件。
- **S4b（实施记录 18，≈3h）**：`frontend/packages/ui-vant` 建立（Vant 4）并迁入 9 件；注入实现（`confirm` Vant 对话框按需加载 / `permission` 共享工厂）；组件契约工厂（`@bms/core/testing/components`）+ Vue 挂载适配（`@bms/vue/testing`）；mobile 9 个容器 spec 随迁（Kiwi 742 ~ 750 原号）；CI `core-check` 切 workspace 根依赖、`ci-frontend` 镜像增 workspace 根 `npm ci`（含 vant）、`build-base.sh` 哈希输入加根锁文件。
- **S4c（实施记录 19，≈4h）**：`git mv frontend/apps/mobile frontend/apps/mobile`（历史保留，镜像路径 / job 名保持）；宿主全量切 `@bms/*`（adapters / stores / api / directives / utils / dev 页）+ `@bms/vue` 补 `useAccess` / `useFrontendBase`；旧 `src/base`（12）/ `components`（31 片段 + 4 域包装件 + 9 件）/ 28 旧 spec 删除；CI / 镜像 / 脚本路径对齐；预算经确认放宽至 140 KB（实测 127.6）；审计 §8 重跑、清单 / 规范 / 计划 / 任务 / README 回写。
- **S6（实施记录 20，≈2h）**：`frontend/apps/desktop` 旧层收口（按 S4c 同口径）——旧 `src/base`（12）/ `src/components`（31 片段 + 4 域包装件）删除；`dynamic-routes` 下沉 core（`buildRoutes` + 反应式 routes）+ `@bms/vue` 新投影 `useDynamicRoutes`；宿主 adapters / stores / api / BasicLayout / menuRoutes 全切 `@bms/*`；审计 §9 重跑、清单 §9 / §10 全表重写、规范 / 计划 / 任务 / README 回写。
- **遗留处理（2026-09-17）**：① **Kiwi 置 `DISABLED`**（工具 `test/scripts/kiwi/update_status.py`，容器 Django shell；16 条 724 ~ 737 / 740 / 741 改后回读核对，快照 `2026-09-17_策展用例快照.json` 150 条）；② **03-2-1 移动端反馈件**（嵌套子任务：`ui-vant` 四件 + 契约套件 + 宿主 `useFeedback`；Kiwi 751 ~ 755 先登记后编码；详细设计 / 实施 / 测试记录均落；移动端预算 128.9 / 140 KB）；③ 包级 ESLint（本会话最后一项，实施后补记）。
- **验证（累计）**：根 `npm run check` = core 65 / vue 13 / ui-ep 78 / ui-vant 64 全绿；`frontend/apps/mobile`（lint / 58 用例 / build / 预算 127.6/140 KB）与 `frontend/apps/desktop`（lint / 77 用例 / build / 预算 208.9/240 KB）全通过；CI pipeline **249 全绿**（S4 推送后，含镜像重建与 mobile 路径 job）。
- **确认口径**：S4 启动 12 项 + 设计补 3 项 + 预算 1 项 + S6 归口 / dynamic-routes / 反馈件 / ESLint 4 项（均 question 逐项确认）。

## 4. 下一步 <a id="next"></a>

| # | 事项 | 说明 |
| --- | --- | --- |
| 1 | **包级 ESLint**（本会话） | 根统一装（eslint + typescript-eslint + eslint-plugin-vue + vue-eslint-parser）+ 各包 `eslint.config.js` + 根 `check` 串联；镜像随根锁重建；完成后提交推送并跟盯 CI |
| 2 | **域 05+ 任务基线** | 后续会话；含原 4 域包装件（`BaseInput` / `BaseDisplay` / `BaseTree` / `BaseEditor`）与域基类组合式按新体系重建；随批对齐组件设计契约与实现的少量命名差异 |
| 3 | ~~原型 html 标题命名重复等收尾润色~~ **已完成**（实施记录 27, 2026-09-17）：9 标题命名 + 7 机制 / 动态路由节点契约对齐 + 4 原型说明文案 | — |
| 4 | 移动端真机视觉核对（容器件 / 反馈件） | 随 `03_05` 组装复核与真机批次 |
| 5 | `export_cases.py` 前端对账与 `--include-ci` 口径缺口 | 台账 §6 待办（测试资产仓） |
| 6 | 推送与 CI | CI 分层放行（S10）推送后按新配置实跑验证（含代码路径，触发一次全量属预期）；其后纯文档推送秒级 |

## 5. 关键约束与惯例（勿忘） <a id="rules"></a>

- **执行纪律**：待拍板事项一律 question 逐项确认（推荐项在前）；提交分笔（代码 / 文档）；**推送需用户明确指令**；全程中文；动手前先读《AI开发规范》与相关设计。
- **架构口径**：「同接口多实现 + 契约测试同一套断言」（契约用例工厂 `@bms/core/testing`）；能力避让组件根通用名（`busy` / `inactive` / `pending` / `metaVersion` / `permVisible` 等）；「不注册不可用」（`BaseError 10002`）；`frontend/packages/core` 禁依赖 Vue / UI 库 / 插件 / 宿主（护栏）；`useXxx` 只存在 `frontend/packages/vue/bindings`。
- **路径（S4c 后）**：宿主 `frontend/apps/desktop` / `frontend/apps/mobile`；插件 `frontend/packages/{core,vue,ui-ep,ui-vant}`；镜像内路径 `/opt/ci/frontend`、`/opt/ci/frontend-mobile`、`/opt/ci/workspace` 与 job 名保持；依赖桥模式见 `.gitlab-ci.yml`。
- **预算**：desktop 109.3 / 120 KB（合计 240 KB）；mobile 127.6 / 140 KB（2026-09-17 经确认放宽，理由入实施记录 19）；`ui-vant` 的 Vant 对话框走按需加载。
- **服务器**：睡眠时先 `python3 scripts/tools/wol/wake_mjbk.py` 唤醒（凭据在 `deploy/.env`）。
- **提交前全套**：`frontend/apps/desktop` 与 `frontend/apps/mobile`（lint / test / build / budget）+ bms 根 `npm run check` + `check-links.py` + `check-base.py`；CI 推送后 `gl_watch_pipeline` 跟盯。

## 6. 遗留项 <a id="todo"></a>

| # | 遗留 | 说明 |
| --- | --- | --- |
| 1 | Kiwi 平台侧旧件用例置 `DISABLED` | **已完成（2026-09-17）**：724 ~ 737 / 740 ~ 741（16 条）停用并回读；台账 §2.1 / §2 行同步 |
| 2 | 移动端反馈件缺失清单 | **已完成（03-2-1，2026-09-17）**：四件 + 状态机（Kiwi 751 ~ 755） |
| 3 | 包级 ESLint（`frontend/packages/*`） | 本会话最后一项（完成后回写） |
| 4 | 域 05+（数据展示 / 表单 / 字段）任务基线 | 本轮不建；含 4 域包装件与域基类组合式按新体系重建 |
| 5 | 设计令牌下沉 `@bms/vue/styles` | 随品牌化任务评估（清单 §9 已登记） |
| 6 | 移动端真机核对（容器 / 反馈件） | 随 `03_05` 组装复核与真机批次 |

## 7. 关键文件索引 <a id="files"></a>

| 类别 | 位置 |
| --- | --- |
| 设计 | [S4 详细设计](设计/01_详细设计_S4_ui-vant插件与移动端收口.md) · [架构设计 · 前端组件体系](../../../../../设计/架构设计/05_架构设计_前端组件体系.md)（「框架无关核心与插件架构」节） |
| 任务 | [02-7 任务文档](02_基础组件类_07_框架无关核心重构.md)（状态 + 阶段记录）· [实施记录 01 ~ 20](实施/) |
| 审计 | [前端基类与片段合规 §7 / §8 / §9](../../../审计/01_审计_前端基类与片段合规.md) · [前端审计规范](../../../../../规范/前端审计规范.md) |
| 清单 / 规范 | [前端基类清单](../../../../../前端基类清单.md)（§9 / §10）· [前端开发规范](../../../../../规范/前端开发规范.md)（§3 新体系分层） |
| 计划 | [前端组件库计划](../../../计划/01_计划_前端组件库.md)（§3 `02_07` 行） |
| 核心包 | `frontend/packages/core/src/`（base / mechanisms / capabilities / domain / contracts）· `frontend/packages/core/testing/`（契约用例工厂） · `frontend/packages/vue/src/bindings/`（投影） |
| 插件 | `frontend/packages/ui-ep/src/`（PC 34 件 + 注入点）· `frontend/packages/ui-vant/src/`（移动端 13 件：容器 8 + 权限按钮 + 反馈件 4 + 注入点） |
| 反馈件（03-2-1） | [任务](../../03_布局组件类/03_布局组件类_02_异常与空状态/03_布局组件类_02_异常与空状态_01_移动端反馈件/03_布局组件类_02_异常与空状态_01_移动端反馈件.md)（如路径变更见 [域 03 任务目录](../../03_布局组件类/)）· [详细设计](../../03_布局组件类/03_布局组件类_02_异常与空状态/03_布局组件类_02_异常与空状态_01_移动端反馈件/设计/01_详细设计_03_02_01_移动端反馈件.md) · [实施](../../03_布局组件类/03_布局组件类_02_异常与空状态/03_布局组件类_02_异常与空状态_01_移动端反馈件/实施/01_实施_03_02_01_移动端反馈件.md) / [测试](../../03_布局组件类/03_布局组件类_02_异常与空状态/03_布局组件类_02_异常与空状态_01_移动端反馈件/测试/01_测试_03_02_01_移动端反馈件.md)记录 |
| 宿主 | `frontend/apps/mobile/src/adapters/`（注入接线 + 宿主根系 sink）· `frontend/apps/desktop/src/adapters/` · 两侧 `vite.config.ts`（`@bms/*` alias + 分包） |
| CI / 部署 | `.gitlab-ci.yml`（`core-check` / `frontend-check` / `frontend-mobile-check` 等）· `deploy/ci/{Dockerfile.frontend,build-base.sh}` |
| 测试仓库 | `test/test文档/用例/Kiwi用例台账.md`（§2.1 迁移与下线） |

> 本文档依《[文档生成规范](../../../../../规范/文档生成规范.md)》编写 · 随进度滚动更新（下一批次启动时先读本文件）
