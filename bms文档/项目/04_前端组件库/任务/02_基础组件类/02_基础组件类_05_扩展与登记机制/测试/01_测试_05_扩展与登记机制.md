# 扩展与登记机制测试记录

> 前端组件库 · 02 基础组件类 · 05 扩展与登记机制 · 测试记录

[文档首页](../../../../../../文档首页.md) › [02-5 扩展与登记机制](../02_基础组件类_05_扩展与登记机制.md) › 01 测试　|　[← 父任务](../02_基础组件类_05_扩展与登记机制.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02-5 扩展与登记机制](../02_基础组件类_05_扩展与登记机制.md) |
| 对应需求 | [02-5](../../../../需求/02_需求_基础组件类.md#r02-5) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_05_扩展与登记机制.md) |
| 实施记录 | [01 实施](../实施/01_实施_05_扩展与登记机制.md) |
| 测试日期 | 2026-09-16 |
| 测试人 | minjian |
| 测试环境 | Linux 开发机（mjpc）；Node v24.20.0 / npm 11.19.0；Vitest 5 + jsdom + `@vue/compiler-sfc` + `typescript`；双端独立执行 |
| Kiwi 用例 | **713**（继承护栏）、**714**（依赖边界）、**715**（清单对账）、**716**（演练与错误码对齐）——双端共用编号 |
| 结论 | 通过（双端各 22 个用例文件 / 171 条用例全绿，本任务新增 23 条；覆盖率与构建体积门禁达标） |

## 2. 测试范围与用例 <a id="scope"></a>

**先登记后编码**（《[测试规范](../../../../../../规范/测试规范.md)》「用例管理（Kiwi TCMS）」节）：本任务按「每类护栏一条」登记 4 条（双端共用编号），登记输入落测试资产仓 `scripts/kiwi/cases/2026-09-16_阶段四02-05_扩展与登记机制.json`。

| Kiwi | 用例 | 自动化文件（双端同款） | 条数 | 覆盖点 |
| --- | --- | --- | --- | --- |
| 713 | 继承护栏：基类链 / 组件包装 / 片段声明（AST） | `tests/guard-inheritance.spec.ts` | 6 | ① 机制层类父类链全绿；② 片段声明与域组合面全绿；③ 组件包装调用组件根与域组合式全绿；④ 违规继承 / 未声明片段 / 包装缺调用 fixture 被拦截；⑤ 未登记片段 key 与域组合式缺组合面 fixture 被拦截；⑥ 问题结构（文件 / 规则 / 说明）稳定 |
| 714 | 依赖边界：反向依赖 / UI 库 / 单向与循环 | `tests/guard-dependency.spec.ts` | 6 | ① 机制层 / 基类层依赖全绿；② 片段依赖图无环；③ 反向依赖 fixture（机制层 import 片段层）被拦截；④ UI 库与上层目录 fixture 被拦截；⑤ 未登记片段依赖与循环 fixture 被拦截；⑥ 横切库重复实现 fixture 被拦截 |
| 715 | 清单对账：漏登记 / 僵尸条目双向 | `tests/guard-manifest.spec.ts` | 5 | ① 清单 §5 与 `fragments.ts` 双向一致（key / depends / 条数 / 实现文件）；② 僵尸条目 fixture 被拦截；③ 漏登记 fixture 被拦截；④ depends 不一致 fixture 被拦截；⑤ 基类 / 域基类实现缺失 fixture 被拦截 |
| 716 | 新增流程演练与错误码对齐 | `tests/guard-evolution.spec.ts` | 6 | ① 新增流程步骤（清单 §8）齐备可执行；② 继承护栏违规 fixture 可拦截；③ 依赖护栏违规 fixture 可拦截；④ 对账护栏差异 fixture 可拦截；⑤ 正样例（当前仓库）三类护栏全绿；⑥ 错误码段位对齐（`19001` / `10001` / `10003` / `10005` / `30001`） |

合计 **23 条**（双端各跑一次；双端合计 171 条含既有用例）。

> 说明：本任务同时修改三处错误码常量（`error.ts` / `capability.ts` / `provider.ts`），既有用例（`base-error` / `base-registry` / `base-capability` / `base-placeholder`）以常量断言自动覆盖；仅 `base-registry.spec.ts` 标题码值同步（`10002` → `10003`）。

## 3. 执行记录与结果 <a id="run"></a>

### 3.1 frontend <a id="run-fe"></a>

| 命令 | 结果 |
| --- | --- |
| `npx prettier --check src/base src/components tests/base-*.spec.ts tests/guard-*.spec.ts tests/helpers` | 通过（本任务文件） |
| `npm run lint`（eslint --max-warnings 0） | 通过（0 error / 0 warning） |
| `npx vue-tsc -b` | 通过（无类型错误） |
| `npm run test:cov` | **22 个用例文件 / 171 条用例全绿**（本任务新增 23 条）；整体 88.08% 语句 / 79.48% 分支 / 85.96% 函数 / 87.96% 行 |
| `npm run build` + `npm run budget` | 构建成功；体积 **71.6 KB**（预算 85 KB）、最大单文件 51.8 KB（预算 63 KB），通过 |

### 3.2 frontend-mobile <a id="run-mo"></a>

| 命令 | 结果 |
| --- | --- |
| `npx prettier --check src/base src/components tests/base-*.spec.ts tests/guard-*.spec.ts tests/helpers` | 通过 |
| `npm run lint` | 通过 |
| `npx vue-tsc -b` | 通过 |
| `npm run test:cov` | **171 条用例全绿**；覆盖率与 frontend 一致（88.08 / 79.48 / 85.96 / 87.96） |
| `npm run build` + `npm run budget` | 构建成功；体积 **70.5 KB**（预算 83 KB）、最大单文件 70.1 KB（预算 83 KB），通过 |

### 3.3 双端一致性 <a id="run-dup"></a>

`diff -r` 比对双端实现与四份护栏用例（含 `tests/helpers/guard/`）逐字一致；护栏随 CI `npm run test:cov` 在 `frontend-check` / `frontend-mobile-check` 自动执行（未改 `.gitlab-ci.yml`）。

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 清单 §2 解析误报后端对照名（说明列含 `BaseProviderRegistry` 等） | 解析限定「基类 / 片段」列；`manifest.ts` 修正 |
| 2 | `useFieldContext` 被继承护栏误判为片段 | 增加 `nonFragmentFiles` 白名单（与清单 §5 注记一致） |
| 3 | 域组合式被误建片段依赖边 | `fragmentFileOf` 排除 `Base` 后缀文件 |
| 4 | 护栏若不可失败则无意义 | 三类违规 fixture + 正样例双口径断言 |
| 5 | 独立克隆无 `bms文档/` | `describe.skipIf` 跳过对账用例（CI 在 monorepo 内执行） |

## 5. 覆盖率 <a id="coverage"></a>

| 范围（双端一致） | 语句 | 分支 | 函数 | 行 |
| --- | --- | --- | --- | --- |
| 全工程 | 88.08% | 79.48% | 85.96% | 87.96% |

> **口径说明**：门禁阈值（`vitest.config.ts`）以**全工程**为单位（四项 ≥ 70%），本任务全工程四项均达标。护栏扫描核心位于 `tests/helpers/guard/`（测试代码，不计入 `src/` 覆盖率）；错误码常量修改由既有用例覆盖（`base-error` / `base-registry` / `base-capability` / `base-placeholder`）。

## 6. 偏差与遗留 <a id="deviations"></a>

| # | 项 | 去向 |
| --- | --- | --- |
| 1 | 护栏扫描范围限于 `src/base/` 与 `src/components/base/` | 后续组件任务接入时按规则表增行 |
| 2 | 契约测试套件（规范 §3.5 第 3 类）未单列 | 随各任务实现同步 |
| 3 | 清单对账依赖 monorepo 路径（独立克隆跳过） | 已知、可接受（设计 §10 注明） |
| 4 | 基础令牌 / 「不注册不可用」 | 计划 §3.1 第 10 行（布局域令牌任务）/ `10_02` |
| 5 | 上游设计登记项（概要设计 36 / 布局设计 html） | 对应设计更新（计划 §3.1 第 20 行） |

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
