# 01 测试 · 总基类与固定三段

> 前端组件库 · 02 基础组件类 · 子任务 01（需求 02-1）· 测试记录

[文档首页](../../../../../../文档首页.md) › [02 基础组件类](../../02_基础组件类.md) › [01 总基类与固定三段](../02_基础组件类_01_总基类与固定三段.md) › 测试记录　|　[← 任务](../02_基础组件类_01_总基类与固定三段.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 总基类与固定三段](../02_基础组件类_01_总基类与固定三段.md) |
| 对应需求 | [02-1](../../../../需求/02_需求_基础组件类.md#r02-1) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_01_总基类与固定三段.md) |
| 实施记录 | [01 实施](../实施/01_实施_01_总基类与固定三段.md) |
| 测试日期 | 2026-09-18 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1） |
| Kiwi 用例 | 待平台登记后回读回填（先写用例、编号后补） |
| 结论 | 通过（16 用例全绿；`npm run core:check` 全绿） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例 | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 总基类 · 命名空间与版本 | 单元 | `tests/base-object.spec.ts` | 缺省值与显式值 |
| 总基类 · 日志委托 | 单元 | 同上 | 注入 sink 收到（级别 / 命名空间前缀 / meta） |
| 总基类 · 错误上报委托 | 单元 | 同上 | 注入 reporter 收到 error 与 meta |
| 总基类 · 配置读取 | 单元 | 同上 | 注入源命中 + fallback |
| 总基类 · 生命周期 | 单元 | 同上 | `dispose` 幂等、`onDispose` 只执行一次、`isDisposed` |
| 能力域基类 · 继承与声明 | 单元 | `tests/capability.spec.ts` | 继承总基类、`key` / `depends` / `describe` |
| 插件基类 · 继承链 | 单元 | `tests/pluggable.spec.ts` | `BasePluggable → BaseCapability → BaseObject` |
| 插件基类 · 字段 | 单元 | 同上 | `pluginKey` / `pluginName` / 缺省 `contractVersion` / `key === pluginKey` |
| 插件基类 · 元信息与装配 | 单元 | 同上 | `describe` 格式、`setup` 缺省空实现 |
| 核心框架无关护栏 | 单元 | `tests/guard-core-framework-agnostic.spec.ts` | 新源码仍零框架依赖（护栏随门禁跑） |

## 3. 执行记录与结果 <a id="run"></a>

```bash
npm run core:check
```

```text
> core:typecheck  tsc --noEmit -p tsconfig.json        （通过）
> core:lint       eslint . --max-warnings 0           （零告警）
> core:test       vitest run
  ✓ tests/guard-core-framework-agnostic.spec.ts (3)
  ✓ tests/capability.spec.ts (2)
  ✓ tests/pluggable.spec.ts (4)
  ✓ tests/error-codes.spec.ts (2)
  ✓ tests/base-object.spec.ts (5)
  Test Files  5 passed (5)   Tests  16 passed (16)
```

## 4. 问题与处置 <a id="issues"></a>

- 无测试过程问题（首次全绿）。

## 5. 覆盖率 <a id="coverage"></a>

- 本期核心门禁为 `vitest run`；覆盖率门禁（整体 ≥ 70%）随宿主 / 插件包落地接入 `test:cov`（归口域 03+）。
- 本任务源码面：`base/BaseObject.ts`、`mechanisms/capability.ts`、`mechanisms/pluggable.ts`（均被对应用例覆盖）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项：能力依赖校验 / 登记（`02_03`）、注册与解析（`02_05`）、sink 宿主接线（后续宿主包）。
- Kiwi 编号回读后回填本记录。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
