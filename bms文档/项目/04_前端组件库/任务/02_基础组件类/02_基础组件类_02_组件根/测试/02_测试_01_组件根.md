# 02 测试 · 组件根

> 前端组件库 · 02 基础组件类 · 子任务 02（需求 02-2）· 测试记录

[文档首页](../../../../../../文档首页.md) › [02 基础组件类](../../02_基础组件类.md) › [02 组件根](../02_基础组件类_02_组件根.md) › 测试记录　|　[← 任务](../02_基础组件类_02_组件根.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02 组件根](../02_基础组件类_02_组件根.md) |
| 对应需求 | [02-2](../../../../需求/02_需求_基础组件类.md#r02-2) |
| 详细设计 | [02 详细设计](../设计/02_详细设计_01_组件根.md) |
| 实施记录 | [02 实施](../实施/02_实施_01_组件根.md) |
| 测试日期 | 2026-09-18 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1） |
| Kiwi 用例 | 待平台登记后回读回填（先写用例、编号后补） |
| 结论 | 通过（44 用例全绿；`pnpm run core:check` 全绿） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例 | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 继承链 | 单元 | `tests/component.spec.ts` | `BaseComponent → BasePluggable → BaseCapability → BaseObject` |
| 标识 / 插件键 / 类名 | 单元 | 同上 | `identifier` / `pluginKey` / `key` / `nsClass` |
| 令牌属性协议 | 单元 | 同上 | `rootAttrs` / `stateClasses` 随档位与状态变化 |
| attrs 透传 | 单元 | 同上 | 保留键与 `_` 前缀过滤、其余透传 |
| 生命周期 | 单元 | 同上 | 多监听、取消幂等、`dispose` 广播 `unmount` |
| 监听异常 | 单元 | 同上 | 上报且不阻断其余监听 |
| 机制装配点 | 单元 | 同上 | `mountMechanism` → `mechanisms`；释放清空 |
| 注释齐备护栏 | 单元 | `tests/guard-jsdoc.spec.ts` | 新源码均有 JSDoc |
| 核心框架无关护栏 | 单元 | `tests/guard-core-framework-agnostic.spec.ts` | 新源码零框架依赖 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
pnpm run core:check
```

```text
> core:typecheck  tsc --noEmit -p tsconfig.json        （通过）
> core:lint       eslint . --max-warnings 0           （零告警）
> core:test       vitest run
  Test Files  13 passed (13)   Tests  44 passed (44)
```

## 4. 问题与处置 <a id="issues"></a>

- 无测试过程问题（首次全绿）。

## 5. 覆盖率 <a id="coverage"></a>

- 本期核心门禁为 `vitest run`；覆盖率门禁（整体 ≥ 70%）随宿主 / 插件包落地接入（归口域 03+）。
- 本任务源码面：`base/BaseComponent.ts`（被 `component.spec.ts` 覆盖）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项：投影层 attrs → 真实 DOM 与令牌属性选择器（渲染插件 / `02_06`）。
- Kiwi 编号回读后回填本记录。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
