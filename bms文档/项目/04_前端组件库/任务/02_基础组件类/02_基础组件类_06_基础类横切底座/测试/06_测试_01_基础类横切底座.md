# 06 测试 · 基础类横切底座

> 前端组件库 · 02 基础组件类 · 子任务 06（需求 02-6）· 测试记录

[文档首页](../../../../../../文档首页.md) › [02 基础组件类](../../02_基础组件类.md) › [06 基础类横切底座](../02_基础组件类_06_基础类横切底座.md) › 测试记录　|　[← 任务](../02_基础组件类_06_基础类横切底座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [06 基础类横切底座](../02_基础组件类_06_基础类横切底座.md) |
| 对应需求 | [02-6](../../../../需求/02_需求_基础组件类.md#r02-6) |
| 详细设计 | [06 详细设计](../设计/06_详细设计_01_基础类横切底座.md) |
| 实施记录 | [06 实施](../实施/06_实施_01_基础类横切底座.md) |
| 测试日期 | 2026-09-18 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1） |
| Kiwi 用例 | 待平台登记后回读回填（先写用例、编号后补） |
| 结论 | 通过（核心 116 + vue 绑定 2 + PC 宿主 19 用例全绿） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例组 | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 格式化 | 单元 | `tests/domain-format.spec.ts` | 日期时区 / 金额货币 / 千分位 / 百分比 / 文件大小 / 时长 / 相对时间 / 脱敏 / 非法值占位 |
| 校验器 | 单元 | `tests/domain-validators.spec.ts` | 必填独立 / 空值跳过 / 边界 |
| 权限判定 | 单元 | `tests/domain-permission-token.spec.ts` | `any` / `all` / `not` 与空要求 |
| 令牌解析 | 单元 | 同上 | 点分路径命中与缺失 |
| 请求契约 | 单元 | `tests/request-contract.spec.ts` | 未注入占位抛 `19001`；注入后按配置调用 |
| 注释齐备 / 框架无关护栏 | 单元 | `tests/guard-*.spec.ts` | 新源码均有 JSDoc、零框架依赖 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
pnpm run core:check
```

```text
> core:typecheck  tsc --noEmit -p tsconfig.json        （通过）
> core:lint       eslint . --max-warnings 0           （零告警）
> core:test       vitest run
  Test Files  22 passed (22)   Tests  116 passed (116)
```

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 空要求下 `not` 判定错误 | 改为 `mode !== 'any'` |
| 2 | 模块级常量缺 JSDoc | 补 JSDoc |

## 5. 覆盖率 <a id="coverage"></a>

- 本期核心门禁为 `vitest run`；覆盖率门禁（整体 ≥ 70%）随宿主 / 插件包落地接入（归口域 03+）。
- 本任务源码面：`domain/{format,validators,permission,token}.ts`、`contracts/request.ts` 均被对应用例覆盖。

## 6. 偏差与遗留 <a id="deviations"></a>

- 宿主用例：`apps/desktop` `tests/{api,perm,format,composables,session,tokens}.spec.ts`（Axios 适配装配未在单测内造网，改用核心 `configureRequestAdapter` 注入假适配器；`tokens.scss` 以文本断言校验选择器）。
- 未覆盖项：移动端宿主与 `ui-vant`（本阶段范围外）；401 刷新真实链路与登录页（认证任务）。
- Kiwi 编号回读后回填本记录。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
