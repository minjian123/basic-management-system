# 权限指令测试记录

> 前端组件库 · 02 基础组件类 · 06 基础类横切底座 · 嵌套 02 权限指令 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [02-6 基础类横切底座](../../02_基础组件类_06_基础类横切底座.md) › [02-6-2 权限指令](../../02_基础组件类_06_基础类横切底座_02_权限指令/02_基础组件类_06_基础类横切底座_02_权限指令.md) › 01 测试　|　[← 父任务](../../02_基础组件类_06_基础类横切底座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02-6-2 权限指令](../../02_基础组件类_06_基础类横切底座_02_权限指令/02_基础组件类_06_基础类横切底座_02_权限指令.md) |
| 对应需求 | [02-6](../../../../../需求/02_需求_基础组件类.md#r02-6) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_06_02_权限指令.md) |
| 实施记录 | [01 实施](../实施/01_实施_06_02_权限指令.md) |
| 测试日期 | 2026-09-16 |
| 测试人 | minjian |
| 测试环境 | Linux 开发机（mjpc）；Node v24.20.0 / npm 11.19.0；Vitest 5 + jsdom + `@vue/test-utils`（`attachTo` 真实 DOM）；双端独立执行 |
| Kiwi 用例 | **719**（指令）、**720**（判定与 store）——双端共用编号 |
| 结论 | 通过（双端各 26 个用例文件 / 197 条用例全绿，本任务新增 11 条；覆盖率与构建体积门禁达标） |

## 2. 测试范围与用例 <a id="scope"></a>

**先登记后编码**（《[测试规范](../../../../../../../规范/测试规范.md)》「用例管理（Kiwi TCMS）」节）：本任务按「指令 / 判定」分层登记 2 条（双端共用编号），登记输入落测试资产仓 `scripts/kiwi/cases/2026-09-16_阶段四02-06-02_权限指令.json`。

| Kiwi | 用例 | 自动化文件（同接口多实现） | 条数 | 覆盖点 |
| --- | --- | --- | --- | --- |
| 719 | 权限指令：anyOf / .all / .not / 版本重评 | `tests/directive-perm.spec.ts` | 6 | ① 有权限保留 / 无权限移除真实 DOM；② 数组默认 anyOf；③ `.all` 全满足；④ `.not` 取反；⑤ 空值 / 空串 / 空数组不限制；⑥ 版本变更重评（恢复回插 / 收回再移除）；⑦ `updated` 值变化重评；⑧ 卸载清理 |
| 720 | 权限判定：store / hasPerm / canAccess / PermButton | `tests/permission.spec.ts` | 5 | ⑨ store 占位（无 loader 不请求、空集降级）；⑩ `load()` 装配集合 / 版本 / 菜单 / `loaded`；⑪ `reset()` 清空；⑫ `permissions` Set / `permVersion` / `setCodes` 版本递增；⑬ `hasPerm` anyOf / all / 空数组；⑭ `canAccess`（码 / 数组 / 节点对象 / `public` / 空对象 / `null`）；⑮ `filterRoutes` 公开与无 permission 保留、递归过滤；⑯ `PermButton` hide；⑰ `PermButton` disable + 提示 + 点击拦截 + 权限恢复后可点 |

合计 **11 条**（双端各跑一次；双端合计 197 条含既有用例）。

## 3. 执行记录与结果 <a id="run"></a>

### 3.1 frontend/apps/desktop <a id="run-fe"></a>

| 命令 | 结果 |
| --- | --- |
| `npx prettier --check src/api src/i18n src/stores/permission.ts src/stores/useUserStore.ts src/utils/perm.ts src/utils/useRequest.ts src/utils/useListPage.ts src/directives src/components/common src/main.ts tests/{directive-perm,permission,http,request,use-list-page,base,utils}.spec.ts` | 通过（本任务文件） |
| `npm run lint`（eslint --max-warnings 0） | 通过（0 error / 0 warning） |
| `npx vue-tsc -b` | 通过（无类型错误） |
| `npm run test:cov` | **26 个用例文件 / 197 条用例全绿**（本任务新增 11 条）；整体 88.51% 语句 / 79.62% 分支 / 86.69% 函数 / 88.41% 行 |
| `npm run build` + `npm run budget` | 构建成功；体积 **94.5 KB**（预算 100 KB）、最大单文件 71.7 KB（预算 75 KB），通过 |

### 3.2 frontend/apps/mobile <a id="run-mo"></a>

| 命令 | 结果 |
| --- | --- |
| 同左（`src/main.ts` 除外，按移动端入口注册指令） | prettier / lint / `vue-tsc` 通过 |
| `npm run test:cov` | **197 条用例全绿**；覆盖率与 frontend/apps/desktop 一致（88.51 / 79.62 / 86.69 / 88.41） |
| `npm run build` + `npm run budget` | 构建成功；体积 **77.4 KB**（预算 83 KB）、最大单文件 77.0 KB（预算 83 KB），通过 |

### 3.3 双端一致性 <a id="run-dup"></a>

`frontend/apps/*/src/stores/permission.ts`、`frontend/apps/*/src/utils/perm.ts`、`frontend/apps/*/src/directives/perm.ts`、`frontend/packages/ui-ep/src/components/common/PermButton.vue`、`frontend/apps/*/src/i18n/` 与两份用例 `diff` 逐字一致；`main.ts` 各自入口注册同款指令。

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 「移除 DOM」断言依赖组件根引用误判 | 用例改 `attachTo` + `document.querySelector` 断言真实 DOM |
| 2 | `.all` 用例期望错误 | 改 `['a','c']` 验证移除 |
| 3 | 空串权限码被当码值 | 归一统一过滤空串 / 空白 |
| 4 | `permVersion` 联合类型自增报错 | 数值自增 / 时间戳回退 |

## 5. 覆盖率 <a id="coverage"></a>

| 范围（双端一致） | 语句 | 分支 | 函数 | 行 |
| --- | --- | --- | --- | --- |
| 全工程 | 88.51% | 79.62% | 86.69% | 88.41% |
| `src/directives`（指令） | 95.65% | 89.28% | 100% | 95.34% |
| `src/stores`（含权限 store） | 96.72% | 70% | 100% | 96.55% |

> **体积口径**：frontend/apps/desktop 94.5 / 71.7 KB（预算 100 / 75，2026-09-16 调整）；frontend/apps/mobile 77.4 / 77.0 KB（预算 83 / 83）。

## 6. 偏差与遗留 <a id="deviations"></a>

| # | 项 | 去向 |
| --- | --- | --- |
| 1 | 路由守卫注册（登录态 + `meta.perm` + 403 跳转） | 登录页 / 菜单任务（计划 §3.1 第 24 行） |
| 2 | 菜单 / 权限接口接入（`configureLoader`） | 认证 / 菜单任务（计划 §3.1 第 25 行） |
| 3 | `PermButton` 皮肤包装 | 组件任务（计划 §3.1 第 26 行） |
| 4 | 移动端指令同款（节点原「不用 PC 指令」调整） | 已回写节点实现口径 |

> 本文档依《[文档生成规范](../../../../../../../规范/文档生成规范.md)》编写
