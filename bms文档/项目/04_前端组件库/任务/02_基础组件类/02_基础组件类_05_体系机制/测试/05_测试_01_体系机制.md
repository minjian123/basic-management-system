# 05 测试 · 体系机制

> 前端组件库 · 02 基础组件类 · 子任务 05（需求 02-5）· 测试记录

[文档首页](../../../../../../文档首页.md) › [02 基础组件类](../../02_基础组件类.md) › [05 体系机制](../02_基础组件类_05_体系机制.md) › 测试记录　|　[← 任务](../02_基础组件类_05_体系机制.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [05 体系机制](../02_基础组件类_05_体系机制.md) |
| 对应需求 | [02-5](../../../../需求/02_需求_基础组件类.md#r02-5) |
| 详细设计 | [05 详细设计](../设计/05_详细设计_01_体系机制.md) |
| 实施记录 | [05 实施](../实施/05_实施_01_体系机制.md) |
| 测试日期 | 2026-09-18 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1） |
| Kiwi 用例 | 待平台登记后回读回填（先写用例、编号后补） |
| 结论 | 通过（35 用例全绿；`npm run core:check` 全绿） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例 | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 注册表 · 登记 / 未命中 / 保序 / 快照 | 单元 | `tests/registry.spec.ts` | `get` 未命中 `undefined`、`keys` / `values` 保序、只读快照 |
| 注册表 · 同键拒重 | 单元 | 同上 | 抛 `BaseError(10003)` |
| 注册项 · 组合轨 | 单元 | `tests/provider.spec.ts` | `BaseProvider` 为 `BaseCapability` 且非 `BasePluggable` |
| 工厂 · 创建 | 单元 | 同上 | `BaseFactory.create` |
| 资源 · 逆序释放 | 单元 | `tests/resource.spec.ts` | 逆序；单个失败不阻断并上报 |
| 资源 · 释放后拒登 | 单元 | 同上 | 抛 `BaseError` |
| 订阅 · 释放取消 | 单元 | 同上 | 取消函数随释放调用 |
| 占位三件 | 单元 | `tests/placeholder.spec.ts` | `placeholder` / `reason`、空实现无副作用、未实现桩抛 `19001` |
| 错误基座 | 单元 | `tests/error.spec.ts` | `instanceof Error` / 堆栈 / `code` / `userMessage` / `cause` / 总基类公共面 |
| 稳定序列化 / 契约基类 | 单元 | `tests/contracts.spec.ts` | 键排序、实体软删除、分页 offset、`BaseApi` 路径与幂等键 |
| 核心框架无关护栏 | 单元 | `tests/guard-core-framework-agnostic.spec.ts` | 新源码仍零框架依赖 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
npm run core:check
```

```text
> core:typecheck  tsc --noEmit -p tsconfig.json        （通过）
> core:lint       eslint . --max-warnings 0           （零告警）
> core:test       vitest run
  Test Files  11 passed (11)   Tests  35 passed (35)
```

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | `BaseEntity.version` 与根字段冲突 | 改名 `recordVersion`（回写设计 / 清单；见实施记录 §4） |

## 5. 覆盖率 <a id="coverage"></a>

- 本期核心门禁为 `vitest run`；覆盖率门禁（整体 ≥ 70%）随宿主 / 插件包落地接入（归口域 03+）。
- 本任务源码面：`base/mixin.ts`、`mechanisms/{error,registry,provider,factory,resource,subscription,placeholder}.ts`、`contracts/{api,data-object,entity,page-query}.ts`、`domain/serialize.ts`（均被对应用例覆盖）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项：扩展点注册表最小实现（域 10）、`BaseApi` 请求执行（`02_06`）、占位降级渲染（组件域）。
- Kiwi 编号回读后回填本记录。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
