# 01 测试 · 图表卡

> 前端组件库 · 07 展示类 · 06 图表卡（需求 07-6）· 测试记录

[文档首页](../../../../../../文档首页.md) › [07 展示类](../../07_展示类.md) › [06 图表卡](../07_展示类_06_图表卡.md) › 测试记录　|　[← 实施记录](../实施/01_实施_01_图表卡.md)　[详细设计 →](../设计/01_详细设计_01_图表卡.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | 07_06 图表卡（[任务](../07_展示类_06_图表卡.md)） |
| 对应需求 | [07-6](../../../../需求/07_需求_展示类.md#r07-6) |
| 依据 | [详细设计](../设计/01_详细设计_01_图表卡.md)（定稿 `97ee1009`）+ [实施记录](../实施/01_实施_01_图表卡.md) |
| 测试日期 | 2026-09-19 |
| 测试人 | minjian |
| 测试环境 | Ubuntu 开发机 · Vitest 5（`core` 纯 TS / `ui-ep` jsdom · Vue Test Utils）· chromium headless（开发态核对页） |
| Kiwi 用例 | 775（分类「平台骨架」/ P2 / CONFIRMED / 标签「自动化」） |
| 结论 | 全部用例通过；完成标准达标（图表卡渲染与四态 / 主题 / 分包可用、`BaseChart` 契约与登记就位、核对页 12/12） |

## 2. 用例清单与结果 <a id="cases"></a>

| 用例文件 | 覆盖范围 | 条数 | 结果 |
| --- | --- | --- | --- |
| `core/tests/domain-chart.spec.ts` | 类型集（13 类、未知回落、数值字段判定）；映射归一（默认 / 剔脏引用去重 / 空映射回落）；配置归一（缺省确定值 / 色板序号夹取）；主题（令牌读取与兜底 / 模式解析）；空判定（无结果 / 空行 / 全非数值 / `table` / 有效数值）与数值转换；选项生成（柱多度量、饼无轴、地图 / 仪表盘 / 雷达 / 散点、高级覆盖、`table` 空选项）；合并策略（全量替换判定 / 深合并 / 节流 / 脏比对 / 色板旋转） | 17 | 通过 |
| `core/tests/chart.spec.ts` | 契约套件 `describeChartContract`（10 条，桩引擎轨迹）；能力身份与依赖图；默认配置；占位与就绪门控（未就绪零请求 / 不写引擎 / 导出 `undefined`）；四态与引擎（空数据不初始化、取数成功置 `ready`、失败置 `error`、`table` 不初始化、销毁释放、视图与主题模式） | 10 | 通过 |
| `ui-ep/tests/display-chart.spec.ts` | 投影 `useBaseChart`（占位零请求 / 就绪注入配置与数据生成选项）；`ChartRenderer`（未就绪降级与分包标记 / 就绪装载引擎渲染画布 / 空数据不初始化 / 加载态 / 卸载释放）；`ChartCard`（未就绪降级 / 就绪卡头与卡头事件 / 注入引擎工厂渲染画布与视图切换 / 脚注 / 详情、移除与状态事件） | 12 | 通过 |
| `ui-ep/tests/display-placeholder.spec.ts`（既有，未改动） | `07_01` 冻结契约回归（`ChartCard` 占位降级、就绪态卡头与 `data-chart-type`、刷新 / 导出 / 图表点击事件） | — | 通过 |
| `core/tests/guard-manifest.spec.ts` / `guard-jsdoc.spec.ts` / `guard-core-framework-agnostic.spec.ts` | 能力登记对账（新增 `chart` 键）/ 注释齐备 / 核心框架无关 | — | 通过 |
| `ui-ep/tests/guard-component-inheritance.spec.ts` / 宿主 `guard-module-style.spec.ts` | 组件挂链（两件均实质挂链）/ 样式作用域（`scoped`、无 `:global`） | — | 通过 |

## 3. 验收映射 <a id="mapping"></a>

| 验收点（需求 07-6 / 任务 07_06） | 用例 | 结果 |
| --- | --- | --- |
| 图表卡可挂数据集渲染 | `display-chart.spec.ts`（注入引擎工厂渲染画布）+ 核对页 ③ | 通过 |
| 令牌色板与深浅主题正确 | `domain-chart.spec.ts`（令牌主题）+ `chart.spec.ts`（主题重建）+ 核对页 ⑦ | 通过 |
| 四态完备（加载 / 空 / 错误 / 降级） | `chart.spec.ts` + `display-chart.spec.ts`（四态）+ 核对页 ⑫ | 通过 |
| 独立分包生效（首屏体积预算内） | `display-chart.spec.ts`（分包标记）+ 宿主 `build` / `budget`（入口块口径） | 通过 |
| `BaseChart` 完成清单登记与契约用例 | `guard-manifest.spec.ts` + `describeChartContract` + 《前端基类清单》§2.1 行 68 | 通过 |
| Vitest 覆盖主题与四态分支 | 上述核心 + ui-ep 用例 | 通过 |
| `vue-tsc`、ESLint、Vitest 全通过 | 根 `npm run check` + 宿主 `vue-tsc -b` / `lint` / `test` | 通过 |

## 4. 覆盖率与门禁 <a id="coverage"></a>

| 命令 | 结果 |
| --- | --- |
| `npm run check`（core + vue + ui-ep） | 56 + 2 + 45 文件 / 658 + 5 + 555 用例全绿 |
| 宿主 `npx vue-tsc -b` / `npm run lint` | 通过 |
| 宿主 `npm run test` | 通过（35 用例） |
| 宿主 `npm run build` / `npm run budget` | 通过（首屏入口块 119.5 KB / 260 KB；最大单文件 104.1 KB / 150 KB；异步块 14 个不计入首屏） |
| 开发态核对页（chromium headless `--dump-dom`） | 12 / 12 自检项通过 |
| `check-links.py` | 通过（断链 0 / 失效锚点 0） |

新增模块覆盖率：`core/src/domain/chart.ts`、`core/src/capabilities/chart.ts`、`core/testing/chart.ts`、`ui-ep/src/composables/useBaseChart.ts` 与两件组件的分支均由上述用例覆盖（含占位零请求、空数据不初始化、类型全量替换、主题重建、节流、导出与释放、分包边界）。

## 5. 问题与偏差 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 核对页首轮 8/12（标题空 / `data-chart-type` 取默认 / ECharts 未初始化） | 按实施记录第 4 节第 6 项处置（补 `:title` 与 `:chart-type`、`ChartRenderer` 增 scoped 撑满样式、`effectiveKind` 配置优先），复跑 12/12 |
| 2 | `ChartCard` 状态事件在挂载前已结算未上抛 | 状态 watcher 加 `{ immediate: true }`，用例断言 `state-change` 通过 |
| 3 | 组件基类未被清单登记致核心护栏红 | 先登记《前端基类清单》再跑核心用例，护栏转绿 |

## 6. 遗留 <a id="leftover"></a>

- **真实数据集取数接口联调**（`POST /rpt/datasets/{id}/query`）随报表 BI 窗口；处理函数未注入即占位零请求。
- **通用表格真实实现**（`07_05`）：图表卡数据表视图本期复用 `07_01` 占位 `DataTable`，待交付后升级。
- **地图地理数据扩充**：本期先落懒加载机制与 `china` 一份，其余地区按需扩展。
- **工作台页面集成**（布局卡槽 / 编辑面板）归首页工作台任务。
- **移动端实现**：本期仅 PC，契约套件就位供后续同契约多实现。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
