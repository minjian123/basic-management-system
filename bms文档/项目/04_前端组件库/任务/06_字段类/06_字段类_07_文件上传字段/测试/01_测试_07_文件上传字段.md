# 01 测试 · 文件上传字段

> 前端组件库 · 06 字段类 · 07 文件上传字段（需求 06-7）· 测试记录

[文档首页](../../../../../../文档首页.md) › [06 字段类](../../06_字段类.md) › [07 文件上传字段](../06_字段类_07_文件上传字段.md) › 测试记录　|　[← 实施记录](../实施/01_实施_07_文件上传字段.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | 07 文件上传字段（[任务](../06_字段类_07_文件上传字段.md)） |
| 对应需求 | [06-7](../../../../需求/06_需求_字段类.md#r06-7) |
| 详细设计 | [01_详细设计_07_文件上传字段.md](../设计/01_详细设计_07_文件上传字段.md) |
| 实施记录 | [01_实施_07_文件上传字段.md](../实施/01_实施_07_文件上传字段.md) |
| 测试日期 | 2026-09-21 |
| 测试人 | minjian |
| 测试环境 | Ubuntu 开发机 · Vitest 5（node + jsdom）· Vue 3.5 · Element Plus（组件替身）· chromium（CDP 轮询导出 DOM）· cropperjs 2.2.0 |
| Kiwi 用例 | 965（分类「平台骨架」/ P2 / CONFIRMED / 标签「自动化」；`// kiwi_id: 965` 标注于 `core/tests/{domain-file,upload-engine,file-upload}.spec.ts`、`ui-ep/tests/field-file-upload.spec.ts`） |
| 结论 | 全部通过（core 1070 + ui-ep 864 用例；核对页 12/12（桩模式）；既有冻结契约与消费方回归原样通过） |

## 2. 测试范围与用例 <a id="scope"></a>

| 层次 | 文件 | 覆盖范围 |
| --- | --- | --- |
| 核心 · 领域 | `core/tests/domain-file.spec.ts` | 增量 SHA-256（标准向量 / 分块一致 / 非破坏性）、类型解析与双重匹配、校验（空文件 / 类型 / 大小 / 图片尺寸比例）、压缩决策（超边长 / 超体积 / 超大图跳过）、元数据归一（snake_case / 失效 / 尺寸 / 会话与分片结果）、受控值归一与本地占位、选择上限 / 排序 / 合并、模式决策与分片计划、进度聚合与缺失分片、退避与错误码 |
| 核心 · 引擎 | `core/tests/upload-engine.spec.ts` | 驱动 `describeUploadEngineContract`（占位零请求、秒传零上传、整包 / 分片决策、并发、失败重试、断点续传、未覆写回落、取消、移除与重置）；身份依赖（父 `BasePlaceholderState` / `upload-engine`）；重试复用会话与已传分片 |
| 核心 · 文件族 | `core/tests/file-upload.spec.ts` | 驱动 `describeFileUploadContract`（校验、上限、受控值同步、排序、批量回显与失效、图片校验、预览下载回落）；身份依赖（入值链 `BaseInput` / `file-upload`）；完成替换本地占位并同步受控值；未就绪零请求；预签名预览缓存；下载回落 |
| 核心 · 映射与回归 | `core/tests/domain-form-render.spec.ts`、`capabilities-batch3.spec.ts` | `file` / `image` → `file-upload` / `image-upload` 映射与 `upload*` 透传；引擎依赖登记变更回归（导入流路径保持） |
| 插件 · 投影与件 | `ui-ep/tests/field-file-upload.spec.ts` | 契约套件适配（`useBaseUploadEngine` / `useBaseFileUpload` / 占位字段投影）+ `FileUploadField`（选择入队 / 校验拒绝 / 只读 / 占位降级）、`ImageUploadField`（选择入队 / 排序与移除 / 裁剪入口 / 头像形态）、`FileListField`（回显 / 失效 / 下载预览事件）、`ImageCropDialog`（装配成功 / 失败降级 / 取消）、工具（HTTP 通路端点与解包与降级、注册表默认键与自定义登记、哈希主线程降级、图片决策与对象 URL 降级） |
| 插件 · 表单渲染协同 | `ui-ep/tests/interaction-form-renderer.spec.ts` | `file` / `image` 映射上传件、`upload*` 属性透传、多选语义单一来源 |
| 护栏 | `core/tests/guard-manifest.spec.ts`、`guard-jsdoc.spec.ts`、`guard-core-framework-agnostic.spec.ts`、`ui-ep/tests/guard-component-inheritance.spec.ts`、`guard-single-source.spec.ts`、`guard-tokens.spec.ts` | 能力基类登记与《前端基类清单》一致（`file-upload` / `upload-engine`）、JSDoc 齐备、核心框架无关（浏览器 API 全在 `ui-ep/utils/`）、四件均经基类投影挂链、组件不直引第三方 / 不直用持久化与浏览器能力、无硬编码色值 |
| 冻结契约 | `ui-ep/tests/field-placeholder.spec.ts` | **未改动即通过**：`FileUploadField` 占位降级文案 / 禁用 / 零请求 / `live` 插槽 / 文件项首个按钮移除 |
| 开发态核对页 | chromium CDP 轮询导出 DOM | 桩模式 12/12（含 cropperjs 真实装配） |

## 3. 执行记录与结果 <a id="run"></a>

| 命令 | 结果 |
| --- | --- |
| 根 `pnpm run core:check` | 84 文件 / 1070 用例通过（新增 3 文件 / 44 用例） |
| 根 `pnpm run ui-ep:check` | 59 文件 / 864 用例通过（新增 1 文件 / 36 用例 + 协同 1 用例） |
| 宿主 `pnpm exec vue-tsc -b` / `pnpm run lint` / `pnpm run test:cov` | 通过 |
| 宿主 `pnpm run build` / `pnpm run budget` | 通过（首屏合计 126.8 KB / 260 KB；首屏最大单文件 110.6 KB / 150 KB；`vendor-cropperjs` 与两个 Worker 均为异步块） |
| 核对页（CDP 轮询导出 DOM，`http://localhost:5173/file-check.html`） | 12 项自检全部通过（连续多次稳定；裁剪弹窗 `cropper-canvas` / `cropper-selection` 真实装配） |
| `python3 scripts/tools/base-check/{check-links,check-base}.py`、`check-docs/check-status.py --stage 04_前端组件库` | 通过 |

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置与落点 |
| --- | --- | --- |
| 1 | 无尺寸能力环境下图片件选择挂起（jsdom shim 抛错 / 解码不落地） | `readImageDimension` 超时降级 + `createObjectUrl` 异常降级。详见[实施记录 §4](../实施/01_实施_07_文件上传字段.md#issues) 问题 4 |
| 2 | 契约套件与核对页异步断言不稳定（`start` 为调度语义） | 改轮询等待空闲（`busy`）。详见实施记录 §4 问题 5 |
| 3 | 模板变量遮蔽 Vue `ref` 触发 lint | 导入别名 `vueRef`。详见实施记录 §4 问题 6 |
| 4 | 核对页 4 项自检初版失败（样例尺寸 / 阈值 / 等待口径） | 修正样例并补 `waitIdle`，12/12。详见实施记录 §4 问题 8 |

## 5. 覆盖率 <a id="coverage"></a>

- 核心新增模块（`domain/file.ts`、`capabilities/{upload-transport,upload-engine,presigned-url,file-upload}`）与投影 / 工具 / 四件均经用例覆盖（归一分支、校验分支、模式决策与分片、秒传与断点、并发与退避重试、取消与释放、回显与失效、预览与下载回落、HTTP 端点与降级、哈希 Worker 降级、图片决策与对象 URL 降级、裁剪装配失败降级）。
- 宿主覆盖率门禁（`test:cov`）通过；本任务新增 dev 核对页不进覆盖率统计（`src/dev` 排除）。
- 未覆盖项：真实浏览器 Worker 路径（jsdom 无 Worker，核对页覆盖主线程与裁剪真实装配）、真实对象存储上传联调（归阶段八）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 真实文件存储与联调（整包 / 分片真实托管、`sys_file`、下载预览预签名、白名单与配额）→ **阶段八「通用能力 · 文件存储」窗口**；权限码 `file:*` → RBAC 阶段；表单元数据下发 `upload*` → 表单定制窗口；移动端实现 → 后续。
- 详细设计偏差 4 项已回写设计 §10 实施对齐记录。
- 既有消费方回归（`08_05` 导入流 / `07_03` 文件预览 / `08_08` 审批图）原样通过，无阻塞遗留。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
