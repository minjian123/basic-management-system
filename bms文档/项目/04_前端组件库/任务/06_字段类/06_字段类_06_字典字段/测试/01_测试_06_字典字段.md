# 01 测试 · 字典字段

> 前端组件库 · 06 字段类 · 06 字典字段（需求 06-6）· 测试记录

[文档首页](../../../../../../文档首页.md) › [06 字段类](../../06_字段类.md) › [06 字典字段](../06_字段类_06_字典字段.md) › 测试记录　|　[← 实施记录](../实施/01_实施_06_字典字段.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | 06 字典字段（[任务](../06_字段类_06_字典字段.md)） |
| 对应需求 | [06-6](../../../../需求/06_需求_字段类.md#r06-6) |
| 详细设计 | [01_详细设计_06_字典字段.md](../设计/01_详细设计_06_字典字段.md) |
| 实施记录 | [01_实施_06_字典字段.md](../实施/01_实施_06_字典字段.md) |
| 测试日期 | 2026-09-21 |
| 测试人 | minjian |
| 测试环境 | Ubuntu 开发机 · Vitest 5（node + jsdom）· Vue 3.5 · Element Plus（组件替身）· chromium（CDP 轮询导出 DOM）· 后端 uvicorn（联调） |
| Kiwi 用例 | 964（分类「平台骨架」/ P2 / CONFIRMED / 标签「自动化」；`// kiwi_id: 964` 标注于 `core/tests/{domain-dict,dict-store,dict-select}.spec.ts`、`ui-ep/tests/field-dict.spec.ts`） |
| 结论 | 全部通过（core 1026 + ui-ep 826 用例；核对页 12/12（桩 + HTTP 真实模式）；前后端联调通过） |

## 2. 测试范围与用例 <a id="scope"></a>

| 层次 | 文件 | 覆盖范围 |
| --- | --- | --- |
| 核心 · 领域 | `core/tests/domain-dict.spec.ts` | 归一（snake_case / 缺省补全 / 脏项剔除）/ 结果三态 / 大小字典判定与探针 / 受控值归一与选择上限 / 标签翻译与过滤与禁用判定 / 合并与查找 / 树构建（孤儿提升 / 排序）/ 参数与缓存键与夹取 / 操作符联动与条件组归一与业务校验 / 方案归一与优先级 / 错误码 |
| 核心 · 缓存基类 | `core/tests/dict-store.spec.ts` | 驱动 `describeDictStoreContract`（占位零请求、未注入零请求、单类型探针与大字典、批量合并一次请求、版本一致复用、子集一次回填与二次零请求、本地二次缓存通道写入与失效）；身份依赖（父 `BasePlaceholderState` / `dict-store`）；语言切换清缓存；远程搜索不写缓存；释放安全 |
| 核心 · 选择族 / 高级查询 | `core/tests/dict-select.spec.ts` | 驱动 `describeDictSelectContract` / `describeDictQueryContract`（小字典过滤、大字典远程与禁用项、级联父值、多选上限、批量回显与选项源语义、元数据并行、条件执行与非法拒绝、业务筛选透传、方案 CRUD）；身份依赖（`BaseOptionSource` 入值链 / `dict-select` / `dict-query`） |
| 插件 · 投影与件 | `ui-ep/tests/field-dict.spec.ts` | 契约套件适配（`useBaseDictSelect` / `useBaseDictQuery`）+ `06_01` 占位契约复用；`DictSelectField`（冻结占位契约、展开首屏、多选上限截断、只读折叠回显、静态选项兼容）；`DictLabel`（缓存命中 / 未命中回退 / 标签形态 / 未注入零请求）；`DictCascaderField`（树加载 / 只读路径）；`ConditionGroupBuilder`（增删与联动）；`DictAdvancedQuery`（元数据 / 执行 / 应用 / 取消）；工具（HTTP 数据源端点与参数与解包与不可用降级、注册表默认键、本地通道、翻译器、防抖调度） |
| 插件 · 表单渲染协同 | `ui-ep/tests/interaction-form-renderer.spec.ts` | `dict` / `dict_multi` 映射字典件、`dictType` 透传、`select + dictType` 优先分发、多选语义单一来源 |
| 核心 · 映射与回归 | `core/tests/domain-form-render.spec.ts` | `dict` / `dict_multi` 语义键映射断言；`normalizeField` 的 `dictType` 透传 |
| 护栏 | `core/tests/guard-manifest.spec.ts`、`guard-jsdoc.spec.ts`、`guard-core-framework-agnostic.spec.ts`、`ui-ep/tests/guard-component-inheritance.spec.ts`、`guard-single-source.spec.ts`、`guard-tokens.spec.ts` | 能力基类登记与《前端基类清单》一致（`dict-store` / `dict-select` / `dict-query`）、JSDoc 齐备、核心框架无关、五件均经基类投影挂链、组件不直引第三方 / 不直用持久化与浏览器能力、无硬编码色值 |
| 冻结契约 | `ui-ep/tests/field-placeholder.spec.ts` | **未改动即通过**：`DictSelectField` 占位降级文案 / 禁用 / 零请求 |
| 开发态核对页 | chromium CDP 轮询导出 DOM | 桩模式 12/12；`?source=http` 真实模式 12/12 |

## 3. 执行记录与结果 <a id="run"></a>

| 命令 | 结果 |
| --- | --- |
| 根 `npm run core:check` | 81 文件 / 1026 用例通过（新增 3 文件 / 38 用例） |
| 根 `npm run ui-ep:check` | 58 文件 / 826 用例通过（新增 1 文件 / 30 用例 + 协同 1 用例） |
| 宿主 `npx vue-tsc -b` / `npm run lint` / `npm run test:cov` | 通过 |
| 宿主 `npm run build` / `npm run budget` | 通过（首屏合计 123.8 KB / 260 KB；首屏最大单文件 107.7 KB / 150 KB） |
| 核对页（CDP 轮询导出 DOM，`http://localhost:5173/dict-check.html`） | 12 项自检全部通过（连续多次稳定） |
| 核对页真实模式（`?source=http`，vite 代理 `/api` → 后端 `127.0.0.1:8000`） | 12 项自检全部通过（数据来自后端种子：用户状态 / 业务类型 / 行政区划 / 属性 schema / 提供者） |
| 前后端联调（curl） | `GET /api/v1/dicts/user_status`（2 条 + 版本）/ `?version=` 一致 `items=null` / `POST /dicts/batch` / `parent_id=` 顶层与子级 / `attrs` / `query-providers` / `advanced-query`（items + business）/ `Accept-Language: en-US`（`Enabled`）/ 写路径（新增类型 + 条目 → 版本递增 → 取数同步；软删除 → 40102） |
| `python3 scripts/tools/base-check/{check-links,check-base}.py`、`check-docs/check-status.py --stage 04_前端组件库` | 通过 |

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置与落点 |
| --- | --- | --- |
| 1 | 契约「大字典」用例跨用例污染（本地二次缓存） | 投影 `storage` 显式语义 + 测试清 localStorage。详见[实施记录 §4](../实施/01_实施_06_字典字段.md#issues) 问题 1 |
| 2 | 件内使用缓存实例报 `#private` 写入失败（Vue 代理） | 缓存类私有字段改下划线命名。详见实施记录 §4 问题 2 |
| 3 | 护栏三处失败（JSDoc / 清单登记 / 挂链） | 常量逐个 JSDoc + 清单登记 + 条件组件经 `useBaseInput` 挂链。详见实施记录 §4 问题 4 |
| 4 | 核对页 HTTP 模式「禁用项」假失败 | 自检断言分模式（真实种子无停用状态条目）。详见实施记录 §4 问题 6 |

## 5. 覆盖率 <a id="coverage"></a>

- 核心新增模块（`domain/dict.ts`、`capabilities/dict-*`）与投影 / 工具 / 五件均经用例覆盖（归一分支、缓存命中与失效、版本三态、大小字典、级联、多选上限、条件组校验与执行、方案 CRUD、HTTP 端点与降级、本地通道、翻译器）。
- 宿主覆盖率门禁（`test:cov`）通过；本任务新增 dev 核对页不进覆盖率统计（`src/dev` 排除）。
- 未覆盖项：真实浏览器虚拟滚动渲染（`el-select-v2` 为替身桩；核对页真实模式覆盖数据通路）、移动端实现。

## 6. 偏差与遗留 <a id="deviations"></a>

- 三库真库与真实 Redis 集成（后端 `02-4-27`）→ **阶段二窗口**；字典管理页面 / 权限码 / 日志审计 / 回收站 → **阶段八字典模块**；表单元数据下发 `dictType` → 表单定制窗口；移动端实现 → 后续。
- 详细设计偏差 10 项已回写设计 §18 实施对齐记录。
- 真实前后端联调已通（核对页 HTTP 模式 12/12 + curl 全接口），无阻塞遗留。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
