# 02 测试 · 模块契约与注册表基座

> 前端组件库 · 10 微前端骨架 · 子任务 02（需求 10-2）· 测试记录

[文档首页](../../../../../../文档首页.md) › [10 微前端骨架](../../10_微前端骨架.md) › [02 模块契约与注册表基座](../10_微前端骨架_02_模块契约与注册表基座.md) › 测试记录　|　[← 任务](../10_微前端骨架_02_模块契约与注册表基座.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02 模块契约与注册表基座](../10_微前端骨架_02_模块契约与注册表基座.md) |
| 对应需求 | [10-2](../../../../需求/10_需求_微前端骨架.md#r10-2) |
| 详细设计 | [02 详细设计](../设计/02_详细设计_01_模块契约与注册表基座.md) |
| 实施记录 | [02 实施](../实施/02_实施_01_模块契约与注册表基座.md) |
| 测试日期 | 2026-09-18 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1 / jsdom） |
| Kiwi 用例 | 待平台登记后回读回填（先写用例、编号后补） |
| 结论 | 通过（core 153 + 宿主 30 用例全绿；样式护栏通过） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例组 | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 注册表集合 | 单元 | `core/tests/registries.spec.ts` | 五注册表产出；路由登记 / 保序 / 快照 / 重复拒重 / 卸载；路径校验 |
| 命名空间键 | 单元 | 同上 | 组件 / 图标 / 卡片键校验（合法与非法分支） |
| 字段渲染器 | 单元 | 同上 | 键解析与按类型解析（未命中返回 `undefined`） |
| 模块注册装配 | 单元 | `apps/desktop/tests/module-registration.spec.ts` | 逐项倒入 + 键前缀校验 + 逆序清理；路由菜单登记（无标题不登记） |
| 演示模块契约使用 | 集成 | 同上 | 挂载后组件 / 图标 / 卡片 / 路由菜单均登记，卸载后清理 |
| 样式作用域护栏 | 单元（静态） | `apps/desktop/tests/guard-module-style.spec.ts` | 模块与组件 `<style>` 须 scoped、禁 `:global`、禁全局原型修改 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
pnpm run check                                    # 根：core + vue + ui-ep
cd frontend/apps/desktop && pnpm run lint && pnpm run test:cov && pnpm run build && pnpm run budget
python3 scripts/tools/base-check/check-links.py
```

```text
> core   28 files / 153 tests passed（+5）
> host   9 files / 30 tests passed（+8）
> ui-ep  16 files / 102 tests；vue 2 files / 5 tests
> links  断链 0 / 失效锚点 0
> build  ✓ built；budget 通过（预算内）
```

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 卸载后注册项仍在 | 键切分由 `split(':', 2)` 改 `indexOf`（保留命名空间键）；用例断言清理后 `get` 为 `undefined` |
| 2 | 注册表类缺抽象成员（类型报错） | 补 `pluginKey` / `pluginName` |

## 5. 覆盖率 <a id="coverage"></a>

- 宿主新增 8 用例（注册装配 5 + 样式护栏 3），覆盖率仍高于 70% 阈值；核心新增注册表用例 5 个。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项：字段渲染器 / 工作台卡片的实际渲染消费（随后续域）；隔离与版本协商（后续运行时加载）。
- Kiwi 编号回读后回填本记录。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
