# 01 测试 · 宿主外壳与模块加载器

> 前端组件库 · 10 微前端骨架 · 子任务 01（需求 10-1）· 测试记录

[文档首页](../../../../../../文档首页.md) › [10 微前端骨架](../../10_微前端骨架.md) › [01 宿主外壳与模块加载器](../10_微前端骨架_01_宿主外壳与模块加载器.md) › 测试记录　|　[← 任务](../10_微前端骨架_01_宿主外壳与模块加载器.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 宿主外壳与模块加载器](../10_微前端骨架_01_宿主外壳与模块加载器.md) |
| 对应需求 | [10-1](../../../../需求/10_需求_微前端骨架.md#r10-1) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_01_宿主外壳与模块加载器.md) |
| 实施记录 | [01 实施](../实施/01_实施_01_宿主外壳与模块加载器.md) |
| 测试日期 | 2026-09-18 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1 / jsdom） |
| Kiwi 用例 | 待平台登记后回读回填（先写用例、编号后补） |
| 结论 | 通过（core 148 + 宿主 22 用例全绿；构建与体积预算通过） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例组 | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 模块定义 | 单元 | `core/tests/module.spec.ts` | 名 / 版本校验并冻结；非法抛错 |
| 本地加载器 | 单元 | 同上 | 加载返回注册声明并透传上下文；未登记报未注册不可用；挂载 / 卸载幂等；重复挂载与同名登记拒重 |
| 模块路由注册 | 单元 | `apps/desktop/tests/module-hosting.spec.ts` | 跳过根路径与已存在路由名；返回路由名清单；按名卸载 |
| 宿主装配 | 集成 | 同上 | 加载演示模块 → 注册路由 → 派生菜单 → 卸载后路由移除 |
| 模块边界 | 人工 / 后续 | —（`10_02` 契约测试补） | 加载失败渲染错误页 + 重试 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
pnpm run check                                    # 根：core + vue + ui-ep
cd frontend/apps/desktop && pnpm run lint && pnpm run test:cov && pnpm run build && pnpm run budget
```

```text
> core   27 files / 148 tests passed（+5）
> host   7 files / 22 tests passed（+3）；覆盖率 83.44%（阈值 70%）
> ui-ep  16 files / 102 tests；vue 2 files / 5 tests
> build  ✓ built；budget 通过：合计 108.2 KB（预算 260 KB）
```

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 卸载后路由仍在 | 改为按路由名卸载（注册函数返回名清单）；用例断言 `hasRoute` 由真转假 |
| 2 | 模块视图未纳入覆盖率 | 属动态 `import` 路由组件；随 `10_02` 契约测试与宿主冒烟用例补齐 |

## 5. 覆盖率 <a id="coverage"></a>

- 宿主覆盖率 83.44%（语句）/ 73.43%（分支）/ 79.24%（函数）/ 84.56%（行），均高于 70% 阈值；核心新增模块契约用例 5 个。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项：远端模块加载与超时（后续运行时加载）；模块契约与 5 注册表（`10_02`）；模块边界渲染用例。
- Kiwi 编号回读后回填本记录。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
