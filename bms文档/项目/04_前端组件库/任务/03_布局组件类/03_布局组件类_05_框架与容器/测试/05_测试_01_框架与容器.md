# 05 测试 · 框架与容器

> 前端组件库 · 03 布局组件类 · 子任务 05（需求 03-5）· 测试记录

[文档首页](../../../../../../文档首页.md) › [03 布局组件类](../../03_布局组件类.md) › [05 框架与容器](../03_布局组件类_05_框架与容器.md) › 测试记录　|　[← 任务](../03_布局组件类_05_框架与容器.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [05 框架与容器](../03_布局组件类_05_框架与容器.md) |
| 对应需求 | [03-5](../../../../需求/03_需求_布局组件类.md) |
| 详细设计 | [05 详细设计](../设计/05_详细设计_01_框架与容器.md) |
| 实施记录 | [05 实施](../实施/05_实施_01_框架与容器.md) |
| 测试日期 | 2026-09-18 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1 / jsdom） |
| Kiwi 用例 | 待平台登记后回读回填（先写用例、编号后补） |
| 结论 | 通过（ui-ep 70 用例全绿；宿主单测 / 构建 / 预算全绿） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例组 | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 主框架区域编排 | 单元 | `ui-ep/tests/main-layout.spec.ts` | 侧栏 / 顶栏 / 标签栏 / 内容区插槽与标题 |
| 主框架交互 | 单元 | 同上 | 菜单选择与页签事件透传；折叠切换与 `sessionStorage` 持久化；标签栏可关闭；断点回传与 `data-breakpoint` |
| 页面容器 | 单元 | `ui-ep/tests/page-container.spec.ts` | 标题 / 描述 / 面包屑 / 工具栏 / 底部；吸顶类；返回事件；无头信息时仅内容区 |
| 表单壳组合式 | 单元 | `ui-ep/tests/form-shell.spec.ts` | 列表固定；详情同记录激活 / 不同记录新开；`cacheDetail` 缓存键；脏数据关闭确认；刷新信号 |
| 表单框架壳 | 单元 | 同上 | 列表 / 详情插槽按激活页签切换；选择事件归一（列表键 / 详情键）；`openMode` 属性 |
| 宿主装配 | 集成（构建） | `apps/desktop` build | 主框架与页面容器分包可构建；体积预算内 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
npm run check                                   # 根：core + vue + ui-ep
cd frontend/apps/desktop && npm run lint && npm run test:cov && npm run build && npm run budget
```

```text
> core   23 files / 126 tests；vue 1 file / 2 tests
> ui-ep  12 files / 70 tests passed（新增 13）
> host   6 files / 19 tests；覆盖率 97.95%
> build  ✓ built；budget 通过：合计 99.4 KB（预算 260 KB）
```

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 投影 ref 与 prop 重名触发 lint 报错 | 投影面改名（`collapsedState`） |
| 2 | 模板传 `Ref` 类型不匹配 | 显式取 `.value` |
| 3 | 路由断点/折叠相关的 `matchMedia` 在 jsdom 缺失 | `useResponsive` 退化 `resize` / 宽屏分支，用例在退化路径下断言 |

## 5. 覆盖率 <a id="coverage"></a>

- `ui-ep` 随根 `core-check` 执行（typecheck + lint + test）；本任务新增 13 用例（70 − 57）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项：真实认证菜单与权限过滤、顶栏专用组件、侧栏折叠远端偏好、详情页签缓存上限；宿主页面 keep-alive 的真实页面接入。
- Kiwi 编号回读后回填本记录。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
