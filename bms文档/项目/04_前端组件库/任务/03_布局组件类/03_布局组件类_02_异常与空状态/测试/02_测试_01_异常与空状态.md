# 02 测试 · 异常与空状态

> 前端组件库 · 03 布局组件类 · 子任务 02（需求 03-2）· 测试记录

[文档首页](../../../../../文档首页.md) › [03 布局组件类](../../03_布局组件类.md) › [02 异常与空状态](../03_布局组件类_02_异常与空状态.md) › 测试记录　|　[← 任务](../03_布局组件类_02_异常与空状态.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [02 异常与空状态](../03_布局组件类_02_异常与空状态.md) |
| 对应需求 | [03-2](../../../需求/03_需求_布局组件类.md#r03-2) |
| 详细设计 | [01 详细设计](../设计/02_详细设计_01_异常与空状态.md) |
| 实施记录 | [02 实施](../实施/02_实施_01_异常与空状态.md) |
| 测试日期 | 2026-09-18 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1 / jsdom） |
| Kiwi 用例 | 待平台登记后回读回填（先写用例、编号后补） |
| 结论 | 通过（ui-ep 20 用例全绿；宿主单测 / 构建 / 预算全绿） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例组 | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 错误页三码 | 单元 | `ui-ep/tests/feedback.spec.ts` | 403 / 404 / 500 缺省文案、状态码渲染、重试可见性 |
| 错误页自定义 | 单元 | 同上 | 自定义标题 / 描述；`retry` / `home` 事件 |
| 空状态矩阵 | 单元 | 同上 | 四场景标题映射、`data-type`、动作插槽、自定义标题 |
| 骨架屏 | 单元 | 同上 | 卡片形态骨架项、形态属性 |
| 内容遮罩 | 单元 | 同上 | 延迟展示防闪烁、`delay=0` 立即展示、关闭即隐 |
| 反馈状态机 | 单元 | 同上 | 四态切换；仅错误态且注入回调时重试 |
| 宿主路由 | 集成（构建 + 视图） | `apps/desktop` build | `/403` `/404` `/500` 与未知路径兜底均可构建 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
npm run check                                   # 根：core + vue + ui-ep
cd frontend/apps/desktop && npm run lint && npm run test:cov && npm run build && npm run budget
```

```text
> ui-ep  Test Files 4 passed (4)   Tests 20 passed (20)
> host   Test Files 6 passed (6)   Tests 19 passed (19)；覆盖率 97.95%
> build  ✓ built（ForbiddenView / NotFoundView / ServerErrorView 分包）
> budget 通过：合计 61.5 KB（预算 260 KB）
```

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 500 重试按钮未按预期渲染（布尔 prop 缺省补 `false`） | 改追加语义 `retry`；用例补 500 重试断言 |
| 2 | 遮罩延迟用例需等待定时器 | 用例以真实定时器等待（延迟 40ms / 等待 80ms） |

## 5. 覆盖率 <a id="coverage"></a>

- `ui-ep` 随根 `core-check` 执行（typecheck + lint + test）；宿主 `apps/desktop` 覆盖率 97.95%（阈值 70%）。
- 插画 SVG 资产以 URL 形式导入，不计入代码覆盖率。

> 上列用例数为**任务交付时口径**；基线整改（组件接继承链 + 护栏）后全量见同任务实施记录 §7（`ui-ep` 9 文件 / 57 用例）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项：四态容器（`04_01`）；品牌化插画；全局运行时错误处理（阶段十二）。
- Kiwi 编号回读后回填本记录。

> 本文档依《[文档生成规范](../../../../../规范/文档生成规范.md)》编写
