# 异常与空状态测试记录

> 前端组件库 · 03 布局组件类 · 02 异常与空状态 · 测试记录

[文档首页](../../../../../../文档首页.md) › [03-2 异常与空状态](../03_布局组件类_02_异常与空状态.md) › 01 测试　|　[← 父任务](../03_布局组件类_02_异常与空状态.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [03-2 异常与空状态](../03_布局组件类_02_异常与空状态.md) |
| 对应需求 | [03-2](../../../../需求/03_需求_布局组件类.md#r03-2) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_02_异常与空状态.md) |
| 实施记录 | [01 实施](../实施/01_实施_02_异常与空状态.md) |
| 测试日期 | 2026-09-16 |
| 测试人 | minjian |
| 测试环境 | Linux 开发机（mjpc）；`frontend/apps/desktop/`（Vitest 5 + jsdom + @vue/test-utils 2 + Element Plus 2.14.5 按需导入 + fake timers） |
| Kiwi 用例 | **726**（错误页）/ **727**（空状态）/ **728**（骨架屏）/ **729**（内容遮罩）/ **730**（反馈状态机）——经 `register_cases.py` 正式登记 |
| 结论 | 通过（frontend/apps/desktop 全量 251 / 251；覆盖率与体积门禁达标） |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi ID | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 726 | 单元（自动化，6 条） | `frontend/packages/ui-ep/tests/feedback-perm.spec.ts` | 三码缺省标题与插画映射；缺省操作（404 1 项 / 403 2 项 / 500 2 项）；点击「返回首页」emit + router push `/`；自定义 `actions` 完全覆盖且 handler 接管；自定义 title / description；路由接入（`/403` `/404` `/500` + 未知路径兜底 `/404`） |
| 727 | 单元（自动化，4 条） | `frontend/packages/ui-ep/tests/feedback-perm.spec.ts` | 四场景缺省文案与插画 + custom 兜底；引导按钮缺省文案（去创建 / 清除筛选）与点击 emit + handler；`actionPerm` 无权限不渲染；自定义插画与 `small` 尺寸 |
| 728 | 单元（自动化，5 条） | `frontend/packages/ui-ep/tests/components.spec.ts` | 四形态结构类；`rows` 行数（list / table 含表头）；card / form 结构块；`animated` 开关；`loading=false` 渲染插槽 |
| 729 | 单元（自动化，4 条） | `frontend/packages/ui-ep/tests/components.spec.ts` | `delay` 防闪（延时内不显示、超时显示）；`loading=false` 立即隐藏；`delay=0` 立即显示；文案缺省与自定义、`fullscreen` 类；包裹内容始终渲染 |
| 730 | 单元（自动化，5 条） | `frontend/apps/desktop/tests/feedback-composable.spec.ts` | `ready` 与 `empty`（`[]` / `null` / `{}`）；`ApiError` → `error`（code + 文案归一、不暴露堆栈）；`retry` 恢复；自定义 `isEmpty` / `errorText` / `onError`；`immediate` 与 `loading` 计算 |

## 3. 执行记录与结果 <a id="run"></a>

| 命令 | 范围 | 结果 |
| --- | --- | --- |
| `npm run lint` | frontend/apps/desktop | 通过（`eslint . --max-warnings 0`，退出码 0） |
| `npx vue-tsc -b` | frontend/apps/desktop | 通过（类型检查无错误） |
| `npm run test:cov` | frontend/apps/desktop | **36 文件 / 251 用例全部通过**（0 失败；此前基线 227，本任务新增 24 条）；覆盖率 All files：语句 88.48% / 分支 79.74% / 函数 86% / 行 88.48% |
| `npm run build` | frontend/apps/desktop | 通过（index JS 205.44 kB / gzip 76.24；ErrorPage 懒加载 chunk 与 CSS 独立拆分） |
| `npm run budget` | frontend/apps/desktop | 通过（合计 108.0 / 130 KB、最大单文件 73.7 / 90 KB；预算经拍板放宽，规范 §14 调整记录） |

- **测试口径说明**：
  1. 插画资源在 Vite 下默认内联为 data URI（小 SVG），测试将资源模块 mock 为可识别标识以精确断言「场景 / 错误码 → 插画」映射；
  2. `ErrorPage` 的「返回首页」经注入测试 router 验证 `push('/')` 调用（`getCurrentInstance` 已在 setup 顶层捕获）；
  3. `LoadingMask` 的 `delay` 使用 `vi.useFakeTimers()` 验证防闪与计时器清理。

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 / 现象 | 原因 | 处置与落点 |
| --- | --- | --- | --- |
| 1 | 插画 `src` 断言失败（data URI） | Vite 小资源内联 | 资源模块 mock（见 §3 口径 1） |
| 2 | 「返回首页」push 未触发 | `getCurrentInstance` 使用时机 | 产品代码修正（setup 顶层捕获实例，见实施记录 §4-1） |
| 3 | 体积预算超限 | 错误页 chunk 入产物 | 经拍板放宽 130 / 90（规范 §14；实施记录 §4-4） |
| 4 | 失败与返工 | — | 无（修正上述口径后全绿） |

## 5. 覆盖率 <a id="coverage"></a>

| 范围 | 语句 | 分支 | 函数 | 行 |
| --- | --- | --- | --- | --- |
| All files（frontend/apps/desktop） | 88.48% | 79.74% | 86% | 88.48% |
| 本任务新增文件（`useFeedback` / 四组件 / `types`） | 纳入整体统计（均高于 70% 门禁） | — | — | — |

- 门禁口径：整体 ≥ 70%（《[测试规范](../../../../../../规范/测试规范.md)》与前端 Vitest 配置），本任务**达标**。

## 6. 偏差与遗留 <a id="deviations"></a>

| # | 项 | 归口 / 去向 |
| --- | --- | --- |
| 1 | 真机视觉核对（插画尺寸 / 颜色 / 遮罩透明度 / 骨架动画） | `03_05` 组装时 dev 人工核对（实施记录 §6 第 7 项） |
| 2 | 断网 / 超时的真实场景（本地缓存回显）联调 | 关键页面任务（阶段十五） |
| 3 | 表格 / 图表内态的触发链路 | 展示类任务（回补波） |

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
