# 移动端反馈件测试记录

> 前端组件库 · 03 布局组件类 · 03-2 异常与空状态 · 03-2-1 移动端反馈件 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [03-2 异常与空状态](../../03_布局组件类_02_异常与空状态.md) › [03-2-1 移动端反馈件](../03_布局组件类_02_异常与空状态_01_移动端反馈件.md) › 01 测试　|　[← 任务文档](../03_布局组件类_02_异常与空状态_01_移动端反馈件.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [03-2-1 移动端反馈件](../03_布局组件类_02_异常与空状态_01_移动端反馈件.md) |
| 对应需求 | [03-2](../../../../../需求/03_需求_布局组件类.md#r03-2) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_03_02_01_移动端反馈件.md) |
| 实施记录 | [01 实施](../实施/01_实施_03_02_01_移动端反馈件.md) |
| 测试日期 | 2026-09-17 |
| 测试人 | minjian |
| 测试环境 | Linux 开发机（mjpc）；`packages/ui-vant`（Vitest 5 + jsdom + @vue/test-utils 2 + Vant 4 子路径按需）+ `apps/mobile` |
| Kiwi 用例 | **751**（错误页）/ **752**（空状态）/ **753**（骨架屏）/ **754**（内容遮罩）/ **755**（反馈状态机）——经 `register_cases.py` 正式登记（编号新取，不复用停用编号 726 ~ 730） |
| 结论 | 通过（`ui-vant` 16 / 82、`ui-ep` 13 / 82（含同套契约）、`apps/mobile` 11 / 63 全绿；构建与预算达标） |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi ID | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 751 | 单元（自动化，4 条） | `packages/ui-vant/tests/feedback-error-page.spec.ts` | 三码缺省标题与插画；缺省操作集（404 → 1 / 403 → 2 / 500 → 2）；点击 `home` emit 并跳转 `/`、`contact` 仅 emit；`retry` 动作接线；自定义 `actions` 完全覆盖并执行 handler；自定义 title / description；不含堆栈 |
| 752 | 单元（自动化，3 条） | `packages/ui-vant/tests/feedback-empty-state.spec.ts` | 四场景缺省标题与 `van-empty` 映射（插画 data URL）；`size='small'` 修饰类；`actionPerm` 未注入不渲染 / 放行点击 emit + handler；自定义 title / description / image |
| 753 | 单元（自动化，3 条） | `packages/ui-vant/tests/feedback-skeleton.spec.ts` | `list` 走 `van-skeleton`（行数）；`table` 表头 + rows 行；`form` / `card` 结构块；`animated` 开关；`loading=false` 渲染插槽 |
| 754 | 单元（自动化，4 条） | `packages/ui-vant/tests/feedback-loading-mask.spec.ts` | `delay` 防闪（快请求不显示 / 慢请求显示 / 关闭即时隐藏）；`delay=0` 立即显示与 `role=status` / `aria-busy`；自定义文案 / `fullscreen` / 包裹插槽；卸载清理计时器 |
| 755 | 单元（自动化，5 条） | `apps/mobile/tests/use-feedback.spec.ts` | 四态（`ready` / `empty`（`[]` / `null` / `{}`）/ `error`）；`ApiError` 归一（code + 文案、不暴露堆栈）；`retry` 恢复；自定义 `isEmpty` / `errorText` / `onError`；`immediate` 与 `loading` |
| 契约 | 一致性护栏 | `packages/{ui-ep,ui-vant}/tests/contracts-feedback.spec.ts` | `describeFeedbackComponentsContract` 同一套断言（4 条 × 双实现）：场景文案 / `small` / 权限按钮 / `data-action` emit / variant 行数 / 遮罩语义 |

> 契约套件属实现一致性护栏（类比 `guard-*.spec.ts`），不单独登记 Kiwi 编号。

## 3. 执行记录与结果 <a id="run"></a>

| 命令 | 范围 | 结果 |
| --- | --- | --- |
| `npm run lint` / `npx vue-tsc -b` | apps/mobile | 通过（零告警 / 类型无错误） |
| `npm run test` | apps/mobile | **11 文件 / 63 用例**全通过（本任务新增 1 文件 / 5 条） |
| `npm run test` | packages/ui-vant | **16 文件 / 82 用例**全通过（本任务新增 5 文件 / 18 条） |
| `npm run test` | packages/ui-ep | **13 文件 / 82 用例**全通过（含契约 4 条） |
| `npm run build` + `npm run budget` | apps/mobile | 通过（合计 128.9 / 140 KB、最大 51.8 / 83 KB） |

## 4. 缺口与说明 <a id="gaps"></a>

- jsdom 无真实导航：`ErrorPage` 的 `retry` 缺省行为（`location.reload`）不可测，契约与用例仅断言动作接线与 emit 面；
- 真机视觉核对（安全区 / 触摸热区 / `van-*` 主题一致性）随 `03_05` 组装复核与真机批次。

> 本文档依《[文档生成规范](../../../../../../../规范/文档生成规范.md)》编写
