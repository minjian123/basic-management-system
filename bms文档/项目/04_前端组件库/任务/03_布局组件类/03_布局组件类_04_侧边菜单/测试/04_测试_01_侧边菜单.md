# 04 测试 · 侧边菜单

> 前端组件库 · 03 布局组件类 · 子任务 04（需求 03-4）· 测试记录

[文档首页](../../../../../../文档首页.md) › [03 布局组件类](../../03_布局组件类.md) › [04 侧边菜单](../03_布局组件类_04_侧边菜单.md) › 测试记录　|　[← 任务](../03_布局组件类_04_侧边菜单.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [04 侧边菜单](../03_布局组件类_04_侧边菜单.md) |
| 对应需求 | [03-4](../../../../需求/03_需求_布局组件类.md#r03-4) |
| 详细设计 | [04 详细设计](../设计/04_详细设计_01_侧边菜单.md) |
| 实施记录 | [04 实施](../实施/04_实施_01_侧边菜单.md) |
| 测试日期 | 2026-09-18 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1 / jsdom） |
| Kiwi 用例 | 待平台登记后回读回填（先写用例、编号后补） |
| 结论 | 通过（核心 126 + ui-ep 55 用例全绿；宿主构建 / 预算全绿） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例组 | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 权限过滤 | 单元 | `core/tests/menu.spec.ts` | 公开项保留 / 权限项判定 / 空父节点剔除 / 父自身有权限保留 |
| 关键词过滤 | 单元 | 同上 | 命中保留祖先链 / 父命中保留子树 / 无命中 / 空关键词 |
| 树工具 | 单元 | 同上 | 先序扁平化 / 按路径查找 / 转路由节点 |
| 占位菜单树 | 单元 | 同上 | 覆盖多级 / 徽标 / 权限项 |
| 菜单组合式 | 单元 | `ui-ep/tests/side-menu.spec.ts` | 权限过滤 + 路由路径构建；关键词过滤；注册去重 / 卸载 |
| 菜单渲染 | 单元 | 同上 | 多级渲染 / 徽标 / 激活路径 / 折叠 / 展开互斥 |
| 搜索与交互 | 单元 | 同上 | 搜索本地过滤 + `search` 事件；可关闭搜索与徽标；`select` 事件 |
| 宿主占位路由 | 集成（构建） | `apps/desktop` build | `PlaceholderView` 分包与路由清单可构建 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
npm run check                                   # 根：core + vue + ui-ep
cd frontend/apps/desktop && npm run lint && npm run test:cov && npm run build && npm run budget
```

```text
> core   23 files / 126 tests passed
> ui-ep  8 files / 55 tests passed
> host   6 files / 19 tests；覆盖率 97.95%
> build  ✓ built；budget 通过：合计 64.5 KB（预算 260 KB）
```

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 核心测试缺少全局 `describe` | 用例显式引入 Vitest API |
| 2 | 权限全拒用例预期与实际（存在公开子项）不符 | 修正预期并补「父无权限且子项全被剔除」用例 |

## 5. 覆盖率 <a id="coverage"></a>

- 核心 `domain/menu.ts` 由 `core/tests/menu.spec.ts` 覆盖全部导出函数与占位菜单树结构；`ui-ep` 侧覆盖组合式与组件渲染 / 交互分支。

> 上列用例数为**任务交付时口径**；基线整改（组件接继承链 + 护栏）后全量见同任务实施记录 §7（`ui-ep` 9 文件 / 57 用例）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项：真实后端菜单拉取与登录态联动（认证 / RBAC 阶段）；菜单图标渲染；主框架装配（`03_05`）。
- Kiwi 编号回读后回填本记录。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
