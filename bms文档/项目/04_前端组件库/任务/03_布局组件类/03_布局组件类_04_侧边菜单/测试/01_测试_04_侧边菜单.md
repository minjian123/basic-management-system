# 侧边菜单测试记录

> 前端组件库 · 03 布局组件类 · 04 侧边菜单 · 测试记录

[文档首页](../../../../../../文档首页.md) › [03-4 侧边菜单](../03_布局组件类_04_侧边菜单.md) › 01 测试　|　[← 父任务](../03_布局组件类_04_侧边菜单.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [03-4 侧边菜单](../03_布局组件类_04_侧边菜单.md) |
| 对应需求 | [03-4](../../../../需求/03_需求_布局组件类.md#r03-4) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_04_侧边菜单.md) |
| 实施记录 | [01 实施](../实施/01_实施_04_侧边菜单.md) |
| 测试日期 | 2026-09-16 |
| 测试人 | minjian |
| 测试环境 | Linux 开发机（mjpc）；`apps/desktop/`（Vitest 5 + jsdom + Pinia + memory router） |
| Kiwi 用例 | **737**（侧边菜单）、**738**（菜单数据层与路由守卫） |
| 结论 | 通过（本任务新增 10 条全绿；全量 290 / 290） |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi ID | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 737 | 单元（5 条） | `tests/menu-side-menu.spec.ts` | 两级渲染 / hidden 过滤 / sort；高亮与父级自动展开；折叠与 `uniqueOpen` 透传；`badge` 可选（默认不配）；叶子点击 emit + push / 外链 `window.open`；搜索（父链保留 / 命中高亮 / 空态） |
| 738 | 单元（5 条） | `tests/menu-store-guard.spec.ts` | store 占位（未注入 loader 空清单不发请求）/ 加载与 `visibleMenus`；展开集 `toggle` / `expandByPath` 父链 / 持久化 / `collapseAll`；搜索 `filteredMenus` 父链；守卫（`public` / 无 token 跳登录 / 权限 → 403 / 有权放行）；登录页未就绪放行占位 |

## 3. 执行记录与结果 <a id="run"></a>

| 命令 | 结果 |
| --- | --- |
| `npm run lint` | 通过（`--max-warnings 0`） |
| `npx vue-tsc -b` | 通过 |
| `npm run test:cov` | 44 文件 / **290 用例全绿**（本任务新增 10 条）；覆盖率语句 87.65% / 分支 79.09% |
| `npm run build` / `npm run budget` | 通过（108.6 / 130 KB、74.4 / 90 KB） |

- 守卫测试以 memory router + `setupRouterGuard` 组合，mock `@/utils/perm`（复刻 `canAccess` 逻辑）；`tokenManager.setAccessToken` 控制登录态。

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 徽标用例 `mountWithPlugins` 解构误用 | 修测试（见实施记录 §4-4） |
| 2 | 守卫导航断言需等待重定向完成 | `await router.push(...)` 后断言 `currentRoute`（memory history） |

## 5. 覆盖率 <a id="coverage"></a>

整体（apps/desktop）：语句 87.65% / 分支 79.09%（≥ 70% 门禁达标）；新增 store / 组件纳入整体统计。

## 6. 偏差与遗留 <a id="deviations"></a>

| # | 项 | 归口 |
| --- | --- | --- |
| 1 | 真实菜单接口与权限下发的守卫联调 | 阶段七 ~ 八 |
| 2 | 折叠 hover 弹层 / 高亮的真机视觉 | `03_05` 组装核对 |
| 3 | 登录页就绪后守卫跳转的真实链路 | 页面任务 + 认证阶段 |

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
