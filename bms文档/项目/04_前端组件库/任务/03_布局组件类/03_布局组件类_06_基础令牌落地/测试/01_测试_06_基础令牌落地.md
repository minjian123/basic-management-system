# 基础令牌落地测试记录

> 前端组件库 · 03 布局组件类 · 06 基础令牌落地 · 测试记录

[文档首页](../../../../../../文档首页.md) › [03-6 基础令牌落地](../03_布局组件类_06_基础令牌落地.md) › 01 测试　|　[← 父任务](../03_布局组件类_06_基础令牌落地.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [03-6 基础令牌落地](../03_布局组件类_06_基础令牌落地.md) |
| 对应需求 | [02-2](../../../../需求/02_需求_基础组件类.md#r02-2)（基础令牌遗留收口） |
| 详细设计 | [01 详细设计](../设计/01_详细设计_06_基础令牌落地.md) |
| 实施记录 | [01 实施](../实施/01_实施_06_基础令牌落地.md) |
| 测试日期 | 2026-09-16 |
| 测试人 | minjian |
| 测试环境 | Linux 开发机（mjpc）；`apps/desktop/`、`frontend-mobile/`（Vitest 5 + jsdom + sass 1.104；Node v24.20.0） |
| Kiwi 用例 | **723**（新增，双端共用编号）；**708**（design-token 用例增补告警断言） |
| 结论 | 通过（双端各 214 / 214 全绿；覆盖率与体积门禁达标） |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi ID | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 723 | 单元（自动化） | `apps/desktop/tests/tokens.spec.ts`（3 条）；`frontend-mobile/tests/tokens.spec.ts` 同款 | ① 基础令牌五组齐备（经 `sass.compile` 编译 tokens.scss 后断言色彩 10 / 字体 13 / 间距 6 / 圆角 3 / 阴影 3）；② size 字号与 density 间距引用基础令牌（6 条）且属性协议选择器保留；③ PC 端 EP 映射（五色 + 30 档色阶 `color-mix()` + `-rgb` 五色 + 圆角 + 字体族 / 基础字号）；④ 移动端 Vant 四语义色映射 |
| 708（增补） | 单元（自动化） | `apps/desktop/tests/base-fragments-reuse.spec.ts`（design-token 用例）；`frontend-mobile/` 同款 | 未定义令牌告警：显式 `fallback` 不告警；无回退告警一次并提示令牌名；同名去重不重复；`spacing(n)` 令牌缺失告警并回落计算值 |

## 3. 执行记录与结果 <a id="run"></a>

| 命令 | 范围 | 结果 |
| --- | --- | --- |
| `npm run lint` | 双端 | 通过（`eslint . --max-warnings 0`） |
| `npx vue-tsc -b` | 双端 | 通过（类型检查无错误） |
| `npm run test:cov` | 双端 | **29 文件 / 214 用例全部通过**（0 失败；此前基线 211，本任务新增 3 条 × 2 端 + 708 增补断言）；覆盖率 All files：语句 88.92% / 分支 80.24% / 函数 87.03% / 行 88.84% |
| `npm run build` | 双端 | 通过（PC `dist` css 18.52 kB / gzip 3.74；移动端 css 3.19 kB / gzip 0.82） |
| `npm run budget` | 双端 | 通过（PC 合计 95.3 / 100 KB、最大单文件 71.8 / 75 KB；移动端合计 77.9 / 83 KB） |
| 构建产物校验（dist css 抽查） | PC / 移动端 | `--el-color-primary:var(--bms-color-primary)` 位于 EP 默认声明（偏移 2048）之后（偏移 15726）→ 覆盖生效；`color-mix` 30 处（五色 × 6 档）；`--el-color-*-rgb` 5 处；`--van-primary-color:var(--bms-color-primary)` 存在 |

- 用例执行输出摘要：`Test Files 29 passed (29)` / `Tests 214 passed (214)`（双端一致，双端共用 Kiwi 编号与断言集）。

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 / 现象 | 原因 | 处置与落点 |
| --- | --- | --- | --- |
| 1 | EP 映射段为 SCSS `@each` 插值，源文件无目标变量字面量，文本断言不可行 | 断言对象应是编译产物 | `tokens.spec.ts` 改用 `sass.compile()` 编译后断言（同验 SCSS 语法 / 插值展开 / 色阶数量） |
| 2 | Kiwi 登记未走 test 仓脚本（容器内 Django shell 直接登记） | 现场按部署说明模板登记 | 编号回读正确（723）；补 `cases/2026-09-16_阶段四03-06_基础令牌.json` 幂等留痕并在台账注明；后续批次统一走脚本 |
| 3 | 失败与返工 | — | 无（双端首次全绿） |

## 5. 覆盖率 <a id="coverage"></a>

| 范围 | 语句 | 分支 | 函数 | 行 |
| --- | --- | --- | --- | --- |
| All files（双端一致） | 88.92% | 80.24% | 87.03% | 88.84% |

- 门禁口径：整体 ≥ 70%（《[测试规范](../../../../../../规范/测试规范.md)》与前端 Vitest 配置），本任务**达标**。
- 本任务无新增 JS / TS 源文件（仅扩充 `useDesignToken.ts` 告警分支，纳入既有文件覆盖率）；`tokens.scss` 不参与 JS 覆盖率统计，以编译产物断言 + 构建校验覆盖。

## 6. 偏差与遗留 <a id="deviations"></a>

| # | 项 | 归口 / 去向 |
| --- | --- | --- |
| 1 | `color-mix()` 真机浏览器视觉未验证（jsdom 不渲染样式，仅验声明与构建） | 随 `03_01` ~ `03_05` 组件任务与前端页面任务真机核对（实施记录 §6 已登记） |
| 2 | EP / Vant 其余变量与暗色 / 租户覆盖未覆盖 | 主题与租户品牌化任务（计划 §3.1 第 38 行） |
| 3 | 布局组件样式消费（硬编码回归）未覆盖 | `03_01` ~ `03_05` 各自用例 |

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
