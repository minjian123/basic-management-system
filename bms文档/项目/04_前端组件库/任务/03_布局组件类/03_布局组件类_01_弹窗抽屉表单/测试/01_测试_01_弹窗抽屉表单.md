# 01 测试 · 弹窗抽屉表单

> 前端组件库 · 03 布局组件类 · 子任务 01（需求 03-1）· 测试记录

[文档首页](../../../../../../文档首页.md) › [03 布局组件类](../../03_布局组件类.md) › [01 弹窗抽屉表单](../03_布局组件类_01_弹窗抽屉表单.md) › 测试记录　|　[← 任务](../03_布局组件类_01_弹窗抽屉表单.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [01 弹窗抽屉表单](../03_布局组件类_01_弹窗抽屉表单.md) |
| 对应需求 | [03-1](../../../../需求/03_需求_布局组件类.md#r03-1) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_01_弹窗抽屉表单.md) |
| 实施记录 | [01 实施](../实施/01_实施_01_弹窗抽屉表单.md) |
| 测试日期 | 2026-09-18 |
| 测试人 | minjian |
| 测试环境 | 本地开发机（Node v24.20.0 / Vitest 5.0.1 / jsdom） |
| Kiwi 用例 | 待平台登记后回读回填（先写用例、编号后补） |
| 结论 | 通过（ui-ep 11 用例全绿；根 `pnpm run check` 全绿） |

## 2. 测试范围与用例 <a id="scope"></a>

| 用例组 | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 三态与标题 | 单元 | `tests/modal.spec.ts` | `create` / `edit` / `detail` 标题组合 |
| 详情态 | 单元 | 同上 | 隐藏提交按钮；编辑态提交事件 |
| 脏数据拦截 | 单元 | 同上 | 脏数据时取消被拦截（不发 `update:modelValue=false`）；非脏数据关闭 |
| 抽屉同契约 | 单元 | 同上 | `FormDrawer` 三态标题与提交 |
| 模态壳投影 | 单元 | 同上 | `useModalShell` 打开 / 关闭 / 拦截 |
| 组合式状态机 | 单元 | `tests/use-form-modal.spec.ts` | 打开 / 标题 / 脏数据 / 关闭；提交 loading 与清脏 |
| 二次确认 | 单元 | `tests/confirm.spec.ts` | `confirm()` Promise 结算（true / false）；组件事件 |

## 3. 执行记录与结果 <a id="run"></a>

```bash
pnpm run check          # 根：core + vue + ui-ep
```

```text
> ui-ep:typecheck / lint            （通过 / 零告警）
> ui-ep:test   vitest run
  ✓ tests/use-form-modal.spec.ts (2)
  ✓ tests/confirm.spec.ts (3)
  ✓ tests/modal.spec.ts (6)
  Test Files  3 passed (3)   Tests  11 passed (11)
```

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 弹窗内容 Teleport 到 body 导致查询失败 | 测试以 stub 替换 EP 组件（保留 title 与插槽） |

## 5. 覆盖率 <a id="coverage"></a>

- `ui-ep` 随根 `core-check` 执行（typecheck + lint + test）；覆盖率门禁随宿主 / 插件包体系评估（当前 PC 宿主 `apps/desktop` 已挂 70% 阈值）。

> 上列用例数为**任务交付时口径**；基线整改（组件接继承链 + 护栏）后全量见同任务实施记录 §7（`ui-ep` 9 文件 / 57 用例）。

## 6. 偏差与遗留 <a id="deviations"></a>

- 未覆盖项：真实字段表单 / 校验（域 06）；页签与菜单关闭接线（`03_03` / `03_04`）。
- Kiwi 编号回读后回填本记录。

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
