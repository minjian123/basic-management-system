# 树形主从布局测试记录

> 前端组件库 · 03 布局组件类 · 03 结构布局 · 03-3-2 树形主从布局 · 测试记录

[文档首页](../../../../../../../文档首页.md) › [03-3 结构布局](../../03_布局组件类_03_结构布局.md) › 03-3-2 树形主从布局 › 01 测试　|　[← 父任务](../../03_布局组件类_03_结构布局.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [03-3-2 树形主从布局](../03_布局组件类_03_结构布局_02_树形主从布局.md) |
| 对应需求 | [03-3](../../../../../需求/03_需求_布局组件类.md#r03-3) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_02_树形主从布局.md) |
| 实施记录 | [01 实施](../实施/01_实施_02_树形主从布局.md) |
| 测试日期 | 2026-09-16 |
| 测试人 | minjian |
| 测试环境 | Linux 开发机（mjpc）；`frontend/`（Vitest 5 + jsdom；fake timers；`matchMedia` stub） |
| Kiwi 用例 | **733**（MasterDetail）、**734**（MasterTree） |
| 结论 | 通过（本嵌套新增 12 条全绿；全量 280 / 280） |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi ID | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 733 | 单元（5 条） | `tests/layout-master-detail.spec.ts` | 主区宽度（px / 默认）；`resizable` 拖动比例更新与持久化（localStorage）；折叠与 `collapse-change`；窄屏抽屉与 `openMaster`；未选中占位与插槽渲染 |
| 734 | 单元（7 条） | `tests/layout-master-tree.spec.ts` | 渲染与 `defaultExpandAll`；搜索防抖过滤（命中可见 / 未命中隐藏）；选中 emit / `checkable` 复选与 check emit；`showCount`；工具栏与 `managePermission` 权限过滤；选中后编辑 / 删除 emit 与右键菜单；空树 EmptyState 与 loading 骨架 |

## 3. 执行记录与结果 <a id="run"></a>

| 命令 | 结果 |
| --- | --- |
| `npm run lint` | 通过（`--max-warnings 0`） |
| `npx vue-tsc -b` | 通过 |
| `npm run test:cov` | 42 文件 / **280 用例全绿**（本嵌套新增 12 条）；覆盖率语句 87.69% / 分支 79.17% |
| `npm run build` / `npm run budget` | 通过（108.4 / 130 KB、74.1 / 90 KB） |

- 测试口径：jsdom 无 `PointerEvent`（以 `MouseEvent` + `pointerId` 模拟）；`el-tree` 过滤断言按节点父级 `display: none`；`node-contextmenu` 以组件实例 `$emit` 触发。

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 过滤断言元素层级错误（display:none 在 `.el-tree-node`） | 修 `visibleNodeTexts` 过滤父级 |
| 2 | fake timers 在 mount 前开启导致防抖不触发 | 移至测试内、`advanceTimersByTime(500)` |
| 3 | 拖动事件 clientX 只读 | 以 `MouseEvent` 手动派发 |

## 5. 覆盖率 <a id="coverage"></a>

整体（frontend）：语句 87.69% / 分支 79.17%（≥ 70% 门禁达标）。

## 6. 偏差与遗留 <a id="deviations"></a>

| # | 项 | 归口 |
| --- | --- | --- |
| 1 | 树真实接口 / 懒加载失败重试交互 | 后端阶段与字段任务 |
| 2 | 窄屏抽屉手势细节 | 移动端任务 |
| 3 | 真机视觉核对 | `03_05` 组装核对 |

> 本文档依《[文档生成规范](../../../../../../../规范/文档生成规范.md)》编写
