# 懒加载与虚拟列表测试记录

> 前端组件库 · 04 容器组件类 · 02 懒加载与虚拟列表 · 测试记录

[文档首页](../../../../../../文档首页.md) › [04 容器组件类](../../04_容器组件类.md) › [04-2 懒加载与虚拟列表](../04_容器组件类_02_懒加载与虚拟列表.md) › 01 测试　|　[← 任务文档](../04_容器组件类_02_懒加载与虚拟列表.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [04-2 懒加载与虚拟列表](../04_容器组件类_02_懒加载与虚拟列表.md) |
| 对应需求 | [04-1](../../../../需求/04_需求_容器组件类.md#r04-1) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_02_懒加载与虚拟列表.md) |
| 实施记录 | [01 实施](../实施/01_实施_02_懒加载与虚拟列表.md) |
| 测试日期 | 2026-09-16 |
| 测试人 | minjian |
| 测试环境 | Linux 开发机（mjpc）；`frontend/` 与 `frontend-mobile/`（Vitest 5 + jsdom；`IOStub` / `ROStub` 手动驱动，`scrollTo` 模拟） |
| Kiwi 用例 | **744**（懒加载容器）、**745**（虚拟列表容器）；双端同号（本批自动化两端各自执行） |
| 结论 | 通过（双端各新增 11 条全绿） |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi ID | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 744 | 单元（5 条） | `tests/container-lazy.spec.ts`（双端） | IO 选项（`rootMargin`）与进入渲染；未进入渲染占位（默认内置骨架 / `#placeholder` 插槽 / `minHeight` 注入）；`once` 渲染后离开不卸载；`once=false` + `unmountOnLeave` 离开卸载并可重渲染；IO 不可用降级直接渲染；`unmountOnLeave` 且 `once` 时忽略 + 开发态告警；`rootMargin` 变更重建观察 |
| 745 | 单元（6 条） | `tests/container-virtual-list.spec.ts`（双端） | 定高可视区（缓冲索引 / 撑高 / `translateY` 偏移 / DOM 数量）；`scroll` 事件参数（尺寸 + 可视区索引）与触底进入一次 / 离开重置；动态高度（RO 测量 → 前缀和重算 → `resetMeasurement` 重建）；缓冲项（可视区上方）测量变化 `scrollTop` 补偿；`scrollToIndex`（定高定位 / 越界 pending 后 items 更新定位）/ `scrollToTop` / `scrollToBottom` / `el`；边界（`keyField` 重复告警 / 空数据 `#empty` 插槽 / `height="auto"` 退化渲染 + 告警） |

## 3. 执行记录与结果 <a id="run"></a>

| 端 | 命令 | 结果 |
| --- | --- | --- |
| frontend | `npm run lint` / `npx vue-tsc -b` | 通过（`--max-warnings 0`） |
| frontend | `npm run test:cov` | 51 文件 / **324 用例全绿**（本任务新增 11）；覆盖率语句 87.84% / 分支 79.18% |
| frontend | `npm run build` / `npm run budget` | 通过（164.3 / 200 KB、49.1 / 100 KB） |
| frontend-mobile | `npm run lint` / `npx vue-tsc -b` | 通过（`--max-warnings 0`） |
| frontend-mobile | `npm run test:cov` | 33 文件 / **237 用例全绿**（本任务新增 11）；覆盖率语句 89.24% / 分支 80.11% |
| frontend-mobile | `npm run build` / `npm run budget` | 通过（78.0 / 83 KB、77.1 / 83 KB） |

- 组件本体双端同款：`diff -r` 无差异（`src/components/container/`）。
- 测试公共 stub 抽取至 `tests/helpers/observers.ts`（`IOStub` / `ROStub`；04_01 用例同步改用，双端一致）。

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | mobile 无 `SkeletonBlock`（导入链断裂） | 默认占位改内置骨架（见实施记录 §4-1） |
| 2 | RO 首屏元素未观察（测量不生效） | `onMounted` 补 observe（见实施记录 §4-2） |
| 3 | 缓冲项补偿未生效 | 补偿基准改可视区真实起点（见实施记录 §4-3） |
| 4 | `height="auto"` 分支取不到渲染项 | 补 `data-testid`（见实施记录 §4-4） |

## 5. 覆盖率 <a id="coverage"></a>

frontend：语句 87.84% / 分支 79.18%；frontend-mobile：语句 89.24% / 分支 80.11%（均 ≥ 70% 门禁达标）；新增两件与可视区组合式纳入各自工程统计。

## 6. 偏差与遗留 <a id="deviations"></a>

| # | 项 | 归口 |
| --- | --- | --- |
| 1 | 移动端用例人工核对（同号 744 / 745：触屏滚动 / 上拉加载） | 域 04 收口核对项 |
| 2 | 真机核对（滚动流畅性 / 动态高度测量表现 / DOM 数量恒定） | 域 04 收口核对项 |
| 3 | 动态高度抖动与 `resetMeasurement()` 兜底 | 已知限制（实施记录 §6-3） |
| 4 | 小列表误用与 `auto` 退化 | 文档建议 + 开发态告警（实施记录 §6-4） |

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
