# 滚动容器测试记录

> 前端组件库 · 04 容器组件类 · 01 滚动容器 · 测试记录

[文档首页](../../../../../../文档首页.md) › [04 容器组件类](../../04_容器组件类.md) › [04-1 滚动容器](../04_容器组件类_01_滚动容器.md) › 01 测试　|　[← 任务文档](../04_容器组件类_01_滚动容器.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [04-1 滚动容器](../04_容器组件类_01_滚动容器.md) |
| 对应需求 | [04-1](../../../../需求/04_需求_容器组件类.md#r04-1) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_01_滚动容器.md) |
| 实施记录 | [01 实施](../实施/01_实施_01_滚动容器.md) |
| 测试日期 | 2026-09-16 |
| 测试人 | minjian |
| 测试环境 | Linux 开发机（mjpc）；`frontend/apps/desktop` 与 `frontend/apps/mobile`（Vitest 5 + jsdom；IO / `scrollTo` 环境缺口经 stub / 模拟） |
| Kiwi 用例 | **742**（滚动容器）、**743**（滚动位置存储与保持）；双端同号（本批自动化两端各自执行） |
| 结论 | 通过（双端各新增 12 条全绿） |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi ID | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 742 | 单元（7 条） | `frontend/packages/ui-ep/tests/container-scroll.spec.ts` + `frontend/packages/ui-vant/tests/container-scroll.spec.ts`（ui-ep + ui-vant 各一） | 高度解析（数字 px / 字符串 / `auto`）；滚动条样式化类与 `nativeScrollbar`；插槽（`header` / `footer` / 默认滚动区）；`scroll` 节流（假时钟 leading + trailing）与尺寸参数；`throttle=0` 直通；IO 哨兵触底触顶（root / rootMargin / 进入一次 / 离开重置）；IO 不可用降级（scroll 计算 + 离开重置 + `threshold` 容差）；暴露方法（`scrollTo` / `scrollToTop` / `scrollToBottom` / `scrollTop` / `el`） |
| 743 | 单元（5 条） | `frontend/packages/ui-vant/tests/container-scroll-position.spec.ts`（仅 ui-vant 实现） | 内存适配器与存储（读写 / 取整 / 负数 / `NaN`）；`sessionStorage` 适配器（前缀 / 可写探测 / 不可用回落内存）；`keepPosition` 卸载记录与挂载恢复；`positionKey` 变更（记录旧键 / 新键恢复）；无 `positionKey` 的 uid 语义（不跨实例恢复） |

## 3. 执行记录与结果 <a id="run"></a>

| 端 | 命令 | 结果 |
| --- | --- | --- |
| frontend/apps/desktop | `npm run lint` / `npx vue-tsc -b` | 通过（`--max-warnings 0`） |
| frontend/apps/desktop | `npm run test:cov` | 49 文件 / **313 用例全绿**（本任务新增 12）；覆盖率语句 87.68% / 分支 79.36% |
| frontend/apps/desktop | `npm run build` / `npm run budget` | 通过（164.3 / 200 KB、49.1 / 100 KB） |
| frontend/apps/mobile | `npm run lint` / `npx vue-tsc -b` | 通过（`--max-warnings 0`） |
| frontend/apps/mobile | `npm run test:cov` | 31 文件 / **226 用例全绿**（本任务新增 12）；覆盖率语句 89.1% / 分支 80.33% |
| frontend/apps/mobile | `npm run build` / `npm run budget` | 通过（78.0 / 83 KB、77.1 / 83 KB） |

- 组件本体双端同接口多实现：契约套件同一套断言（`frontend/packages/ui-ep/src/components/container/` / `frontend/packages/ui-vant/src/components/container/` 与令牌变更）。
- IO 路径以 `IOStub` 手动驱动（断言 `root` / `rootMargin` 与进入 / 离开语义）；降级路径以 `vi.stubGlobal('IntersectionObserver', undefined)` + 尺寸定义驱动。

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | jsdom 未实现 `Element.scrollTo` | 用例内同步赋值模拟（见实施记录 §4-1） |
| 2 | jsdom 无 `IntersectionObserver` | `IOStub` + 降级路径双覆盖（见实施记录 §4-2） |
| 3 | 位置用例双实例交互断言不稳 | 拆分为两段独立断言（见实施记录 §4-3） |

## 5. 覆盖率 <a id="coverage"></a>

frontend/apps/desktop：语句 87.68% / 分支 79.36%；frontend/apps/mobile：语句 89.1% / 分支 80.33%（均 ≥ 70% 门禁达标）；新增容器件 / 存储模块纳入各自工程统计。

## 6. 偏差与遗留 <a id="deviations"></a>

| # | 项 | 归口 |
| --- | --- | --- |
| 1 | 移动端用例人工核对（同号 742 / 743：触屏滚动 / 上拉加载） | 域 04 收口核对项（随移动端页面任务复查） |
| 2 | 真机视觉（滚动条样式 / hover / 移动端隐藏） | 域 04 收口核对项 |
| 3 | 内容异步渲染下的位置恢复偏差 | 已知限制（实施记录 §6-3） |
| 4 | 暗色主题滚动条令牌 | 品牌化 / 暗色任务 |

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
