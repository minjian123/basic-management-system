# 状态与分区测试记录

> 前端组件库 · 04 容器组件类 · 04 状态与分区 · 测试记录

[文档首页](../../../../../../文档首页.md) › [04 容器组件类](../../04_容器组件类.md) › [04-4 状态与分区](../04_容器组件类_04_状态与分区.md) › 01 测试　|　[← 任务文档](../04_容器组件类_04_状态与分区.md)

## 1. 测试信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 任务 | [04-4 状态与分区](../04_容器组件类_04_状态与分区.md) |
| 对应需求 | [04-1](../../../../需求/04_需求_容器组件类.md#r04-1) |
| 详细设计 | [01 详细设计](../设计/01_详细设计_04_状态与分区.md) |
| 实施记录 | [01 实施](../实施/01_实施_04_状态与分区.md) |
| 测试日期 | 2026-09-16 |
| 测试人 | minjian |
| 测试环境 | Linux 开发机（mjpc）；`frontend/` 与 `frontend-mobile/`（Vitest 5 + jsdom；`delay` 假时钟） |
| Kiwi 用例 | **749**（加载遮罩容器）、**750**（分区容器）；双端同号（本批自动化两端各自执行） |
| 结论 | 通过（双端各新增 9 条全绿；**域 04 收官批次**） |

## 2. 测试范围与用例 <a id="scope"></a>

| Kiwi ID | 类型 | 自动化文件 | 覆盖点 |
| --- | --- | --- | --- |
| 749 | 单元（5 条） | `tests/container-loading.spec.ts`（双端） | 四态互斥优先级（loading > error > empty > content）；`delay` 防闪烁（快请求不显示 / 慢请求显示 / 显示后即时隐藏）；`mode` 三形态与 `#loading` 插槽优先；`role`（status / alert）；`#empty` / `#error` 插槽优先与缺省 i18n 文案；重试自锁与 `loading` 复位；`minHeight` 注入 |
| 750 | 单元（4 条） | `tests/container-section.spec.ts`（双端） | 标题 / 描述 / `#title` / `#extra` / `#footer` 插槽；`divider` 开关；`padding` 档位类与区域属性透传（`data-padding` 映射 / `data-collapsed`）；折叠非受控（toggle / 内容 `v-if` / 标题保留 / 事件）；折叠受控（回写 / 外部切换）；`collapsible=false` 无开关 |

## 3. 执行记录与结果 <a id="run"></a>

| 端 | 命令 | 结果 |
| --- | --- | --- |
| frontend | `npm run lint` / `npx vue-tsc -b` | 通过（`--max-warnings 0`） |
| frontend | `npm run test:cov` | 56 文件 / **346 用例全绿**（本任务新增 9）；覆盖率语句 87.86% / 分支 79.1% |
| frontend | `npm run build` / `npm run budget` | 通过（164.4 / 200 KB、49.1 / 100 KB） |
| frontend-mobile | `npm run lint` / `npx vue-tsc -b` | 通过（`--max-warnings 0`） |
| frontend-mobile | `npm run test:cov` | 38 文件 / **259 用例全绿**（本任务新增 9）；覆盖率语句 89.17% / 分支 79.88% |
| frontend-mobile | `npm run build` / `npm run budget` | 通过（78.1 / 83 KB、77.3 / 83 KB） |

- 组件本体双端同款：`diff -r` 无差异（`src/components/container/` 与 i18n 增量）。
- `delay` 防闪烁经 `vi.useFakeTimers()` 驱动（快 / 慢请求两路径）。

## 4. 问题与处置 <a id="issues"></a>

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 反馈件移植口径澄清 | 改「内置轻量四态 + 插槽」（见实施记录 §4-1） |
| 2 | 档位命名差异（片段 ↔ 节点） | 映射仅用于区域属性（见实施记录 §4-2） |
| 3 | `collapsed` 受控传参坑 | 受控时才展开传参（见实施记录 §4-4） |

## 5. 覆盖率 <a id="coverage"></a>

frontend：语句 87.86% / 分支 79.1%；frontend-mobile：语句 89.17% / 分支 79.88%（均 ≥ 70% 门禁达标）；新增两件纳入各自工程统计。

## 6. 偏差与遗留 <a id="deviations"></a>

| # | 项 | 归口 |
| --- | --- | --- |
| 1 | 真机核对（四态视觉 / 骨架 / spin / 折叠） | 域 04 收口核对项 |
| 2 | 移动端用例人工核对（同号 749 / 750） | 域 04 收口核对项 |
| 3 | 域 03 反馈件移动端本体 | 移动端任务（域 03 口径未变） |
| 4 | 折叠动画 / 最小显示时长 | 机会项（按需评估） |

> 本文档依《[文档生成规范](../../../../../../规范/文档生成规范.md)》编写
