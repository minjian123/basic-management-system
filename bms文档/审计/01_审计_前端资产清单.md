# 前端资产清单（审计）

> 前端合规审计的资产台账：检查维度 → 护栏 / 用例 / 脚本 → 落点 → 现状（随实施与审计复跑回写）

[文档首页](../文档首页.md) › 审计 › 前端资产清单　|　[同级：后端资产清单 →](02_审计_后端资产清单.md)　|　[配套：《前端审计规范》](../规范/前端审计规范.md)

## 1. 用途与维护 <a id="purpose"></a>

- **定位**：前端审计维度的**实现侧台账**——回答「有哪些护栏 / 用例 / 对账脚本、跑在哪、现状如何」。审计的**流程、分级、报告与护栏沉淀口径**见《[前端审计规范](../规范/前端审计规范.md)》，本清单不重复定义规则。
- **与报告的关系**：历次审计报告记录**结论与违规处置**（报告只增不改），本表记录**资产与现状**（随变更更新）；审计时以本表为资产清单、以报告为结论。
- **维护（强制）**：新增 / 调整护栏、用例或对账脚本即登记；每次审计复跑后回写「现状」列；人工项机器化后同步本表。
- **不写死明细**：具体文件、目录与命令**不在本表展开**（实现变化频繁），以《[前端基类清单](../前端基类清单.md)》与工程说明为准。

## 2. 维度与资产 <a id="fe"></a>

| # | 维度 | 资产（护栏 / 用例 / 脚本） | 落点（语义） | 现状 |
| --- | --- | --- | --- | --- |
| B1 | 核心框架无关 | 核心护栏 | 核心包测试（CI 执行） | 全绿 |
| B2 | 能力类继承与登记 | 核心用例 + 人工核对 | 核心能力层 | 预置能力全部登记 |
| B3 | 组件根调用 | 组件契约套件 + 人工 | 各渲染插件组件 | 双端组件全挂组件根 |
| B4 | 契约一致性（同接口多实现） | 契约用例工厂（同一套断言） | 核心测试工具 | 全绿 |
| B5 | 依赖方向 | 核心护栏 + 人工 | 核心包 | 全绿 |
| B6 | 令牌消费 | 脚本盘点 + 人工 | 插件与宿主样式 | 现行口径 |
| B7 | 清单对账 | **护栏待重建**（候选） | — | 待办 |
| B8 | 命名 | 脚本 + 人工 | 全量源码 | 现行命名口径 |
| B9 | 历史归档 | — | 历次报告 | 旧体系结论留档（经 git 追溯） |

> 维度框架见《[前端审计规范](../规范/前端审计规范.md)》§5；对象、目录与落点登记见《[前端基类清单](../前端基类清单.md)》。

## 3. 相关审计文档 <a id="related"></a>

本表为**总览索引**（每个维度一行）；**对象明细、执行过程与结论**见下列具体审计文档与历次审计报告，本表不承载：

| 文档 | 落点 |
| --- | --- |
| 前端基类与能力合规审计（首审） | `项目/04_前端组件库/审计/` 下对应审计文档 |
| 阶段级审计报告 | 各阶段 `项目/<阶段>/审计/` |
| 跨阶段审计主题 | `资料/` 下对应目录（保留现有落点） |

## 4. 维护约定 <a id="maintain"></a>

- 审计修复沉淀的**每一类违规**须在本表登记其护栏 / 用例资产与落点（与规范 §8「护栏沉淀」联动）。
- 复跑后回写「现状」列；资产下线或改名同步更新，避免台账与代码脱节。
- 跨端同语义维度（清单对账、命名等）保持与后端口径一致，变更时两端同步。

> 前端资产清单 · 与《前端审计规范》《前端基类清单》配套
