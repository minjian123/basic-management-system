# 后端资产清单（审计）

> 后端合规审计的资产台账：检查维度 → 护栏 / 用例 / 脚本 → 落点 → 现状（随实施与审计复跑回写）

[文档首页](../文档首页.md) › 审计 › 后端资产清单　|　[← 上一个：前端资产清单](01_审计_前端资产清单.md)　|　[配套：《后端审计规范》](../规范/后端审计规范.md)

## 1. 用途与维护 <a id="purpose"></a>

- **定位**：后端审计维度的**实现侧台账**——回答「有哪些护栏 / 用例 / 对账脚本、跑在哪、现状如何」。审计的**流程、分级、报告与护栏沉淀口径**见《[后端审计规范](../规范/后端审计规范.md)》，本清单不重复定义规则。
- **与报告的关系**：历次审计报告记录**结论与违规处置**（报告只增不改），本表记录**资产与现状**（随变更更新）；审计时以本表为资产清单、以报告为结论。
- **维护（强制）**：新增 / 调整护栏、用例或对账脚本即登记；每次审计复跑后回写「现状」列；人工项机器化后同步本表。
- **不写死明细**：具体文件、目录与命令**不在本表展开**（实现变化频繁），以《[后端基类清单](../后端基类清单.md)》与工程说明为准。

## 2. 维度与资产 <a id="be"></a>

| # | 维度 | 资产（护栏 / 用例 / 脚本） | 落点（语义） | 现状 |
| --- | --- | --- | --- | --- |
| B1 | 基座继承链 | 静态护栏（CI）+ 清单对账脚本 | 核心测试 / 治理脚本 | 已交付并接入 CI |
| B2 | 横切件挂接 | 横切件用例 | 横切件测试目录 | 已交付 |
| B3 | 模块边界与依赖方向 | 边界用例 | 横切件测试目录 | 已交付 |
| B4 | 契约套件（基类 / 提供者 / 默认实现） | 契约用例 | 契约测试目录 | 已交付 |
| B5 | 清单对账 | 对账脚本（基类登记 + 模块注册 + 插件装配） | 治理脚本目录 | 已沉淀 |
| B6 | 命名与错误码段位 | 对账脚本（错误码段位）+ 人工 | 治理脚本目录 | 已沉淀 |
| B7 | 事件名与主题 | 人工（与平台事件表逐项对账） | — | 现行口径 |
| B8 | 安全审计日志 | 审计用例 + 人工 | 审计测试目录 | 已交付 |
| B9 | 三库方言与迁移 | 三库用例 + 人工 | 集成测试 | 已交付 |
| B10 | 治理与度量协作 | 治理脚本（阶段度量与复盘） | 治理脚本目录 | 已交付 |

> 维度框架见《[后端审计规范](../规范/后端审计规范.md)》§5；对象、目录与落点登记见《[后端基类清单](../后端基类清单.md)》。

## 3. 相关审计文档 <a id="related"></a>

本表为**总览索引**（每个维度一行）；**对象明细、执行过程与结论**见下列具体审计文档与历次审计报告，本表不承载：

| 文档 | 落点 |
| --- | --- |
| 后端基座合规审计（首审） | `资料/审计/` 下对应审计文档（跨阶段主题存资料） |
| 阶段级审计报告 | 各阶段 `项目/<阶段>/审计/` |
| 跨阶段审计主题 | `资料/` 下对应目录（保留现有落点） |

## 4. 维护约定 <a id="maintain"></a>

- 审计修复沉淀的**每一类违规**须在本表登记其护栏 / 用例资产与落点（与规范 §8「护栏沉淀」联动）。
- 复跑后回写「现状」列；资产下线或改名同步更新，避免台账与代码脱节。
- 跨端同语义维度（清单对账、命名与错误码段位等）保持与前端口径一致，变更时两端同步。

> 后端资产清单 · 与《后端审计规范》《后端基类清单》配套
