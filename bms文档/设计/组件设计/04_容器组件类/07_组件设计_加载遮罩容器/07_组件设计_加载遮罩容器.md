# 组件设计 · 加载遮罩容器

> LoadingContainer（继承 基础组件类 容器片段）

[文档首页](../../../../文档首页.md) › [项目规划说明](../../../../规划/项目规划说明.md) › [组件设计](../../01_组件设计_总览.md) › 加载遮罩容器　|　[← 虚拟列表容器](../06_组件设计_虚拟列表容器/06_组件设计_虚拟列表容器.md)　[分区容器 →](../08_组件设计_分区容器/08_组件设计_分区容器.md)

> 可交互原型：[07_原型设计_加载遮罩容器.html](07_原型设计_加载遮罩容器.html)（演示加载遮罩/骨架、延迟显示、错误态与重试、局部加载）

## 1. 目的与适用范围 <a id="purpose"></a>

定义 BMS 前端 `frontend/`（`frontend-mobile/` 同款）**加载遮罩容器** `LoadingContainer` 的组件设计：包裹内容并统一处理 **loading（遮罩/骨架）/ 空 / 错误 / 重试** 四态，用于列表、图表、详情等异步区域。继承 [基础组件类 容器片段](../../02_基础组件类/02_能力片段/04_组件设计_容器片段/04_组件设计_容器片段.md)。

> 本篇为 **容器组件类「加载遮罩容器」**。范围边界：整块空状态/错误页归 [展示类「异常与空状态」](../../07_展示类/05_组件设计_异常与空状态/05_组件设计_异常与空状态.md)（本容器复用其骨架/空/错误组件）；懒加载归 [容器组件类「懒加载容器」](../03_组件设计_懒加载容器/03_组件设计_懒加载容器.md)。

## 2. 组件清单与职责 <a id="components"></a>

| 组件 / 工具 | 位置 | 职责 |
| --- | --- | --- |
| `LoadingContainer.vue` | `src/components/container/` | 四态容器：loading（mask/skeleton/spin）、empty、error+retry、content |

## 3. 契约（Props 与事件） <a id="contract"></a>

| 属性 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `loading` | boolean | false | 加载中 |
| `mode` | 'mask' \| 'skeleton' \| 'spin' | 'mask' | 加载形态 |
| `delay` | number | 200 | 延迟显示（防闪烁，ms） |
| `empty` | boolean | false | 空态 |
| `error` | string \| false | false | 错误信息 |
| `minHeight` | number \| string | 令牌 | 占位高度 |

| 事件 | 说明 |
| --- | --- |
| `retry` | 点击重试 |

- 插槽：`default`、`loading`（自定义加载）、`empty`、`error`。

## 4. 行为与实现约定 <a id="behavior"></a>

| 项 | 设计 |
| --- | --- |
| 延迟显示 | 快请求（< `delay`）不显示 loading，避免闪烁 |
| 形态 | mask 遮罩 / skeleton 骨架 / spin 旋转 |
| 四态互斥 | loading > error > empty > content（优先级） |
| 重试 | `error` 显示重试按钮触发 `retry` |
| 令牌 | 遮罩/骨架/空态令牌 |

## 5. 场景集成 <a id="scenes"></a>

| 场景 | 说明 |
| --- | --- |
| 列表/图表 | 加载/空/错误统一 |
| 详情区 | 局部加载遮罩 |
| 工作台卡片 | 骨架加载 |

## 6. 依赖接口与上游变更 <a id="api"></a>

### 6.1 上游接口与口径 <a id="api-contract"></a>

| 项 | 说明 | 目标文档 |
| --- | --- | --- |
| 四态口径 | loading/empty/error/内容 | [《布局设计 · 异常与空状态》](../../../布局设计/12_布局设计_异常与空状态.html)（登记项①） |
| 骨架/遮罩 | 组件复用 | [展示类「异常与空状态」](../../07_展示类/05_组件设计_异常与空状态/05_组件设计_异常与空状态.md) |
| 容器片段 | 插槽 | [容器片段](../../02_基础组件类/02_能力片段/04_组件设计_容器片段/04_组件设计_容器片段.md) |
| 新增依赖 | **无** | — |

### 6.2 需登记的上游变更 <a id="api-upstream"></a>

| 变更点 | 目标文档 | 内容 |
| --- | --- | --- |
| ① 四态容器契约 | [《布局设计 · 异常与空状态》](../../../布局设计/12_布局设计_异常与空状态.html)「加载状态」节 | 明确 loading/empty/error/内容 四态优先级、延迟显示防闪烁、重试；本节点为契约依据 |
| ② 延迟显示 | [《布局设计 · 异常与空状态》](../../../布局设计/12_布局设计_异常与空状态.html)「加载状态」节 | 明确快请求不显示 loading（`delay`）与骨架使用场景 |
| ③ 容器组件类 | [《架构设计 · 前端架构》](../../../架构设计/08_架构设计_前端架构.md)「组件分层」节 | 登记 `LoadingContainer` 属 容器组件类 |

> 按[《文档生成规范》](../../../../规范/文档生成规范.md)「引用与追溯」节，上述变更需用户确认后由上游文档同步。

## 7. 权限 / i18n / 移动端 <a id="cooperate"></a>

- **权限**：错误信息不泄露技术细节（[《安全开发规范》](../../../../规范/安全开发规范.md)）。
- **i18n**：空态/错误/重试文案走 i18n。
- **移动端**：遮罩轻量、骨架适配。

## 8. 性能与边界 <a id="performance"></a>

| 场景 | 设计 |
| --- | --- |
| 渲染 | 仅当前态渲染；骨架轻量 |
| 边界 | 闪烁（延迟显示）、快速切换态竞态、错误信息脱敏、`minHeight` 抖动、重试重复点击 |
| 不支持 | 空状态组件本体（展示类「异常与空状态」）、懒加载（容器组件类「懒加载容器」） |

## 9. 检查清单 <a id="checklist"></a>

- □ 契约：`loading`/`mode`/`delay`/`empty`/`error`/`minHeight` + `retry`
- □ 四态优先级、延迟显示、重试、令牌
- □ 继承 02 容器片段；与 展示类「异常与空状态」/ 懒加载容器 边界清晰
- □ 权限/i18n/移动端符合第 7 节；边界符合第 8 节
- ☑ 三项上游登记已确认（2026-09-16，`04_04`：布局设计 12「加载状态」与「组件契约」/ 布局设计 08「表单组织」/ 设计令牌 02「容器内边距与分隔」/ 架构 08「组件分层」均已回写）

> 组件设计节点 · 与[《布局设计 · 异常与空状态》](../../../布局设计/12_布局设计_异常与空状态.html)[展示类「异常与空状态」](../../07_展示类/05_组件设计_异常与空状态/05_组件设计_异常与空状态.md)[《安全开发规范》](../../../../规范/安全开发规范.md)[《架构设计 · 前端架构》](../../../架构设计/08_架构设计_前端架构.md)[容器片段](../../02_基础组件类/02_能力片段/04_组件设计_容器片段/04_组件设计_容器片段.md)《命名规范》配套
