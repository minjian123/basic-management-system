# 架构设计 · 子系统 · 事件总线

> BMS · RocketMQ 事件模型、消费与推送

[架构设计](01_架构设计_总览.md) › 16-事件总线　|　[← 15-权限计算引擎](15_架构设计_子系统_权限计算引擎.md) · [17-任务调度 →](17_架构设计_子系统_任务调度.md)

## 1. 概述 <a id="overview"></a>

事件总线基于 RocketMQ 5.x：作事件总线承载操作日志异步化、Webhook 推送、审计链分区有序消费、
模块解耦与跨域数据同步。对应[《项目规划说明》](../../规划/项目规划说明.md)的「消息队列」「事件总线事件模型」「Webhook 管理」章节。

## 2. 事件模型 <a id="model"></a>

- 事件信封 EventEnvelope：event_id（幂等键）、type（`{域}.{对象}.{动作}`）、tenant_id、occurred_at、payload
- topic 命名：`bms.{事件域}.{事件}`（如 bms.log.operation、bms.wf.task、bms.pur.apply、bms.pay.approved；产品事件域按 sys_module 登记）
- 事件命名：`user.updated`、`wf.task.completed`、`pur.apply.submitted`、`pay.approved` 等（完整清单见 [09-接口与集成节点](09_架构设计_接口与集成.md)）
- 默认清单开发期可扩展；Webhook 订阅按事件名匹配

## 3. 同步 / 异步判定 <a id="sync-async"></a>

> 本节判定**跨模块协作的形态**，不是「能不能用 MQ」。默认同步，只有明确落在异步清单内的协作才走事件；**判错方向（把读或强一致写成异步）比不做异步更危险**。

| 协作形态 | 判定 | 做法 | 典型场景 |
| --- | --- | --- | --- |
| 副作用（写后附加动作） | **异步（默认走事件）** | 业务事务提交后发布事件，消费端幂等；主链路不等待结果 | 审计落库、操作 / 登录日志、通知与站内信、Webhook 推送、ES 索引同步、embedding、待办生成 |
| 读（跨模块取数） | **同步（禁止异步化）** | 经服务层接口 + DTO 或本地只读投影；可异步的只有**投影刷新** | 列表展示部门名 / 用户名 / 字典翻译 |
| 强一致写（跨模块写与状态迁移） | **同步 + 本地事务** | 同一本地事务内完成，不得拆成事件 + 补偿 | 审批状态机、软删除复合唯一约束、乐观锁、哈希链序 |

- **判定流程**：① 结果是否需立即返回调用方（读 / 状态迁移）→ 同步；② 是否为权威主数据写入 → 同步本地事务；③ 其余（通知、记录、索引、推送、外部集成）→ 异步事件。
- **读己之写**：异步投影存在延迟，写操作须返回更新后的完整对象（或由前端本地合并），不得依赖「写完立刻从投影读回」。
- **例外登记**：确需把某条同步读降级为异步投影，或把某条写改为事件 + 补偿（Saga）时，在对应概要设计登记「原同步语义 / 一致性影响 / 幂等与补偿方案 / 用户体验兜底」，经评审后执行；**不得因性能或解耦目的擅自改判**。
- **总线未就绪时的过渡**：RocketMQ 与 Outbox 投递器落地前，异步清单内的协作先同步直写 / 进程内调用实现并预留发布点；事件发布失败不阻塞主事务（MQ 故障时审计 / 日志降级为同步直写，见[故障降级](31_架构设计_性能与安全.md)）。
- **前置依赖**：消息队列部署、发件箱与投递器实现之前，本节异步清单不具备实施条件（现状：事件域为接口占位 + 默认实现）。

## 3. 生产一致性 <a id="produce"></a>

- **基线采用事务性发件箱（Outbox）**：业务事务内同时写业务数据与 `sys_outbox`（事件与业务同库同事务），由投递器（轮询，后续可切 CDC）转发到 RocketMQ——可无 MQ 独立工作、可重放、与审计链协同；阶段二落 `sys_outbox` 表与投递器接口**占位**，本子系统实现时填投递实现
- **消费侧幂等**：事件 ID 唯一约束 + 短窗口原子去重，保证至少一次投递下「效果一次」
- 生产者封装统一事件发布组件，业务代码不直接操作 MQ 客户端；备选：RocketMQ 事务消息（半消息 → 本地事务回调 → 确认发送），若启用则替换投递器方案并重新评估（当前不作基线）

## 5. 消费组架构 <a id="consume"></a>

| 消费组 | 职责 | 有序性 |
| --- | --- | --- |
| 审计链组 | 审计日志（操作/登录/开放接口/数据变更）串行追加 + 哈希链计算 | 分区有序（分区键=租户） |
| 日志落库组 | 操作日志/登录日志异步落库（降低主库写压力） | 并行 |
| Webhook 推送组 | 事件按订阅匹配推送（HMAC 签名、退避重试） | 并行 |
| 通知组 | 站内信落库 + 触发 Socket.IO 推送 | 并行 |
| ES 同步组（阶段十八） | 主数据/审计日志/文件内容索引同步 | 并行 |
| embedding 组（阶段十九） | 文件分段向量化入 Milvus | 并行 |

## 6. 幂等与重试 <a id="reliability"></a>

- 幂等：event_id 去重——Redis SETNX 短窗口 + 业务唯一约束兜底（如日志表事件 id 唯一），重试不产生重复副作用
- 重试：消费失败指数退避重试（有限次）→ 转死信 topic 落库标记，监控告警人工介入
- 审计链消费分区键 = 租户：保证同租户日志串行追加，哈希链不乱序（见审计哈希链子系统）

## 7. Webhook 推送 <a id="webhook"></a>

- 订阅管理：sys_webhook（name、url、events、secret、status、fail_count），Webhook 管理页面注册（open 业务）
- 推送：HMAC 签名校验（sha256(secret, body)）；指数退避重试；失败记录 sys_webhook_log（webhook_id、event、payload、status、retry_count、next_retry_at、error_msg）
- 出站调用统一 httpx 封装（超时/重试/熔断）

## 8. 与其他子系统的协调 <a id="coordination"></a>

- **[审计哈希链](19_架构设计_子系统_审计哈希链.md)**：审计事件经本总线分区有序消费，保证链序
- **[通知公告](22_架构设计_子系统_通知公告.md)**：通知类事件（notification.sent）触发站内信与推送
- **[工作流](21_架构设计_子系统_工作流.md)**：wf.* 事件（deployed/started/task.completed/rejected/finished）驱动待办提醒与外部订阅
- **[全文检索](25_架构设计_子系统_全文检索.md)**：事件驱动 ES 索引同步与重建（重放事件流）
- **[任务调度](17_架构设计_子系统_任务调度.md)**：任务执行完成落 task.completed 事件（与 Celery 任务队列解耦，互不干扰）
- **[故障降级](31_架构设计_性能与安全.md)**：RocketMQ 故障时审计/日志降级为同步直写（标记降级时段，恢复后补发）

> 架构设计节点 · 与《项目规划说明》《文档生成规范》《命名规范》配套