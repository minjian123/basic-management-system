# 架构设计 · 子系统 · 文件管理

> BMS · 上传下载、分片状态机、导入导出与预览

[架构设计](01_架构设计_总览.md) › 20-文件管理　|　[← 19-审计哈希链](19_架构设计_子系统_审计哈希链.md) · [21-工作流 →](21_架构设计_子系统_工作流.md)

## 1. 概述 <a id="overview"></a>

文件管理子系统承载文件上传/下载/预览、分片上传与断点续传、Excel 导入导出。
存储基于 MinIO（S3 兼容对象存储，多实例共享；本地开发可用文件系统模拟）。
对应[《项目规划说明》](../../规划/项目规划说明.md)的「文件管理」「文件上传」「Excel 处理」「导入导出」章节。

## 2. 分片状态机 <a id="state"></a>

| 状态 | 迁移 | 说明 |
| --- | --- | --- |
| 上传中（uploading） | 初始化上传 → 上传中 | 记录 upload_id、total_parts；sys_file 跟踪每片 sha256/状态 |
| 完成（completed） | 全部片校验通过合并后 → 完成 | 合并为最终对象并清理 .part；落 file.uploaded 事件 |
| 失败（failed） | 校验失败/合并失败 → 失败 | 清理临时对象；允许重新初始化上传 |
| 孤儿（orphan） | 超时未完成 → 回收 | Celery 扫描超时 .part 与超期 uploading 记录，物理清理 |

- 大小规则：≤20MB 整包上传；超限分片上传 + 断点续传（单文件上限可配置如 2GB）
- 分片临时对象 key 加 .part 标识；合并用 MinIO 组合对象
- 断点续传：前端按 part_no 状态列表仅重传缺失片
- **秒传与基座边界（2026-09-20 登记，前端组件库 06_07）**：同内容（校验值 + 大小）命中既有对象时**秒传**，直接复用既有对象引用不重传；分片上传的**编排与状态机**（初始化 / 分片 / 合并 / 取消、临时对象与孤儿回收）归文件管理模块，底层**分片托管与秒传判定**经对象存储扩展出口提供——二者职责不重叠；上传会话幂等，重传与重复初始化不产生重复对象
- **预签名消费口径**：下载 / 预览一律用限时预签名地址（后端不代理文件流），地址失效由消费方重新取址、不可达时降级下载；预览生成（图片 / PDF 直开）与 Office 转换不在 MVP
- **契约基座（2026-09-20 登记，补充三 02-45）**：分片上传与秒传判定以统一契约承载（初始化会话 / 分片 / 合并 / 取消 / 秒传判定五方法），分片托管与秒传判定在基座、状态机与孤儿回收在上层；上传会话幂等经幂等基座承载，重传与重复初始化不产生重复对象。契约边界与命名见《[后端基类清单](../../后端基类清单.md)》「跨阶段基座」节

## 3. 安全控制 <a id="security"></a>

- 类型白名单 + 大小上限（默认 20MB，超限分片）
- 分片 SHA256 校验 + 整文件校验；文件元数据记录 sha256（sys_file）
- 存储于 MinIO 私有桶（bms-files），按租户 bucket 前缀隔离
- 下载用预签名 URL（限时有效），后端不代理文件流；类型与大小检查在下载前复核
- 配额联动：上传前校验租户存储配额（storage_bytes，见多租户路由子系统）

## 4. 在线预览与下载 <a id="preview"></a>

- 图片/PDF 经预签名 URL 直开（PC + 移动端；移动端审批附件预览）
- 权限码：file:upload、file:download、file:manage
- 对象存储故障时上传/下载接口报错并告警（见部署与运维节点故障应对）

## 5. Excel 导入导出 <a id="excel"></a>

- openpyxl 读写：用户等批量导入（import:execute）、列表导出（export）
- 导入：模板下载 → 上传解析 → 校验预览 → 批量入库（幂等键防重复提交；唯一约束兜底）
- 导出：列表查询结果导出（含脱敏字段掩码）；大数据量分页拉取

## 6. 与其他子系统的协调 <a id="coordination"></a>

- **[任务调度](17_架构设计_子系统_任务调度.md)**：孤儿分片回收任务（清理中断遗留）
- **[事件总线](16_架构设计_子系统_事件总线.md)**：file.uploaded 事件（Webhook 可订阅）；全文检索文件内容索引（后续）经事件驱动
- **[审计哈希链](19_架构设计_子系统_审计哈希链.md)**：上传/下载关键操作落操作日志；文件表变更落字段级审计
- **[多租户路由](12_架构设计_子系统_多租户路由.md)**：存储配额（storage_bytes）超限拦截
- **[备份与归档](33_架构设计_部署与运维.md)**：MinIO 对象数据定期增量备份 + 生命周期策略；归档文件亦存 MinIO

> 架构设计节点 · 与《项目规划说明》《文档生成规范》《命名规范》配套