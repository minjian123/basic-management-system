# 方言特性 · 达梦 DM8

> BMS · 数据库设计 · 方言特性 · 库 03（达梦 DM8）

[文档首页](../../../文档首页.md) › [数据库设计总览](../01_数据库设计_总览.md) › [数据规范](../02_数据库设计_数据规范.md) › 达梦 DM8　|　[MySQL](01_MySQL.md) · [PostgreSQL](02_PostgreSQL.md)

## 1. 归属与依据 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 库与版本 | 达梦 DM8（mjbk 常驻官方镜像 `dmdbms/dm8`，含试用授权；商业交付需采购商业许可） |
| 应用侧接入 | 驱动 `dmPython` + `dmSQLAlchemy`（方言 `dm+dmPython`）——**官方同步驱动，无异步方言**（应用运行期与迁移均走同步引擎分支） |
| 字符集 | UTF8 |
| 库级隔离 | 以**模式（schema）**承载库语义（无 `CREATE DATABASE` 语义的库级隔离；用户模式即隔离单位） |
| 上游依据 | [数据规范](../02_数据库设计_数据规范.md)、《[架构设计 · 数据架构](../../架构设计/07_架构设计_数据架构.md)》「数据规范」「迁移策略」节、《[架构设计 · 数据访问与分片](../../架构设计/13_架构设计_子系统_数据访问与分片.md)》「多数据源管理」「查询规范」节、《[开发部署规划](../../../规划/开发部署规划.md)》「常驻数据库」节 |
| 结论口径 | 本文每条结论标注 **实测**（已真库验证）或 **待验**（尚未真库验证，实施前须实测并回写） |
| 状态 | 已实测（2026-09-22 三库迁移演练 + 模式级建删；同日三库真库集成与方言四项实测，见第 8 节） |

## 2. 类型映射 <a id="type"></a>

| 逻辑类型（设计侧写法） | 达梦 DM8 落点 | 结论 |
| --- | --- | --- |
| `BIGINT`（主键 / 外键） | BIGINT | 实测（DDL 编译 + 真库建表） |
| `DATETIME`（各 `*_at`） | DATETIME | 实测 |
| `INT` / `SMALLINT` | INTEGER / SMALLINT | 实测 |
| `VARCHAR(n)` | **VARCHAR2(n CHAR)**（按字符计长） | 实测（n 以字符为单位，与 MySQL / PostgreSQL 语义一致） |
| `TEXT` | TEXT | 实测 |
| `BOOLEAN` | SMALLINT | 实测（布尔经小整数等价表达，见 [数据规范](../02_数据库设计_数据规范.md) 第 3 节） |
| `JSON`（多值字段） | JSON | 实测（建表与写入通过；**读出为字符串**——经 dmPython 读取需应用层 `json.loads`，见第 4 / 8 节；JSON 检索 **待验**） |
| `DECIMAL(20,4)`（金额） | DECIMAL(20,4) | 实测（DDL 编译） |

- 设计侧**禁止**直接写 `VARCHAR2`：沿用逻辑类型 `VARCHAR(n)`，由方言层映射（迁移脚本不写方言类型）。
- 需按时间比较的列统一 DATETIME（UTC naive），不依赖库端时间函数。

## 3. DDL 与对象差异 <a id="ddl"></a>

| 事项 | 达梦 DM8 行为 | 对平台的影响 |
| --- | --- | --- |
| 库级隔离 | 模式为单位：`CREATE SCHEMA <名>` / `DROP SCHEMA <名> CASCADE`（**实测**） | 建删库经 `ops/db_admin.py`（达梦分支建删模式，同步助手线程执行）；**建删模式须用「去模式段」连接串 + 显式模式名**（模式尚不存在时无法作为连接参数） |
| 模式段即连接 schema | 连接串的模式段（`…/BMS_TEST_DM`）映射为 dmPython `schema` 连接参数 → 连接落在该模式内（**实测**） | 运行期连接串带模式段即可，无需额外会话切换；迁移则用去模式段连接串 + `-x schema=` |
| 会话模式切换 | 迁移前 `SET SCHEMA <模式>`（会话级，**实测**）；不支持时回落 `ALTER SESSION SET CURRENT_SCHEMA` | 迁移命令加 `-x schema=<模式名>`，由 `apply_session_schema` 写入（非达梦为空操作） |
| 模式存在性检查 | `ALL_USERS` **不含** `CREATE SCHEMA` 建出的模式；须查 `SYS.SYSOBJECTS WHERE TYPE$ = 'SCH'`（**实测**） | 建删库存在性判断按此实现（否则误判「不存在」而跳过） |
| Alembic DDL impl | Alembic **未内建** `dm` 方言实现，`MigrationContext` 会直接 `KeyError: 'dm'`（**实测**） | `alembic/env.py` 注册 `DMImpl`（`__dialect__ = "dm"`，DDL 沿用通用实现） |
| 隐式事务 | `SET SCHEMA` 会打开隐式事务；若不结束，Alembic 视为外部事务而**不再提交** → 版本行为空（表已建）（**实测**） | `apply_session_schema` 切换后显式结束隐式事务（提交） |
| 「表不存在」异常类 | 报 `DatabaseError`（与 PostgreSQL / MySQL 的 `ProgrammingError`、SQLite 的 `OperationalError` 不同）（**实测**） | 「是否已迁移」判断须同时捕获三类异常 |
| 幂等建表 | 支持 `CREATE TABLE IF NOT EXISTS` | 与其余库形态一致 |

- 迁移、建删库与批量迁移命令口径见《[数据库开发规范](../../../规范/数据库开发规范.md)》「迁移与建表口径」节；模式口径细节回写见 01_04 实施 / 测试记录。

## 4. 空值与排序 <a id="null"></a>

| 事项 | 达梦 DM8 行为 | 平台口径 |
| --- | --- | --- |
| 默认 NULL 位次 | 待验（库默认位次未单独实测） | **不依赖库默认**：统一「NULL 恒排末位」，由 ORDER BY 内 `列 IS NULL` 排序键实现（**实测**：真库升 / 降序 NULL 均末位，四库一致） |
| `NULLS FIRST/LAST` 语法 | 不使用 | 禁止写入 SQL（各库支持度不一） |
| 唯一索引与多 NULL | **实测（差异）**：复合唯一含 NULL 列时达梦视 NULL **相等** → `(唯一字段, deleted_at)` 下第二条同键未删行被唯一约束拒绝（`-6602`）；**单列**唯一 `(仅一列)` 的多 NULL 仍允许 | 平台统一口径为「未删行（`deleted_at IS NULL`）靠复合唯一保证唯一、软删后释放」，**达梦下不成立**——差异已登记（影响面与兜底方案归口信创适配阶段，见平台《数据库开发规范》「迁移与建表口径」节与任务 01_05 测试记录）；软删后同键可复用三库一致 |
| JSON 读出类型 | **实测（差异）**：`JSON` 列经 dmPython 读出为**字符串**（其余库为原生对象） | 应用层读取 JSON 字段需 `json.loads`（若模块直接下标访问 JSON 字段，须适配，归口后续） |
| 空串与 NULL | 达梦可将空串与 NULL 视为等价（Oracle 兼容口径，**待验**：受实例参数影响） | 不用空串表达「无值」；业务空值统一 NULL，字符串非空校验在契约层完成 |

## 5. 标识符与模式 <a id="identifier"></a>

- 未加引号的标识符**默认折叠为大写**存储（**实测**：`ALL_TABLES.TABLE_NAME` 等目录视图返回大写）；查询与运维脚本须注意大小写（平台统一小写书写、由数据库折叠）。
- **方言反射归一为小写**（**实测**：`inspect().get_table_names()` 返回 `sys_tenant` 等小写名），故元数据展示口径与库内目录口径不同——断言「大写存储」须查目录视图，不能依赖反射结果。
- 模式名（库级隔离单位）实测以大写呈现（如 `BMS_MIGRCHECK` / `BMS_TEST_DM`）；迁移脚本不写模式前缀（由会话当前模式决定，连接串模式段即 `schema` 参数）。
- 标识符引用用双引号；设计侧不依赖大小写敏感命名（统一 snake_case 小写）。

## 6. 索引与约束 <a id="index"></a>

- 索引与约束名在**模式内**唯一；平台命名含表名（`idx_{表}_{列}`）以对齐其余库口径。
- 每表索引 ≤ 5 个（平台强制）；唯一约束对 NULL 的行为见第 4 节（**实测差异**：复合唯一视 NULL 相等）。
- 无物理外键；逻辑外键由应用侧维护（与其余库一致）。
- 索引重命名语法与 MySQL / PostgreSQL 均不同（**待验**：需真库确认后再用于迁移脚本）。

## 7. 事务与连接 <a id="tx"></a>

| 事项 | 达梦 DM8 行为 / 平台做法 | 结论 |
| --- | --- | --- |
| 异步方言 | **无**异步驱动；应用运行期与迁移均走同步引擎（`create_engine`/`dmPython`） | 实测（引擎分支按方言判定） |
| DDL 与事务 | DDL 参与事务；`SET SCHEMA` 的隐式事务须显式结束（见第 3 节） | 实测（版本行提交结论） |
| 隔离级别 | 默认按实例配置（Oracle 兼容语义下多为读已提交） | 待验（不依赖隔离级别差异） |
| 连接池 | `pool_size` / `max_overflow` / `pool_timeout` / `pool_recycle` / `connect_timeout` 取配置 `[database.*.pool]` | 实测（配置项生效） |
| 慢查询日志 | 开启（开发联调） | 实测（已配置） |
| 运行期并发 | 同步驱动在异步应用内经线程池执行（**同步门面**：`SyncSession` + `asyncio.to_thread`，经统一会话入口按方言分流） | 实测（2026-09-22 三库真库集成用例经该路径执行） |

- 三库测试库（需库级隔离）与临时演练一律用**模式**：`CREATE SCHEMA` → 迁移 → 校验 → `DROP SCHEMA … CASCADE`（实测全流程通过）。

## 8. 实测记录 <a id="measured"></a>

| 日期 | 项 | 结论 | 证据 |
| --- | --- | --- | --- |
| 2026-09-22 | 平台链迁移（模式 `BMS_MIGRCHECK`） | 建模式 → 迁移 → 校验（`0001_sys_tenant_module`，表 4/4）→ 清理，通过 | 01_04 测试记录「三库真库迁移演练」 |
| 2026-09-22 | 租户链迁移（模式 `BMS_MIGRCHECK_TENANT`） | 建模式 → 迁移 → 校验（`0001_dict_query_scheme`，表 8/8）→ 清理，通过 | 同上 |
| 2026-09-22 | DDL impl 注册 / 模式切换 / 版本行提交 / 模式存在性检查 | 四项结论见第 3 节（均为实施中实测发现并修正） | 01_04 实施记录「问题与处置」 |
| 2026-09-22 | 类型映射与 DDL 编译 | 四方言 DDL 编译断言通过（`VARCHAR2(n CHAR)` / `SMALLINT` 布尔 / `JSON`） | `tests/models/test_type_mapping.py` |
| 2026-09-22 | 三库真库集成（模式 `BMS_TEST_DM` / `BMS_TEST_DM_T1`） | 建模式 → 分链迁移（`0001_sys_tenant_module` / `0001_dict_query_scheme`）→ 集成用例 13 条通过（多数据源 / 隔离 / 副本路由 / 分片键 / 类型往返 / NULL 位次） | 01_05 测试记录「三库真库集成用例」 |
| 2026-09-22 | 复合唯一 NULL 语义（差异取证） | 复合唯一 `(name, d)` 下 `(a, NULL)` 第二条被拒（`-6602`）；`(a, 非空)` 两条允许；单列唯一 `(name)` 多 NULL 允许 → **达梦视复合唯一中的 NULL 相等**（与 MySQL / PostgreSQL 相反） | 01_05 测试记录「差异取证」与集成用例（方言分支断言） |
| 2026-09-22 | JSON 读出类型 | `JSON` 列写入正确、经 dmPython 读出为字符串（`'{"k":"v"}'`），应用层需 `json.loads` | 01_05 集成用例（归一化断言） |
| 2026-09-22 | 标识符大小写与模式段 | 库内目录大写、方言反射小写；连接串模式段即 dmPython `schema` 参数（连接落在该模式内） | 01_05 集成用例（双断言） |
| 2026-09-22 | 布尔与字符长度 | 布尔落 `SMALLINT`（0/1）往返正确；`VARCHAR2(16 CHAR)` 接受 16 个中文字符、拒绝 17 个 | 01_05 集成用例 |
| 待验 | 库默认 NULL 位次、空串与 NULL 语义、隔离级别、索引重命名语法、JSON 检索、复合唯一多 NULL 的兜底方案（归口信创适配阶段） | — | — |

> 数据库设计 · 与《[数据库开发规范](../../../规范/数据库开发规范.md)》「表与字段口径」至「数据库设计文档体系」各节配套
