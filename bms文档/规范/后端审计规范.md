# 后端审计规范

> 后端合规审计：基座继承链 · 横切件 · 模块边界 · 契约 · 清单对账 · 错误码与事件名 · 流程与报告

[文档首页](../文档首页.md) › [规范](文档生成规范.md) › 后端审计规范　|　[← 上一个：前端审计规范](前端审计规范.md)　|　[下一个：日志规范 →](日志规范.md)

## 1. 目的与适用范围 <a id="purpose"></a>

规范**后端**（`backend/`，FastAPI）的**合规审计**：以「机器护栏 + 脚本盘点 + 人工判定」对已交付的后端代码与后端文档做结构性核验，并按「修复 → 回写 → 护栏沉淀 → 复跑」闭环。流程与报告口径与《[前端审计规范](前端审计规范.md)》**同一套**（审计 ≠ 测试：测试查「行为正确」见《[测试规范](测试规范.md)》）。

审计细则**引用式**：《[后端开发规范](后端开发规范.md)》§2（目录与分层职责）/ §3（后端基座体系）与《[后端基类清单](../后端基类清单.md)》为唯一口径来源，本规范不重复定义；检查维度见 §5 与附录。

## 2. 审计类型 <a id="types"></a>

| 类型 | 内容 | 主要依据 |
| --- | --- | --- |
| **合规审计**（默认） | 基座继承链（根系 / 根基类 / 集合体系 / 模块基类 / core 横切）、横切件挂接、模块边界与依赖方向、契约套件、清单对账、命名与错误码段位、事件名与 topic | 《[后端开发规范](后端开发规范.md)》§2 / §3、《[后端基类清单](../后端基类清单.md)》、《[命名规范](命名规范.md)》、《[API接口规范](API接口规范.md)》 |
| 安全审计（后端侧） | 敏感操作审计日志留痕、密钥与配置、权限判定链、依赖漏洞 | 《[安全开发规范](安全开发规范.md)》 |
| 性能审计 | 数据库索引与查询、缓存策略、N+1 与分页、任务与定时 | 《[数据库开发规范](数据库开发规范.md)》、平台《架构设计》性能节 |
| 文档审计（后端） | 后端任务目录 / 命名 / 链接自洽 / 基座文档清单一致 | 《[文档生成规范](文档生成规范.md)》、《[命名规范](命名规范.md)》 |

> 一次审计可覆盖多类型；默认指合规审计。

## 3. 触发时机 <a id="trigger"></a>

1. **阶段收口前（默认）**：阶段内后端交付（基座 / 插件 / 业务模块等）收口时执行一次后端合规审计，结论作为收口门禁项之一（与测试报告并列）；
2. **按需**：用户 / 评审要求时；重大重构、分层调整、批量模块交付后；
3. **例外**：纯文档改动或单点修复可不触发（在阶段收口时合并执行）。

## 4. 审计流程 <a id="flow"></a>

```mermaid
flowchart LR
    A["触发（§3）"] --> B["范围界定（对象清单 + 排除项）"]
    B --> C["方法：护栏复跑 + 脚本盘点 + 人工判定"]
    C --> D["问题分级（§7）"]
    D --> E["修复（偏差回写设计后改）"]
    E --> F["回写（设计 / 实施 / 规范 / 计划）"]
    F --> G["护栏沉淀（§8：pytest 护栏或白名单）"]
    G --> H["复跑（后端测试门禁 + 脚本复核零违规）"]
    H --> I["报告（§6）+ 索引登记"]
```

- **范围界定**：列明审计对象清单与**排除项**（如迁移脚本 / 本地开发工具不适用模块基类强制），排除必须写明理由；
- **方法三件套**：① 护栏复跑（`backend/tests/{crosscut,contracts}/` 既有用例）；② 脚本盘点（全量文件级检查，结论可复算）；③ 人工判定（语义项逐处记录依据）；
- **治理协作**：审计结论与度量随 `scripts/tools/governance/` 脚本采集（`collect_metrics.py` / `review_stage.py`），阶段复盘引用；
- **修复原则**：违规必修；修改前**回写设计/规范**（偏差回写口径同《[AI开发规范](AI开发规范.md)》）。

## 5. 检查维度框架（写实） <a id="checklist"></a>

| # | 维度 | 依据（唯一来源） | 执行方式（现状 → 首审计校准） |
| --- | --- | --- | --- |
| B1 | 基座继承链（根系 / 根基类 → 集合体系 → 模块基类 → core 横切） | 《[后端开发规范](后端开发规范.md)》§3、《[后端基类清单](../后端基类清单.md)》 | 契约 / 人工；**AST 护栏待首次审计沉淀**（参照前端 `guard-inheritance`） |
| B2 | 横切件挂接（中间件 / 依赖注入 / 事件 / 任务注册） | 《[后端开发规范](后端开发规范.md)》§3 | `pytest backend/tests/crosscut/` |
| B3 | 模块边界与依赖方向（不反向 / 不越层） | 《[后端开发规范](后端开发规范.md)》§2 | `pytest backend/tests/crosscut/test_module_boundary.py` |
| B4 | 契约套件（基类 / 插件 Provider / Null 占位） | 《[后端开发规范](后端开发规范.md)》 | `pytest backend/tests/contracts/` |
| B5 | 清单对账（后端基类清单 ↔ 代码双向） | 《[后端基类清单](../后端基类清单.md)》 | 脚本待首次审计沉淀；当前人工 |
| B6 | 命名与错误码段位 | 《[命名规范](命名规范.md)》§9、《[API接口规范](API接口规范.md)》 | 脚本待沉淀 + 人工 |
| B7 | 事件名与 topic（命名 / 与事件表同源） | 《[命名规范](命名规范.md)》、《[后端开发规范](后端开发规范.md)》事件节 | 人工（与平台事件表逐项对账） |
| B8 | 安全审计日志（敏感操作留痕 / 脱敏） | 《[安全开发规范](安全开发规范.md)》 | `pytest backend/tests/audit/` + 人工 |
| B9 | 三库方言与迁移（MySQL / PostgreSQL / 达梦 DM8） | 《[数据库开发规范](数据库开发规范.md)》 | 三库用例 + 人工核对迁移脚本 |
| B10 | 治理与度量协作（阶段度量 / 复盘清单） | 《[项目管理规范](项目管理规范.md)》§25 | `scripts/tools/governance/{collect_metrics,review_stage}.py` |

> 检查明细以《[后端开发规范](后端开发规范.md)》强制口径为准；首次后端审计须：① 逐项核 B1 ~ B10 并记录结论；② 将 B1 / B5 / B6 的机器护栏补齐（缺失项按 §8 沉淀）；③ 结论写入报告（§6）并把本表「现状」列更新为实测（报告只增不改，本表随审计报告校准）。

## 6. 报告规范 <a id="report"></a>

| 项 | 约定 |
| --- | --- |
| 落点 | `项目/<阶段>/审计/`（阶段级目录；跨阶段主题可存 `资料/` 并说明） |
| 命名 | `<NN>_审计_<主题>.md`（`NN` 两位序号；如 `01_审计_后端基座合规.md`） |
| 结构 | ① 审计信息（日期 / 人 / 触发 / 范围与排除 / 依据 / 方法 / 结论）；② 结果逐项（B1 ~ B10 × 审计点 × 结果）；③ 违规与修复（位置 / 修复 / 回写）；④ 分级处置；⑤ 护栏补强；⑥ 复跑证据（后端门禁命令与结果）；⑦ 遗留与去向 |
| 索引 | 报告在所属阶段 README「文档索引」登记一行；阶段收口回写时引用结论 |
| 入库 | 报告与证据随仓库提交；本地报告产物（`.coverage` / `coverage/`）不入库（见《[项目管理规范](项目管理规范.md)》§25.2） |

## 7. 问题分级与处理 <a id="grade"></a>

| 分级 | 判定 | 处理 |
| --- | --- | --- |
| **违规** | 违反《[后端开发规范](后端开发规范.md)》**强制口径**（绕过基类、越层依赖、错误码自造段位、敏感操作无审计日志等） | **必修**（回写设计后修复 + 护栏拦截同类） |
| **偏离** | 与参考口径 / 设计节点不一致但非强制 | 评估修复或**登记去向**（设计对齐记录 + 计划 §3.1） |
| **机会项** | 优化空间（索引 / 缓存 / 查询合并等） | 登记计划 §3.1（明确归口与时点） |
| **白名单** | 结构性常量豁免（协议常量 / 方言分支写法 / 迁移脚本本地方言等） | 写入本规范或《[后端开发规范](后端开发规范.md)》豁免节；首次审计时定义 |

## 8. 护栏沉淀（强制） <a id="guard"></a>

- 审计修复的**每一类违规**必须沉淀为机器护栏（新 pytest 用例或扩大既有扫描面），随 CI 后端测试 job 执行；
- 护栏需配 **fixture 拦截用例**（构造违规样例断言被拦），与正向用例成对；
- 暂无法机器化的检查项（如事件名语义）在报告中标注「人工项」，下次审计复跑时复核；
- 护栏与白名单口径变更须同步《[后端开发规范](后端开发规范.md)》与历次审计报告（报告只增不改）。

## 9. 与相邻规范的关系 <a id="relation"></a>

| 规范 | 分工 |
| --- | --- |
| 《[测试规范](测试规范.md)》 | 行为正确性（分层 / 用例 / 覆盖率 / 报告）；审计复跑复用其后端门禁命令与证据口径 |
| 《[后端开发规范](后端开发规范.md)》 | 后端强制口径与扩展规则的**唯一来源**；审计检查项引用之，不重复定义 |
| 《[前端审计规范](前端审计规范.md)》 | 前端对称口径（同一套流程，前端检查维度与执行方式） |
| 《[文档生成规范](文档生成规范.md)》 | 报告与索引的格式 / 命名 / 链接要求 |
| 《[AI开发规范](AI开发规范.md)》 | 审计作为「检查与回写」环节的一部分（审计任务启动时增读本规范） |

## 附录：后端审计检查维度与现有资产（首审待校准） <a id="appendix"></a>

| 资产 | 位置 | 状态 |
| --- | --- | --- |
| 横切件用例 | `backend/tests/crosscut/`（`test_module_boundary.py` / `test_plugin_providers.py` / `test_null_migration.py` 等） | 已交付（阶段一 / 三），首审计逐项核 |
| 契约套件 | `backend/tests/contracts/` | 已交付，首审计逐项核 |
| 审计日志用例 | `backend/tests/audit/` | 已交付（阶段一），首审计逐项核 |
| 治理与度量 | `scripts/tools/governance/{collect_metrics,review_stage}.py` | 已交付（阶段一 / 三）；审计报告引用其采集口径 |
| 基座继承 / 清单对账 / 错误码段位护栏 | — | **待首次后端审计沉淀**（B1 / B5 / B6） |

> 本文档依《[文档生成规范](文档生成规范.md)》编写
