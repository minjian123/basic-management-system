# 前端审计规范

> 前端合规审计：基类体系 · 组件根 · 能力 · 令牌 · 清单对账 · 契约一致性 · 流程与报告

[文档首页](../文档首页.md) › [规范](文档生成规范.md) › 前端审计规范　|　[← 上一个：测试规范](测试规范.md)　|　[下一个：后端审计规范 →](后端审计规范.md)

## 1. 目的与适用范围 <a id="purpose"></a>

规范**前端**（`frontend/packages/*` 基座包与 `frontend/apps/desktop/`、`frontend/apps/mobile/` 宿主）的**合规审计**：以「机器护栏 + 脚本盘点 + 人工判定」对已交付的前端代码与前端文档做结构性核验，并按「修复 → 回写 → 护栏沉淀 → 复跑」闭环。**审计 ≠ 测试**：测试查「行为正确」（《[测试规范](测试规范.md)》），前端审计查「结构是否按《[前端开发规范](前端开发规范.md)》」；后端对称口径见《[后端审计规范](后端审计规范.md)》。

审计细则**引用式**：《[前端开发规范](前端开发规范.md)》§3（强制用法 / 扩展规则 / 执行护栏）与《[前端基类清单](../前端基类清单.md)》为唯一口径来源，本规范不重复定义。

## 2. 审计类型 <a id="types"></a>

| 类型 | 内容 | 主要依据 |
| --- | --- | --- |
| **合规审计**（默认） | 基类体系（继承链 / 能力声明与登记 / 组件根）、命名（类 / 投影 / 能力 key / 文件）、令牌消费、清单对账、依赖方向、契约一致性（同接口多实现） | 《[前端开发规范](前端开发规范.md)》§3 / §9、《[前端基类清单](../前端基类清单.md)》、《[命名规范](命名规范.md)》 |
| 安全审计（前端侧） | 密钥不入前端包、敏感信息展示脱敏、请求层 401 / 403 口径 | 《[安全开发规范](安全开发规范.md)》 |
| 性能审计 | 构建体积门禁（`budget`）、分包与懒加载、长列表与渲染路径 | 《[前端开发规范](前端开发规范.md)》构建体积预算节 |
| 文档审计（前端） | 前端任务目录 / 命名 / 链接自洽 / 公开文档红线（README 等） | 《[文档生成规范](文档生成规范.md)》、《[命名规范](命名规范.md)》 |

> 一次审计可覆盖多类型；默认指合规审计。

## 3. 触发时机 <a id="trigger"></a>

1. **阶段收口前（默认）**：阶段内前端交付（组件域 / 页面任务等）收口时执行一次前端合规审计，结论作为收口门禁项之一（与测试报告并列）；
2. **按需**：用户 / 评审要求时；重大重构、架构调整、批量组件交付后；
3. **例外**：纯文档改动或单点修复可不触发（在阶段收口时合并执行）。

## 4. 审计流程 <a id="flow"></a>

```mermaid
flowchart LR
    A["触发（§3）"] --> B["范围界定（对象清单 + 排除项）"]
    B --> C["方法：护栏复跑 + 脚本盘点 + 人工判定"]
    C --> D["问题分级（§7）"]
    D --> E["修复（偏差回写设计后改）"]
    E --> F["回写（设计 / 实施 / 规范 / 计划）"]
    F --> G["护栏沉淀（§8：机器护栏或白名单）"]
    G --> H["复跑（全量验证 + 脚本复核零违规）"]
    H --> I["报告（§6）+ 索引登记"]
```

- **范围界定**：列明审计对象清单与**排除项**（如应用装配层不适用组件根强制、开发专用资产不纳入产物口径），排除必须写明理由；
- **方法三件套**：① 护栏复跑（核心 `frontend/packages/core/tests/guard-core-framework-agnostic.spec.ts` + 契约套件 `frontend/packages/core/testing/` 与各插件 spec）；② 脚本盘点（全量文件级检查，结论可复算）；③ 人工判定（脚本无法判定的语义项，逐处记录依据）；
- **修复原则**：违规必修；修改前**回写设计/规范**（偏差回写口径同《[AI开发规范](AI开发规范.md)》）；修改契约须**双端（多实现）同步**并跑契约套件（若适用）。

## 5. 检查项框架（引用式） <a id="checklist"></a>

检查项**不在本规范写死**，按「维度 + 引用来源 + 执行方式」组织；各端检查明细见附录与本项目历次审计报告：

| 维度 | 引用来源 | 执行方式 |
| --- | --- | --- |
| 继承链（根 → 组件根 → 能力类 / 机制基类 → 具体组件） | 开发规范基类体系节 + 基类清单 | 护栏 + 人工 |
| 能力声明与登记（`BaseCapability` 继承 / `manifest.ts` `depends` / key 登记） | 同上 | 护栏（核心）+ 人工 |
| 组件根调用（UI 组件必须挂组件根：`useComponentBase` 投影） | 开发规范 | 组件契约套件 + 人工 |
| 令牌消费（禁硬编码色值；尺寸白名单） | 设计令牌文档 + 开发规范 | 脚本盘点 + 人工 |
| 清单对账（基类清单 ↔ `manifest.ts` / 插件导出双向） | 前端基类清单 | **护栏待重建**（候选） |
| 依赖方向（宿主 → 插件 → core；core 零框架依赖；能力不反向 / 不循环） | 开发规范 | 核心护栏 + 人工 |
| 命名（核心类 / 投影 / 能力 key / 文件 / 标识符） | 《[命名规范](命名规范.md)》 | 脚本 + 人工 |
| 契约一致性（同一契约多实现：`@bms/core/testing` 同一套断言） | 产品口径（同接口多实现） | 契约套件 |
| 文档（目录 / 链接 / 清单 / 公开红线） | 《[文档生成规范](文档生成规范.md)》 | 脚本（`check-links` / `check-base`） |

## 6. 报告规范 <a id="report"></a>

| 项 | 约定 |
| --- | --- |
| 落点 | `项目/<阶段>/审计/`（阶段级目录；跨阶段主题可存 `资料/` 并说明） |
| 命名 | `<NN>_审计_<主题>.md`（`NN` 两位序号；如 `01_审计_前端基类与能力合规.md`） |
| 结构 | ① 审计信息（日期 / 人 / 触发 / 范围与排除 / 依据 / 方法 / 结论）；② 结果逐类（对象 × 审计点 × 结果）；③ 违规与修复（位置 / 修复 / 回写）；④ 分级处置（违规 / 白名单 / 机会项）；⑤ 护栏补强；⑥ 复跑证据；⑦ 遗留与去向 |
| 索引 | 报告在所属阶段 README「文档索引」登记一行；阶段收口回写时引用结论 |
| 入库 | 报告与证据（截图等）随仓库提交（二进制证据放报告同级「资源」目录） |

## 7. 问题分级与处理 <a id="grade"></a>

| 分级 | 判定 | 处理 |
| --- | --- | --- |
| **违规** | 违反开发规范**强制口径**（如未挂组件根、硬编码色值、未登记能力） | **必修**（回写设计后修复 + 护栏 / 契约套件拦截同类） |
| **偏离** | 与参考口径 / 设计节点不一致但非强制（如档位命名差异、视觉常量） | 评估修复或**登记去向**（设计对齐记录 + 计划 §3.1） |
| **机会项** | 优化空间（动画 / 性能 / 精细度） | 登记计划 §3.1（明确归口与时点） |
| **白名单** | 结构性常量豁免（1px 描边 / 断点 / 递归缩进 / 运行时内联值等） | 写入宿主规范豁免节（如《[前端开发规范](前端开发规范.md)》§3.1 尺寸硬编码白名单），色值不设豁免 |

## 8. 护栏沉淀（强制） <a id="guard"></a>

- 审计修复的**每一类违规**必须沉淀为机器护栏（新规则或扩大既有护栏扫描面）：核心护栏落 `frontend/packages/core/tests/`、契约用例落 `frontend/packages/core/testing/` 与各插件 `tests/`，随 CI `core-check`（根 `npm run check`）与双端 `test:cov` 执行；
- 护栏规则需配 **fixture 拦截用例**（构造违规样例断言被拦），与正向「全绿」用例成对；
- 暂无法机器化的检查项（如语义判定）在报告中标注「人工项」，并在下次审计复跑时复核；
- 护栏与白名单的口径变更须同步宿主规范与历次审计报告（报告只增不改，变更以新报告 / 规范修订记录）。

## 9. 与相邻规范的关系 <a id="relation"></a>

| 规范 | 分工 |
| --- | --- |
| 《[测试规范](测试规范.md)》 | 行为正确性（分层 / 用例 / 覆盖率 / 报告）；审计的复跑复用其门禁命令与证据口径 |
| 《[前端开发规范](前端开发规范.md)》 | 前端强制口径与扩展规则的**唯一来源**；审计检查项引用之，不重复定义 |
| 《[后端审计规范](后端审计规范.md)》 | 后端对称口径（同一套流程，后端检查维度与执行方式） |
| 《[文档生成规范](文档生成规范.md)》 | 报告与索引的格式 / 命名 / 链接要求 |
| 《[AI开发规范](AI开发规范.md)》 | 审计作为「检查与回写」环节的一部分（审计任务启动时增读本规范） |

## 附录：前端审计检查维度 <a id="appendix-fe"></a>

### 历次首例（2026-09-16，旧体系；结论留档）

| # | 维度 | 对象 | 执行 | 首例结论 |
| --- | --- | --- | --- | --- |
| A1 | 机制基类父类链 | `src/base/*.ts`（旧） | 护栏 `guard-inheritance` ① | 全绿 |
| A2 | 片段声明与登记 | `src/components/base/*/use*.ts`（旧） | 护栏 ②（`declareFragment` + key 登记） | 31/31 合规 |
| A3 | 域组合式（组合面齐备、不声明片段） | `use*Base.ts`（旧） | 护栏 ② | 4/4 合规 |
| A4 | 组件根调用 | `src/components/**/*.vue`（旧） | 护栏 ⑤ | 修复 2 件后全绿 |
| A5 | 样式硬编码色值 | 同上（`<style>` 段） | 护栏 ⑥ | 修复 3 处后零违规 |
| A6 | 尺寸令牌 / 白名单 | 同上 | 脚本盘点 + 人工分级 | 精确项已换；白名单 + 品牌化清单登记 |
| A7 | 清单对账 | 《前端基类清单》↔ 代码（旧） | 护栏 `guard-manifest` | 全绿 |
| A8 | 依赖方向 | 片段间 / 层级间（旧） | 护栏 `guard-dependency` | 全绿 |
| A9 | 双端一致性 | `frontend/apps/desktop` ↔ `frontend/apps/mobile`（旧） | `diff -r` | 同款 |

> 旧体系护栏与对象（`src/base`、`src/components/base` 片段层）已随 S4c / S6 删除，历史结论经 git 追溯；首例报告为平台《前端基类与片段合规审计》（2026-09-16，其 §7 / §8 / §9 为 S5c / S4c / S6 重跑记录）。

### 现行检查维度（S8 后，新体系）

| # | 维度 | 对象 | 执行 | 现状 |
| --- | --- | --- | --- | --- |
| B1 | 核心框架无关 | `frontend/packages/core/src` | 护栏 `guard-core-framework-agnostic`（CI `core-check`） | 全绿 |
| B2 | 能力类继承与登记 | `frontend/packages/core/src/capabilities` + `manifest.ts` | 核心用例 + 人工核对 | 31/31 登记 |
| B3 | 组件根调用 | `frontend/packages/{ui-ep,ui-vant}/src/components/**/*.vue` | 组件契约套件 + 人工 | 34 + 13 件全挂组件根 |
| B4 | 契约一致性（同接口多实现） | `ui-ep` ↔ `ui-vant`（确认 / 权限 / 容器件 / 权限按钮 / 反馈件） | `@bms/core/testing` 同一套断言 | 全绿 |
| B5 | 依赖方向 | 宿主 → 插件 → core；插件互不依赖 | 核心护栏 + 人工 | 全绿 |
| B6 | 令牌消费 | 插件与宿主样式（禁硬编码色值；尺寸白名单） | 脚本盘点 + 人工 | `03_06` 口径 |
| B7 | 清单对账 | 《前端基类清单》↔ `manifest.ts` / 插件导出 | **护栏待重建**（候选） | 待办（域 05+ 前） |
| B8 | 命名 | 核心类 / 投影 / 能力 key / 文件 | 脚本 + 人工 | 现行命名口径 |
| B9 | 历史归档 | 报告 §7 / §8 / §9 重跑记录 | — | 2026-09-17 在案 |

> 本文档依《[文档生成规范](文档生成规范.md)》编写
