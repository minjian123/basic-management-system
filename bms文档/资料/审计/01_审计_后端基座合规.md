# 后端基座合规审计

> 后端首次合规审计（B1 ~ B10）· 继承链 · 横切件 · 模块边界 · 清单对账 · 错误码段位 · 报告与护栏沉淀

[文档首页](../../文档首页.md) › 资料 / 审计　|　[依据：后端审计规范 →](../../规范/后端审计规范.md)

## 1. 审计信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 审计日期 | 2026-09-16 |
| 审计人 | minjian |
| 触发 | 用户要求 + 首次合规审计（《[后端审计规范](../../规范/后端审计规范.md)》§3）；落点按 §6「跨阶段主题存 `资料/`」（后端基座由阶段一交付、阶段三扩展） |
| 审计范围 | `backend/` 全量：`app/core/`（21 件机制与横切基座）、44 个能力域 / 模块目录、`ops/`（模块 / 插件校验）、`tests/`（contracts / crosscut / audit 等）；**排除**：`backend/tests/archive`（历史归档）、`bms_platform.db`（本地数据文件） |
| 审计依据 | 《[后端审计规范](../../规范/后端审计规范.md)》B1 ~ B10、《[后端开发规范](../../规范/后端开发规范.md)》§2 / §3、《[后端基类清单](../../后端基类清单.md)》、《[命名规范](../../规范/命名规范.md)》§9、《[API接口规范](../../规范/API接口规范.md)》 |
| 审计方法 | ① 清单对账脚本（新增 `scripts/tools/base-check/check-backend-base.py`：继承链 / 清单登记 / 错误码段位 / 迁移链 + `--self-test`）；② 既有护栏与校验复跑（`pytest tests/{crosscut,contracts,audit}`、`ops/check_modules`、`ops/check_plugins`）；③ 后端门禁全量（ruff / pyright / pytest + 覆盖率）；④ 人工判定（无对象项与豁免项） |
| 结论 | **合规**（代码零违规；清单补登记 4 项；新增护栏脚本 + CI 接入） |

## 2. 审计结果（B1 ~ B10） <a id="result"></a>

| # | 维度 | 检查内容 | 执行方式 / 结果 | 结论 |
| --- | --- | --- | --- | --- |
| B1 | 基座继承链 | 《后端基类清单》§10 继承链 ↔ 代码 `class A(B)` 相邻关系 | `check-backend-base.py`：**216 条相邻关系全部对齐**（根系 / 集合体系 / 模块基类 / 中间层 / core 横切 / 跨阶段基座 / 40 个能力域） | ✅ |
| B2 | 横切件挂接 | 中间件 / 依赖注入 / 事件 / 任务注册 | `pytest tests/crosscut/`（含 `test_module_boundary` / `test_plugin_providers` / `test_null_migration` 等） | ✅ |
| B3 | 模块边界与依赖方向 | 模块间不反向 / 不越层 | `pytest tests/crosscut/test_module_boundary.py` 全绿 | ✅ |
| B4 | 契约套件 | 域注册 / 插件契约 | `pytest tests/contracts/`（`test_domain_registry_contracts` / `test_plugin_contracts`）全绿 | ✅ |
| B5 | 清单对账（双向） | ① 清单 → 代码（B1）；② 代码 → 清单（直接继承 `BaseObject` 的基座类必须登记）；③ 模块注册 / 插件装配 | `check-backend-base.py`：**67 个基座类全部登记**；`ops/check_modules.py`（4 项）；`ops/check_plugins.py`（41 项）——**发现 4 项未登记（已补，见 §3）** | ✅（修复后） |
| B6 | 命名与错误码段位 | 平台码 5 位、万位 1~9；产品段（10xxxx 起）不得混入 | `check-backend-base.py`：**11 个平台码全部合规** | ✅ |
| B7 | 事件名与 topic | 点分小写 / 与事件表同源 | **无对象**：事件发布 / 消费均为占位（真实实现随 M10 集成消息）；命名口径已在《命名规范》登记，随事件实现审计 | —（登记） |
| B8 | 安全审计日志 | 敏感操作留痕 / 脱敏 | `pytest tests/audit/` 全绿（117 用例子集含 audit 组） | ✅ |
| B9 | 三库方言与迁移 | MySQL / PostgreSQL / 达梦 DM8 | **迁移未建**（随阶段六）：`alembic/versions/` 为空，脚本内链检查随之生效；三库集成测试开关随阶段六激活 | —（登记） |
| B10 | 治理与度量协作 | 阶段度量 / 复盘清单 | `scripts/tools/governance/{collect_metrics,review_stage}.py` 已交付（阶段一 / 三），审计证据引用其采集口径 | ✅ |

## 3. 违规与修复 <a id="fix"></a>

| # | 发现 | 判定 | 修复 |
| --- | --- | --- | --- |
| 1 | `ModuleRecord` / `ModuleRegistry`（`app/services/module_registry.py`）未在《后端基类清单》登记 | 清单缺登记（平台机制类：模块注册制） | 清单 §9「跨阶段基座」补登记（2026-09-16） |
| 2 | `PluginWiring`（`app/core/assembly.py`）未登记 | 清单缺登记（core 装配基座） | 清单 §7「core 横切基座」补登记 |
| 3 | `RequestLoggingMiddleware`（`app/api/middleware.py`）未登记 | 清单缺登记（HTTP 中间件；`TraceIdMiddleware` 同处） | 清单 §7 补「HTTP 中间件」行（含 `TraceIdMiddleware`） |
| 4 | 误报排除：`_EmptyExporterStream`（`app/transfer/null.py`） | **白名单**：`_` 前缀私有内部类（非公共基座 API） | 脚本对账跳过私有类（豁免规则） |

> **代码零违规**：继承链 / 错误码 / 模块边界 / 契约 / 审计日志 均未发现违反强制口径的实现。

## 4. 护栏沉淀（新增） <a id="guard"></a>

| 项 | 内容 |
| --- | --- |
| 脚本 | `scripts/tools/base-check/check-backend-base.py`：① 继承链对账（清单 §10 ↔ 代码相邻关系，含反引号 / `A / B → C / D` 组配对 / `Xxx*` 占位 / 全大写缩写过滤等解析规则）；② 清单登记对账（直接继承 `BaseObject` 的基座类，跳过 `_` 私有类）；③ 错误码段位；④ alembic 迁移链（head 唯一 / 无断链；当前空目录自动跳过） |
| 自检 | `--self-test`：三个 fixture（清单与代码一致 → 放行；清单登记未存在的类 → 拦截；错误码越段 → 拦截） |
| CI | `base-integrity` job 新增两步（脚本 + 自检）；**触发面补 `scripts/tools/**`**（脚本改动亦进入流水线） |
| 规范校准 | 《[后端审计规范](../../规范/后端审计规范.md)》§5 表 B1 / B5 / B6 执行方式列更新为「已沉淀」，附录资产表同步（报告只增不改，规范随审计校准） |

## 5. 复跑证据（修复后） <a id="verify"></a>

| 项 | 命令 | 结果 |
| --- | --- | --- |
| 对账脚本 | `python3 scripts/tools/base-check/check-backend-base.py` | **通过**：216 条相邻关系 / 67 个基座类登记 / 11 个平台码 / 迁移链（空，跳过） |
| 护栏自检 | `... check-backend-base.py --self-test` | **3/3 通过**（放行 1 + 拦截 2） |
| 模块 / 插件校验 | `uv run python -m ops.check_modules` / `check_plugins` | 4 项 / 41 项 校验通过 |
| 契约 / 横切 / 审计用例 | `uv run pytest tests/{crosscut,contracts,audit} -q` | **117 passed** |
| 后端门禁全量 | `uv run pytest --cov=app --cov-branch --cov-fail-under=70 -q` | **562 passed / 8 skipped / 覆盖率 99.34%** |
| 静态检查 | `uv run ruff check .` / `uv run pyright` | **All checks passed / 0 errors** |
| 文档基座校验 | `python3 scripts/tools/base-check/check-base.py` | 通过（无跨层引用 / 清单一致） |

## 6. 遗留与去向 <a id="rest"></a>

| # | 项 | 去向 |
| --- | --- | --- |
| 1 | 事件名与 topic 对账（B7） | 随 M10（集成消息可用）事件真实实现后按本报告流程复审计 |
| 2 | alembic 迁移链与三库方言（B9） | 随阶段六建立迁移后自动生效（脚本已就绪）；三库集成测试随阶段六开关激活 |
| 3 | 清单对账脚本的模块级扩展（模块注册段位 ↔ `sys_module.errcode_segment` 逐项对账） | 当前由 `ops/check_modules.py` 覆盖唯一性与格式；逐项段位对账随模块注册制深化评估 |
| 4 | 前端对称首例报告 | 见阶段四 `项目/04_前端组件库/审计/01_审计_前端基类与片段合规.md`（2026-09-16） |

> 本文档依《[文档生成规范](../../规范/文档生成规范.md)》编写
