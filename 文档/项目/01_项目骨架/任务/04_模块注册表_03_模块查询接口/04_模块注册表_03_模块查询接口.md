# 03 模块查询接口

> 项目骨架 · 04 模块注册表 · 子任务 03

[文档首页](../../../../文档首页.md) › [04 模块注册表](../04_模块注册表.md) › 03 模块查询接口　|　[← 父任务](../04_模块注册表.md)

## 1. 任务信息 <a id="meta"></a>

| 项 | 值 |
| --- | --- |
| 编号 | 03 |
| 父任务 | [04 模块注册表](../04_模块注册表.md) |
| 对应需求 | [04-3](../../需求/04_需求_模块注册表.md#r04-3) |
| 工时（重估） | 1h |
| 依赖 | 01（注册表就位） |
| 负责人 | minjian |
| 状态 | 未开始 |
| 完成日期 | — |

## 2. 任务内容 <a id="content"></a>

1. `GET /api/v1/modules`：平台库 sys_module 只读查询；响应 `{code: 0, message: "ok", data: {list: [{module_key, name, table_prefix, errcode_segment, event_domain, status}], total}}`；不分页、按 module_key 排序；查询参数 status 可选；软删除过滤；归属平台接口（豁免租户解析）
2. 鉴权口径：阶段一暂不鉴权（OpenAPI 标注 TODO：阶段三接入 platform:manage）；无任何写接口（POST/PUT/DELETE 不存在，404）
3. 实现约束：repository 层查询、响应模型显式声明（不直接返回 ORM 对象）、接口 docstring 完整（进入 OpenAPI 契约）

## 3. 完成标准 <a id="accept"></a>

`GET /api/v1/modules` 返回 9 条注册记录、字段与响应契约一致；`?status=0` 返回 5 条规划态；POST 该路径返回 404；接口在 /docs 可见且 docstring 完整。

## 4. 参考文档 <a id="ref"></a>

- 《架构设计 · 模块注册》3.3
- 《API接口规范》第 2/3 节
- 《后端开发规范》第 4/6 节

> 本文档依《文档生成规范》编写 · 生成日期：2026-08-23
