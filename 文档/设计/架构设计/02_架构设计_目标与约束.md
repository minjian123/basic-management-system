# 架构设计 · 目标与约束

> BMS · 质量属性与设计约束

[架构设计](01_架构设计_总览.md) › 02-目标与约束　|　[← 01-架构设计（总纲）](01_架构设计_总览.md) · [03-总体架构 →](03_架构设计_总体架构.md)

## 1. 概述 <a id="overview"></a>

本节点定义 BMS 架构设计必须满足的质量属性与技术约束，以及规划阶段**明确不采用**的技术
（防止后续反复讨论）。所有子系统设计均以此为准绳，任何违反约束的设计需先修订本节点。

## 2. 质量属性 <a id="quality"></a>

| 质量属性 | 目标 | 架构落地 |
| --- | --- | --- |
| 性能 | ≥ 500 并发用户；P99 ≤ 1s（登录、列表查询、审批提交主链路），验收前压测验证 | 异步全栈（FastAPI + 异步 SQLAlchemy + redis.asyncio）；Redis 缓存三防；操作日志/登录日志经 RocketMQ 异步写入降低主库压力；慢查询日志定期走查 |
| 可用性 | RTO ≤ 4 小时、RPO ≤ 1 小时；故障可降级 | 无状态多副本；故障应对表（见部署与运维节点）；数据库每日全量备份 + Binlog/WAL（PITR） |
| 安全性 | 等保管理要求对齐 | 三权分立；审计哈希链防篡改；数据脱敏；双 token + Redis 会话校验；密码策略合规 |
| 多租户隔离 | 租户间数据绝对隔离 | 一租户一独立库物理隔离 + 查询强制 tenant 过滤 + 跨租户访问拒绝测试 |
| 可扩展性 | 集群横向扩展，实例可随时停机/扩容 | 无状态设计（会话/黑名单/限流计数存 Redis，文件存 MinIO）；租户引擎懒加载控制连接成本；事件驱动模块解耦 |
| 可维护性 | 代码可理解、可定位 | 单体模块化 + 分层（api/services/repositories/db）；知识图谱（graphify）辅助代码理解与架构分析 |

## 3. 技术约束 <a id="constraints"></a>

### 3.1 语言与生态 <a id="constraints-lang"></a>

- Python 3.14+，uv 管理依赖与虚拟环境；版本兼容口径：阶段一逐依赖验证（FastAPI/SQLAlchemy/Pydantic/psycopg/Celery/SpiffWorkflow 等），任一核心依赖不兼容则整体回退 Python 3.13
- 前端 Node 版本由 `.nvmrc` 固定（≥ 20.19），volta/fnm 管理；包管理 npm，frontend 与 frontend-mobile 双工程彻底独立（各自 lock 与 ESLint/Prettier/TS 配置，不共享、不生成额外配置文件）
- 测试环境与开发环境一致使用 SQLite（aiosqlite）

### 3.2 数据库兼容 <a id="constraints-db"></a>

- 开发/测试 SQLite；生产同时支持 MySQL 8.x、PostgreSQL 16+ 与达梦 DM8（信创选配）三库并行
- ORM 层禁用方言特有功能：JSON 等类型使用跨方言通用类型（达梦 JSON 支持有限，用通用类型兜底），分页统一 limit/offset
- Alembic 维护 MySQL / PostgreSQL / 达梦 DM8 三套方言迁移；SQLite 开发库自动建表；CI 三库方言集成测试

### 3.3 系统形态 <a id="constraints-shape"></a>

- 多租户 SaaS（租户独立库）为既定形态，见多租户路由子系统
- 单体模块化 + 独立 worker 进程（celery-worker / celery-beat / event-worker），跨域调用走事件总线
- 配置：config.toml 按环境分层（BMS_ENV）+ 环境变量覆盖；密钥只走环境变量 / Secret 管理
- 部署：Docker Compose 编排，数据量或规模增长后演进 Kubernetes

## 4. 明确不采用 <a id="not-adopted"></a>

| 技术 | 不采用理由 | 重新评估条件 |
| --- | --- | --- |
| Kubernetes | MVP 用 Docker Compose 部署即可满足 | 数据量或部署规模增长后 |
| Kafka | 消息量级未到，RocketMQ 已覆盖事件总线，Redis 已覆盖任务队列 | 消息量级显著增长 |
| MFA / 2FA | 账号密码 + 图形验证码 + 找回密码双通道 + 限流已满足 MVP 安全基线 | 安全基线升级需求 |
| 原生 WebSocket | 统一采用 Socket.IO（自动重连、Redis 跨实例广播） | 性能瓶颈且协议约束无法满足 |
| APNs / FCM 移动端系统推送 | MVP 站内信 + 企业微信/钉钉内免登即达 | 移动端独立 App 场景 |
| 微服务拆分 | 保持单体模块化 + 独立 worker 进程 | 团队规模/部署边界需要时 |
| 前端 SSR / Nuxt | 管理端与 H5 均为 SPA | SEO 需求出现 |

## 5. 与规划文档的对应 <a id="trace"></a>

本节点对应[《项目规划说明》](../../规划/项目规划说明.html)的「项目概述」「选型说明」「性能目标」「明确不采用」等章节。后续每个节点文件开头均标注其对应的规划文档章节，保证规划 → 架构可追溯。

## 6. 关联节点 <a id="related"></a>

- [总体架构](03_架构设计_总体架构.md)：约束落地：分层/进程/技术栈全景
- [核心交互时序](04_架构设计_核心交互时序.md)：质量属性在主流程中的体现
- [性能与安全](25_架构设计_性能与安全.md)：质量属性中的性能/安全目标落地
- [决策与风险](29_架构设计_决策与风险.md)：明确不采用清单与重新评估条件

> 本文档为 AI 生成 · 架构设计节点 · 与《项目规划说明》《文档生成规范》《命名规范》配套 · 生成日期：2026-08-06