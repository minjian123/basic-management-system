# 架构设计 · 数据架构

> BMS · 数据分布、表清单、规范与缓存

[架构设计](01_架构设计_总览.md) › 05-数据架构　|　[← 04-核心交互时序](04_架构设计_核心交互时序.md) · [06-接口与集成 →](06_架构设计_接口与集成.md)

## 1. 概述 <a id="overview"></a>

本节点定义 BMS 的数据架构：库划分与数据分布、核心数据表清单、数据规范、缓存架构与迁移策略。
对应[《项目规划说明》](../../规划/项目规划说明.md)的「核心数据表清单」「多数据源策略」「数据库兼容」「数据规范」「缓存策略」章节。
这是数据库详细设计（逐表字段）与数据访问子系统设计的依据。

## 2. 数据分布 <a id="distribution"></a>

| 库 | 名称 | 内容 | 归属 |
| --- | --- | --- | --- |
| 平台库 | `bms_platform` | 租户注册、平台超管、SSO 全局身份映射、平台统一维护的菜单/表单/按钮/字段与业务/动作权限码、AI 模型端点配置 | 平台运营（跨租户） |
| 租户库 | `bms_tenant_{code}` | 一租户一库：该租户全部业务数据（未标注【平台库】的表均在此） | 租户（物理隔离） |
| 归档库 | `bms_archive` | 统一收存各租户归档数据（带 tenant_id 与原库表标识，只读查询） | 治理（只读） |

- 跨库引用一律用逻辑外键（code），不建物理外键
- 开发环境用多个 SQLite 文件模拟平台库、租户库与归档库
- 环境库命名：`bms_dev` / `bms_test` / `bms_prod`

## 3. 核心数据表清单 <a id="tables"></a>

全部业务表统一规范：雪花 ID 主键、软删除（deleted_at）、审计字段（created_at/created_by/updated_at/updated_by）、乐观锁（version）、时间 UTC 存储；关联表用唯一联合主键。表前缀：平台域固定——系统 sys_、工作流 wf_、报表 rpt_、AI 域 ai_；业务模块表用各模块独立前缀（注册制，如采购 pur_、收付款 pay_、供应商 sup_，登记表见《[项目规划说明](../../规划/项目规划说明.md)》「附加业务模块规划」节）。

### 3.1 平台库表 <a id="tables-platform"></a>

| 表 | 关键字段 | 说明 |
| --- | --- | --- |
| sys_business | code、name、status | 业务权限码平台统一维护，租户可见、可分配、不可增删 |
| sys_action | code、name、business_id、status | 动作权限码归属业务 |
| sys_module | module_key、name、table_prefix、errcode_segment、event_domain、status | 业务模块注册表：模块简称/表前缀/错误码段/事件域平台统一注册，启动与 CI 校验唯一；平台域内置种子 sys/wf/rpt/ai |
| sys_menu | parent_id、name、path、component、icon、sort、hidden、status | 菜单树平台统一维护，动态路由依据；多语言名存 sys_menu_i18n |
| sys_form | menu_id、business_id、component、status | 表单：菜单 1:1 挂接业务权限（菜单→表单→业务） |
| sys_field | form_id、field_key、name、type、sort、status | 表单界面字段实体，字段权限控制依据 |
| sys_button | form_id、action_id、name、type（toolbar/interface）、sort | 表单按钮：按钮 1:1 挂接动作权限 |
| sys_tenant | code、name、domain、db_key、status、expire_at | 租户注册，db_key 指向数据源配置，租户路由依据 |
| sys_tenant_quota | tenant_id、quota_key（user_count/storage_bytes/open_calls）、limit_value、used_value | 租户配额，超限拦截与告警 |
| sys_tenant_module | tenant_id、module_key、status（enabled/disabled）、opened_by、opened_at、closed_at | 租户-模块关系表：租户开通的模块清单与启停状态（只引用已注册启用模块），租户开通勾选写入、平台超管运营期启停；停用后菜单/接口不可用、数据保留 |
| sys_admin | username、password_hash、name、status | 平台运营账号（平台超管） |
| sys_user_identity | idp_key、external_id、tenant_id、user_id | SSO 全局身份映射，登录时定位租户与用户 |
| sys_ai_provider | provider_key、name、type（llm/embedding/ocr）、endpoint、model、api_key_ref、status | AI 模型端点配置（外部 API/私有化），租户按需引用 |

### 3.2 租户库表 <a id="tables-tenant"></a>

| 域 | 表 | 说明 |
| --- | --- | --- |
| 组织与权限 | sys_user、sys_post、sys_user_post、sys_dept | 用户（含密码策略字段/语言时区偏好）、岗位、用户-岗位、部门树 |
| 组织与权限 | sys_user_preference | 用户喜好（主题/列表页个性化/通知偏好/工作台自定义），user_id + pref_key 联合主键，pref_value 存 JSON，跨端同步 |
| 组织与权限 | sys_role、sys_role_assign | 角色；主体-角色通用绑定（subject_type + subject_id + role_id，支持任意主体链） |
| 组织与权限 | sys_role_permission、sys_role_field | 角色授予菜单/表单权限（业务权限按表单→业务对照关系推导）/动作权限；字段权限（visible/editable，默认全部可见可编辑） |
| 组织与权限 | sys_data_scope | 动作级数据范围（规则表达式存 config，见权限设计文档） |
| 组织与权限 | sys_dict_type / sys_dict_item | 字典（平台默认、租户覆盖/自建）；多语言 label 走 _i18n 附表 |
|  | sys_config | 系统参数（平台默认、租户覆盖） |
| 认证与治理 | sys_session、sys_account_lock | 会话记录；账号锁定（lock_type: fail_limit/inactive/manual） |
| 认证与治理 | sys_identity_provider、sys_client | 租户外部 IdP 配置；第三方应用（OAuth2/OIDC 客户端） |
| 认证与治理 | sys_webhook / sys_webhook_log、sys_open_log | Webhook 订阅；开放接口调用审计（按月分片 + 哈希链） |
| 认证与治理 | sys_task / sys_task_log | 定时任务定义与执行记录 |
| 通用能力 | sys_file、sys_notification、sys_notice / sys_notice_read | 文件元数据（分片状态跟踪）；站内信/待办；公告与已读回执 |
| 通用能力 | sys_mail_template / sys_mail_log、sys_sms_template / sys_sms_log | 邮件/短信模板（页面在线编辑）与发送记录 |
| 通用能力 | sys_help_category、sys_help_article、sys_help_article_i18n、sys_help_feedback | 帮助分类树；帮助文章（target_type/target_key 挂页面内帮助、状态机 draft/published/offline、阅读与反馈计数）；文章多语言（title/content）；阅读反馈（联合主键防重复） |
|  | sys_i18n_locale、sys_i18n_message | 语言清单（rtl 标记）；前后端固定文案语言包 |
|  | sys_dashboard_card / sys_dashboard_template | 工作台卡片注册表（card_type: builtin 内置功能卡/dataset 数据集图表卡，多语言存 sys_dashboard_card_i18n）与工作台模板（scope: platform/role，layout_config 存卡片引用 + gridstack 网格位置与尺寸，三级优先级：个人布局 → 角色模板 → 平台默认） |
|  | sys_form_layout、sys_field_ext | 表单三级布局（scope: platform/tenant/role，layout_config 存分区/分组/列数/跨列/顺序/标签宽度与查询区/明细区配置，空布局回退字段 sort 默认栅格）；租户自建字段（form_id 逻辑引用平台 sys_form，column_name 为 ext_ 物理列、停用不删列、多语言存 sys_field_ext_i18n） |
| 工作流与业务 | wf_process、wf_instance、wf_task、wf_record | 流程定义（definition_key+version 唯一）、实例、待办、审批记录 |
| 工作流与业务 | pur_purchase_apply、pur_purchase_order、sup_supplier | 采购申请单、采购订单、供应商档案 |
|  | pay_receipt、pay_payment、pay_refund、pay_payment_channel、pay_payment_flow | 收款单、付款单、退款单、支付渠道配置（渠道抽象预留）、统一收付款流水台账 |
| 审计与治理 | sys_operation_log、sys_login_log、sys_data_audit_log | 操作/登录/字段级变更审计（按月分片 + 哈希链） |
| 审计与治理 | sys_archive_policy / sys_archive_log、sys_backup_task / sys_backup_log | 归档策略与执行记录；备份任务与记录（含恢复演练登记） |
| 报表与 AI | rpt_dataset、rpt_report、rpt_screen | 报表数据集（SQL 建模）、报表（ECharts 配置）、大屏（画布配置） |
| 报表与 AI | ai_chat_log、ai_file_segment | AI 交互审计（按月分片）；文件内容分段（向量存 Milvus） |

### 3.3 特殊表 <a id="tables-special"></a>

| 表 | 说明 |
| --- | --- |
| `{主表}_i18n` 附表 | sys_menu_i18n（name）、sys_business_i18n、sys_action_i18n、sys_field_i18n、sys_dict_type_i18n、sys_dict_item_i18n、sys_mail_template_i18n（subject、content）、sys_dashboard_card_i18n、sys_field_ext_i18n；联合主键（主表 ID + locale），查询按当前 locale 关联，缺省回退主表默认文案 |
| 分片表 | sys_operation_log / sys_open_log / sys_data_audit_log / ai_chat_log 按月分片：逻辑表名不带后缀，物理表名带 `yyyyMM`（如 sys_operation_log_202608），分片键=时间；新月份分片表由 Celery 预创建 |
| 回收站 | 无独立表：软删除数据复用 deleted_at 查看/恢复；过期数据由 Celery 物理清理 |

## 4. 数据规范 <a id="rules"></a>

- 主键：业务表雪花 ID（yitter/IdGenerator）；关联表用唯一联合主键，不设 id
- 软删除：业务数据 deleted_at；唯一约束统一建为 (唯一字段, deleted_at) 复合唯一索引——未删行 deleted_at 为 NULL，SQLite/MySQL/PostgreSQL/达梦 DM8 四库均允许多个 NULL 不冲突（达梦按 Oracle 兼容口径，阶段一验证）；PostgreSQL 可另加部分唯一索引优化
- 审计字段：created_at / created_by / updated_at / updated_by，由 ORM 事件自动填充
- 乐观锁：version 字段，更新时比对，防止多实例并发覆盖
- 时间：统一 UTC 存储，前端按本地时区展示
- 字符集：MySQL utf8mb4、PostgreSQL UTF8、达梦 DM8 UTF8
- 索引：排序字段、外键、数据权限过滤字段必须建索引；唯一性约束用唯一索引（idx_/uq_ 前缀）
- SQL 规范：禁止 SELECT *；批量写入用 bulk；事务尽量短；定期走查慢查询日志

### 4.1 字典与系统参数管理 <a id="rules-dict"></a>

- 字典（sys_dict_type / sys_dict_item）与系统参数（sys_config）：平台库存默认，租户库可覆盖/自建（租户开通种子自动下发默认项）
- 字典多语言 label 存各自 _i18n 附表，按 locale 关联查询
- 缓存：只读热数据 Redis 缓存（`bms:{tenant}:dict:{locale}:{type}`），短 TTL + 随机偏移；字典/参数变更后删除缓存 key（先写库后删缓存协议）并递增全局版本号 `bms:{tenant}:dict:version`；后端进程内内存字典缓存（dogpile.cache 双 Region：小字典整类型 + 超大字典已查条目局部子集，按版本惰性比对）供字典查询接口与业务翻译（导出/通知/报表/审批/明细行批量）共用，零网络往返；超大字典批量翻译走「局部缓存 + 一次性 IN 查询」防 N+1
- 字典/参数是权限体系与业务模块的通用依赖（状态、类型等基础字典），页面管理权限码 dict:manage / config:manage

### 4.2 回收站机制 <a id="rules-recycle"></a>

- 无独立表：回收站复用软删除字段（deleted_at），软删除数据查看与恢复（权限码 recycle:restore）
- 恢复动作落操作日志与字段级审计（sys_data_audit_log）——审计留痕；恢复即清除 deleted_at
- 过期软删除残留由 Celery 定时物理清理，与归档清理任务衔接（清理顺序：先归档可归档数据，再物理清理过期残留）
- 唯一约束 (唯一字段, deleted_at) 复合唯一索引保证删除后释放唯一键、恢复不冲突

## 5. 缓存架构 <a id="cache"></a>

统一 Redis 缓存作为共享缓存源；一般业务数据不引入本地内存缓存（多实例内存独立会造成数据不一致），**字典为只读热配置例外**——进程内内存字典缓存（整类型 dict，全局版本号惰性比对保证多实例一致，见 4.1）。key 格式 `bms:{租户|global}:{域}:{业务键}`，全小写冒号分隔，所有 key 必须携带 TTL。

### 5.1 key 空间规划 <a id="cache-keyspace"></a>

| 域 | key 示例 | TTL 策略 |
| --- | --- | --- |
| 缓存类 | `bms:{tenant}:dict:{locale}:{type}`、`bms:{tenant}:dict:version`（字典全局版本，INCR）、`bms:global:menu:{locale}`、`bms:{tenant}:perm:{user_id}:{version}` | 短 TTL + 随机偏移（防雪崩）；版本键持久（随变更 INCR） |
| 会话与认证 | `bms:{tenant}:sess:{session_id}`、`bms:global:token:blacklist:{jti}` | 与业务有效期对齐（14 天 / 黑名单短窗） |
| 验证码与限流 | `bms:global:captcha:{uuid}`（5 分钟）、`bms:global:rate:{维度}:{目标}` | 短 TTL，与策略窗口对齐 |
| 锁与幂等 | `bms:global:lock:{资源}`、`bms:{tenant}:idem:{key}` | SETNX 带过期，防死锁 |
| 空值缓存 | 查询不存在的 key 值 = EMPTY 标记 | 短 TTL（防穿透） |

### 5.2 三防与一致性协议 <a id="cache-protocol"></a>

- 防穿透：不存在数据缓存空值（短 TTL）
- 防击穿：热点 key 过期用互斥锁（SETNX）单实例重建
- 防雪崩：TTL 加随机偏移，避免大量 key 同时失效
- 一致性：先写库后删缓存；写操作后同一会话内读主库，避免读到从库旧数据
- 权限缓存失效：权限相关变更 → 权限版本号 +1，用户缓存按版本校验，保证变更即时生效
- 字典内存缓存失效：字典变更 → INCR 全局版本键，各实例按版本惰性比对自动重载（与权限版本号同模式）

## 6. 迁移策略 <a id="migration"></a>

- Alembic 按数据源分别维护迁移脚本，只管理各库 schema；MySQL / PostgreSQL / 达梦 DM8 三套方言迁移脚本
- 租户库批量迁移：遍历 sys_tenant 逐库执行（批量迁移脚本）；新租户开通自动执行
- SQLite 开发库自动建表（不依赖迁移脚本）
- 测试环境与开发环境一致使用 SQLite；三库方言差异以 CI 集成测试覆盖

## 7. 关联节点 <a id="related"></a>

- [多租户路由](08_架构设计_子系统_多租户路由.md)：租户独立库与 db_key
- [数据访问与分片](09_架构设计_子系统_数据访问与分片.md)：引擎/路由/分片实现
- [审计哈希链](15_架构设计_子系统_审计哈希链.md)：审计分表与哈希链
- [国际化](20_架构设计_子系统_国际化.md)：i18n 附表数据规范
- [性能与安全](25_架构设计_性能与安全.md)：缓存三防与一致性

> 本文档为 AI 生成 · 架构设计节点 · 与《项目规划说明》《文档生成规范》《命名规范》配套 · 生成日期：2026-08-15