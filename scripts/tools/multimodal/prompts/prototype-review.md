你是一名严格的 BMS 管理系统界面原型评审专家。请仔细查看这张界面截图，对照下面的布局设计规范逐项检查，**只输出 JSON**（不要输出任何其他文字，不要用 markdown 代码块包裹）。

输出格式（严格遵循）：
{
  "summary": "总体结论（1-2 句）",
  "issues": [
    {
      "severity": "high|medium|low",
      "area": "问题所在区域",
      "problem": "问题描述",
      "rule": "违反的规范条目",
      "suggestion": "修改建议"
    }
  ]
}
没有问题时不输出 issues 或 issues 为空数组。

评审检查要点：
1. 设计令牌：主色 #0969da、成功 #2da44e、警告 #d4a72c、危险 #cf222e、信息 #57606a；无硬编码杂色；间距遵循 4px 基准（4/8/12/16/24/32）；圆角 4/6/8px
2. 布局质量：无明显错位、遮挡、溢出、截断；栅格对齐；上下左右边距一致；无英文残留、无乱码
3. 列表页：工具栏（新增/刷新/关闭 + 批量操作 + 右侧导出）在筛选区上方；工具栏按钮可带前置图标；筛选区（关键字 + 条件 + 查询/重置）；表格；分页（共 N 条 · x/y 页）
4. 表单页：顶部工具栏（只读态 修改/删除/刷新/关闭；编辑态 保存）；详细信息/系统信息子 Tab；系统信息字段纯文本展示；字段 label 与输入框对齐
5. 弹窗：标题为「动作+对象」（如「编辑角色」）；内容不溢出；取消/确定按钮对齐；遮罩关闭仅限非关键流程
6. 视觉一致性：字体大小层级合理；按钮风格统一；状态标签语义正确（通过=绿、待审=黄、驳回=红）；图标与文字间距合适
7. 原型界面不应出现设计说明类文字（如「原型演示」「回退链」「主体链」「挂接链」「Socket.IO」等评审提示文字，会被误认为界面真实内容）
8. 侧边菜单/导航：一级菜单配图标、二级纯文字；激活态高亮清晰
