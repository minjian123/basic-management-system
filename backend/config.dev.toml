# dev 环境覆盖（可读日志 + 放行本地前端开发服务器）

[app]
debug = true

[log]
level = "DEBUG"
format = "console"
# 慢请求阈值（dev 严：便于发现性能问题）
slow_request_ms = 500

[cors]
allow_origins = ["http://localhost:5173", "http://127.0.0.1:5173"]
