/**
 * 模块契约版本（平台侧唯一常量；模块与平台之间的契约代号）。
 *
 * **递增口径**（不向后兼容的契约变更）：注入上下文项增删改、注册声明通道增删、
 * 清单字段语义变更、加载校验链变化。模块经 `manifest.contractVersion` 自报构建时契约版本，
 * 加载器校验一致才允许加载（契约升级须模块适配并重新构建后发布，见任务 03_02 详细设计 §3.2）。
 */

/** 当前模块契约版本（平台支持的唯一版本）。 */
export const MODULE_CONTRACT_VERSION = 1
