/**
 * 能力域出口：编辑器内核片段（`editor-kernel`）。
 *
 * 契约见《组件设计 · 编辑器内核片段》；片段经 `useCapabilityBase` 声明标识与依赖，
 * 依赖登记见 `src/components/base/fragments.ts`（与《前端基类清单》「能力片段」节一致）。
 */

export * from './useEditorKernel'
