# Grafana provisioning：面板加载器（从 /var/lib/grafana/dashboards 读取 JSON）。
apiVersion: 1

providers:
  - name: BMS
    orgId: 1
    folder: BMS
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
